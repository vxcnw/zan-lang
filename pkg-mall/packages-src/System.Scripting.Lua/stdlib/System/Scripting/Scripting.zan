using System;

namespace System.Scripting;

/// Lua scripting facade (official marketplace sample package).
class Scripting {
    static string Version() { return "lua-5.4-sample"; }

    static int Add(int a, int b) { return a + b; }
}

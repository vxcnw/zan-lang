# Native [DllImport] libraries owned by this module (one -l basename per line).
# 空行 and '#' 注释 are ignored. zanc discovers this 文件 to bundle
# the driver on --publish; see src/compiler/main.c (zan_discover_drivers).
zan_sdl3

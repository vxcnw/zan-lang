using System;
using System.Json;

namespace Gui;

/// NaiveUI 主题 token。所有颜色打包为 0xAARRGGBB 存入 int。
class Theme {
    int primary;
    int primaryHover;
    int primaryPressed;
    int info;
    int infoHover;
    int infoPressed;
    int success;
    int successHover;
    int successPressed;
    int warning;
    int warningHover;
    int warningPressed;
    int error;
    int errorHover;
    int errorPressed;

    int textPrimary;
    int textSecondary;
    int textTertiary;
    int textDisabled;
    int textInverse;

    int bgPrimary;
    int bgSecondary;
    int bgTertiary;
    int bgHover;
    int bgActive;
    int bgDisabled;

    int borderPrimary;
    int borderSecondary;
    int borderHover;
    int borderFocus;
    int divider;
    int overlay;

    // 磨砂玻璃主题时为 1：面板模糊其背后的背景
    // （通过 Canvas.BlurRect），并以半透明色调填充。
    int glass;

    // 玻璃表面的形态参数，皮肤可用 `--glass-blur` 等 token 覆写。
    // 它们不直接画东西：Style.RootFromTheme 把它们导出成
    // `--surface-*` token，由 skins/base.css 的组件规则决定怎么画，
    // 因此玻璃的观感全部由 CSS 声明，皮肤可逐项改写。
    int glassBlur;        // backdrop-filter 的模糊半径
    int glassTint;        // 表面色调保留的 bgPrimary alpha（千分比）
    int glassChromeTint;  // 停靠区/外壳这类大面积表面的色调（千分比）
    int glassSheen;       // -zan-sheen 强度（千分比）
    int glassRadius;      // 表面圆角
    int glassShadowDy;    // 柔和投影的下沉量
    int glassShadowBlur;  // 柔和投影的扩散量

    // 内容背景为垂直渐变（bgGradTop ->
    // bgGradBottom）而非纯色 bgPrimary 时为 1——现代 AI / Aurora / Glass
    // 壁纸。面板随后使用半透明表面，让背景透出。
    int gradient;
    int bgGradTop;
    int bgGradBottom;

    // 拟物化皮肤为 1：控件与页面同色，通过
    // 左上亮 + 右下暗的柔和阴影对实现凸起/内陷，
    // （见共享控件中的 Emboss 辅助函数），而非使用边框/填充。
    int neu;
    // 新粗野主义皮肤为 1：实心、偏移（无模糊）的投影、
    // 粗硬边框和锐利圆角（配合 radius 0 / 粗
    // borderWidth token 和不透明 shadowColor）。
    int brutal;

    // 该皮肤的动画背景效果（BackdropFx.Kind* 常量，0 = 无）。
    // App.RenderBackground 在节流的动画帧上把效果叠加到静态壁纸
    // 快照上，使皮肤带有标志性的动感。
    int fx;

    int scrollbar;
    int scrollbarHover;

    // 必须在任何皮肤/背景上保持可读的浮层表面：工具提示
    // 和弹出层在所有主题下都用不透明的深色石板色，媒体/照片层
    // 用近不透明的遮罩压暗页面，并在其上浮动白色半透明小圆片。
    int tooltipBg;
    int tooltipBorder;
    int tooltipText;
    int tooltipTextMuted;
    int scrim;          // 模态媒体层后面的全窗口压暗层
    int scrimChip;      // 悬浮在遮罩上的待机圆形控件
    int scrimChipHover; // 悬停变体
    int onScrim;        // 直接绘制在遮罩上的文本/图标
    int statusDebugBg;  // 调试会话进行中时状态栏的色调

    // 图表定性调色板（Chart.Palette 的皮肤化数据源）：
    // 0 表示"未由皮肤定义"，运行时回退到 ECharts 默认调色板。
    // CSS 侧通过 --chart-1 .. --chart-8 token 覆写。
    int chart1;
    int chart2;
    int chart3;
    int chart4;
    int chart5;
    int chart6;
    int chart7;
    int chart8;

    int borderRadiusSmall;
    int borderRadiusMedium;
    int borderRadiusLarge;
    int borderWidth;

    int fontSizeTiny;
    int fontSizeSmall;
    int fontSizeMedium;
    int fontSizeLarge;
    int fontSizeHuge;

    int heightTiny;
    int heightSmall;
    int heightMedium;
    int heightLarge;

    int paddingTiny;
    int paddingSmall;
    int paddingMedium;
    int paddingLarge;

    int gapSmall;
    int gapMedium;
    int gapLarge;

    // 面板 / 列表列的可读宽度区间：比例面板
    // 不会缩到小于第一个，也不会超过第二个，因此
    // 布局只需声明比例（见 Stack.SlotPane）。

    int iconSizeSmall;
    int iconSizeMedium;
    int iconSizeLarge;
    int iconSizeCaption;

    int shadowColor;

    int animFastMs;
    int animMediumMs;
    int animSlowMs;

    /// 浅色预设主题（NaiveUI 风格 token 的默认取值）。
    static Theme Light() {
        Theme t = new Theme();
        t.primary        = -15163304;
        t.primaryHover   = -13193878;
        t.primaryPressed = -15959485;
        t.info           = -14647056;
        t.infoHover      = -12543748;
        t.infoPressed    = -15703863;
        t.success        = -15163304;
        t.successHover   = -13193878;
        t.successPressed = -15959485;
        t.warning        = -1007584;
        t.warningHover   = -217024;
        t.warningPressed = -3572720;
        t.error          = -3133360;
        t.errorHover     = -2205843;
        t.errorPressed   = -5562561;

        t.textPrimary    = -14736859;
        t.textSecondary  = -13420999;
        t.textTertiary   = -9012094;
        t.textDisabled   = -4013374;
        t.textInverse    = -1;

        t.bgPrimary      = -1;
        t.bgSecondary    = -328964;
        t.bgTertiary     = -789515;
        t.bgHover        = -789515;
        t.bgActive       = -1184275;
        t.bgDisabled     = -328964;

        t.borderPrimary   = -2039578;
        t.borderSecondary = -1052683;
        t.borderHover     = -13193878;
        t.borderFocus     = -15163304;
        t.divider         = -1052683;
        t.overlay         = 1291845632;
        t.glass           = 0;
        t.glassBlur       = 36;
        t.glassTint       = 260;
        t.glassChromeTint = 520;
        t.glassSheen      = 220;
        t.glassRadius     = 14;
        t.glassShadowDy   = 8;
        t.glassShadowBlur = 24;
        t.gradient        = 0;
        t.bgGradTop       = 0;
        t.bgGradBottom    = 0;
        t.neu             = 0;
        t.brutal          = 0;
        t.fx              = 0;

        t.scrollbar       = -3618616;
        t.scrollbarHover  = -5592406;

        t.tooltipBg         = (int)0xFF1F2430;
        t.tooltipBorder     = (int)0xFF3A4150;
        t.tooltipText       = (int)0xFFF2F5FA;
        t.tooltipTextMuted  = (int)0xFFBFC7D5;
        t.scrim             = (int)0xE6000000;
        t.scrimChip         = 0x40FFFFFF;
        t.scrimChipHover    = 0x66FFFFFF;
        t.onScrim           = (int)0xFFFFFFFF;
        t.statusDebugBg     = (int)0xFFCC6633;
        // 图表调色板默认未定义（0）：配色是图表皮肤（ChartSkin）的
        // 独立板块，不进全局皮肤主题；Chart.Palette 回退到当前
        // 图表皮肤，未定义时再落 ECharts 默认色。
        t.chart1 = 0; t.chart2 = 0; t.chart3 = 0; t.chart4 = 0;
        t.chart5 = 0; t.chart6 = 0; t.chart7 = 0; t.chart8 = 0;

        t.borderRadiusSmall  = 3;
        t.borderRadiusMedium = 3;
        t.borderRadiusLarge  = 6;
        t.borderWidth        = 1;

        t.fontSizeTiny   = 12;
        t.fontSizeSmall  = 13;
        t.fontSizeMedium = 14;
        t.fontSizeLarge  = 16;
        t.fontSizeHuge   = 20;

        t.heightTiny  = 22;
        t.heightSmall = 28;
        t.heightMedium = 34;
        t.heightLarge = 40;

        t.paddingTiny  = 4;
        t.paddingSmall = 8;
        t.paddingMedium = 12;
        t.paddingLarge = 16;

        t.gapSmall  = 6;
        t.gapMedium = 8;
        t.gapLarge  = 12;

        t.iconSizeSmall  = 16;
        t.iconSizeMedium = 20;
        t.iconSizeLarge  = 24;
        t.iconSizeCaption = 11;

        t.shadowColor = 436207616;

        t.animFastMs   = 100;
        t.animMediumMs = 200;
        t.animSlowMs   = 300;

        return t;
    }

    /// 深色预设主题，动画背景默认为星空（BackdropFx.KindStars）。
    static Theme Dark() {
        Theme t = new Theme();
        t.primary        = -10231113;
        t.primaryHover   = -8394812;
        t.primaryPressed = -11742557;
        t.info           = -9387800;
        t.infoHover      = -7417362;
        t.infoPressed    = -11357984;
        t.success        = -10231113;
        t.successHover   = -8394812;
        t.successPressed = -11742557;
        t.warning        = -1007584;
        t.warningHover   = -217024;
        t.warningPressed = -3572720;
        t.error          = -1539968;
        t.errorHover     = -1271399;
        t.errorPressed   = -2005145;

        t.textPrimary    = -1;
        t.textSecondary  = -6380632;
        t.textTertiary   = -9012094;
        t.textDisabled   = -11184811;
        t.textInverse    = -14736859;

        t.bgPrimary      = -15198180;
        t.bgSecondary    = -14671836;
        t.bgTertiary     = -13882318;
        t.bgHover        = -13487560;
        t.bgActive       = -13092802;
        t.bgDisabled     = -14671836;

        t.borderPrimary   = -12237491;
        t.borderSecondary = -13092802;
        t.borderHover     = -8394812;
        t.borderFocus     = -10231113;
        t.divider         = -13092802;
        t.overlay         = 1291845632;
        t.glass           = 0;
        t.glassBlur       = 36;
        t.glassTint       = 260;
        t.glassChromeTint = 520;
        t.glassSheen      = 220;
        t.glassRadius     = 14;
        t.glassShadowDy   = 8;
        t.glassShadowBlur = 24;
        t.gradient        = 0;
        t.bgGradTop       = 0;
        t.bgGradBottom    = 0;
        t.neu             = 0;
        t.brutal          = 0;
        t.fx              = BackdropFx.KindStars();

        t.scrollbar       = -12237491;
        t.scrollbarHover  = -10197908;

        t.tooltipBg         = (int)0xFF1F2430;
        t.tooltipBorder     = (int)0xFF3A4150;
        t.tooltipText       = (int)0xFFF2F5FA;
        t.tooltipTextMuted  = (int)0xFFBFC7D5;
        t.scrim             = (int)0xE6000000;
        t.scrimChip         = 0x40FFFFFF;
        t.scrimChipHover    = 0x66FFFFFF;
        t.onScrim           = (int)0xFFFFFFFF;
        t.statusDebugBg     = (int)0xFFCC6633;
        // 图表调色板默认未定义（0）：配色是图表皮肤（ChartSkin）的
        // 独立板块，不进全局皮肤主题；Chart.Palette 回退到当前
        // 图表皮肤，未定义时再落 ECharts 默认色。
        t.chart1 = 0; t.chart2 = 0; t.chart3 = 0; t.chart4 = 0;
        t.chart5 = 0; t.chart6 = 0; t.chart7 = 0; t.chart8 = 0;

        t.borderRadiusSmall  = 3;
        t.borderRadiusMedium = 3;
        t.borderRadiusLarge  = 6;
        t.borderWidth        = 1;

        t.fontSizeTiny   = 12;
        t.fontSizeSmall  = 13;
        t.fontSizeMedium = 14;
        t.fontSizeLarge  = 16;
        t.fontSizeHuge   = 20;

        t.heightTiny  = 22;
        t.heightSmall = 28;
        t.heightMedium = 34;
        t.heightLarge = 40;

        t.paddingTiny  = 4;
        t.paddingSmall = 8;
        t.paddingMedium = 12;
        t.paddingLarge = 16;

        t.gapSmall  = 6;
        t.gapMedium = 8;
        t.gapLarge  = 12;

        t.iconSizeSmall  = 16;
        t.iconSizeMedium = 20;
        t.iconSizeLarge  = 24;
        t.iconSizeCaption = 11;

        t.shadowColor = 637534208;

        t.animFastMs   = 100;
        t.animMediumMs = 200;
        t.animSlowMs   = 300;

        return t;
    }

    // ---- 颜色打包辅助（int 为 64 位；按 0xAARRGGBB 存储）----

    /// 由 8 位 r/g/b 生成不透明颜色。
    static int Rgb(int r, int g, int b) {
        return (int)4278190080 + r * 65536 + g * 256 + b;
    }

    /// 由 8 位 r/g/b 与 8 位 alpha（0 透明 .. 255 不透明）生成颜色。
    static int Rgba(int r, int g, int b, int a) {
        return a * 16777216 + r * 65536 + g * 256 + b;
    }

    /// OS 玻璃合成器使用的当前主题着色。
    int GlassTint() {
        int rgb = bgPrimary % 16777216;
        if (rgb < 0) { rgb = rgb + 16777216; }
        return App.WithAlpha(rgb, 150);
    }

    /// 当前主题表面的 RGB 亮度是否达到浅色阈值。
    bool IsLightBg() {
        int rgb = bgPrimary % 16777216;
        if (rgb < 0) { rgb = rgb + 16777216; }
        int r = rgb / 65536;
        int g = (rgb / 256) % 256;
        int b = rgb % 256;
        return r + g + b > 384;
    }

    /// 应用外观抽屉选择的强调色覆盖。
    void ApplyAccent(int color) {
        primary = color;
        borderFocus = color;
        borderHover = color;
    }

    /// 让三层窗口表面恢复完全不透明。
    void MakeSurfacesOpaque() {
        bgPrimary = App.WithAlpha(bgPrimary, 255);
        bgSecondary = App.WithAlpha(bgSecondary, 255);
        bgTertiary = App.WithAlpha(bgTertiary, 255);
    }

    /// 按已推导出的 alpha 写回三层窗口表面。
    void ApplyTranslucency(int surface, int chrome, int rail) {
        bgPrimary = App.WithAlpha(bgPrimary, surface);
        bgSecondary = App.WithAlpha(bgSecondary, chrome);
        bgTertiary = App.WithAlpha(bgTertiary, rail);
    }

    // ---- 数据驱动皮肤（CSS/JSON token 映射）----
    // 皮肤可以用 JSON 设计 token 样式表编写，而无需
    // 手写工厂，例如：
    //   { "base": "dark",
    //     "primary": "#63e2b7", "bgPrimary": "#101014",
    //     "borderRadiusMedium": 6, "glass": 1 }
    // 键是 Theme token 名称（接受前导 “--”，CSS 自定义
    // 属性风格）。颜色接受 “#RGB”/“#RRGGBB”/“#AARRGGBB”/“0x..”/int；
    // 其余都是 int。“base” 选择起始 Theme（“light”、
    // “dark” 或 PresetNames() 索引），token 随后覆盖。现有
    // 预设不受影响；这是加法式、向后兼容的路径。

    /// 颜色 token：解析 “#..”/“0x..”/十进制字符串，或直接接受原始 int。
    static int TokenColor(JsonValue v) {
        if (v == null) { return 0; }
        if (v.IsNumber()) { return v.AsInt(0); }
        return StyleSheet.ParseColor(v.AsString(""));
    }

    /// 按 Theme 字段名将一条 token 应用到 `t`（去掉前导 “--”）。
    static void SetToken(Theme t, string key, JsonValue v) {
        string k = key;
        if (k.Length >= 2 && k.Substring(0, 2) == "--") { k = k.Substring(2, k.Length - 2); }
        // 颜色
        if (k == "primary") { t.primary = Theme.TokenColor(v); }
        else if (k == "primaryHover") { t.primaryHover = Theme.TokenColor(v); }
        else if (k == "primaryPressed") { t.primaryPressed = Theme.TokenColor(v); }
        else if (k == "info") { t.info = Theme.TokenColor(v); }
        else if (k == "infoHover") { t.infoHover = Theme.TokenColor(v); }
        else if (k == "infoPressed") { t.infoPressed = Theme.TokenColor(v); }
        else if (k == "success") { t.success = Theme.TokenColor(v); }
        else if (k == "successHover") { t.successHover = Theme.TokenColor(v); }
        else if (k == "successPressed") { t.successPressed = Theme.TokenColor(v); }
        else if (k == "warning") { t.warning = Theme.TokenColor(v); }
        else if (k == "warningHover") { t.warningHover = Theme.TokenColor(v); }
        else if (k == "warningPressed") { t.warningPressed = Theme.TokenColor(v); }
        else if (k == "error") { t.error = Theme.TokenColor(v); }
        else if (k == "errorHover") { t.errorHover = Theme.TokenColor(v); }
        else if (k == "errorPressed") { t.errorPressed = Theme.TokenColor(v); }
        else if (k == "textPrimary") { t.textPrimary = Theme.TokenColor(v); }
        else if (k == "textSecondary") { t.textSecondary = Theme.TokenColor(v); }
        else if (k == "textTertiary") { t.textTertiary = Theme.TokenColor(v); }
        else if (k == "textDisabled") { t.textDisabled = Theme.TokenColor(v); }
        else if (k == "textInverse") { t.textInverse = Theme.TokenColor(v); }
        else if (k == "bgPrimary") { t.bgPrimary = Theme.TokenColor(v); }
        else if (k == "bgSecondary") { t.bgSecondary = Theme.TokenColor(v); }
        else if (k == "bgTertiary") { t.bgTertiary = Theme.TokenColor(v); }
        else if (k == "bgHover") { t.bgHover = Theme.TokenColor(v); }
        else if (k == "bgActive") { t.bgActive = Theme.TokenColor(v); }
        else if (k == "bgDisabled") { t.bgDisabled = Theme.TokenColor(v); }
        else if (k == "borderPrimary") { t.borderPrimary = Theme.TokenColor(v); }
        else if (k == "borderSecondary") { t.borderSecondary = Theme.TokenColor(v); }
        else if (k == "borderHover") { t.borderHover = Theme.TokenColor(v); }
        else if (k == "borderFocus") { t.borderFocus = Theme.TokenColor(v); }
        else if (k == "divider") { t.divider = Theme.TokenColor(v); }
        else if (k == "overlay") { t.overlay = Theme.TokenColor(v); }
        else if (k == "bgGradTop") { t.bgGradTop = Theme.TokenColor(v); }
        else if (k == "bgGradBottom") { t.bgGradBottom = Theme.TokenColor(v); }
        else if (k == "scrollbar") { t.scrollbar = Theme.TokenColor(v); }
        else if (k == "scrollbarHover") { t.scrollbarHover = Theme.TokenColor(v); }
        else if (k == "tooltipBg") { t.tooltipBg = Theme.TokenColor(v); }
        else if (k == "tooltipBorder") { t.tooltipBorder = Theme.TokenColor(v); }
        else if (k == "tooltipText") { t.tooltipText = Theme.TokenColor(v); }
        else if (k == "tooltipTextMuted") { t.tooltipTextMuted = Theme.TokenColor(v); }
        else if (k == "scrim") { t.scrim = Theme.TokenColor(v); }
        else if (k == "scrimChip") { t.scrimChip = Theme.TokenColor(v); }
        else if (k == "scrimChipHover") { t.scrimChipHover = Theme.TokenColor(v); }
        else if (k == "onScrim") { t.onScrim = Theme.TokenColor(v); }
        else if (k == "statusDebugBg") { t.statusDebugBg = Theme.TokenColor(v); }
        else if (k == "shadowColor") { t.shadowColor = Theme.TokenColor(v); }
        // 图表定性调色板（Chart.Palette 消费；0 = 未定义则回退默认）
        else if (k == "chart1") { t.chart1 = Theme.TokenColor(v); }
        else if (k == "chart2") { t.chart2 = Theme.TokenColor(v); }
        else if (k == "chart3") { t.chart3 = Theme.TokenColor(v); }
        else if (k == "chart4") { t.chart4 = Theme.TokenColor(v); }
        else if (k == "chart5") { t.chart5 = Theme.TokenColor(v); }
        else if (k == "chart6") { t.chart6 = Theme.TokenColor(v); }
        else if (k == "chart7") { t.chart7 = Theme.TokenColor(v); }
        else if (k == "chart8") { t.chart8 = Theme.TokenColor(v); }
        // 标志与度量（纯 int）
        else if (k == "glass") { t.glass = v.AsInt(0); }
        else if (k == "glassBlur") { t.glassBlur = v.AsInt(0); }
        else if (k == "glassTint") { t.glassTint = v.AsInt(0); }
        else if (k == "glassChromeTint") { t.glassChromeTint = v.AsInt(0); }
        else if (k == "glassSheen") { t.glassSheen = v.AsInt(0); }
        else if (k == "glassRadius") { t.glassRadius = v.AsInt(0); }
        else if (k == "glassShadowDy") { t.glassShadowDy = v.AsInt(0); }
        else if (k == "glassShadowBlur") { t.glassShadowBlur = v.AsInt(0); }
        else if (k == "gradient") { t.gradient = v.AsInt(0); }
        else if (k == "neu") { t.neu = v.AsInt(0); }
        else if (k == "brutal") { t.brutal = v.AsInt(0); }
        else if (k == "fx") { t.fx = v.AsInt(0); }
        else if (k == "borderRadiusSmall") { t.borderRadiusSmall = v.AsInt(0); }
        else if (k == "borderRadiusMedium") { t.borderRadiusMedium = v.AsInt(0); }
        else if (k == "borderRadiusLarge") { t.borderRadiusLarge = v.AsInt(0); }
        else if (k == "borderWidth") { t.borderWidth = v.AsInt(0); }
        else if (k == "fontSizeTiny") { t.fontSizeTiny = v.AsInt(0); }
        else if (k == "fontSizeSmall") { t.fontSizeSmall = v.AsInt(0); }
        else if (k == "fontSizeMedium") { t.fontSizeMedium = v.AsInt(0); }
        else if (k == "fontSizeLarge") { t.fontSizeLarge = v.AsInt(0); }
        else if (k == "fontSizeHuge") { t.fontSizeHuge = v.AsInt(0); }
        else if (k == "heightTiny") { t.heightTiny = v.AsInt(0); }
        else if (k == "heightSmall") { t.heightSmall = v.AsInt(0); }
        else if (k == "heightMedium") { t.heightMedium = v.AsInt(0); }
        else if (k == "heightLarge") { t.heightLarge = v.AsInt(0); }
        else if (k == "paddingTiny") { t.paddingTiny = v.AsInt(0); }
        else if (k == "paddingSmall") { t.paddingSmall = v.AsInt(0); }
        else if (k == "paddingMedium") { t.paddingMedium = v.AsInt(0); }
        else if (k == "paddingLarge") { t.paddingLarge = v.AsInt(0); }
        else if (k == "gapSmall") { t.gapSmall = v.AsInt(0); }
        else if (k == "gapMedium") { t.gapMedium = v.AsInt(0); }
        else if (k == "gapLarge") { t.gapLarge = v.AsInt(0); }
        else if (k == "iconSizeSmall") { t.iconSizeSmall = v.AsInt(0); }
        else if (k == "iconSizeMedium") { t.iconSizeMedium = v.AsInt(0); }
        else if (k == "iconSizeLarge") { t.iconSizeLarge = v.AsInt(0); }
        else if (k == "iconSizeCaption") { t.iconSizeCaption = v.AsInt(0); }
        else if (k == "animFastMs") { t.animFastMs = v.AsInt(0); }
        else if (k == "animMediumMs") { t.animMediumMs = v.AsInt(0); }
        else if (k == "animSlowMs") { t.animSlowMs = v.AsInt(0); }
    }

    /// “base” token 命名的起始 Theme：“light” 或 “dark”（皮肤是
    /// CSS 打包的，因此皮肤绝不会基于另一皮肤的代码）。
    static Theme BaseTheme(JsonValue b) {
        if (b == null) { return Theme.Dark(); }
        string s = b.AsString("");
        if (s == "light" || s == "Light") { return Theme.Light(); }
        return Theme.Dark();
    }

    /// 将 JSON 对象中的每个 token 应用到 `t`（跳过 “base” 键）。
    static void ApplyTokens(Theme t, JsonValue obj) {
        if (obj == null || !obj.IsObject()) { return; }
        for (int i = 0; i < obj.Count(); i = i + 1) {
            string k = obj.KeyAt(i);
            if (k != "base") { Theme.SetToken(t, k, obj.ValAt(i)); }
        }
    }

    /// 从 JSON token 样式表构建 Theme：“base” token 选择
    /// 起始 Theme，其余覆盖单个 token。格式错误 / 空的
    /// 文档产生默认（Dark）主题，坏皮肤永远不会崩溃。
    static Theme FromTokens(string json) {
        JsonValue v = JsonValue.ParseLenient(json);
        if (v == null || !v.IsObject()) { return Theme.Dark(); }
        Theme t = Theme.BaseTheme(v.Get("base"));
        Theme.ApplyTokens(t, v);
        return t;
    }

    /// 按 DPI 百分比整体放大字号/高度/内边距/间距/图标/圆角等
    /// 度量；dpiPercent ≤ 100 时不变，边框宽下限钳制为 1。
    void ScaleByDpi(int dpiPercent) {
        if (dpiPercent <= 100) { return; }

        fontSizeTiny   = fontSizeTiny * dpiPercent / 100;
        fontSizeSmall  = fontSizeSmall * dpiPercent / 100;
        fontSizeMedium = fontSizeMedium * dpiPercent / 100;
        fontSizeLarge  = fontSizeLarge * dpiPercent / 100;
        fontSizeHuge   = fontSizeHuge * dpiPercent / 100;

        heightTiny  = heightTiny * dpiPercent / 100;
        heightSmall = heightSmall * dpiPercent / 100;
        heightMedium = heightMedium * dpiPercent / 100;
        heightLarge = heightLarge * dpiPercent / 100;

        paddingTiny  = paddingTiny * dpiPercent / 100;
        paddingSmall = paddingSmall * dpiPercent / 100;
        paddingMedium = paddingMedium * dpiPercent / 100;
        paddingLarge = paddingLarge * dpiPercent / 100;

        gapSmall  = gapSmall * dpiPercent / 100;
        gapMedium = gapMedium * dpiPercent / 100;
        gapLarge  = gapLarge * dpiPercent / 100;

        iconSizeSmall  = iconSizeSmall * dpiPercent / 100;
        iconSizeMedium = iconSizeMedium * dpiPercent / 100;
        iconSizeLarge  = iconSizeLarge * dpiPercent / 100;
        iconSizeCaption = iconSizeCaption * dpiPercent / 100;

        borderRadiusSmall  = borderRadiusSmall * dpiPercent / 100;
        borderRadiusMedium = borderRadiusMedium * dpiPercent / 100;
        borderRadiusLarge  = borderRadiusLarge * dpiPercent / 100;
        borderWidth        = borderWidth * dpiPercent / 100;
        if (borderWidth < 1) { borderWidth = 1; }
    }

    /// 把所有随缩放变化的度量（字号/高度/内边距/间距/图标/圆角/边框）
    /// 快照成一个列表，顺序与 ApplyMetricsScaled 一一对应。用于设计器
    /// 在渲染真实控件预览前保存基线，之后精确还原。
    List<int> SnapshotMetrics() {
        return new List<int>{
            fontSizeTiny, fontSizeSmall, fontSizeMedium, fontSizeLarge, fontSizeHuge,
            heightTiny, heightSmall, heightMedium, heightLarge,
            paddingTiny, paddingSmall, paddingMedium, paddingLarge,
            gapSmall, gapMedium, gapLarge,
            iconSizeSmall, iconSizeMedium, iconSizeLarge, iconSizeCaption,
            borderRadiusSmall, borderRadiusMedium, borderRadiusLarge, borderWidth };
    }

    /// 以 `bm`（SnapshotMetrics 的返回值）为基线，按 num/den 缩放所有
    /// 度量并写回。与 ScaleByDpi 不同，它支持缩小（num < den），且始终从
    /// 基线计算，所以用 num == den 调用即可精确还原、不累积舍入误差。
    void ApplyMetricsScaled(List<int> bm, int num, int den) {
        if (bm == null || bm.Count < 24 || den <= 0) { return; }
        fontSizeTiny   = bm[0] * num / den;
        fontSizeSmall  = bm[1] * num / den;
        fontSizeMedium = bm[2] * num / den;
        fontSizeLarge  = bm[3] * num / den;
        fontSizeHuge   = bm[4] * num / den;
        if (fontSizeTiny < 6) { fontSizeTiny = 6; }
        if (fontSizeSmall < 6) { fontSizeSmall = 6; }
        if (fontSizeMedium < 7) { fontSizeMedium = 7; }
        if (fontSizeLarge < 8) { fontSizeLarge = 8; }
        if (fontSizeHuge < 9) { fontSizeHuge = 9; }
        heightTiny  = bm[5] * num / den;
        heightSmall = bm[6] * num / den;
        heightMedium = bm[7] * num / den;
        heightLarge = bm[8] * num / den;
        paddingTiny  = bm[9] * num / den;
        paddingSmall = bm[10] * num / den;
        paddingMedium = bm[11] * num / den;
        paddingLarge = bm[12] * num / den;
        gapSmall  = bm[13] * num / den;
        gapMedium = bm[14] * num / den;
        gapLarge  = bm[15] * num / den;
        iconSizeSmall  = bm[16] * num / den;
        iconSizeMedium = bm[17] * num / den;
        iconSizeLarge  = bm[18] * num / den;
        iconSizeCaption = bm[19] * num / den;
        borderRadiusSmall  = bm[20] * num / den;
        borderRadiusMedium = bm[21] * num / den;
        borderRadiusLarge  = bm[22] * num / den;
        borderWidth        = bm[23] * num / den;
        if (borderWidth < 1) { borderWidth = 1; }
    }

    // ---- 密度档（大 / 中 / 小）---------------------------------------------
    // 全局 UI 密度：0 = 小（紧凑），1 = 中（默认），2 = 大（宽松）。
    // 中档 = 1000‰ 原始度量；小/大档在换肤、DPI 变化时也
    // 永远从快照基线重新推导，不叠加缩放。

    /// 档位对应的度量缩放比（千分比）。档位是经验值而非线性
    /// 百分比：小档字号别小于 12（可读性下限），大档约放大 15%。
    static int DensityPermille(int density) {
        if (density <= 0) { return 880; }
        if (density >= 2) { return 1150; }
        return 1000;
    }

    /// 当前密度档的显示名（选择器 / 配置持久化共用）。
    static string DensityName(int density) {
        if (density <= 0) { return "Small"; }
        if (density >= 2) { return "Large"; }
        return "Medium";
    }

    /// 密度档名（"small"/"medium"/"large"，大小写不敏感）转档位，
    /// 无法识别时返回 1（中）。
    static int DensityOf(string name) {
        string s = name;
        if (s == null) { return 1; }
        s = s.ToLower();
        if (s == "small" || s == "compact" || s == "tight") { return 0; }
        if (s == "large" || s == "comfortable" || s == "loose") { return 2; }
        return 1;
    }

    /// 以 `bm`（SnapshotMetrics 基线）为基线应用密度档：中档即基线，
    /// 小/大档整体缩放后按可读性/可点性钳制下限。与
    /// ApplyMetricsScaled 相同的基线语义——任何档位切换都从
    /// 快照重推，往返切换不累积舍入误差。
    void ApplyDensityScaled(List<int> bm, int density) {
        this.ApplyMetricsScaled(bm, Theme.DensityPermille(density), 1000);
    }
}

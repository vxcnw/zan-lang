{
  "version": 1,
  "note": "Hand-written half of knowledge/zform.json: the .zform document schema exactly as stdlib/System/Compiler/GenForm.zan reads it. The controls/ half is generated from the stdlib sources by scripts/gen_zform.ps1; tests/run_zform_schema.cmake fails if a key here stops existing in GenForm.zan or GenForm.zan starts reading a key that is not documented here. Designer-internal passthrough (not read by GenForm): a field may carry 'pvOff':true — the designer's canvas-preview toggle for user-component reference nodes; it round-trips through the unmodeled-keys channel.",
  "source": "stdlib/System/Compiler/GenForm.zan",
  "pipeline": ".zform (JSON) --zanc formgen--> partial class projection (no file on disk to edit) --> compiled together with the hand-written code-behind (src/App.zan, static void OnLoad(Form form)). Never edit generated layout code; edit the .zform.",
  "document": {
    "note": "Root object of a .zform document.",
    "keys": [
      {
        "key": "name",
        "type": "string",
        "default": "<file base name>, else \"Form\"",
        "doc": "Class name of the projected partial class; must be a valid Zan identifier or it falls back to the file's base name."
      },
      {
        "key": "role",
        "type": "string",
        "default": "window",
        "options": [
          "window",
          "control"
        ],
        "doc": "\"control\" projects an embeddable `partial class X : Control` (panel/view/card) whose named children are instance fields; a control document cannot be the project entry. Default/\"window\" projects a window whose named children are static fields."
      },
      {
        "key": "winTitle",
        "type": "string",
        "default": "<name>",
        "doc": "Window caption."
      },
      {
        "key": "winW",
        "type": "int",
        "default": 480,
        "doc": "Logical window width; values <= 0 are ignored."
      },
      {
        "key": "winH",
        "type": "int",
        "default": 520,
        "doc": "Logical window height; values <= 0 are ignored."
      },
      {
        "key": "winCenter",
        "type": "bool",
        "default": false,
        "doc": "Center the window on the work area instead of using winPosX/winPosY."
      },
      {
        "key": "winPosX",
        "type": "int",
        "default": 0,
        "doc": "Window left, used when winCenter is false."
      },
      {
        "key": "winPosY",
        "type": "int",
        "default": 0,
        "doc": "Window top, used when winCenter is false."
      },
      {
        "key": "winResizable",
        "type": "bool",
        "default": true,
        "doc": "Allow the user to resize the window."
      },
      {
        "key": "winTool",
        "type": "bool",
        "default": false,
        "doc": "Tool-window style: thin title bar, no taskbar/Alt-Tab entry; for palettes and dialogs."
      },
      {
        "key": "winChrome",
        "type": "bool",
        "default": true,
        "doc": "Draw the standard title bar / frame; false gives a chromeless window."
      },
      {
        "key": "winOpacity",
        "type": "int",
        "default": 0,
        "doc": "Window opacity in percent; 0 means opaque (no per-window alpha)."
      },
      {
        "key": "winGlass",
        "type": "bool",
        "default": false,
        "doc": "Blur-behind (acrylic) window background."
      },
      {
        "key": "winShape",
        "type": "int|array",
        "default": 0,
        "doc": "Silhouette. As an int: 0 = rectangular window, 1 = rounded rect (radius from winShapeRadius), 2 = ellipse filling the window box. As an array it is a region list, each region {t,x,y,w,h,r} in logical px with t: 1 = rect, 2 = ellipse, 3 = shadow band; regions with w<=0 or h<=0 are dropped."
      },
      {
        "key": "winShapeRadius",
        "type": "int",
        "default": 0,
        "doc": "Corner radius for winShape = 1."
      },
      {
        "key": "winRound",
        "type": "bool",
        "default": false,
        "doc": "Designer's \"round window\" switch; same silhouette as winShape = 2."
      },
      {
        "key": "winShadow",
        "type": "int",
        "default": 0,
        "doc": "Drop-shadow spread in logical px; prepends a type-3 shadow band to the silhouette (clamped to 8..64 including the largest corner radius). Only applies when the window has a silhouette."
      },
      {
        "key": "layoutMode",
        "type": "int",
        "default": 0,
        "doc": "0 = flow: fields are docked top-to-bottom inside an FbFlow grid and each field takes `span` of 24 columns. 1 = free: every field is positioned with fx/fy/fw/fh and `dock`."
      },
      {
        "key": "fields",
        "type": "array",
        "default": [],
        "doc": "Control tree; each element is a field object (see fields.keys). Containers nest through \"kids\"."
      }
    ]
  },
  "fields": {
    "note": "One object per control in \"fields\" / \"kids\". Every key is optional except \"kind\".",
    "keys": [
      {
        "key": "kind",
        "type": "string",
        "required": true,
        "doc": "The Zan *class name* of the control, e.g. \"Button\", \"Panel\", \"SelectBox\", \"CefBrowserBox\". The projection emits `new <kind>()`, so any Control class the compiler can resolve is legal - not just the designer palette. Note this is the class name, not Control.Kind() (SelectBox's Kind() is \"Select\"). Compile error if it is not a valid identifier."
      },
      {
        "key": "of",
        "type": "string",
        "default": "",
        "doc": "Type arguments for a generic control: \"kind\": \"ListView\", \"of\": \"string\" projects ListView<string>; comma-separate multiple arguments. Every argument must be a Zan type name (dotted names allowed). ListView/Dropdown/DataGrid default to \"string\" when \"of\" is absent."
      },
      {
        "key": "name",
        "type": "string",
        "default": "",
        "doc": "Field name for the projected control. A valid, unique identifier becomes a typed field (OkButton, TitleLabel, ...) reachable from the code-behind; otherwise the control stays anonymous. Also assigned to control.name."
      },
      {
        "key": "label",
        "type": "string",
        "default": "",
        "doc": "Design-time caption: projected as SetDesignText(...), which controls that publish a text property (Label/Button/Checkbox/...) use as their content; containers ignore it."
      },
      {
        "key": "class",
        "type": "string",
        "default": "",
        "doc": "CSS class(es) for the control (skin / size variants), projected as control.Class."
      },
      {
        "key": "props",
        "type": "object",
        "default": {},
        "doc": "Pass-through table of the control's own properties, projected as SetProp(key, value) - keys are the control's PropSpec keys (see controls[] in this file), values are strings. On a user-component reference node (with 'ref'), this object instead carries instance values for the component's declared props, keyed by declaration key; the generator forwards them to the declared target controls."
      },
      {
        "key": "ref",
        "type": "string",
        "default": "",
        "doc": "User-component reference: names a saved component (components/<ref>.zcomp). The generator expands the node into a Panel shell that keeps all of the node's own keys, with the component's fields attached as anonymous, shell-name-prefixed kids (internal events unwired). Component props declared in the .zcomp root ('props' array) receive instance values from this node's 'props' object keyed by the declaration's key."
      },
      {
        "key": "compAbs",
        "type": "bool",
        "default": false,
        "doc": "Generator-internal marker written by ref expansion: the expanded component uses absolute layout (the component doc's layoutMode was 1). Not authored by hand."
      },
      {
        "key": "compAnon",
        "type": "bool",
        "default": false,
        "doc": "Generator-internal marker written by ref expansion: the shell's kids are anonymous (no named fields or handler wiring are projected for them). Not authored by hand."
      },
      {
        "key": "options",
        "type": "array",
        "default": [],
        "doc": "String choices for choice controls; joined with '|' and projected as SetProp(\"options\", ...)."
      },
      {
        "key": "placeholder",
        "type": "string",
        "default": "",
        "doc": "Projected as SetProp(\"placeholder\", ...)."
      },
      {
        "key": "required",
        "type": "bool",
        "default": false,
        "doc": "Projected as SetProp(\"required\", \"true\") and included in the generated validation pass."
      },
      {
        "key": "defOn",
        "type": "bool",
        "default": false,
        "doc": "Default-on switch/checkbox; projected as SetProp(\"defOn\", \"true\")."
      },
      {
        "key": "bind",
        "type": "string",
        "default": "",
        "doc": "Two-way binding path into the state entity, projected as control.bindPath; the host syncs both sides around each frame."
      },
      {
        "key": "bindProp",
        "type": "string",
        "default": "",
        "doc": "Property written by \"bind\"; defaults to the control's value property. Ignored without \"bind\"."
      },
      {
        "key": "kids",
        "type": "array",
        "default": [],
        "doc": "Child field objects. A field with a \"kids\" array is treated as a container (padding/gap apply, children are projected into it)."
      },
      {
        "key": "childTab",
        "type": "int",
        "default": -1,
        "doc": "Slot index inside the parent container (tab page, split pane); children are added to parent.SlotHost(index) instead of the container itself. Without it, children of a multi-slot container all overlap."
      },
      {
        "key": "span",
        "type": "int",
        "default": 24,
        "doc": "Flow layout only (layoutMode 0): width of the field in a 24-column grid, clamped to 1..24."
      },
      {
        "key": "fx",
        "type": "int",
        "default": 0,
        "doc": "Free layout only (layoutMode 1, dock 0): logical x."
      },
      {
        "key": "fy",
        "type": "int",
        "default": 0,
        "doc": "Free layout only (layoutMode 1, dock 0): logical y."
      },
      {
        "key": "fw",
        "type": "int",
        "default": 0,
        "doc": "Preferred logical width. Omit it on a docked control to have the control measure itself; for dock 0 (absolute) it is the box width. Also used as the fixed extent for dock Left/Right."
      },
      {
        "key": "fh",
        "type": "int",
        "default": 0,
        "doc": "Preferred logical height (32 in flow layout when absent). Also used as the fixed extent for dock Top/Bottom."
      },
      {
        "key": "ph",
        "type": "int",
        "default": -1,
        "doc": "Container preferred height in px in flow layout; -1 keeps the default (44 + 44 per child). Ref expansion writes the component canvas height here so an expanded component keeps its designed height."
      },
      {
        "key": "dock",
        "type": "int",
        "default": 0,
        "options": [
          "0 = manual/absolute (fx,fy,fw,fh)",
          "1 = top",
          "2 = bottom",
          "3 = left",
          "4 = right",
          "5 = fill"
        ],
        "doc": "Free layout only (layoutMode 1)."
      },
      {
        "key": "pad",
        "type": "int",
        "default": -1,
        "doc": "Container inner padding in logical px (free layout, containers only). Absent means the style sheet decides."
      },
      {
        "key": "gap",
        "type": "int",
        "default": -1,
        "doc": "Container child spacing in logical px (free layout, containers only). Absent means the style sheet decides."
      },
      {
        "key": "on<Event>",
        "type": "string",
        "default": "",
        "doc": "Handler binding: any key starting with \"on\" binds the event named after the prefix to the handler named by the value, e.g. \"onClick\": \"Save\" binds Click to the code-behind's Save handler. Legal event names are the control's Events() (see controls[].events)."
      },
      {
        "key": "columns",
        "type": "array",
        "default": [],
        "doc": "DataGrid only: declarative typed columns (see columns.keys). Requires \"of\" naming the row entity type; each column projects a typed accessor lambda (`__r => __r.<field>`) onto the generic column API (Col/NumCol/RealCol/BoolCol/DateCol), so a wrong field name or type is a compile error on the generated code. Rows stay the caller's List<of> - bind data from the code-behind with grid.Bind(list); no DataRow / string-row conversion is involved."
      },
      {
        "key": "rowFactory",
        "type": "bool",
        "default": false,
        "doc": "DataGrid only: project RowFactory(() => new <of>()) so the add-row placeholder (allowAddRow) can append new entity rows. Requires \"of\"."
      }
    ]
  },
  "columns": {
    "note": "One object per DataGrid column in a field's \"columns\" array. Only \"field\" is required.",
    "keys": [
      {
        "key": "field",
        "type": "string",
        "required": true,
        "doc": "Field name on the row entity (the \"of\" type); becomes the typed accessor `__r => __r.<field>` and the engine-side stable column name (.Field). Must be an identifier (dotted paths allowed)."
      },
      {
        "key": "title",
        "type": "string",
        "default": "<field>",
        "doc": "Header caption."
      },
      {
        "key": "width",
        "type": "int",
        "default": 100,
        "doc": "Column width in logical px."
      },
      {
        "key": "type",
        "type": "string",
        "default": "text",
        "options": [
          "text",
          "num",
          "real",
          "bool",
          "date"
        ],
        "doc": "Column value type; selects the generic column API (Col/NumCol/RealCol/BoolCol/DateCol) and therefore the accessor's expected field type (string/int/double/bool/string)."
      },
      {
        "key": "decimals",
        "type": "int",
        "default": 2,
        "doc": "type \"real\" only: decimal places."
      },
      {
        "key": "dateOrder",
        "type": "int",
        "default": 0,
        "doc": "type \"date\" only: DateCol sort-order code."
      },
      {
        "key": "align",
        "type": "string",
        "default": "",
        "options": [
          "right",
          "center"
        ],
        "doc": "Cell alignment (numeric columns already right-align by default)."
      },
      {
        "key": "money",
        "type": "string",
        "default": "",
        "doc": "Currency symbol; projects .Money(symbol)."
      },
      {
        "key": "percent",
        "type": "bool",
        "default": false,
        "doc": "Render as percentage; projects .Percent()."
      },
      {
        "key": "prefix",
        "type": "string",
        "default": "",
        "doc": "Literal text before the value; with \"suffix\" projects .Affix(prefix, suffix)."
      },
      {
        "key": "suffix",
        "type": "string",
        "default": "",
        "doc": "Literal text after the value."
      },
      {
        "key": "sum",
        "type": "bool",
        "default": false,
        "doc": "Summary-row aggregate: total."
      },
      {
        "key": "avg",
        "type": "bool",
        "default": false,
        "doc": "Summary-row aggregate: average."
      },
      {
        "key": "min",
        "type": "bool",
        "default": false,
        "doc": "Summary-row aggregate: minimum."
      },
      {
        "key": "max",
        "type": "bool",
        "default": false,
        "doc": "Summary-row aggregate: maximum."
      },
      {
        "key": "count",
        "type": "bool",
        "default": false,
        "doc": "Summary-row aggregate: row count."
      },
      {
        "key": "pin",
        "type": "string",
        "default": "",
        "options": [
          "left",
          "right"
        ],
        "doc": "Freeze the column at the given edge; projects .Pin() / .PinRight()."
      },
      {
        "key": "band",
        "type": "string",
        "default": "",
        "doc": "Header band (group) path; projects .Band(path)."
      },
      {
        "key": "sortable",
        "type": "bool",
        "default": true,
        "doc": "false projects .NotSortable()."
      },
      {
        "key": "resizable",
        "type": "bool",
        "default": true,
        "doc": "false projects .NotResizable()."
      },
      {
        "key": "grouped",
        "type": "bool",
        "default": false,
        "doc": "Group rows by this column initially; projects .Grouped()."
      },
      {
        "key": "editable",
        "type": "bool",
        "default": false,
        "doc": "In-cell editing; projects .Editable((__r, __v) => ...) writing the parsed value back to the entity field by column type."
      }
    ]
  },
  "commonEvents": [
    "Click",
    "DoubleClick",
    "RightClick",
    "MouseDown",
    "MouseUp",
    "Enter",
    "Leave",
    "Move",
    "Wheel",
    "Focus",
    "Blur",
    "KeyDown",
    "KeyUp",
    "KeyPress",
    "Tap",
    "LongPress",
    "Swipe",
    "DragStart",
    "Drag",
    "Drop",
    "Resize",
    "Show",
    "Hide"
  ],
  "propKinds": {
    "0": "text",
    "1": "integer",
    "2": "boolean",
    "3": "enum",
    "4": "color",
    "5": "section"
  }
}

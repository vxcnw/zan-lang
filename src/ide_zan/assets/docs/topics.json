{
 "version": 1,
 "sections": [
  {
   "en": "Getting Started",
   "zh": "入门",
   "cats": [
    "Getting Started"
   ]
  },
  {
   "en": "Language",
   "zh": "语言",
   "cats": [
    "Language",
    "Type System"
   ]
  },
  {
   "en": "Standard Library",
   "zh": "标准库",
   "cats": [
    "Collections & Strings",
    "Files & IO",
    "Text & Regex",
    "JSON",
    "LINQ",
    "Async & Threads",
    "Cryptography",
    "Diagnostics",
    "Standard Library"
   ]
  },
  {
   "en": "Networking",
   "zh": "网络",
   "cats": [
    "HTTP Client",
    "HTTP Server",
    "WebSocket",
    "Sockets",
    "Messaging",
    "Remote Calls",
    "Web Framework",
    "Networking"
   ]
  },
  {
   "en": "Database",
   "zh": "数据库",
   "cats": [
    "Database",
    "SQL Basics",
    "ORM",
    "Query Builder",
    "Redis",
    "ZanDb"
   ]
  },
  {
   "en": "GUI",
   "zh": "界面",
   "cats": [
    "GUI - Basics",
    "GUI - Widgets",
    "GUI - Skins & CSS"
   ]
  },
  {
   "en": "Project & Build",
   "zh": "项目与构建",
   "cats": [
    "Project & Build",
    "Errors"
   ]
  }
 ],
 "topics": [
  {
   "cat": {
    "en": "Getting Started",
    "zh": "入门"
   },
   "title": {
    "en": "Hello World",
    "zh": "Hello World"
   },
   "body": {
    "en": "Every Zan program starts in a static Main method. Console.WriteLine prints a line to standard output.",
    "zh": "每个 Zan 程序都从静态 Main 方法开始。Console.WriteLine 向标准输出打印一行。"
   },
   "code": "using System;\n\nclass Program {\n    static void Main() {\n        Console.WriteLine(\"Hello, Zan!\");\n    }\n}"
  },
  {
   "cat": {
    "en": "Getting Started",
    "zh": "入门"
   },
   "title": {
    "en": "Comments",
    "zh": "注释"
   },
   "body": {
    "en": "Use // for a single-line comment and /* ... */ for a block comment. Comments are ignored by the compiler.",
    "zh": "使用 // 写单行注释，使用 /* ... */ 写块注释。注释会被编译器忽略。"
   },
   "code": "// a single-line comment\nint x = 1; // trailing comment\n/*\n  a block comment\n  spanning lines\n*/"
  },
  {
   "cat": {
    "en": "Getting Started",
    "zh": "入门"
   },
   "title": {
    "en": "Command Line Arguments",
    "zh": "命令行参数"
   },
   "body": {
    "en": "Environment.ArgCount() returns how many arguments the program was launched with and ArgAt(i) reads one. Index 0 is the executable path itself, so real options start at 1.",
    "zh": "Environment.ArgCount() 返回程序启动时收到的参数个数，ArgAt(i) 读取某一个。下标 0 是可执行文件自身的路径，真正的选项从 1 开始。"
   },
   "code": "using System;\n\nclass Program {\n    static void Main() {\n        int argc = Environment.ArgCount();\n        for (int i = 1; i < argc; i = i + 1) {\n            Console.WriteLine(Environment.ArgAt(i));\n        }\n    }\n}"
  },
  {
   "cat": {
    "en": "Getting Started",
    "zh": "入门"
   },
   "title": {
    "en": "Console Input & Colors",
    "zh": "控制台输入与颜色"
   },
   "body": {
    "en": "Console.ReadLine reads a whole line of text, ReadKey waits for the next key press. Write prints without a newline, WriteLine adds one. ForegroundColor / BackgroundColor pick a ConsoleColor for the text that follows; ResetColor restores the defaults.",
    "zh": "Console.ReadLine 读取一整行文本，ReadKey 等待下一次按键。Write 不换行，WriteLine 换行。ForegroundColor / BackgroundColor 设置后续文本的前景/背景 ConsoleColor；ResetColor 恢复默认。"
   },
   "code": "using System;\n\nstring line = Console.ReadLine();       // a whole line, \"\" at EOF\nint key = Console.ReadKey();           // next key press\n\nConsole.Write(\"no newline\");\nConsole.WriteLine(\"with newline\");\n\nConsole.ForegroundColor = ConsoleColor.Green;\nConsole.WriteLine(\"green text\");\nConsole.ResetColor();"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Variables & Types",
    "zh": "变量与类型"
   },
   "body": {
    "en": "Declare a variable with its type followed by a name. Common built-in types are int, double, bool and string.",
    "zh": "声明变量时先写类型再写名称。常见内置类型有 int、double、bool 和 string。"
   },
   "code": "int count = 3;\ndouble ratio = 0.5;\nbool ready = true;\nstring name = \"Zan\";"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Operators",
    "zh": "运算符"
   },
   "body": {
    "en": "Arithmetic (+ - * / %), comparison (== != < > <= >=) and logical (&& || !) operators work as in C-family languages.",
    "zh": "算术（+ - * / %）、比较（== != < > <= >=）和逻辑（&& || !）运算符与 C 系语言一致。"
   },
   "code": "int sum = a + b;\nbool equal = (a == b);\nbool both = ready && equal;\nint rem = a % b;"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "If / Else",
    "zh": "If / Else 条件"
   },
   "body": {
    "en": "Branch with if/else if/else. Conditions are boolean expressions.",
    "zh": "使用 if / else if / else 分支。条件为布尔表达式。"
   },
   "code": "if (score >= 90) {\n    grade = \"A\";\n} else if (score >= 80) {\n    grade = \"B\";\n} else {\n    grade = \"C\";\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "While Loop",
    "zh": "While 循环"
   },
   "body": {
    "en": "A while loop repeats while its condition holds. Use it when the number of iterations is not known up front; prefer for when you are counting and foreach when you are walking a collection.",
    "zh": "while 在条件成立时反复执行。适合次数事先未知的循环；计数用 for，遍历集合用 foreach。"
   },
   "code": "// while suits loops with no fixed count: read until the stream ends\nstring buffer = \"\";\nwhile (true) {\n    string chunk = reader.Read(4096);\n    if (chunk.Length == 0) { break; }\n    buffer = buffer + chunk;\n}\n\n// counting loops read better as for, and collections as foreach\nfor (int i = 0; i < 10; i = i + 1) { Console.WriteLine(Convert.ToString(i)); }"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "For Loop",
    "zh": "For 循环"
   },
   "body": {
    "en": "A for loop packs initialization, condition and step into one header.",
    "zh": "for 循环把初始化、条件和步进合并到一个头部。"
   },
   "code": "for (int i = 0; i < 10; i = i + 1) {\n    Console.WriteLine(Convert.ToString(i));\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Foreach Loop",
    "zh": "Foreach 循环"
   },
   "body": {
    "en": "foreach visits every item in a collection without manually managing an index.",
    "zh": "foreach 会依次访问集合中的每个元素，不需要手动维护索引。"
   },
   "code": "List<int> numbers = new List<int> { 7, 11 };\nforeach (int number in numbers) {\n    Console.WriteLine(Convert.ToString(number));\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "String Interpolation",
    "zh": "字符串插值"
   },
   "body": {
    "en": "Prefix a string with $ and place expressions inside braces to format readable output.",
    "zh": "在字符串前加 $，并把表达式放进花括号中，即可生成易读的格式化文本。"
   },
   "code": "string name = \"Zan\";\nint version = 1;\nConsole.WriteLine($\"Hello {name} v{version}\");"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Functions",
    "zh": "函数"
   },
   "body": {
    "en": "Functions are static methods: return type, name, typed parameters, then a body. Use return to yield a value.",
    "zh": "函数即静态方法：返回类型、名称、带类型的参数，然后是函数体。用 return 返回值。"
   },
   "code": "static int Add(int a, int b) {\n    return a + b;\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Classes & Objects",
    "zh": "类与对象"
   },
   "body": {
    "en": "A class groups fields and methods. Fields are declared with a type; methods can read them directly.",
    "zh": "类把字段和方法组织在一起。字段带类型声明；方法可直接读取它们。"
   },
   "code": "class Point {\n    int x;\n    int y;\n    int Sum() { return x + y; }\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Creating Objects",
    "zh": "创建对象"
   },
   "body": {
    "en": "Create an instance with new, then access members with the dot operator.",
    "zh": "用 new 创建实例，然后用点运算符访问成员。"
   },
   "code": "Point p = new Point();\np.x = 3;\np.y = 4;\nint s = p.Sum();"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "var & const",
    "zh": "类型推断与常量"
   },
   "body": {
    "en": "var infers the variable type from its initializer. const declares a compile-time constant, which must be computable at compile time and never changes.",
    "zh": "var 从初始化表达式推断变量类型。const 声明编译期常量，其值必须在编译期可算出，且之后不可改变。"
   },
   "code": "var count = 3;                // int\nvar name = \"Zan\";               // string\nvar items = new List<int>();    // List<int>\n\nconst int MaxRetries = 3;\nconst string AppName = \"Zan\";"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Switch Statement",
    "zh": "Switch 语句"
   },
   "body": {
    "en": "switch tests a value against constant cases; empty case bodies fall through to the next case, a non-empty body must end with break (or return). default handles everything else. Type patterns (case Button b:) are also supported.",
    "zh": "switch 用常量 case 与值比较；空 case 体会贯穿到下一个 case，非空 case 体必须以 break（或 return）结束。default 兜底。也支持类型模式（case Button b:）。"
   },
   "code": "string grade = \"\";\nswitch (score / 10) {\n    case 10:\n    case 9:\n        grade = \"A\";\n        break;\n    case 8:\n        grade = \"B\";\n        break;\n    default:\n        grade = \"F\";\n        break;\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Ternary & Null-Coalescing",
    "zh": "三元与空值合并"
   },
   "body": {
    "en": "cond ? a : b evaluates to a when the condition is true, b otherwise. x ?? y yields x when it is not null, y otherwise - the compact way to supply a fallback for a nullable value.",
    "zh": "cond ? a : b 在条件成立时取 a，否则取 b。x ?? y 在 x 不为 null 时取 x，否则取 y——给可空值提供后备的紧凑写法。"
   },
   "code": "int score = 82;\nstring result = score >= 60 ? \"pass\" : \"fail\";\n\nstring? nickname = null;\nstring shown = nickname ?? \"anonymous\";"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Do-While & Loop Control",
    "zh": "Do-While 与循环控制"
   },
   "body": {
    "en": "A do-while runs its body once before testing the condition. Inside any loop, break exits the loop immediately and continue jumps to the next iteration.",
    "zh": "do-while 先执行一次循环体再判断条件。在任何循环里，break 立即退出循环，continue 跳到下一次迭代。"
   },
   "code": "int i = 0;\ndo {\n    i = i + 1;\n} while (i < 5);\n\nint sum = 0;\nfor (int n = 0; n < 10; n = n + 1) {\n    if (n % 2 == 0) { continue; }   // skip evens\n    if (sum > 20) { break; }        // stop early\n    sum = sum + n;\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "ref & out Parameters",
    "zh": "ref 与 out 参数"
   },
   "body": {
    "en": "out parameters are written by the callee: int.TryParse(text, out int v) stores the parsed value in v and returns whether it succeeded. Dictionary.TryGetValue works the same way. The out variable can be declared inline at the call site.",
    "zh": "out 参数由被调方写入：int.TryParse(text, out int v) 把解析结果存入 v 并返回是否成功。Dictionary.TryGetValue 同样如此。out 变量可以在调用处直接声明。"
   },
   "code": "bool ok = int.TryParse(\"42\", out int parsed);\nif (ok) {\n    Console.WriteLine(Convert.ToString(parsed));\n}\n\nDictionary<string, int> ages = new Dictionary<string, int>();\nages.Add(\"alice\", 30);\nif (ages.TryGetValue(\"alice\", out int age)) {\n    Console.WriteLine(Convert.ToString(age));\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Lambdas & Delegates",
    "zh": "Lambda 与委托"
   },
   "body": {
    "en": "A delegate names a function shape; a lambda is an inline function that matches it. Lambdas are the standard way to pass behavior around: collection filters, sort keys and UI callbacks all take them.",
    "zh": "delegate 声明一种函数形态；lambda 是与之匹配的内联函数。传递行为是 lambda 的典型用途：集合筛选、排序键、UI 回调都接收 lambda。"
   },
   "code": "delegate int BinOp(int a, int b);\n\nstatic int Add(int a, int b) { return a + b; }\n\nBinOp op = Add;                  // method group\nBinOp mul = (int x, int y) => x * y;   // lambda\nint total = op(2, 3) + mul(2, 3);\n\nList<int> evens = numbers.Where((int n) => n % 2 == 0).ToList();\nbutton.OnClick(() => Save());"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Exceptions: try / catch / finally",
    "zh": "异常处理"
   },
   "body": {
    "en": "try wraps code that may fail; catch (Exception e) handles it, with e.Message carrying the reason. More specific catch clauses run before broader ones. finally always runs, with or without an exception. throw raises a new exception.",
    "zh": "try 包住可能失败的代码；catch (Exception e) 处理异常，e.Message 是原因。更具体的 catch 分支先于宽泛分支执行。finally 无论是否抛出异常都会执行。throw 抛出新的异常。"
   },
   "code": "try {\n    string content = File.ReadAllText(\"notes.txt\");\n    Console.WriteLine(content);\n} catch (Exception e) {\n    Console.WriteLine(\"error: \" + e.Message);\n} finally {\n    Console.WriteLine(\"done\");\n}\n\nif (name.Length == 0) {\n    throw new ArgumentException(\"name is required\");\n}"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "String Literals & Escapes",
    "zh": "字符串字面量与转义"
   },
   "body": {
    "en": "Regular strings process escape sequences (\\n \\r \\t \\\\ \\\" \\0). Verbatim strings @\"...\" keep backslashes as-is, handy for paths. $\"...\" interpolates expressions in braces. Character literals use single quotes.",
    "zh": "普通字符串处理转义序列（\\n \\r \\t \\\\ \\\" \\0）。逐字字符串 @\"...\" 保留反斜杠原样，适合写路径。$\"...\" 在花括号里插入表达式。字符字面量用单引号。"
   },
   "code": "string escaped = \"line1\\nline2\";       // newline inside\nstring verbatim = @\"C:\\path\\to\\file\";   // backslashes kept\nstring interp = $\"v{version}\";\nchar newline = '\\n';"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Namespace Declarations",
    "zh": "声明命名空间"
   },
   "body": {
    "en": "A file-scoped namespace applies to the whole file; the block form scopes a section. Types inside a namespace are referenced by their short name within it, and by the full name (or a using import) elsewhere.",
    "zh": "文件级命名空间作用于整个文件；块形式限定一段作用域。命名空间内的类型在内部用短名引用，外部用完整名或 using 导入后引用。"
   },
   "code": "namespace MyApp.Core;      // file-scoped\n\nclass Helper { }\n\nnamespace MyApp.Core {        // block-scoped\n    class Another { }\n}"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Constructors",
    "zh": "构造函数"
   },
   "body": {
    "en": "A constructor has the same name as the class and runs when you use new. It initializes fields.",
    "zh": "构造函数与类同名，在使用 new 时运行，用于初始化字段。"
   },
   "code": "class Point {\n    int x;\n    int y;\n    Point(int px, int py) {\n        x = px;\n        y = py;\n    }\n}\n\nPoint p = new Point(3, 4);"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Inheritance",
    "zh": "继承"
   },
   "body": {
    "en": "A class can extend a base class, inheriting its fields and methods, and override virtual methods.",
    "zh": "类可以继承基类，获得其字段和方法，并可重写虚方法。"
   },
   "code": "class Shape {\n    virtual double Area() { return 0.0; }\n}\n\nclass Circle : Shape {\n    double r;\n    override double Area() { return 3.14159 * r * r; }\n}"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Interfaces",
    "zh": "接口"
   },
   "body": {
    "en": "An interface declares methods a class must implement. Classes implement it with the : syntax.",
    "zh": "接口声明类必须实现的方法。类通过 : 语法实现接口。"
   },
   "code": "interface IShape {\n    double Area();\n}\n\nclass Square : IShape {\n    double side;\n    double Area() { return side * side; }\n}"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Generic Collections",
    "zh": "泛型集合"
   },
   "body": {
    "en": "List<T> is the built-in generic collection: the element type goes in angle brackets. Add appends, Count is the length and [i] reads an element. The same List<T> works for any element type.",
    "zh": "List<T> 是内置的泛型集合：元素类型写在尖括号中。Add 追加，Count 为长度，[i] 读取元素。同一个 List<T> 适用于任意元素类型。"
   },
   "code": "List<int> numbers = new List<int> { 1, 2, 3 };\n\nint sum = 0;\nforeach (int n in numbers) {\n    sum = sum + n;\n}\nConsole.WriteLine(\"sum = \" + Convert.ToString(sum));\n\nList<string> names = new List<string> { \"Zan\", \"lang\" };\nConsole.WriteLine(names[0] + \" \" + names[1]);"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Nullable Values",
    "zh": "可空值"
   },
   "body": {
    "en": "T? represents a value that may be null. Use ?? to provide a fallback value.",
    "zh": "T? 表示可能为 null 的值；使用 ?? 提供后备值。"
   },
   "code": "string? nickname = null;\nstring shown = nickname ?? \"anonymous\";\nConsole.WriteLine(shown);"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Enums",
    "zh": "枚举"
   },
   "body": {
    "en": "An enum defines a fixed set of named values.",
    "zh": "枚举定义一组固定的命名值。"
   },
   "code": "enum Status {\n    Pending,\n    Running,\n    Done\n}\n\nStatus current = Status.Running;"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Arrays",
    "zh": "数组"
   },
   "body": {
    "en": "T[] is a fixed-size array: new int[5] zero-initializes five slots, Length is the element count, and [i] reads or writes one element. Initializers size the array automatically. byte[] is the binary-data type, and arrays work in foreach.",
    "zh": "T[] 是定长数组：new int[5] 生成 5 个零值槽位，Length 是元素个数，[i] 读写单个元素。带初始化器时数组长度自动确定。byte[] 是二进制数据的载体，数组可用于 foreach。"
   },
   "code": "int[] a = new int[5];      // zero-initialized\nint n = a.Length;            // 5\na[0] = 7;\n\nint[] b = new int[] { 1, 2, 3 };   // sized by the initializer\nbyte[] raw = new byte[] { 0x01, 0xFF };\n\nforeach (int v in b) {\n    Console.WriteLine(Convert.ToString(v));\n}"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Structs",
    "zh": "结构体"
   },
   "body": {
    "en": "A struct is a value type: it lives on the stack (or inline inside its container) and is copied on assignment, so two variables never share one instance. Structs can have constructors and methods but cannot inherit.",
    "zh": "struct 是值类型：分配在栈上（或内嵌在容器里），赋值即拷贝，两个变量永远不会共享同一实例。结构体可以有构造函数和方法，但不能继承。"
   },
   "code": "struct Vector2 {\n    double X;\n    double Y;\n    Vector2(double x, double y) { X = x; Y = y; }\n    double Length() { return Math.Sqrt(X * X + Y * Y); }\n}\n\nVector2 a = new Vector2(1, 2);\nVector2 b = a;          // copy - b.X = 99 leaves a.X alone"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Properties",
    "zh": "属性"
   },
   "body": {
    "en": "A property pairs a getter and an optional setter, so reads and writes can run code (validation, invalidation) instead of touching a field directly. Inside the setter, value is the incoming value.",
    "zh": "属性把 getter 和可选的 setter 配对，读写时可以执行代码（校验、失效通知）而不是直接碰字段。setter 内部用 value 表示传入值。"
   },
   "code": "class Counter {\n    int _value;\n    int Value {\n        get { return _value; }\n        set { _value = value; }        // `value` is the implicit parameter\n    }\n    string Label { get { return \"count=\" + Convert.ToString(_value); } }\n}\n\nCounter c = new Counter();\nc.Value = 5;                          // calls the setter\nConsole.WriteLine(c.Label);           // calls the getter"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Custom Generics",
    "zh": "自定义泛型"
   },
   "body": {
    "en": "A class or method can take type parameters in angle brackets, so one definition serves every element type. where T : IShape constrains T to types that implement the interface (or a base class, or struct for value types).",
    "zh": "类或方法可以在尖括号里携带类型参数，一个定义服务所有元素类型。where T : IShape 把 T 限制为实现该接口的类型（也可以是基类，或 struct 限定值类型）。"
   },
   "code": "class Box<T> {\n    T item;\n    void Put(T v) { item = v; }\n    T Get() { return item; }\n}\n\ninterface IShape { double Area(); }\nclass Shapes<T> where T : IShape { }\n\nBox<int> ints = new Box<int>();\nints.Put(7);\nint v = ints.Get();"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Weak References",
    "zh": "弱引用"
   },
   "body": {
    "en": "Normal class fields keep their object alive (ARC). A weak field does not: it holds the object without preventing its deallocation, which breaks reference cycles such as parent-child back-pointers.",
    "zh": "普通类字段会保持对象存活（ARC）。weak 字段不会：它持有对象但不阻止其释放，用来打破父子互相引用这类引用环。"
   },
   "code": "class Parent {\n    Child? child;\n}\n\nclass Child {\n    weak Parent? parent;    // weak - does not keep the Parent alive\n}"
  },
  {
   "cat": {
    "en": "Type System",
    "zh": "类型系统"
   },
   "title": {
    "en": "Casting & Type Tests",
    "zh": "类型转换与类型测试"
   },
   "body": {
    "en": "(T)x converts x to type T; narrowing a double to int requires such a cast. is tests a value's type at runtime and, with a pattern variable, binds it in the same step.",
    "zh": "(T)x 把 x 转换成类型 T；double 收窄为 int 必须这样显式转换。is 在运行时测试值的类型，配合模式变量可以在同一句里完成测试并绑定。"
   },
   "code": "object o = \"text\";\nif (o is string s) {          // type test + bind\n    Console.WriteLine(s.Length);\n}\n\nint n = (int)Math.Max(1, 2);  // double -> int needs a cast"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "Lists",
    "zh": "列表"
   },
   "body": {
    "en": "List<T> is a growable array. Add appends, Count is the length, and [i] indexes.",
    "zh": "List<T> 是可增长的数组。Add 追加，Count 为长度，[i] 用于索引。"
   },
   "code": "List<string> items = new List<string> { \"first\", \"second\" };\nint n = items.Count;\nstring a = items[0];"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "Iterating a List",
    "zh": "遍历列表"
   },
   "body": {
    "en": "foreach walks every element without an index; use for when you also need the position.",
    "zh": "foreach 直接遍历每个元素，不需要索引；需要下标时再用 for。"
   },
   "code": "List<string> items = new List<string> { \"alpha\", \"beta\", \"gamma\" };\n\nforeach (string item in items) {\n    Console.WriteLine(item);\n}\n\n// when the position matters, index with for\nfor (int i = 0; i < items.Count; i = i + 1) {\n    Console.WriteLine(Convert.ToString(i) + \": \" + items[i]);\n}"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "Strings",
    "zh": "字符串"
   },
   "body": {
    "en": "Strings support Length, Substring(start, len) and concatenation with +.",
    "zh": "字符串支持 Length、Substring(start, len) 以及用 + 拼接。"
   },
   "code": "string s = \"Zan\";\nint len = s.Length;\nstring sub = s.Substring(0, 2);\nstring joined = s + \" lang\";"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "Convert & Parse",
    "zh": "转换与解析"
   },
   "body": {
    "en": "Convert.ToString formats a value as text. Convert.ToInt32 parses an integer from text.",
    "zh": "Convert.ToString 把值格式化为文本；Convert.ToInt32 从文本解析整数。"
   },
   "code": "int n = Convert.ToInt32(\"42\");\nstring text = Convert.ToString(n);\nConsole.WriteLine(\"n = \" + text);"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "Dictionary",
    "zh": "字典"
   },
   "body": {
    "en": "Dictionary<K,V> maps keys to values. Add inserts a new pair, the indexer reads and writes, ContainsKey tests presence and TryGetValue combines both without throwing. Keys / Values list the sides, Remove deletes.",
    "zh": "Dictionary<K,V> 建立键到值的映射。Add 插入新键值对，索引器读写，ContainsKey 判断存在，TryGetValue 一次完成查询且不抛异常。Keys / Values 列出两侧，Remove 删除。"
   },
   "code": "Dictionary<string, int> ages = new Dictionary<string, int>();\nages.Add(\"alice\", 30);\nages[\"bob\"] = 25;                  // set via indexer\n\nif (ages.TryGetValue(\"bob\", out int bobAge)) {\n    Console.WriteLine(Convert.ToString(bobAge));\n}\nint count = ages.Count;\nList<string> keys = ages.Keys;\nages.Remove(\"bob\");"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "String Methods",
    "zh": "字符串方法"
   },
   "body": {
    "en": "Strings are immutable; every method returns a new string or value. Split cuts on a separator into a List<string>, Join (the other way) merges a list, and Contains / StartsWith / EndsWith / IndexOf search. Replace, Trim, ToUpper / ToLower and Substring shape text.",
    "zh": "字符串不可变，每个方法都返回新字符串或新值。Split 按分隔符切成 List<string>，Join 反向合并列表，Contains / StartsWith / EndsWith / IndexOf 负责查找。Replace、Trim、ToUpper / ToLower、Substring 负责整形。"
   },
   "code": "string s = \"zan,lang\";\nList<string> parts = s.Split(\",\");\nstring joined = String.Join(\"-\", parts);      // \"zan-lang\"\n\nbool has = s.Contains(\"an\");\nbool start = s.StartsWith(\"za\");\nint i = s.IndexOf(\",\");\nstring up = s.ToUpper();\nstring t = \"  x  \".Trim();\nstring r = s.Replace(\",\", \"!\");\nstring sub = s.Substring(0, 3);"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "String.Format & Join",
    "zh": "String.Format 与 Join"
   },
   "body": {
    "en": "String.Format(pattern, args...) fills {0}, {1} placeholders with arguments in order. String.Join(separator, list) merges a list of strings. IsNullOrEmpty is the safe emptiness test.",
    "zh": "String.Format(模板, 参数...) 按顺序把 {0}、{1} 占位符替换成参数。String.Join(分隔符, 列表) 合并字符串列表。IsNullOrEmpty 是安全的空值判断。"
   },
   "code": "string a = String.Format(\"{0} scored {1}\", \"alice\", 95);\nstring b = $\"alice scored {95}\";\nstring c = String.Join(\", \", new List<string> { \"x\", \"y\" });\nif (!String.IsNullOrEmpty(a)) { Console.WriteLine(a); }"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "StringBuilder",
    "zh": "StringBuilder"
   },
   "body": {
    "en": "String concatenation in a loop copies the string each time; StringBuilder appends in place. Use it to assemble large text: Append / AppendLine add pieces, ToString produces the final string.",
    "zh": "循环里做字符串拼接每次都会复制整串；StringBuilder 原地追加。组装大文本时用它：Append / AppendLine 追加片段，ToString 产出最终字符串。"
   },
   "code": "StringBuilder sb = new StringBuilder();\nfor (int i = 0; i < 100; i = i + 1) {\n    sb.AppendLine(Convert.ToString(i));\n}\nstring text = sb.ToString();"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "Stack, Queue & HashSet",
    "zh": "栈、队列与集合"
   },
   "body": {
    "en": "Stack<T> is last-in-first-out (Push / Pop / Peek), Queue<T> is first-in-first-out (Enqueue / Dequeue), and HashSet<T> tracks unique items (Add returns false for duplicates, Contains tests membership).",
    "zh": "Stack<T> 后进先出（Push / Pop / Peek），Queue<T> 先进先出（Enqueue / Dequeue），HashSet<T> 维护唯一元素（Add 遇到重复返回 false，Contains 判断成员）。"
   },
   "code": "using System.Collections;\n\nStack<string> undo = new Stack<string>();\nundo.Push(\"a\");\nstring top = undo.Peek();\nstring last = undo.Pop();\n\nQueue<int> jobs = new Queue<int>();\njobs.Enqueue(1);\nint next = jobs.Dequeue();\n\nHashSet<string> seen = new HashSet<string>();\nseen.Add(\"x\");\nbool dup = !seen.Add(\"x\");        // false: already present\nbool inSet = seen.Contains(\"x\");"
  },
  {
   "cat": {
    "en": "Collections & Strings",
    "zh": "集合与字符串"
   },
   "title": {
    "en": "Sorting a List",
    "zh": "排序列表"
   },
   "body": {
    "en": "Sort sorts a List<int>, List<double> or List<string> ascending in place; Reverse flips the order. For other element types or custom orderings, use the LINQ OrderBy / OrderByDescending chains.",
    "zh": "Sort 对 List<int>、List<double> 或 List<string> 原地升序排序；Reverse 反转顺序。其它元素类型或自定义顺序用 LINQ 的 OrderBy / OrderByDescending 链。"
   },
   "code": "List<int> nums = new List<int> { 3, 1, 2 };\nnums.Sort();          // { 1, 2, 3 }\nnums.Reverse();       // { 3, 2, 1 }\n\nList<string> words = new List<string> { \"bb\", \"a\" };\nwords.Sort();         // { \"a\", \"bb\" }"
  },
  {
   "cat": {
    "en": "Language",
    "zh": "语言"
   },
   "title": {
    "en": "Namespaces & using",
    "zh": "命名空间与 using"
   },
   "body": {
    "en": "Bring library types into scope with using at the top of the file.",
    "zh": "在文件顶部用 using 把库中的类型引入作用域。"
   },
   "code": "using System;\nusing Gui;\nusing Gui.Widget;"
  },
  {
   "cat": {
    "en": "Files & IO",
    "zh": "文件与 IO"
   },
   "title": {
    "en": "Read a File",
    "zh": "读取文件"
   },
   "body": {
    "en": "File.ReadAllText reads an entire text file into a string. File.Exists checks first.",
    "zh": "File.ReadAllText 把整个文本文件读入字符串。可先用 File.Exists 检查。"
   },
   "code": "using System.IO;\n\nif (File.Exists(\"notes.txt\")) {\n    string content = File.ReadAllText(\"notes.txt\");\n    Console.WriteLine(content);\n}"
  },
  {
   "cat": {
    "en": "Files & IO",
    "zh": "文件与 IO"
   },
   "title": {
    "en": "Write a File",
    "zh": "写入文件"
   },
   "body": {
    "en": "File.WriteAllText writes a string to a file, creating or overwriting it.",
    "zh": "File.WriteAllText 把字符串写入文件，若不存在则创建，存在则覆盖。"
   },
   "code": "using System.IO;\n\nFile.WriteAllText(\"out.txt\", \"Hello from Zan\");"
  },
  {
   "cat": {
    "en": "Files & IO",
    "zh": "文件与 IO"
   },
   "title": {
    "en": "List a Directory",
    "zh": "列出目录"
   },
   "body": {
    "en": "Directory.GetFiles returns the files in a folder; loop over them by index.",
    "zh": "Directory.GetFiles 返回文件夹中的文件；按索引遍历它们。"
   },
   "code": "using System.IO;\n\nList<string> files = Directory.GetFiles(\".\");\nforeach (string file in files) {\n    Console.WriteLine(file);\n}"
  },
  {
   "cat": {
    "en": "Files & IO",
    "zh": "文件与 IO"
   },
   "title": {
    "en": "Read & Write Lines",
    "zh": "按行读写"
   },
   "body": {
    "en": "ReadAllLines returns the file as a list of lines, WriteAllLines writes one. AppendAllText adds to the end of a file, creating it if needed - the usual way to keep a log.",
    "zh": "ReadAllLines 把文件读成字符串列表，WriteAllLines 写入列表。AppendAllText 追加到文件末尾（不存在则创建）——记日志的常规做法。"
   },
   "code": "using System.IO;\n\nList<string> lines = File.ReadAllLines(\"data.txt\");\nFile.WriteAllLines(\"out.txt\", lines);\nFile.AppendAllText(\"log.txt\", \"one more line\\n\");"
  },
  {
   "cat": {
    "en": "Files & IO",
    "zh": "文件与 IO"
   },
   "title": {
    "en": "File & Directory Management",
    "zh": "文件与目录管理"
   },
   "body": {
    "en": "File.Exists / Delete / Copy / Move manage individual files, GetSize reports bytes. Directory.CreateDirectory makes a folder (all missing parents), GetDirectories and GetFiles list a folder's contents.",
    "zh": "File.Exists / Delete / Copy / Move 管理单个文件，GetSize 返回字节数。Directory.CreateDirectory 创建目录（父目录一并创建），GetDirectories 与 GetFiles 列出目录内容。"
   },
   "code": "using System.IO;\n\nif (File.Exists(\"a.txt\")) { File.Delete(\"a.txt\"); }\nFile.Copy(\"src.txt\", \"dst.txt\");\nFile.Move(\"dst.txt\", \"renamed.txt\");\nint size = File.GetSize(\"renamed.txt\");\n\nDirectory.CreateDirectory(\"backup\");\nList<string> dirs = Directory.GetDirectories(\".\");\nList<string> files = Directory.GetFiles(\".\");"
  },
  {
   "cat": {
    "en": "Files & IO",
    "zh": "文件与 IO"
   },
   "title": {
    "en": "Path Helpers",
    "zh": "路径工具"
   },
   "body": {
    "en": "Path.Combine joins parts with the platform separator; GetFileName / GetExtension / GetDirectoryName / GetFileNameWithoutExtension take a full path apart. Normalize cleans up . and .. segments.",
    "zh": "Path.Combine 用平台分隔符拼接路径；GetFileName / GetExtension / GetDirectoryName / GetFileNameWithoutExtension 拆分完整路径。Normalize 清理 . 与 .. 段。"
   },
   "code": "using System.IO;\n\nstring p = Path.Combine(\"data\", \"notes.txt\");\nstring name = Path.GetFileName(p);                // \"notes.txt\"\nstring ext = Path.GetExtension(p);                // \".txt\"\nstring dir = Path.GetDirectoryName(p);            // \"data\"\nstring stem = Path.GetFileNameWithoutExtension(p);// \"notes\""
  },
  {
   "cat": {
    "en": "Files & IO",
    "zh": "文件与 IO"
   },
   "title": {
    "en": "Streams & Readers",
    "zh": "流与读取器"
   },
   "body": {
    "en": "For large files, stream instead of loading it all: FileStream.OpenRead opens a file handle, StreamReader wraps it with line- and text-oriented reads, and StreamWriter buffers writes. Close releases the handle.",
    "zh": "大文件用流而不是整体载入：FileStream.OpenRead 打开文件句柄，StreamReader 包装出面向行/文本的读取，StreamWriter 缓冲写入。Close 释放句柄。"
   },
   "code": "using System.IO;\n\nFileStream fs = FileStream.OpenRead(\"data.txt\");\nStreamReader reader = StreamReader.Wrap(fs);\nstring all = reader.ReadToEnd();\nreader.Close();\n\nStreamWriter writer = new StreamWriter(\"out.txt\");\nwriter.WriteLine(\"first\");\nwriter.Close();"
  },
  {
   "cat": {
    "en": "Files & IO",
    "zh": "文件与 IO"
   },
   "title": {
    "en": "Binary Files",
    "zh": "二进制文件"
   },
   "body": {
    "en": "ReadAllBytes loads a whole file as byte[] and WriteAllBytes stores one. byte[] data can be written directly, and File.GetSize tells you how many bytes to expect. Binary documents use the ByteBuffer reader/writer for structured data.",
    "zh": "ReadAllBytes 把整个文件读成 byte[]，WriteAllBytes 写回。byte[] 可直接写入，File.GetSize 告诉你会读到多少字节。结构化二进制数据用 ByteBuffer 读写。"
   },
   "code": "using System.IO;\n\nbyte[] data = File.ReadAllBytes(\"app.bin\");\nFile.WriteAllBytes(\"copy.bin\", data);"
  },
  {
   "cat": {
    "en": "Text & Regex",
    "zh": "文本与正则"
   },
   "title": {
    "en": "Regex Basics",
    "zh": "正则基础"
   },
   "body": {
    "en": "Regex.Compile(pattern) builds a matcher; IsMatch tests, Find returns the first match (Match.Success() / Value() / Index() tell the result), Matches lists every match and Split cuts the input at matches. Regex.Matched / Regex.Replaced are one-shot helpers.",
    "zh": "Regex.Compile(模式) 构建匹配器；IsMatch 测试，Find 返回第一个匹配（Match.Success() / Value() / Index() 给出结果），Matches 列出全部匹配，Split 按匹配切分输入。Regex.Matched / Regex.Replaced 是一次性助手。"
   },
   "code": "using System.Text.RegularExpressions;\n\nRegex re = Regex.Compile(\"z[a-z]n\");\nif (re.IsMatch(\"zan\")) {\n    Match m = re.Find(\"a zan lang\");\n    if (m.Success()) {\n        string value = m.Value();     // \"zan\"\n        int at = m.Index();           // 2\n    }\n}\nList<Match> all = re.Matches(\"zan zan\");\nList<string> pieces = re.Split(\"a,zan,b\");\nstring out_ = Regex.Replaced(\"zan\", \"z.n\", \"X\");   // \"X\""
  },
  {
   "cat": {
    "en": "Text & Regex",
    "zh": "文本与正则"
   },
   "title": {
    "en": "Regex Groups & Replace",
    "zh": "正则分组与替换"
   },
   "body": {
    "en": "Named groups (?<name>...) make parts of a match addressable: Match.Group(\"name\") reads one (or Group(n) by number). Replacement strings can reference groups with $1, ${name} and $&; Regex.Escape neutralizes metacharacters in literal text.",
    "zh": "命名分组 (?<name>...) 让匹配的一部分可寻址：Match.Group(\"name\") 读取（或按编号 Group(n)）。替换串可用 $1、${name}、$& 引用分组；Regex.Escape 给字面文本中的元字符加转义。"
   },
   "code": "using System.Text.RegularExpressions;\n\nRegex re = Regex.Compile(\"(?<word>\\\\w+)\");\nMatch m = re.Find(\"hello world\");\nif (m.Success()) {\n    string word = m.Group(\"word\");   // \"hello\"\n    string first = m.Group(1);        // same, by number\n}\nstring esc = Regex.Escape(\"a.b\");   // \"a\\\\.b\""
  },
  {
   "cat": {
    "en": "Text & Regex",
    "zh": "文本与正则"
   },
   "title": {
    "en": "CSV",
    "zh": "CSV"
   },
   "body": {
    "en": "Csv.Parse splits CSV text into rows of fields, handling quoted fields and \"\" escapes; Csv.Serialize does the reverse. ParseSep / SerializeSep work with a custom separator for TSV and friends.",
    "zh": "Csv.Parse 把 CSV 文本拆成行与字段，处理引号字段和 \"\" 转义；Csv.Serialize 反向生成。ParseSep / SerializeSep 支持自定义分隔符（如 TSV）。"
   },
   "code": "using System.Text;\n\nList<List<string>> rows = Csv.Parse(\"name,age\\nalice,30\\nbob,25\");\nstring name = rows[1][0];            // \"bob\"\nstring csv = Csv.Serialize(rows);\n\nList<List<string>> tsv = Csv.ParseSep(\"a\\tb\", \"\\t\");"
  },
  {
   "cat": {
    "en": "Text & Regex",
    "zh": "文本与正则"
   },
   "title": {
    "en": "Markdown & TextTable",
    "zh": "Markdown 与文本表格"
   },
   "body": {
    "en": "Markdown.ToHtml renders a deterministic subset (headings, paragraphs, lists, code, emphasis, links) to HTML. TextTable lays out rows into an aligned ASCII table for console output.",
    "zh": "Markdown.ToHtml 把确定子集（标题、段落、列表、代码、强调、链接）渲染成 HTML。TextTable 把数据行排版成对齐的 ASCII 表格，适合控制台输出。"
   },
   "code": "using System.Text;\n\nstring html = Markdown.ToHtml(\"# Heading\\n\\nSome **bold** text.\");\nConsole.WriteLine(html);\n\nTextTable table = new TextTable(new List<string> { \"Name\", \"Score\" });\ntable.AddRow(new List<string> { \"alice\", \"95\" });\ntable.AddRow(new List<string> { \"bob\", \"88\" });\nConsole.WriteLine(table.Render());"
  },
  {
   "cat": {
    "en": "Text & Regex",
    "zh": "文本与正则"
   },
   "title": {
    "en": "Encoding Helpers",
    "zh": "编码工具"
   },
   "body": {
    "en": "Encoding holds the everyday text converters: UrlEncode / UrlDecode for query strings, HtmlEncode for markup, JsonEscape for embedding text in JSON, Base64Encode / Base64Decode for binary-as-text.",
    "zh": "Encoding 汇集常用文本转换：UrlEncode / UrlDecode 处理查询串，HtmlEncode 处理标记，JsonEscape 把文本嵌入 JSON，Base64Encode / Base64Decode 做二进制转文本。"
   },
   "code": "using System.Text;\n\nstring url = Encoding.UrlEncode(\"a b&c\");\nstring html = Encoding.HtmlEncode(\"<b>\");\nstring json = Encoding.JsonEscape(\"say \\\"hi\\\"\");\nstring b64 = Encoding.Base64Encode(\"zan\");"
  },
  {
   "cat": {
    "en": "JSON",
    "zh": "JSON"
   },
   "title": {
    "en": "Build JSON",
    "zh": "构造 JSON"
   },
   "body": {
    "en": "JsonValue builds documents from typed nodes: NewObject / NewArray plus NewStr / NewNum / NewBool / NewNull. Set adds or replaces a key, Append grows an array. ToJson emits compact text, PrettyToJson indents it.",
    "zh": "JsonValue 用类型化节点构造文档：NewObject / NewArray 搭配 NewStr / NewNum / NewBool / NewNull。Set 添加或替换键，Append 追加数组元素。ToJson 输出紧凑文本，PrettyToJson 带缩进。"
   },
   "code": "using System.Json;\n\nJsonValue user = JsonValue.NewObject();\nuser.Set(\"name\", JsonValue.NewStr(\"alice\"));\nuser.Set(\"age\", JsonValue.NewNum(\"30\"));\nuser.Set(\"admin\", JsonValue.NewBool(false));\n\nJsonValue tags = JsonValue.NewArray();\ntags.Append(JsonValue.NewStr(\"zan\"));\ntags.Append(JsonValue.NewStr(\"lang\"));\nuser.Set(\"tags\", tags);\n\nstring compact = user.ToJson();\nstring pretty = user.PrettyToJson();"
  },
  {
   "cat": {
    "en": "JSON",
    "zh": "JSON"
   },
   "title": {
    "en": "Parse & Read JSON",
    "zh": "解析与读取 JSON"
   },
   "body": {
    "en": "JsonValue.ParseLenient(text) parses without throwing (Parse is the strict variant). Typed readers take a default for missing keys: Str / Int / Long / Double / Bool. IsObject / IsArray / IsString / IsNumber tell the node kind.",
    "zh": "JsonValue.ParseLenient(文本) 解析且不抛异常（Parse 是严格版）。类型化读取器为缺失键提供默认值：Str / Int / Long / Double / Bool。IsObject / IsArray / IsString / IsNumber 判断节点类型。"
   },
   "code": "using System.Json;\n\nstring src = \"{\\\"name\\\":\\\"alice\\\",\\\"age\\\":\\\"30\\\"}\";\nJsonValue doc = JsonValue.ParseLenient(src);\n\nstring name = doc.Str(\"name\", \"?\");   // typed reads with defaults\nint age = doc.Int(\"age\", 0);\nbool ok = doc.Bool(\"enabled\", false);\n\nif (doc.IsObject()) { Console.WriteLine(name); }"
  },
  {
   "cat": {
    "en": "JSON",
    "zh": "JSON"
   },
   "title": {
    "en": "Arrays & Nested Objects",
    "zh": "数组与嵌套对象"
   },
   "body": {
    "en": "Get(key) returns a child node - check it for null before using it, since a missing key reads as null. Count() and At(i) walk arrays. PathGet / PathStr / PathInt address nested values with a dotted path like \"user.id\" in one call.",
    "zh": "Get(键) 返回子节点——使用前先判空，因为缺失的键读出来是 null。Count() 与 At(i) 遍历数组。PathGet / PathStr / PathInt 用 \"user.id\" 这样的点路径一次取到嵌套值。"
   },
   "code": "using System.Json;\n\nJsonValue doc = JsonValue.ParseLenient(\n    \"{\\\"tags\\\":[\\\"a\\\",\\\"b\\\"],\\\"user\\\":{\\\"id\\\":\\\"7\\\"}}\");\n\nJsonValue tags = doc.Get(\"tags\");\nif (tags != null) {\n    int n = tags.Count();\n    for (int i = 0; i < n; i = i + 1) {\n        Console.WriteLine(tags.At(i).AsString(\"\"));\n    }\n}\n\nstring id = doc.PathStr(\"user.id\", \"?\");   // \"7\""
  },
  {
   "cat": {
    "en": "LINQ",
    "zh": "LINQ"
   },
   "title": {
    "en": "Where & Select",
    "zh": "Where 与 Select"
   },
   "body": {
    "en": "LINQ chains turn a list into a new list. Where keeps the items that pass a predicate, Select projects each item to a new value (possibly a different type). Every chain ends in ToList().",
    "zh": "LINQ 链把列表变成新列表。Where 保留通过谓词的元素，Select 把每个元素投影成新值（可以是另一种类型）。每条链以 ToList() 收尾。"
   },
   "code": "using System.Linq;\n\nList<int> nums = new List<int> { 1, 2, 3, 4, 5 };\nList<int> evens = nums.Where((int n) => n % 2 == 0).ToList();\nList<int> doubled = nums.Select((int n) => n * 2).ToList();\n\nList<string> words = new List<string> { \"zan\", \"lang\" };\nList<int> lens = words.Select((string w) => w.Length).ToList();"
  },
  {
   "cat": {
    "en": "LINQ",
    "zh": "LINQ"
   },
   "title": {
    "en": "OrderBy & GroupBy",
    "zh": "OrderBy 与 GroupBy"
   },
   "body": {
    "en": "OrderBy sorts by the projected key (numbers or strings), OrderByDescending reverses it; both return new lists. GroupBy buckets items by a key and yields Grouping<T> rows with Key and Items.",
    "zh": "OrderBy 按投影出的键排序（数字或字符串），OrderByDescending 反向；两者都返回新列表。GroupBy 按键分桶，产出带 Key 和 Items 的 Grouping<T>。"
   },
   "code": "using System.Linq;\n\nList<string> words = new List<string> { \"bb\", \"a\", \"ccc\" };\nList<string> sorted = words.OrderBy((string w) => w).ToList();\nList<string> byLen = words.OrderBy((string w) => w.Length).ToList();\nList<string> desc = words.OrderByDescending((string w) => w.Length).ToList();\n\nList<Grouping<string>> groups = words.GroupBy((string w) => w).ToList();\nforeach (Grouping<string> g in groups) {\n    Console.WriteLine(g.Key + \" x\" + Convert.ToString(g.Items.Count));\n}"
  },
  {
   "cat": {
    "en": "LINQ",
    "zh": "LINQ"
   },
   "title": {
    "en": "Aggregates & Checks",
    "zh": "聚合与判定"
   },
   "body": {
    "en": "Sum / Max / Min / Average reduce a list to one number (overloads take a key selector for custom fields), First returns the first element - or throws on an empty list, so guard with a predicate or Any() first. Any / All test the elements.",
    "zh": "Sum / Max / Min / Average 把列表归结成一个数（重载接受键选择器用于自定义字段），First 返回第一个元素——列表为空时抛异常，先用谓词或 Any() 保护。Any / All 测试元素。"
   },
   "code": "using System.Linq;\n\nList<int> nums = new List<int> { 3, 1, 4, 1, 5 };\nint total = nums.Sum();\nint mx = nums.Max();\nint mn = nums.Min();\ndouble avg = nums.Average();\n\nbool anyBig = nums.Any((int n) => n > 4);   // true\nbool allPos = nums.All((int n) => n > 0);   // true\nif (nums.Count > 0) {\n    int first = nums.First();               // 3\n}"
  },
  {
   "cat": {
    "en": "Async & Threads",
    "zh": "异步与线程"
   },
   "title": {
    "en": "async / await",
    "zh": "async / await"
   },
   "body": {
    "en": "An async function returns a coroutine: call it and await it, and execution suspends at the await without blocking the thread. Task.Delay(ms) is the timed suspension. The entry point awaits once: static void Main() { int r = await Run(); }.",
    "zh": "async 函数返回协程：调用后 await 它，执行在 await 处挂起而不阻塞线程。Task.Delay(毫秒) 是定时挂起。入口处 await 一次：static void Main() { int r = await Run(); }。"
   },
   "code": "using System;\n\nclass Program {\n    static async int Run() {\n        Console.WriteLine(\"start\");\n        await Task.Delay(1000);      // suspend this coroutine, not the thread\n        Console.WriteLine(\"after 1s\");\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "Async & Threads",
    "zh": "异步与线程"
   },
   "title": {
    "en": "Task: Spawn & WhenAll",
    "zh": "Task：Spawn 与 WhenAll"
   },
   "body": {
    "en": "Task.Spawn(call) starts a coroutine in the background and returns its handle; Task.IsDone(handle) polls it. Task.WhenAll(handles) suspends until every one finishes, WhenAny until the first does - the standard way to run requests in parallel.",
    "zh": "Task.Spawn(调用) 在后台启动一个协程并返回句柄；Task.IsDone(句柄) 轮询完成状态。Task.WhenAll(句柄列表) 挂起直到全部完成，WhenAny 直到第一个完成——并行发起请求的标准做法。"
   },
   "code": "static async int Fetch(string name) {\n    await Task.Delay(50);\n    Console.WriteLine(name);\n    return 0;\n}\n\nlong h1 = Task.Spawn(Fetch(\"/a\"));\nlong h2 = Task.Spawn(Fetch(\"/b\"));\n\nint done = await Task.WhenAll(new List<long> { h1, h2 });\nbool finished = Task.IsDone(h1);"
  },
  {
   "cat": {
    "en": "Async & Threads",
    "zh": "异步与线程"
   },
   "title": {
    "en": "lock & Thread",
    "zh": "lock 与线程"
   },
   "body": {
    "en": "lock (obj) { ... } runs the block exclusively: only one coroutine or thread at a time may hold the lock. Thread.Start launches a detached background thread, Thread.Sleep blocks the calling thread, Thread.CurrentId identifies it.",
    "zh": "lock (obj) { ... } 让代码块互斥执行：同一时刻只有一个协程或线程能持有锁。Thread.Start 启动分离的后台线程，Thread.Sleep 阻塞当前线程，Thread.CurrentId 标识线程。"
   },
   "code": "using System.Threading;\n\nobject guard = new object();\nlock (guard) {\n    // critical section: one holder at a time\n}\n\nbool started = Thread.Start(() => {\n    Thread.Sleep(100);\n});\nlong id = Thread.CurrentId();"
  },
  {
   "cat": {
    "en": "Async & Threads",
    "zh": "异步与线程"
   },
   "title": {
    "en": "Gate & Channel",
    "zh": "Gate 与 Channel"
   },
   "body": {
    "en": "Gate is a suspend/resume signal: a waiter awaits gate.Wait() until another coroutine calls Signal(). Channel is a buffered queue between coroutines: Send enqueues, Receive suspends when it is empty - a ready-made producer/consumer.",
    "zh": "Gate 是挂起/唤醒信号：等待方 await gate.Wait() 直到别的协程调用 Signal()。Channel 是协程间的缓冲队列：Send 入队，Receive 在队列空时挂起——开箱即用的生产者/消费者。"
   },
   "code": "using System.Threading;\n\nGate gate = new Gate();\n// a worker coroutine calls gate.Signal() when work is ready\nbool ready = await gate.Wait();      // suspends until signalled\n\nChannel jobs = new Channel(8);       // buffered, 8 slots\njobs.Send(\"job\");                    // producer\nstring job = await jobs.Receive();    // consumer, suspends when empty"
  },
  {
   "cat": {
    "en": "Cryptography",
    "zh": "加密与安全"
   },
   "title": {
    "en": "SHA-256 Hashing",
    "zh": "SHA-256 哈希"
   },
   "body": {
    "en": "Sha256.Hash(message, length) returns the 32-byte digest as byte[]; Sha512, Sha1 (legacy) and Md5 (legacy) share the shape. Hex.Encode renders a digest for storage or comparison - the standard way to fingerprint a payload.",
    "zh": "Sha256.Hash(消息, 长度) 返回 32 字节摘要（byte[]）；Sha512、Sha1（旧）与 Md5（旧）同形。Hex.Encode 把摘要渲染成十六进制便于存储与比对——给负载做指纹的标准方式。"
   },
   "code": "using System.Security.Cryptography;\n\nbyte[] digest = Sha256.Hash(\"zan\", 3);\nstring hex = Hex.Encode(digest, digest.Length);   // lowercase hex\nConsole.WriteLine(hex);"
  },
  {
   "cat": {
    "en": "Cryptography",
    "zh": "加密与安全"
   },
   "title": {
    "en": "HMAC & HKDF",
    "zh": "HMAC 与 HKDF"
   },
   "body": {
    "en": "Hmac.Sha256Mac(key, keyLen, message, msgLen) computes a keyed hash: the same key both sides share proves the message was not tampered with. Hkdf derives key material: Extract(salt, ikm) then Expand(prk, info, outLen), or Derive for both in one call.",
    "zh": "Hmac.Sha256Mac(密钥, 密钥长度, 消息, 消息长度) 计算带密钥的哈希：双方共享同一密钥即可证明消息未被篡改。Hkdf 派生密钥材料：Extract(盐, ikm) 后 Expand(prk, info, 输出长度)，或 Derive 一步完成。"
   },
   "code": "using System.Security.Cryptography;\n\nbyte[] mac = Hmac.Sha256Mac(\"secret\", 6, \"payload\", 7);\nstring macHex = Hex.Encode(mac, mac.Length);\n\nbyte[] prk = Hkdf.Extract(\"salt\", 4, \"ikm\", 3);\nbyte[] key = Hkdf.Expand(prk, \"info\", 4, 32);"
  },
  {
   "cat": {
    "en": "Cryptography",
    "zh": "加密与安全"
   },
   "title": {
    "en": "AES-GCM",
    "zh": "AES-GCM 加密"
   },
   "body": {
    "en": "AesGcm.Encrypt(key, keyLen, iv, aad, aadLen, plaintext, ptLen, tagOut) encrypts in one call: pass a 32-byte key, a 12-byte IV and any associated data; the auth tag comes back through tagOut and the ciphertext is returned (check it for null). Decrypt reverses it.",
    "zh": "AesGcm.Encrypt(密钥, 密钥长度, iv, aad, aad长度, 明文, 明文长度, tagOut) 一步完成加密：传入 32 字节密钥、12 字节 IV 和任意关联数据；认证标签经 tagOut 返回，密文作为返回值（判空后再用）。Decrypt 反向解密。"
   },
   "code": "using System.Security.Cryptography;\n\nbyte[] key = RandomNumberGenerator.GetBytes(32);\nbyte[] iv = RandomNumberGenerator.GetBytes(12);\n\nstring tagOut = \"\";\nbyte[] cipher = AesGcm.Encrypt(key, 32, iv, \"\", 0, \"hello\", 5, tagOut);\nif (cipher != null) {\n    List<int> ok = new List<int>();\n    byte[] plain = AesGcm.Decrypt(key, 32, iv, \"\", 0, cipher, cipher.Length,\n                                  tagOut, ok);\n}"
  },
  {
   "cat": {
    "en": "Cryptography",
    "zh": "加密与安全"
   },
   "title": {
    "en": "Base64 & Hex",
    "zh": "Base64 与 Hex"
   },
   "body": {
    "en": "Base64 and Hex both encode bytes as text: Base64 for compact transport (URLs, tokens), Hex for readable digests. Encode takes the buffer and its length; Decode returns the bytes. RandomNumberGenerator.GetBytes(n) supplies cryptographically random bytes.",
    "zh": "Base64 与 Hex 都把字节编码成文本：Base64 用于紧凑传输（URL、令牌），Hex 用于可读摘要。Encode 接收缓冲区与长度；Decode 返回字节。RandomNumberGenerator.GetBytes(n) 提供密码学安全随机字节。"
   },
   "code": "using System.Security.Cryptography;\n\nstring b64 = Base64.Encode(\"zan\", 3);\nbyte[] back = Base64.Decode(b64, new List<int>());\n\nstring hex = Hex.Encode(\"zan\", 3);\nbyte[] raw = Hex.Decode(hex);\n\nbyte[] rnd = RandomNumberGenerator.GetBytes(16);   // for keys and IVs"
  },
  {
   "cat": {
    "en": "Diagnostics",
    "zh": "诊断"
   },
   "title": {
    "en": "Stopwatch",
    "zh": "计时器"
   },
   "body": {
    "en": "Stopwatch.StartNew() starts measuring; Stop() freezes it, and ElapsedMilliseconds / ElapsedSeconds report the interval. Restart() zeroes and starts again - the way to time a code path.",
    "zh": "Stopwatch.StartNew() 开始计时；Stop() 停止，ElapsedMilliseconds / ElapsedSeconds 报告间隔。Restart() 清零并重新开始——给代码路径计时的标准方式。"
   },
   "code": "using System.Diagnostics;\n\nStopwatch sw = Stopwatch.StartNew();\nDoWork();\nsw.Stop();\ndouble ms = sw.ElapsedMilliseconds;\nConsole.WriteLine(\"took \" + Convert.ToString(ms) + \" ms\");"
  },
  {
   "cat": {
    "en": "Diagnostics",
    "zh": "诊断"
   },
   "title": {
    "en": "Run External Commands",
    "zh": "运行外部命令"
   },
   "body": {
    "en": "Process.RunCapture(command) runs a program and returns its standard output line by line; Capture returns a ProcessResult with Ok() / Text() so the exit code and full output stay together. Run fires it without capturing.",
    "zh": "Process.RunCapture(命令) 运行程序并逐行返回其标准输出；Capture 返回 ProcessResult，Ok() / Text() 把退出码与完整输出放在一起。Run 只运行不捕获。"
   },
   "code": "using System.Diagnostics;\n\nList<string> lines = Process.RunCapture(\"echo hello\");\nforeach (string line in lines) { Console.WriteLine(line); }\n\nProcessResult r = Process.Capture(\"cmd /c ver\");\nif (r.Ok()) { Console.WriteLine(r.Text()); }"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Create a Window",
    "zh": "创建窗口"
   },
   "body": {
    "en": "App.CreateDark(title, width, height) opens a dark-themed window. app.Run drives the whole loop: events, redraw and window chrome.",
    "zh": "App.CreateDark(title, width, height) 打开一个深色主题窗口。app.Run 驱动整个循环：事件、重绘和窗口外框。"
   },
   "code": "using System;\nusing Gui;\n\nclass Program {\n    static void Main() {\n        App app = App.CreateDark(\"My App\", 800, 600);\n        app.Run(\"My App\");\n    }\n}"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Custom Drawing Loop",
    "zh": "自定义绘制循环"
   },
   "body": {
    "en": "To draw your own content, run the frame loop yourself and paint with the Canvas each frame.",
    "zh": "若要绘制自定义内容，可自行运行帧循环，并在每帧用 Canvas 绘制。"
   },
   "code": "using Gui;\n\nclass Program {\n    static void Main() {\n        App app = App.CreateDark(\"My App\", 800, 600);\n        app.Show();\n        while (app.isRunning) {\n            if (!app.ProcessEvent()) { break; }\n            if (!app.needsRedraw) { continue; }\n            app.BeginFrame();\n            app.canvas.DrawText(40, 80, \"Hello\", app.theme.textPrimary, 16);\n            app.RenderChrome(\"My App\");\n            app.PresentFrame();\n        }\n    }\n}"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Reactive State",
    "zh": "响应式状态"
   },
   "body": {
    "en": "SignalInt, SignalBool and SignalString hold widget state. Get reads the current value; Set updates it and requests a redraw through the bound widget.",
    "zh": "SignalInt、SignalBool 和 SignalString 保存组件状态。Get 读取当前值，Set 更新状态；绑定的组件会据此重绘。"
   },
   "code": "using Gui;\n\nSignalInt count = new SignalInt(0);\nSignalBool enabled = new SignalBool(true);\nSignalString name = new SignalString(\"Zan\");\n\ncount.Set(count.Get() + 1);"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Canvas Drawing",
    "zh": "Canvas 绘制"
   },
   "body": {
    "en": "The canvas is an immediate drawing surface: every frame, issue the draw calls in order. Colors are packed 0xAARRGGBB ints. FillRect paints a solid box, DrawRect outlines one, and FillRoundRect / FillCircle / DrawLine / DrawText cover the rest. Text sizes come from the theme or a plain pixel size.",
    "zh": "canvas 是即时绘制表面：每帧按顺序发出绘制调用。颜色是打包的 0xAARRGGBB int。FillRect 画实心矩形，DrawRect 画边框，FillRoundRect / FillCircle / DrawLine / DrawText 覆盖其余。字号用主题值或普通像素值。"
   },
   "code": "using Gui;\n\nCanvas c = app.canvas;\nc.FillRect(20, 20, 200, 60, 0xFF334455);        // solid box\nc.DrawRect(20, 20, 200, 60, 0xFFFF5555, 2);     // outlined\nc.FillRoundRect(40, 100, 160, 40, 10, 0xFF2f6f3f);\nc.FillCircle(80, 160, 20, 0xFF2f6f3f);\nc.DrawLine(20, 200, 220, 200, 0xFF888888, 1);\nc.DrawText(20, 220, \"Hello\", app.theme.textPrimary, 16);\nint w = Canvas.MeasureText(\"Hello\", 16);       // width in px"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Colors & Theme",
    "zh": "颜色与主题"
   },
   "body": {
    "en": "Write colors as 0xAARRGGBB literals, or build them with Color.Parse(\"#2f6f3f\") / Color.FromRGBA(r, g, b, a) and convert with ToArgb(). The theme object carries the palette (textPrimary, bgSecondary, primary...) and the metric set (fontSizeMedium, heightMedium...), so a control styled from the theme adapts to skins automatically.",
    "zh": "颜色直接写 0xAARRGGBB 字面量，或用 Color.Parse(\"#2f6f3f\") / Color.FromRGBA(r, g, b, a) 构造后 ToArgb() 转换。theme 对象携带调色板（textPrimary、bgSecondary、primary……）与度量集（fontSizeMedium、heightMedium……），用主题取值的控件会自动适配皮肤。"
   },
   "code": "using Gui;\n\nint col = 0xFF2f6f3f;                       // 0xAARRGGBB\nint css = Color.Parse(\"#2f6f3f\").ToArgb();  // CSS string -> int\nColor rgba = Color.FromRGBA(47, 111, 63, 255);\nint packed = rgba.ToArgb();\n\nCanvas c = app.canvas;\nc.FillRect(0, 0, 100, 40, app.theme.primary);\nc.FillRect(0, 50, 100, 40, app.theme.bgSecondary);\nc.DrawText(0, 100, \"body\", app.theme.textPrimary, app.theme.fontSizeMedium);"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Stack Layout",
    "zh": "Stack 布局"
   },
   "body": {
    "en": "Stack is a layout cursor: create a row or column inside a rectangle, then carve it up. Slot(n) takes the next n pixels, Fill() takes everything left, SetGap / SetPad add spacing. It is the immediate-mode way to line up controls without manual math.",
    "zh": "Stack 是一个布局游标：在矩形内创建行或列，然后逐段切分。Slot(n) 取走接下来 n 像素，Fill() 取走全部剩余，SetGap / SetPad 加间距。这是即时模式下对齐控件的做法，不用手算坐标。"
   },
   "code": "using Gui;\n\nStack row = Stack.Row(40, 40, 400, 30);\nrow.SetGap(8);\nRect first = row.Slot(120);     // 120 px slot\nRect rest = row.Fill();         // whatever remains\nbool room = row.HasRoom(64);    // enough left?\n\nStack col = Stack.ColumnIn(new Rect(40, 80, 200, 300));\ncol.SetPad(8);\nRect s = col.Slot(24);          // one 24 px row"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Control Tree & Dock",
    "zh": "控件树与停靠"
   },
   "body": {
    "en": "A Control is a retained node with children: Add appends, Dock picks the edge it attaches to (Left / Right / Top / Bottom / Fill), Prefer sets its preferred size and Pad its padding. Panel.Row() / Panel.Column() are flow containers for child lists. RenderTree draws the whole hierarchy.",
    "zh": "Control 是带子节点的保留模式节点：Add 追加，Dock 选择停靠边（Left / Right / Top / Bottom / Fill），Prefer 设置首选尺寸，Pad 设置内边距。Panel.Row() / Panel.Column() 是子控件按流式排布的容器。RenderTree 绘制整棵层级。"
   },
   "code": "using Gui;\n\nControl root = new Control();\nControl sidebar = new Control();\nsidebar.Dock(Dock.Left());      // attach to the left edge\nsidebar.Prefer(240, 0);         // preferred width 240\nsidebar.Pad(8);\nroot.Add(sidebar);\n\nControl content = new Control();\ncontent.Dock(Dock.Fill());      // take the rest\nroot.Add(content);\nroot.RenderTree(app);"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Mouse & Keyboard Events",
    "zh": "鼠标与键盘事件"
   },
   "body": {
    "en": "Every widget Render returns an id; the Ui helpers answer questions about it for the current frame. Clicked / PressedDown / Hovered are the mouse basics, Entered fires once on hover-in. Keyboard state is global: KeyDown returns this frame's key code, KeyPressed tests one, and Keys.* names the codes.",
    "zh": "每个控件的 Render 都返回 id；Ui 助手针对当前帧回答关于它的查询。Clicked / PressedDown / Hovered 是鼠标基础，Entered 在悬停进入时触发一次。键盘状态是全局的：KeyDown 返回本帧按键码，KeyPressed 测试某个键，Keys.* 给出键码常量。"
   },
   "code": "using Gui;\n\nButton save = new Button { Text = \"Save\" };\nint id = save.Render(app, 40, 80, 120);\n\nif (Ui.Clicked(app, id)) { Save(); }\nif (Ui.Hovered(app, id)) { ShowTip(); }\nif (Ui.PressedDown(app, id)) { PressFx(); }\n\nint code = Ui.KeyDown(app);                 // key this frame, 0 if none\nbool esc = Ui.KeyPressed(app, Keys.Escape());\nbool ctrl = Modifiers.HasCtrl(app.window.EventMods());"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Images",
    "zh": "图片"
   },
   "body": {
    "en": "Images are decoded by path and drawn by the canvas, cached after the first load. DrawImage paints at natural size; BlitImage scales and crops from a source rectangle (sw = 0 / sh = 0 means the whole image). ImageWidth / ImageHeight query dimensions before laying out.",
    "zh": "图片按路径解码、由 canvas 绘制，首次加载后缓存。DrawImage 按原始尺寸绘制；BlitImage 从源矩形缩放裁剪（sw = 0 / sh = 0 表示整图）。布局前用 ImageWidth / ImageHeight 查询尺寸。"
   },
   "code": "using Gui;\n\nstring path = \"assets/logo.png\";\nint w = Canvas.ImageWidth(path);\nint h = Canvas.ImageHeight(path);\n\napp.canvas.DrawImage(path, 40, 40);                       // natural size\napp.canvas.BlitImage(path, 40, 80, 100, 50, 0, 0, 0, 0);  // scaled to 100x50"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Animations & Fx",
    "zh": "动画与特效"
   },
   "body": {
    "en": "App.AnimTo(key, target, ms) tweens an eased 0..1000 value toward a target and AnimIntro runs a one-shot 0 -> 1000, so frame code just reads the value and positions by it. Fx draws ready-made animated decorations - BorderGlow, Specular, CardSheen - that self-clip to a rectangle.",
    "zh": "App.AnimTo(键, 目标, 毫秒) 把缓动的 0..1000 值推向目标，AnimIntro 跑一次 0 -> 1000 的入场；帧代码读取该值来定位即可。Fx 提供现成的动画装饰——BorderGlow、Specular、CardSheen——自裁剪到指定矩形。"
   },
   "code": "using Gui;\n\nint p = app.AnimTo(1, 1000, 500);      // eased 0..1000 over 500 ms\nint intro = app.AnimIntro(2, 400);     // one-shot 0 -> 1000\n\n// drive position by the eased value\nint y = 40 + (p * 20) / 1000;\napp.canvas.FillRect(40, y, 80, 24, 0xFF2f6f3f);\n\nFx.BorderGlow(app, 40, 200, 160, 40, 8, 0xFF2f6f3f, 2600, 120);"
  },
  {
   "cat": {
    "en": "GUI - Basics",
    "zh": "GUI - 基础"
   },
   "title": {
    "en": "Form & Retained UI",
    "zh": "Form 保留模式窗口"
   },
   "body": {
    "en": "A Form is a retained window: build the control tree once, then form.Run() drives the event loop, redraws and child windows. Every(ms, action) schedules a repeating callback (timers, polls), OnExit runs once after the window closes.",
    "zh": "Form 是保留模式窗口：控件树只构建一次，然后 form.Run() 驱动事件循环、重绘与子窗口。Every(毫秒, 回调) 安排周期回调（定时器、轮询），OnExit 在窗口关闭后执行一次。"
   },
   "code": "using System;\nusing Gui;\nusing Gui.Widget;\n\nForm form = Form.CreateDark(\"App\", 800, 600);\nform.Add(new Button { Text = \"Save\" });\nform.Every(1000, () => Console.WriteLine(\"tick\"));\nform.OnExit(() => Console.WriteLine(\"bye\"));\nform.Run();"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Button",
    "zh": "Button"
   },
   "body": {
    "en": "new Button { Text = \"Save\", Class = \"primary\" } declares a button; Render draws it (x, y, width) and returns its id. Ui.Clicked tests for a click this frame.",
    "zh": "new Button { Text = \"保存\", Class = \"primary\" } 声明按钮；Render 绘制它（x, y, width）并返回 id。Ui.Clicked 判断本帧是否被点击。"
   },
   "code": "Button b = new Button { Text = \"Save\" };\nint id = b.Render(app, 40, 80, 120);\nif (Ui.Clicked(app, id)) {\n    Console.WriteLine(\"clicked\");\n}"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Label",
    "zh": "Label"
   },
   "body": {
    "en": "Labels draw static text; classes pick the size and colour (secondary/hint, tiny/small/large/title).",
    "zh": "Label 绘制静态文本；字号与颜色由 class 决定（secondary/hint，tiny/small/large/title）。"
   },
   "code": "Label title = new Label { Text = \"A big title\", Class = \"title\" };\ntitle.RenderInside(app, new Rect(40, 40, 240, 30));\nnew Label { Text = \"subtle caption\", Class = \"secondary\" }\n    .RenderInside(app, new Rect(40, 74, 240, 24));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Text Input",
    "zh": "文本输入框"
   },
   "body": {
    "en": "Input binds a plain string field via `.data` (compiler-lowered two-way binding). Construct with a hint, assign `.data`, then Render each frame.",
    "zh": "Input 通过 `.data`（编译器降级的双向绑定）绑定一个普通字符串字段。用提示文本构造，赋值 `.data`，每帧 Render。"
   },
   "code": "class Form { string name; }\nForm form = new Form();\nInput name = new Input(\"your name\");\nname.data = form.name;   // two-way binding\nname.Render(app, 40, 80, 200, 28);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Checkbox",
    "zh": "Checkbox"
   },
   "body": {
    "en": "Checkbox binds a plain bool field via `.data`; the field tracks the checked state both ways.",
    "zh": "Checkbox 通过 `.data` 绑定一个普通 bool 字段；字段与勾选状态双向同步。"
   },
   "code": "class Form { bool agree; }\nForm form = new Form();\nCheckbox agree = new Checkbox(\"I agree\");\nagree.data = form.agree;\nagree.Render(app, 40, 80);\nif (form.agree) {\n    // enabled\n}"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Radio Group",
    "zh": "单选组"
   },
   "body": {
    "en": "Radio buttons bind the SAME int field via `.data`; each has a distinct option value. RadioGroup is the container form: AddOption hands labels to the group; options can carry custom values / disabled flags (AddOption(label, value[, disabled]) or SetOptions(List<RadioOption>)); Value is a group-level two-way binding by option value; Buttons = true renders joined button segments (el-radio-button) and Size rescales the whole group; OnChange fires once per user toggle.",
    "zh": "多个单选按钮通过 `.data` 绑定同一个 int 字段；每个有各自的选项值。RadioGroup 是容器形态：AddOption 把选项交给组排布；选项可携带自定义 value / 禁用位（AddOption(label, value[, disabled]) 或 SetOptions(List<RadioOption>)）；Value 是按选项 value 的组级双向绑定；Buttons = true 切成连体按钮段（el-radio-button），Size 一次作用于整组；OnChange 在用户切换时触发一次。"
   },
   "code": "class Form { int choice; int city; }\nForm form = new Form();\nRadio a = new Radio(new SignalInt(0), 0, \"Option A\");\nRadio b = new Radio(new SignalInt(0), 1, \"Option B\");\na.data = form.choice;\nb.data = form.choice;\na.Render(app, 40, 80, \"Option A\");\nb.Render(app, 40, 110, \"Option B\");\n\n// RadioGroup: options with values + group-level binding\nRadioGroup city = new RadioGroup();\ncity.AddOption(\"北京\", 10);\ncity.AddOption(\"上海\", 20);\ncity.AddOption(\"广州\", 30, true);  // disabled\ncity.Value = form.city;              // two-way by value\ncity.OnChange(() => { Refresh(); });\n\n// button segments (el-radio-button):\nRadioGroup ship = new RadioGroup();\nship.AddOption(\"当日达配送\");\nship.AddOption(\"门店自提\");\nship.Buttons = true;\nship.Size = \"large\";"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Switch",
    "zh": "Switch"
   },
   "body": {
    "en": "A toggle switch that binds a plain bool field via `.data`. Props: size (tiny/small/medium/large skin classes), loading (thumb spinner + dimmed track; clicks ignored while on), round (false gives the square shape), checkedText / uncheckedText and checkedIcon / uncheckedIcon (inner content, the track widens to fit), checkedColor / uncheckedColor (0 = skin), and the int value axis: bind `.value` with checkedValue / uncheckedValue so the switch means \"value == checkedValue\" (or Switch.BindValue(signal, on, off)).",
    "zh": "通过 `.data` 绑定普通 bool 字段的开关切换控件。Props：size（tiny/small/medium/large 皮肤类）、loading（拇指转圈 + 轨道变暗，加载期间点击失聪）、round（false 得方形）、checkedText / uncheckedText 与 checkedIcon / uncheckedIcon（内嵌文字/图标，轨道自动撑宽）、checkedColor / uncheckedColor（0 = 跟随皮肤），以及 int 值轴：绑定 `.value` 配 checkedValue / uncheckedValue，开关表达「值 == 选中值」（或 Switch.BindValue(信号, 开值, 关值)）。"
   },
   "code": "class Form { bool on; int stage; }\nForm form = new Form();\nform.stage = 20;\nSwitch sw = new Switch();\nsw.data = form.on;\nsw.Render(app, 40, 80);\n// inner content + square + custom colors:\nSwitch cn = new Switch { CheckedText = \"开\", UncheckedText = \"关\", Round = false };\ncn.Render(app, 120, 80);\n// int value axis: on means form.stage == 10\nSwitch st = new Switch { CheckedValue = 10, UncheckedValue = 20 };\nst.value = form.stage;\nst.Render(app, 200, 80);",
   "preview": "Switch"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Slider",
    "zh": "Slider"
   },
   "body": {
    "en": "Slider binds a plain int field via `.data`; construct with the range, then Render(app, x, y, width).",
    "zh": "Slider 通过 `.data` 绑定一个普通 int 字段；用区间构造，调用 Render(app, x, y, width)。"
   },
   "code": "class Form { int volume; }\nForm form = new Form();\nSlider vol = new Slider(0, 100, new SignalInt(50));\nvol.data = form.volume;\nvol.Render(app, 40, 80, 200);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Select (SelectBox)",
    "zh": "下拉选择"
   },
   "body": {
    "en": "SelectBox shows a dropdown of options; the chosen index binds to a plain int field via `.data`.",
    "zh": "Select 显示下拉选项；选中的索引通过 `.data` 绑定到一个普通 int 字段。"
   },
   "code": "class Form { int color; }\nForm form = new Form();\nList<string> opts = new List<string> { \"Red\", \"Green\", \"Blue\" };\nSelectBox color = SelectBox.FromOptions(opts, \"pick a color\");\ncolor.data = form.color;\ncolor.Render(app, 40, 80, 200);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Tabs",
    "zh": "Tabs"
   },
   "body": {
    "en": "Tabs is an instance component: Create it, Add labels, Render the strip, and read Active().",
    "zh": "Tabs 是实例组件：Create 之后 Add 标签，Render 标签条，用 Active() 读取当前索引。"
   },
   "code": "Tabs tabs = new Tabs(0);\ntabs.Add(\"General\", false);\ntabs.Add(\"Advanced\", false);\ntabs.Render(app, 40, 40, 400);\nint active = tabs.Active();"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Card",
    "zh": "Card"
   },
   "body": {
    "en": "Card draws a titled panel. Card.ContentY gives the y where your content should start.",
    "zh": "Card 绘制带标题的面板。Card.ContentY 给出内容应开始的 y 坐标。"
   },
   "code": "Card c = new Card(\"Profile\");\nc.Add(new Label { Text = \"Card body goes here\" });\nc.RenderInside(app, new Rect(40, 40, 260, 160));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Progress",
    "zh": "Progress"
   },
   "body": {
    "en": "new Progress(percent, type) draws a bar; Ring() makes it a gauge, percent 0-100.",
    "zh": "new Progress(percent, type) 绘制进度条，Ring() 切换为环形，percent 为 0-100。"
   },
   "code": "new Progress(65, 0).RenderAt(app, 40, 80, 240);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Tag",
    "zh": "Tag"
   },
   "body": {
    "en": "Tag draws a small colored label. The last argument selects the color variant.",
    "zh": "Tag 绘制一个彩色小标签。最后一个参数选择颜色样式。"
   },
   "code": "Tag.Chip(app, 40, 80, \"stable\", \"primary\");"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "DynamicTags",
    "zh": "DynamicTags"
   },
   "body": {
    "en": "DynamicTags is an editable tag row: the dashed '+ Add' trigger opens an inline input on the spot; Enter or an outside click commits, Esc cancels, each tag's x removes it, Max caps the count and greys the trigger out.",
    "zh": "DynamicTags 是可编辑标签行：虚线「+ 新建标签」触发器点击后原位变成输入框，回车或点击别处提交、Esc 放弃；标签尾部的 x 移除该条；Max 限制数量，达到后触发器置灰。"
   },
   "code": "DynamicTags tags = new DynamicTags();\ntags.SetItems(new List<string>{ \"调查中\", \"已发布\" });\ntags.Change += () => { Save(tags.Items()); };\ntags.Render(app, 40, 80);",
   "preview": "DynamicTags"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Alert",
    "zh": "Alert"
   },
   "body": {
    "en": "Alerts come from Layer: Layer.DrawNotify paints an inline banner, Layer.Msg / Layer.Notify queue transient ones, and LayerState.Alert opens a modal dialog. Type: 2 info, 3 success, 4 warning, 5 error.",
    "zh": "提示统一由 Layer 提供：Layer.DrawNotify 画内联横幅，Layer.Msg / Layer.Notify 推送临时提示，LayerState.Alert 打开模态对话框。类型：2 信息，3 成功，4 警告，5 错误。"
   },
   "code": "Layer.DrawNotify(app, 40, 80, 360, \"Your changes were saved.\", 3);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Tooltip",
    "zh": "Tooltip"
   },
   "body": {
    "en": "Tooltip draws a small floating hint anchored at a point.",
    "zh": "Tooltip 在某个点绘制一个浮动的小提示。"
   },
   "code": "Tooltip.Render(app, 120, 80, \"More information\");"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Project Layout",
    "zh": "项目结构"
   },
   "body": {
    "en": "A project has zan.proj describing the entry file, target platforms, icon and resources. Source files live alongside it.",
    "zh": "项目包含 zan.proj，描述入口文件、目标平台、图标和资源。源文件与它放在一起。"
   },
   "code": "MyApp/\n  zan.proj        // project config (entry, platforms, icon)\n  MyApp.zan       // source / code-behind\n  MyApp.zform     // visual form layout (Window App)\n  assets/         // bundled resources"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Run & Build",
    "zh": "运行与构建"
   },
   "body": {
    "en": "Run compiles and launches the active file (or the project entry). Build produces an executable without running it.",
    "zh": "运行会编译并启动当前文件（或项目入口）。构建只生成可执行文件而不运行。"
   },
   "code": "// Ribbon: Run (F5) or Build.\n// A stray single file compiles on its own;\n// a project (zan.proj) compiles the whole entry."
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Standalone Source Files",
    "zh": "独立源文件"
   },
   "body": {
    "en": "Open can load a .zan file without creating a project. The explorer remains empty; Run, Build and Debug use the file's containing folder for temporary output.",
    "zh": "“打开”可直接载入 .zan 文件而无需创建项目。资源管理器保持为空；运行、构建和调试会在文件所在目录使用临时输出。"
   },
   "code": "// Ribbon: Open -> choose example.zan\n// Run / Build / Debug operate on example.zan.\n// Add `using Gui;` to link the GUI runtime automatically."
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Debugging",
    "zh": "调试"
   },
   "body": {
    "en": "Debug first validates a fresh debug build. It pauses at the first breakpoint, or at the first executable line when no breakpoint exists. Use Continue, Step Over, Step Into and Stop from the debug toolbar.",
    "zh": "调试会先执行新的调试构建。若有断点则停在首个断点；没有断点时停在首个可执行行。可用调试工具栏中的继续、单步跳过、单步进入和停止。"
   },
   "code": "// Click a line number to toggle a breakpoint.\n// Ribbon: Debug\n// Fix compiler errors before the debug session starts."
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Window Designer (.zform)",
    "zh": "窗口设计器 (.zform)"
   },
   "body": {
    "en": "A Window App pairs a .zform layout with a .zan code-behind. Edit the layout visually; write your logic in OnLoad(app).",
    "zh": "窗口程序由 .zform 布局与 .zan 代码后置配对组成。可视化编辑布局；在 OnLoad(app) 中编写逻辑。"
   },
   "code": "// MyApp.zform  -> visual layout (auto-generated region)\n// MyApp.zan    -> code-behind\nstatic void OnLoad(App app) {\n    // your logic here\n}"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Publish",
    "zh": "发布"
   },
   "body": {
    "en": "Publish produces a standalone executable per selected platform under publish/. Tick a platform in the publish window; the app icon is embedded into the exe. Windows exe files can also be packed into a single self-extracting file (DLLs bundled). Linux targets are cross-compiled here as a single static file to run on a Linux server or container. Library projects instead publish static libraries (.lib on Windows, .a elsewhere).",
    "zh": "发布会为每个所选平台在 publish/ 下生成独立可执行文件。在发布窗口中勾选目标平台；应用图标会嵌入 exe。Windows 的 exe 还可打包为单文件自解压程序（DLL 一并捆绑）。Linux 目标在本机交叉编译为静态单文件，供 Linux 服务器或容器运行。库项目则发布静态库（Windows 为 .lib，其余平台为 .a）。"
   },
   "code": "// Ribbon: Publish -> choose platforms\n// Output: publish/MyApp-windows-x64.exe\n// Linux  : cross-compiled static single file\n// Single : Windows exe with DLLs bundled (self-extracting)"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Library Projects",
    "zh": "库项目"
   },
   "body": {
    "en": "A Library project compiles to a DLL instead of an exe: Run/Build produces the shared library only, with no entry point to launch. The Class Library template ships reusable classes; Export DLL exposes functions other programs can call. Publishing a library emits static libraries (.lib / .a) for each platform.",
    "zh": "库项目编译为 DLL 而非 exe：运行/构建只产出共享库，没有可启动的入口。类库模板提供可复用的类；导出 DLL 暴露可供其他程序调用的函数。发布库项目会为每个平台产出静态库（.lib / .a）。"
   },
   "code": "// New Project -> Library -> Class Library\n// Output: build/MyLib.dll\n// Publish -> static libraries (.lib on Windows, .a elsewhere)"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Asset Manager",
    "zh": "资源管理器"
   },
   "body": {
    "en": "The Asset Manager manages everything bundled with your app. Import files or whole folders (copied into assets/), link resources referenced in place, or embed assets encrypted into a .zrp. Pick a file to rename or delete it, and author 8-direction or frame-sequence animations for sprites. Project Configuration shows the icon picker and a resource summary.",
    "zh": "资源管理器管理随应用打包的所有资源。可导入文件或整个文件夹（复制到 assets/）、按原位置链接资源，或将资源加密嵌入 .zrp。选中文件可重命名或删除，还可为精灵编辑八方向或序列帧动画。项目配置提供图标选择与资源摘要。"
   },
   "code": "// Ribbon: Project Configuration -> Open Asset Manager\n// Import file / Import folder / Link file\n// Publish strategy: copy files vs embed (encrypted .zrp)"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Project Templates",
    "zh": "项目模板"
   },
   "body": {
    "en": "New Project offers Console (Hello World, command-line args, multi-file), Window App (empty, component showcase, WebView, free layout and fixed sizes: phone 390x844, tablet 768x1024, desktop 1280x720, small screen 480x320, QVGA 320x240, WVGA 800x480, watch 466x466 round), Library (Class Library, Export DLL) and Server (HTTP, WebSocket gateway, Web MVC, MQTT/IoT, TCP echo) templates. Fixed-size window templates are free-layout canvases that also support breakpoint layouts for extra sizes in the designer.",
    "zh": "新建项目提供多种模板：控制台（Hello World、命令行参数、多文件）、窗口程序（空窗口、组件展示、WebView、自由布局，以及固定尺寸：手机 390x844、平板 768x1024、桌面 1280x720、小屏 480x320、QVGA 320x240、WVGA 800x480、手表 466x466 圆形）、库（类库、导出 DLL）和服务器（HTTP、WebSocket 网关、Web MVC、MQTT/物联网、TCP 回显）。固定尺寸窗口模板是自由布局画布，还可在设计器中为更多尺寸保存断点布局。"
   },
   "code": "// New Project -> category list:\n//   Console / Window App / Library / Server\n// Window App sizes: 320x240 .. 1280x720, 466x466 round"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "zan.proj Reference",
    "zh": "zan.proj 配置参考"
   },
   "body": {
    "en": "zan.proj is the project file: one key = value per line. type selects console / gui / library, entry points at the main .zan or .zform, platform names the target. icon embeds the app icon, singleFile packs the Windows exe with its DLLs into one self-extracting file, staticLib also emits a .lib / .a for library projects.",
    "zh": "zan.proj 是项目文件：每行一个 key = value。type 选择 console / gui / library，entry 指向主 .zan 或 .zform，platform 指定目标平台。icon 嵌入应用图标，singleFile 把 Windows exe 与 DLL 打包成单个自解压文件，staticLib 让库项目额外产出 .lib / .a。"
   },
   "code": "# MyApp/zan.proj\nname = MyApp\ntype = gui          # console | gui | library\ntarget = exe\nentry = src/App.zform\nplatform = windows-x64\nicon = assets/app.ico\nsingleFile = true   # pack exe + DLLs into one file (Windows)\nstaticLib = true    # library projects also emit .lib / .a\nskins = 1           # bundle skin packs"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "zanc Command Line",
    "zh": "zanc 命令行"
   },
   "body": {
    "en": "zanc compiles a source file directly - a project is just the entry file. --auto-stdlib locates the standard library, -o names the output, --publish builds an optimized release, --emit-lib produces a shared library, and --target cross-compiles (--list-targets shows them). -g keeps debug info, --check-leaks reports unreleased objects at exit.",
    "zh": "zanc 直接编译源文件——项目就是入口文件。--auto-stdlib 自动定位标准库，-o 指定输出，--publish 构建优化发布版，--emit-lib 产出共享库，--target 交叉编译（--list-targets 列出目标）。-g 保留调试信息，--check-leaks 在退出时报告未释放对象。"
   },
   "code": "zanc src/main.zan --auto-stdlib -o out.exe\nzanc src/main.zan --auto-stdlib --publish -o out.exe\nzanc src/lib.zan --emit-lib -o lib.dll\nzanc src/main.zan --target linux-musl --link-mode static -o app\nzanc --list-targets"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "Form Code-Behind",
    "zh": "窗口代码后置"
   },
   "body": {
    "en": "A designed window is a partial class: the form compiler projects the named controls from the .zform as typed fields, and OnLoad(form) runs once after the window is built - the place to wire events with OnClick / OnChange lambdas and to seed the state.",
    "zh": "设计窗口是 partial 类：表单编译器把 .zform 里命名的控件投影成类型化字段，OnLoad(form) 在窗口构建完成后执行一次——在这里用 OnClick / OnChange lambda 接事件、初始化状态。"
   },
   "code": "using System;\nusing Gui;\nusing Gui.Widget;\n\npartial class MyApp {\n    static void OnLoad(Form form) {\n        OkButton.OnClick(() => {\n            TitleLabel.Text = \"Hello!\";\n        });\n    }\n}"
  },
  {
   "cat": {
    "en": "Project & Build",
    "zh": "项目与构建"
   },
   "title": {
    "en": "AI Assistant",
    "zh": "AI 助手"
   },
   "body": {
    "en": "The AI assistant panel is a tool loop: the model replies with a fenced zan-tool block, the IDE runs that tool inside your project and appends the result to the transcript. Read tools (read_file, list_dir, grep, repo_map) run silently; mutating tools (write_file, edit_file, build, run, ui_check) wait for your Approve unless Auto-execute is on. Every file tool is confined to the project directory, and ui_check drives the real UI with synthetic input to verify a fix against actual pixels.",
    "zh": "AI 助手面板是一个工具循环：模型用 fenced 的 zan-tool 代码块回复，IDE 在项目内运行该工具并把结果追加到对话记录。只读工具（read_file、list_dir、grep、repo_map）静默执行；改动工具（write_file、edit_file、build、run、ui_check）在未开启“自动执行”时等待你的批准。所有文件工具都被限制在项目目录内；ui_check 会用合成输入驱动真实 UI，按实际像素验证修复。"
   },
   "code": "```zan-tool\n{\"tool\": \"read_file\", \"path\": \"src/Main.zan\", \"offset\": 1, \"limit\": 200}\n```"
  },
  {
   "cat": {
    "en": "Errors",
    "zh": "错误排查"
   },
   "title": {
    "en": "Reading Compiler Errors",
    "zh": "读懂编译器错误"
   },
   "body": {
    "en": "After Run/Build, errors appear as red underlines in the editor; the reason shows above the current line. Click a problem to jump to it.",
    "zh": "运行/构建后，错误会在编辑器中显示为红色下划线；原因显示在当前行上方。点击问题即可跳转。"
   },
   "code": "// error: expected ';'\nint x = 1   // <- missing semicolon\nint y = 2;"
  },
  {
   "cat": {
    "en": "Errors",
    "zh": "错误排查"
   },
   "title": {
    "en": "Add a Missing using",
    "zh": "补全缺失的 using"
   },
   "body": {
    "en": "When an unknown type belongs to System, System.IO, Gui or Gui.Widget, the Error List shows an Add using action. Click it to insert the import once at the top of the active source file.",
    "zh": "当未知类型属于 System、System.IO、Gui 或 Gui.Widget 时，错误列表会显示“添加 using”操作。点击后会在当前源文件顶部插入一次对应导入。"
   },
   "code": "// Error List: unknown type 'Button'\n// Click: Add using Gui.Widget\nusing Gui.Widget;"
  },
  {
   "cat": {
    "en": "Errors",
    "zh": "错误排查"
   },
   "title": {
    "en": "Undefined Symbol",
    "zh": "未定义的符号"
   },
   "body": {
    "en": "An 'undefined reference' at link time usually means a needed library was not linked. GUI apps must include the Gui runtime.",
    "zh": "链接期出现 “undefined reference” 通常意味着缺少所需的库。GUI 程序必须包含 Gui 运行时。"
   },
   "code": "// LNK2019 undefined symbol: zan_gui_draw_text\n// Fix: use App/Canvas from the Gui library so the\n// GUI runtime is linked in.\nusing Gui;"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Typography",
    "zh": "Typography"
   },
   "body": {
    "en": "Headings, paragraphs and inline text styles for documentation and application content.",
    "zh": "用于文档和应用内容的标题、段落与行内文本样式。"
   },
   "code": "Typography.Heading(app, 40, 40, \"Heading\", 1);\nTypography.Paragraph(app, 40, 84, \"Readable body text.\");"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Avatar",
    "zh": "Avatar"
   },
   "body": {
    "en": "Render an initial or icon inside a circular avatar.",
    "zh": "在圆形头像中渲染首字母或图标。"
   },
   "code": "new Avatar(\"Z\", 40, \"\").RenderAt(app, 40, 60);\nnew Avatar(\"\", 40, \"user\").RenderAt(app, 96, 60);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Badge",
    "zh": "Badge"
   },
   "body": {
    "en": "Use a status dot or numeric count badge next to another control.",
    "zh": "在其他控件旁显示状态圆点或数字计数徽标。"
   },
   "code": "Badge.Dot(app, 48, 60);\nBadge.CountAt(app, 96, 60, 12);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Breadcrumb",
    "zh": "Breadcrumb"
   },
   "body": {
    "en": "Show the current location as a sequence of navigation labels.",
    "zh": "用一组导航标签显示当前位置。"
   },
   "code": "List<string> items = new List<string> { \"Home\", \"Projects\", \"Zan\" };\nBreadcrumb.Of(items).RenderAt(app, 40, 60);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Calendar",
    "zh": "Calendar"
   },
   "body": {
    "en": "Naive UI style calendar: a full month card (mode month) or a compact panel (mode panel) with per-day marks, today highlight and disabled dates.",
    "zh": "Naive UI 风格日历：mode=month 整幅月历卡，mode=panel 紧凑面板，支持逐日议程、今天高亮与禁用日期。"
   },
   "code": "Calendar cal = new Calendar();\ncal.OnChange(() => { Console.WriteLine(cal.GetDate().ToDateString()); });\ncal.AddMark(DateTime.FromDate(2026, 8, 28), \"发布日\", 1);\ncal.RenderInside(app, new Rect(20, 60, 560, 300));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Carousel",
    "zh": "Carousel"
   },
   "body": {
    "en": "Rotate through a list of slide labels with a SignalInt active index.",
    "zh": "使用 SignalInt 当前索引在多张幻灯片标签之间切换。"
   },
   "code": "List<string> slides = new List<string> { \"First\", \"Second\" };\nSignalInt slide = new SignalInt(0);\nCarousel.Of(slides, slide).RenderInside(app, new Rect(40, 60, 420, 180));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Chart",
    "zh": "Chart"
   },
   "body": {
    "en": "Draw line, area, bar, pie, donut and scatter charts from an ECharts-style ChartOption.",
    "zh": "从 ECharts 风格的 ChartOption 绘制折线图、面积图、柱状图、饼图、环图和散点图。"
   },
   "code": "List<string> labels = new List<string> { \"Q1\", \"Q2\", \"Q3\" };\nList<int> values = new List<int> { 12, 28, 20 };\nChartOption opt = ChartOption.Line(\"Quarterly sales\", labels);\nopt.Add(\"Sales\", values);\nChartView.Of(opt).Render(app, 40, 60, 480, 240);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Collapse",
    "zh": "Collapse"
   },
   "body": {
    "en": "Display expandable sections controlled by a SignalInt open index.",
    "zh": "显示由 SignalInt 展开索引控制的可折叠区段。"
   },
   "code": "List<CollapsePanel> panels = new List<CollapsePanel> {\n    new CollapsePanel(\"General\", \"General settings\"),\n    new CollapsePanel(\"Advanced\", \"Advanced settings\")\n};\nSignalInt open = new SignalInt(0);\nCollapse.Of(panels, open).RenderAt(app, 40, 60, 420);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "DataTable",
    "zh": "DataTable"
   },
   "body": {
    "en": "A stateful table with typed columns, sorting, filtering and CSV export.",
    "zh": "支持类型列、排序、筛选和 CSV 导出的有状态表格。"
   },
   "code": "List<DataColumn> cols = new List<DataColumn> {\n    DataColumn.Text(\"Name\", 180),\n    DataColumn.Numeric(\"Score\", 100)\n};\nList<DataRow> rows = new List<DataRow>();\nDataTableState state = new DataTableState();\nDataTable.Render(app, 40, 60, 520, 260, cols, rows, state);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Dialog",
    "zh": "Dialog"
   },
   "body": {
    "en": "Render a modal dialog with a title, message and action buttons.",
    "zh": "渲染带标题、消息和操作按钮的模态对话框。"
   },
   "code": "int action = Dialog.Render(app, 420, 220, 0, \"Confirm\", \"Save changes?\", \"Save\", \"Cancel\");"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Divider",
    "zh": "Divider"
   },
   "body": {
    "en": "Separate content horizontally, vertically or with a text label.",
    "zh": "用水平线、垂直线或带文字的分隔线划分内容。"
   },
   "code": "new Divider().RenderInside(app, new Rect(40, 80, 420, 8));\nnew Divider(\"Section\").RenderInside(app, new Rect(40, 120, 420, 24));\nnew Divider { Class = \"vertical\" }.RenderInside(app, new Rect(500, 60, 8, 120));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Drawer",
    "zh": "Drawer"
   },
   "body": {
    "en": "Show a side panel attached to an edge of the current window.",
    "zh": "显示附着在当前窗口边缘的侧边面板。"
   },
   "code": "int action = Layer.Modal(app, 400, 320, \"Details\");\nint bodyY = Layer.BodyTop(app, layer);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Dropdown",
    "zh": "Dropdown"
   },
   "body": {
    "en": "Retained action menu: click the trigger (or call Open()/Close() from caller state) to drop a menu of commands in the overlay pass; picking one fires Change — run the command there and Clear() so the trigger returns to its hint. A command menu, not a value picker (that is SelectBox). BindMenu takes a MenuItem tree with icons, separators, group headers, disabled/danger items and a flyout submenu; long menus scroll (wheel and drag).",
    "zh": "保留态动作菜单：点击触发器（或由调用方 Open()/Close() 手动开合）在覆盖层弹出一组命令；选中一项触发 Change——在处理器里执行命令并 Clear()，触发器回到提示语。它是命令菜单而不是选值控件（选值用 SelectBox）。BindMenu 接 MenuItem 树——图标、分隔线、分组标题、禁用/危险项和悬停展开的子菜单，长菜单自动出滚动条（滚轮与拖动）。"
   },
   "code": "Dropdown<string> menu = new Dropdown<string>(s => s);\nmenu.Hint(\"File menu\");\nmenu.BindMenu(new List<MenuItem> {\n    MenuItem.Item(\"Rename...\", \"edit\", 1),\n    MenuItem.Header(\"Export\"),\n    MenuItem.Submenu(\"Export as...\", \"download\", kids),\n    MenuItem.Danger(\"Delete\", \"trash\", 5),\n});\nmenu.OnChange(OnAct);          // menu.Action() = picked leaf action\nparent.Add(menu);              // size and position come from the layout\n// flat Bind(list) also works: picked index lands in Selected()"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Ellipsis",
    "zh": "Ellipsis"
   },
   "body": {
    "en": "Measure and truncate long text so it fits a fixed width.",
    "zh": "测量并截断长文本，使其适合固定宽度。"
   },
   "code": "string shown = Ellipsis.Fit(\"A very long file name.zan\", 160, app.theme.fontSizeMedium);\nEllipsis.Render(app, 40, 60, 160, shown);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Empty",
    "zh": "Empty"
   },
   "body": {
    "en": "Display a clear empty-state placeholder when no data is available.",
    "zh": "没有数据时显示清晰的空状态占位内容。"
   },
   "code": "new Empty(\"No records yet\").RenderInside(app, new Rect(40, 60, 420, 180));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "FilePicker",
    "zh": "FilePicker"
   },
   "body": {
    "en": "Open the reusable custom folder, open-file or save-file child window.",
    "zh": "打开可复用的自绘文件夹、打开文件或保存文件子窗口。"
   },
   "code": "FilePicker picker = new FilePicker();\npicker.BeginOpenFile(\".\", \".zan\", \"Open Zan source\");\n// Each outer loop: picker.RenderWindow();\n// Then inspect picker.TakeResult() and picker.SelectedPath()."
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "FloatButton",
    "zh": "FloatButton"
   },
   "body": {
    "en": "A compact circular floating action button with an icon.",
    "zh": "带图标的紧凑圆形浮动操作按钮。"
   },
   "code": "int id = new FloatButton(\"plus\") { Size = 48, Class = \"primary\" }\n    .Render(app, 520, 360);\nif (Ui.Clicked(app, id)) {\n    // create an item\n}"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "ListView",
    "zh": "ListView"
   },
   "body": {
    "en": "Render a compact vertical list of strings.",
    "zh": "渲染紧凑的纵向字符串列表。"
   },
   "code": "List<string> items = new List<string> { \"Alpha\", \"Beta\" };\nListView<string> list = new ListView<string>(new ListColumn<string>(\"\", s => s));\nlist.Bind(items);\nside.Add(list);      // CSS gives it its box"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Loading",
    "zh": "Loading"
   },
   "body": {
    "en": "Draw an animated loading indicator (Spin; Spin.WithTip adds a caption).",
    "zh": "绘制动画加载指示器（Spin；Spin.WithTip 附带说明文字）。"
   },
   "code": "new Spin(36).RenderAt(app, 80, 80);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Menu",
    "zh": "Menu"
   },
   "body": {
    "en": "A vertical application menu with a SignalInt active item.",
    "zh": "使用 SignalInt 当前项的纵向应用菜单。"
   },
   "code": "List<string> labels = new List<string> { \"Home\", \"Settings\" };\nSignalInt active = new SignalInt(0);\nint first = Menu.RenderVertical(app, 40, 60, 220, 300, 36, labels, active, 0);\nMenu.HandleClick(app, first, labels.Count, active);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Message",
    "zh": "Message"
   },
   "body": {
    "en": "Render a temporary inline toast message in a screen slot.",
    "zh": "在屏幕位置中渲染临时行内消息提示。"
   },
   "code": "Layer.Msg(\"Saved successfully\", 1);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Modal",
    "zh": "Modal"
   },
   "body": {
    "en": "Draw a centered modal surface and receive its close action.",
    "zh": "绘制居中的模态表面并接收关闭操作。"
   },
   "code": "int action = Layer.Modal(app, 460, 260, \"Account settings\");"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Notification",
    "zh": "Notification"
   },
   "body": {
    "en": "Show a notification card at a chosen position.",
    "zh": "在指定位置显示通知卡片。"
   },
   "code": "Layer.Notify(\"Build completed\", 1);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "PageHeader",
    "zh": "PageHeader"
   },
   "body": {
    "en": "Render a page title, subtitle and back action.",
    "zh": "渲染页面标题、副标题与返回操作。"
   },
   "code": "PageHeader h = new PageHeader(\"Components\", \"GUI reference\");\nh.RenderAt(app, 40, 40, 520);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Pagination",
    "zh": "Pagination"
   },
   "body": {
    "en": "Navigate pages with a SignalInt current page.",
    "zh": "使用 SignalInt 当前页在多个页面之间导航。"
   },
   "code": "SignalInt page = new SignalInt(1);\nPagination.Of(12, page).RenderAt(app, 40, 60);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Panel",
    "zh": "Panel"
   },
   "body": {
    "en": "Create card, section or colored surface rectangles for custom content.",
    "zh": "为自定义内容创建卡片、区段或彩色表面矩形。"
   },
   "code": "Panel box = new Panel(\"card\");\nbox.With(new Label { Text = \"Inside the panel\" });\nbox.RenderInside(app, new Rect(40, 60, 420, 220));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "PivotView",
    "zh": "PivotView"
   },
   "body": {
    "en": "Pivot is a view mode of the DataTable itself: SetPivot switches the grid into a cross-tab (row x column dimension, aggregated) with a totals row; ExitPivot restores. Aggregate: 0 sum / 1 average / 2 count / 3 max / 4 min.",
    "zh": "透视是 DataTable 自身的视图形态：SetPivot 把网格整体切换为交叉表（行维度 × 列维度聚合）并带总计行，ExitPivot 还原。聚合：0 求和 / 1 平均 / 2 计数 / 3 最大 / 4 最小。"
   },
   "code": "DataTableState st = new DataTableState();\nDataTable.SetPivot(st, cols, src, 0, 1, 2, 0);\nDataTable.RenderSource(app, 40, 60, 560, 280, cols, src, st);\nDataTable.ExitPivot(st);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Popconfirm",
    "zh": "Popconfirm"
   },
   "body": {
    "en": "Ask for confirmation next to an invoking control.",
    "zh": "在触发控件旁请求用户确认。"
   },
   "code": "int action = Layer.DrawPopconfirm(app, 180, 80, \"Delete this item?\", \"Delete\", \"Cancel\");"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Popover",
    "zh": "Popover"
   },
   "body": {
    "en": "Display titled explanatory content next to an anchor.",
    "zh": "在锚点旁显示带标题的说明内容。"
   },
   "code": "Popover.Render(app, 180, 80, 260, 140, \"Details\", \"Additional information\");"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Rate",
    "zh": "Rate"
   },
   "body": {
    "en": "An interactive star rating that binds a plain int field via `.data`.",
    "zh": "通过 `.data` 绑定普通 int 字段的交互式星级评分。"
   },
   "code": "class Form { int rating; }\nForm form = new Form();\nRate rate = new Rate(new SignalInt(3), 5);\nrate.data = form.rating;\nrate.Render(app, 40, 60);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Result",
    "zh": "Result"
   },
   "body": {
    "en": "Show a success, warning or error result with a title and description.",
    "zh": "显示带标题和说明的成功、警告或错误结果。"
   },
   "code": "Result r = new Result(\"Published\", \"The executable is ready.\");\nr.Class = \"success\";\nr.RenderAt(app, 40, 60, 460);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Ribbon",
    "zh": "Ribbon"
   },
   "body": {
    "en": "Build command groups for an IDE-style ribbon toolbar.",
    "zh": "为 IDE 风格的功能区工具栏构建命令分组。"
   },
   "code": "List<RibbonCmd> commands = new List<RibbonCmd> {\n    RibbonCmd.Group(\"play\", \"Run\"),\n    new RibbonCmd(\"build\", \"Build\")\n};\nRibbon.Render(app, 0, 40, 800, Ribbon.Height(app), commands);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Scrollbar",
    "zh": "Scrollbar"
   },
   "body": {
    "en": "Draw a vertical scrollbar for a custom scrollable surface.",
    "zh": "为自定义可滚动表面绘制纵向滚动条。"
   },
   "code": "Scrollbar.RenderVertical(app, 500, 60, 240, 900, 120);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Skeleton",
    "zh": "Skeleton"
   },
   "body": {
    "en": "Compose rectangle, circle and text placeholders while content loads.",
    "zh": "在内容加载时组合矩形、圆形和文本骨架占位。"
   },
   "code": "Skeleton dot = new Skeleton(40, 40);\ndot.Class = \"circle\";\ndot.RenderAt(app, 40, 60);\nSkeleton.Line(app, 240).RenderAt(app, 96, 62);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Spin",
    "zh": "Spin"
   },
   "body": {
    "en": "Draw a spinner by itself or centered with a loading tip.",
    "zh": "单独绘制旋转器，或将其与加载提示居中显示。"
   },
   "code": "new Spin(36).RenderAt(app, 80, 80);\nSpin s = new Spin(36);\ns.Tip = \"Loading data...\";\ns.RenderInside(app, new Rect(40, 120, 280, 100));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Split",
    "zh": "Split"
   },
   "body": {
    "en": "Create draggable vertical or horizontal panes backed by a SignalInt size.",
    "zh": "创建由 SignalInt 尺寸控制、可拖动的纵向或横向分栏。"
   },
   "code": "SignalInt firstWidth = new SignalInt(240);\nRect area = new Rect(0, 60, 800, 500);\nSplit panes = Split.Vertical(app, area, firstWidth, 160, 240);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Statistic",
    "zh": "Statistic"
   },
   "body": {
    "en": "Show a highlighted numeric value with a descriptive label.",
    "zh": "显示带说明标签的突出数值。"
   },
   "code": "new Statistic(\"Active users\", \"12,480\").RenderAt(app, 40, 60);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Steps",
    "zh": "Steps"
   },
   "body": {
    "en": "Show progress through a sequence of labeled steps.",
    "zh": "显示一组带标签步骤的完成进度。"
   },
   "code": "List<string> labels = new List<string> { \"Details\", \"Review\", \"Done\" };\nSignalInt current = new SignalInt(1);\nSteps.Of(labels, current).RenderAt(app, 40, 60, 520);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Table",
    "zh": "Table"
   },
   "body": {
    "en": "Render a lightweight table header and striped rows.",
    "zh": "渲染轻量表头与交替底色的数据行。"
   },
   "code": "List<string> cols = new List<string> { \"Name\", \"Status\" };\nList<int> widths = new List<int> { 220, 120 };\nTable.RenderHeader(app, 40, 60, cols, widths);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Timeline",
    "zh": "Timeline"
   },
   "body": {
    "en": "Display ordered events with titles and descriptions; supports size variants, right mirroring, horizontal layout, per-item icons and a pending (dashed-tail) state.",
    "zh": "按顺序显示带标题与说明的事件；支持大小档、右向镜像、水平排布、条目自定义图标与待办态（虚线尾）。"
   },
   "code": "List<TimelineItem> items = new List<TimelineItem> {\n    new TimelineItem(\"\", \"Created\", \"Project initialized\", \"success\", \"eye\"),\n    new TimelineItem(\"\", \"Published\", \"Executable generated\")\n};\nTimeline tl = Timeline.Of(items);\ntl.Size = \"large\";              // small / medium / large\ntl.Class = \"horizontal\";        // 或 \"right\"；items[i].pending = true 出虚线尾\ntl.RenderAt(app, 40, 60, 520);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "TreeView",
    "zh": "TreeView"
   },
   "body": {
    "en": "Render expandable directory and file nodes with scrolling and selection.",
    "zh": "渲染带滚动与选择的可展开目录和文件节点。"
   },
   "code": "List<TreeNode> nodes = new List<TreeNode> {\n    TreeNode.Dir(\"src\", 0, true),\n    TreeNode.FileIcon(\"main.zan\", 1, \"file\")\n};\n\nTreeView tree = new TreeView();\ntree.Bind(nodes);\ntree.OnSelect(OnPick);\ntree.OnActivate(OnOpen);\nparent.Add(tree);   // size and position come from the layout"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "Wizard",
    "zh": "Wizard"
   },
   "body": {
    "en": "Build a full child-window template or project creation workflow.",
    "zh": "构建完整的子窗口模板或项目创建流程。"
   },
   "code": "List<WizardItem> items = new List<WizardItem> {\n    new WizardItem(\"Console\", \"menu\", \"A console app.\"),\n    new WizardItem(\"Library\", \"grid\", \"A reusable library.\")\n};\nSignalInt selected = new SignalInt(0);\nInput name = new Input(\"project name\");\nInput location = new Input(\"location\");\nWizard wiz = new Wizard(\"New Project\", items, selected, name, location, true);\nint result = wiz.Show(app);"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "CodeEditor",
    "zh": "CodeEditor"
   },
   "body": {
    "en": "A full source editor with diagnostics, undo/redo, multiple carets, breakpoints and completion.",
    "zh": "完整的源代码编辑器，支持诊断、撤销/重做、多光标、断点与补全。"
   },
   "code": "CodeEditor editor = new CodeEditor();\neditor.LoadText(\"class Program {\\n}\\n\");\neditor.Render(app, new Rect(40, 60, 680, 420));"
  },
  {
   "cat": {
    "en": "GUI - Widgets",
    "zh": "GUI - 组件"
   },
   "title": {
    "en": "WebView",
    "zh": "WebView"
   },
   "body": {
    "en": "WebView embeds a native browser surface (WebView2 on Windows). EnsureCreated once, then Navigate to a URL or LoadHtml for inline content; Render draws it at a rectangle every frame. Eval runs JavaScript and returns the result; Back / Forward / Reload steer like a browser.",
    "zh": "WebView 嵌入原生浏览器表面（Windows 上是 WebView2）。EnsureCreated 一次，然后 Navigate 打开 URL 或 LoadHtml 显示内联内容；每帧用 Render 在矩形内绘制。Eval 执行 JavaScript 并返回结果；Back / Forward / Reload 与浏览器一致。"
   },
   "code": "using Gui;\nusing Gui.Component.WebView;\n\nWebView web = new WebView();\nweb.EnsureCreated(app);\nweb.Navigate(\"https://example.com\");\nweb.Render(app, 0, 0, 800, 600);"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Skins are CSS",
    "zh": "皮肤就是 CSS"
   },
   "body": {
    "en": "A skin is a folder with skin.css. The engine loads skins/base.css first (the built-in look of every widget), then the pack's skin.css on top, then the app's own sheet. Skins are found under $ZAN_GUI_SKINS, the exe's skins/ or assets/skins/, and stdlib/Gui/skins; a pack that is missing falls back to the current theme instead of failing. App.UseSkin(name) applies one, App.ReloadSkin() re-reads it from disk for live editing, App.UseCss(css) sets the app sheet and App.UseAppCss(css) keeps it layered over every skin.",
    "zh": "皮肤就是一个带 skin.css 的目录。引擎先加载 skins/base.css（所有控件的内置外观），再叠加皮肤包的 skin.css，最后叠加应用自己的样式表。皮肤在 $ZAN_GUI_SKINS、exe 旁的 skins/ 或 assets/skins/、以及 stdlib/Gui/skins 中查找；找不到的皮肤会退回当前主题而不是报错。App.UseSkin(name) 应用皮肤，App.ReloadSkin() 从磁盘重新读取（改 CSS 免重启），App.UseCss(css) 设置应用样式表，App.UseAppCss(css) 则让它一直叠在每个皮肤之上。"
   },
   "code": "/* skins/mypack/skin.css */\n:root {\n  --name: \"My Pack\";   /* skin picker label */\n  --order: 20;          /* sort order in the picker */\n  --base: dark;         /* dark | light -- the token baseline */\n  --accent: #d92b2b;\n}\n\nbutton.primary       { background: var(--accent); radius: 999; }\nbutton.primary:hover { background: #ff5555; }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Selectors",
    "zh": "选择器"
   },
   "body": {
    "en": "One selector is type.class#id::part:state -- every piece optional, in that order. Selector lists (a, b { }) and /* comments */ are supported; :root holds custom properties. Descendant/child combinators (a b, a > b), attribute selectors and * are NOT modelled: such a rule never matches. Specificity: state (1000000) > id (10000) > each class (100) > type (1); on a tie the later rule wins. Properties outside the set the engine models are parsed and then dropped: an unknown declaration is silently ignored, never an error.",
    "zh": "一个选择器的形状是 type.class#id::part:state —— 每一段都可省略，顺序固定。支持选择器列表（a, b { }）和 /* 注释 */；:root 用来放自定义属性。后代/子选择器（a b、a > b）、属性选择器和 * 不在建模范围内：这类规则永远不会命中。特异性：状态（1000000）> id（10000）> 每个类（100）> 类型（1）；同权重时后写的规则胜出。 引擎建模之外的属性会被解析后丢弃：未知声明只是静默忽略，不会报错。"
   },
   "code": "input               { border: 1 #3a3a3a; }      /* type */\ninput.search        { radius: 999; }            /* type + class */\n#Save               { background: #2f6f3f; }    /* control name */\ninput::placeholder  { color: #6b6b6b; }         /* part */\ninput:focus         { border: 1 #4c8dff; }      /* state */\nbutton, tab         { font-size: 14; }          /* list */"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Classes and ids",
    "zh": "类与 id"
   },
   "body": {
    "en": "A control carries three handles the cascade matches on: its type (the widget name, or Kind() for a custom control), its class list and its name. Class is a space-separated string: set it in code (btn.Class = \"primary small\"), add one at a time with AddClass(\"loading\"), or write \"class\": \"primary small\" in a .zform / UiDoc field -- and name becomes the #id selector. The built-in widgets already put their variants there, so a skin restyles them without touching code: role primary / info / success / warning / error, fill outline / text / dashed, size tiny / small / medium / large, plus widget-specific ones such as button.circle, button.stacked and button.loading.",
    "zh": "级联能匹配的是控件身上的三样东西：类型（控件名，自定义控件是 Kind()）、类列表和 name。Class 是空格分隔的字符串：代码里直接赋值（btn.Class = \"primary small\"），或用 AddClass(\"loading\") 追加一个，或者在 .zform / UiDoc 的字段里写 \"class\": \"primary small\"；name 则对应 #id 选择器。内置控件的变体本来就放在这里，所以皮肤不改代码就能重新样式化它们：角色 primary / info / success / warning / error，填充 outline / text / dashed，尺寸 tiny / small / medium / large，以及各控件自有的类，比如 button.circle、button.stacked、button.loading。"
   },
   "code": "// Zan\nButton save = new Button { Text = \"Save\", Class = \"primary small\" };\nsave.name = \"Save\";\nif (busy) { save.AddClass(\"loading\"); }\n\n/* .zform field */\n// { \"kind\": \"Button\", \"name\": \"Save\", \"class\": \"primary small\" }\n\n/* CSS */\nbutton.primary.small { height: 26; font-size: 13; }\nbutton.loading       { opacity: 0.6; cursor: default; }\n#Save                { min-width: 96; }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "States and parts",
    "zh": "状态与部件"
   },
   "body": {
    "en": "States are the pseudo-classes the engine tracks: :hover, :active, :focus (:focus-visible is the same), :disabled, :selected, :checked. A state rule is layered on top of the base look, so it only needs the properties that change. Parts address a piece inside a widget: input::placeholder / ::caret / ::selection, select::arrow / ::option / ::popup / ::placeholder / ::clear / ::checkmark / ::scrollbar, checkbox::box / ::check / ::label, radio::indicator / ::dot / ::label, switch::track / ::thumb, slider::track / ::fill / ::thumb, progress::track / ::fill / ::label, collapse::header / ::body / ::arrow, carousel::slide / ::dot / ::arrow, pagination::item / ::arrow / ::ellipsis / ::label, rate::star / ::empty, codeblock::line / ::caption.",
    "zh": "状态就是引擎跟踪的伪类：:hover、:active、:focus（:focus-visible 等价）、:disabled、:selected、:checked。状态规则叠加在基础外观之上，因此只写变化的属性即可。部件用来指向控件内部的一块：input::placeholder / ::caret / ::selection，select::arrow / ::option / ::popup / ::placeholder / ::clear / ::checkmark / ::scrollbar，checkbox::box / ::check / ::label，radio::indicator / ::dot / ::label，switch::track / ::thumb，slider::track / ::fill / ::thumb，progress::track / ::fill / ::label，collapse::header / ::body / ::arrow，carousel::slide / ::dot / ::arrow，pagination::item / ::arrow / ::ellipsis / ::label，rate::star / ::empty，codeblock::line / ::caption。"
   },
   "code": "switch::track          { background: #3a3a3a; radius: 999; }\nswitch:checked::track  { background: #2f6f3f; }\nswitch::thumb          { background: #ffffff; radius: 999; }\n\nslider::track { height: 4; background: #333; }\nslider::fill  { background: var(--accent); }\nslider::thumb { width: 14; height: 14; radius: 999; }\n\nbutton:disabled { opacity: 0.45; }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Colors and backgrounds",
    "zh": "颜色与背景"
   },
   "body": {
    "en": "Color values: #rgb, #rgba, #rrggbb, #aarrggbb, rgb()/rgba() (alpha 0..1 or %), hsl()/hsla(), 0xAARRGGBB, transparent / none / currentcolor, and the named colors (white, black, red, green, blue, gold, crimson, teal, ...). background also takes linear-gradient(<dir>, a, b[, c]) with two or three stops; the direction keyword is to bottom / to right / to bottom right / to top (any deg angle snaps to the nearest of those). opacity takes 0..1 or a %, and filter / backdrop-filter reads blur(N) -- that is what frosted-glass skins use.",
    "zh": "颜色值：#rgb、#rgba、#rrggbb、#aarrggbb、rgb()/rgba()（alpha 取 0..1 或百分比）、hsl()/hsla()、0xAARRGGBB、transparent / none / currentcolor，以及具名颜色（white、black、red、green、blue、gold、crimson、teal ……）。background 还接受 linear-gradient(<方向>, a, b[, c])，两到三个色标；方向关键字为 to bottom / to right / to bottom right / to top（任意 deg 角度会吸附到最近的一个）。opacity 取 0..1 或百分比，filter / backdrop-filter 读 blur(N) —— 磨砂玻璃皮肤就靠它。"
   },
   "code": "card    { background: linear-gradient(to bottom, #2a2f3a, #1b1f27); }\ntag.hot { background: rgba(217, 43, 43, 0.18); color: #ff8a8a; }\npopup   { background: #cc1b1f27; backdrop-filter: blur(36); }\nlabel   { color: currentcolor; opacity: 0.7; }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Borders, radius and shadow",
    "zh": "边框、圆角与阴影"
   },
   "body": {
    "en": "border / border-top / -right / -bottom / -left take '<width> [solid|dashed|dotted|none] <color>' in any order; the per-side long forms border-*-width and border-*-color work too. border-radius (radius) takes one value or four corners. box-shadow (shadow) takes '<dx> <dy> [blur] <color>' plus the inset keyword. border-style only distinguishes solid from dashed/dotted. Two non-CSS decorations exist for skin packs: emboss: raised|inset (neumorphic edge pair) and specular: 1 (cursor-following sheen).",
    "zh": "border / border-top / -right / -bottom / -left 接受“<宽度> [solid|dashed|dotted|none] <颜色>”，顺序任意；也可以用 border-*-width、border-*-color 这类单边写法。border-radius（radius）接受一个值或四个角。box-shadow（shadow）接受“<dx> <dy> [模糊] <颜色>”并支持 inset 关键字。border-style 只区分实线与虚线/点线。另有两个不属于 CSS 的装饰供皮肤使用：emboss: raised|inset（拟物凸起/内陷边）和 specular: 1（跟随光标的光泽）。"
   },
   "code": "card   { border: 1 solid #2c2c2c; radius: 12; shadow: 1 2 #40000000; }\ntag    { radius: 999; border: 0; }\ninput  { border-bottom: 2 var(--accent); }\npanel  { emboss: raised; }\nbutton.primary { specular: 1; }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Box metrics",
    "zh": "盒子度量"
   },
   "body": {
    "en": "padding (pad) and margin take 1, 2 or 4 values, plus the single-side forms padding-left/-top/-right/-bottom and margin-*. width / height / min-* / max-* take pixels or a % of the containing box. gap sets the space between a container's docked or flex children. Numbers are plain pixels at 100% DPI -- a unit suffix such as px is accepted and ignored, and the engine scales the value for the current DPI.",
    "zh": "padding（pad）和 margin 接受 1、2 或 4 个值，也可以用 padding-left/-top/-right/-bottom 和 margin-* 的单边写法。width / height / min-* / max-* 接受像素或包含块的百分比。gap 设置容器内停靠或 flex 子节点之间的间距。数字是 100% DPI 下的像素 —— 带 px 之类的单位会被接受并忽略，引擎按当前 DPI 缩放。"
   },
   "code": "panel      { padding: 12 16; gap: 8; }\ncard       { padding: 24; }\nbutton     { height: 32; min-width: 88; }\nsidebar    { width: 25%; max-width: 320; }\ntag        { margin-right: 6; }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Layout properties",
    "zh": "布局属性"
   },
   "body": {
    "en": "display: flex turns a container into one flex line: flex-direction (row | column), justify-content (flex-start | center | flex-end | space-between | space-around), align-items (stretch | flex-start | center | flex-end), flex-wrap, and flex / flex-grow plus order on the children. display: none removes the node from layout. position: relative | absolute with top / right / bottom / left offsets the node inside its parent; overflow (auto | hidden | scroll | clip) clips it; visibility: hidden keeps the space but stops the paint; z-index orders overlapping siblings and cursor picks the pointer (pointer | text | ew-resize | ns-resize).",
    "zh": "display: flex 让容器变成一条 flex 行：flex-direction（row | column）、justify-content（flex-start | center | flex-end | space-between | space-around）、align-items（stretch | flex-start | center | flex-end）、flex-wrap，子节点上还有 flex / flex-grow 和 order。display: none 让节点不参与布局。position: relative | absolute 配合 top / right / bottom / left 在父节点内偏移；overflow（auto | hidden | scroll | clip）裁剪它；visibility: hidden 保留占位但不绘制；z-index 决定重叠兄弟的层序，cursor 选择指针形状（pointer | text | ew-resize | ns-resize）。"
   },
   "code": "toolbar {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  gap: 8;\n}\ntoolbar .spacer { flex-grow: 1; }\nbadge   { position: absolute; top: -4; right: -4; z-index: 2; }\nbutton  { cursor: pointer; }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Motion",
    "zh": "动效"
   },
   "body": {
    "en": "transition / transition-duration set how long a style change eases (ms, or seconds), transition-timing-function picks ease | linear | ease-in | ease-out | ease-in-out. transform supports translate / translateX / translateY, scale and rotate. animation takes a built-in loop by name -- spin, pulse, breath, shimmer, float, glow, aurora, motes -- plus a duration and an easing keyword; animation-duration sets the period on its own.",
    "zh": "transition / transition-duration 决定样式变化的过渡时长（毫秒，或秒），transition-timing-function 选择 ease | linear | ease-in | ease-out | ease-in-out。transform 支持 translate / translateX / translateY、scale 和 rotate。animation 按名字取一个内置循环 —— spin、pulse、breath、shimmer、float、glow、aurora、motes —— 再加时长和缓动关键字；animation-duration 也可单独设置周期。"
   },
   "code": "button        { transition: 120ms; transition-timing-function: ease-out; }\nbutton:hover  { transform: translateY(-1) scale(1.02); }\nspin          { animation: spin 900ms linear; }\ncard.featured { animation: glow 2s ease-in-out; }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Theme tokens",
    "zh": "主题令牌"
   },
   "body": {
    "en": "The :root block of a skin is more than variables: every --token that names a Theme field overwrites it, so one pack restyles every widget at once. --base picks the baseline (dark | light) and --dark forces the window frame. CSS spelling maps to the field name (--bg-primary sets bgPrimary). Colors: primary/info/success/warning/error (+Hover/+Pressed), textPrimary/Secondary/Tertiary/Disabled/Inverse, bgPrimary/Secondary/Tertiary/Hover/Active/Disabled, borderPrimary/Secondary/Hover/Focus, divider, overlay, scrollbar, tooltip*, shadowColor. Metrics: borderRadiusSmall/Medium/Large, borderWidth, fontSize*, height*, padding*, gap*, iconSize*, animFastMs/animMediumMs/animSlowMs. Family flags: glass, gradient, neu, brutal. Other names stay ordinary variables for var().",
    "zh": "皮肤的 :root 不只是变量：每个与 Theme 字段同名的 --token 都会覆盖该字段，所以一个皮肤包能一次改掉所有控件。--base 选基线（dark | light），--dark 强制窗口边框深浅。CSS 拼写会映射到字段名（--bg-primary 设置 bgPrimary）。颜色：primary/info/success/warning/error（及 +Hover/+Pressed）、textPrimary/Secondary/Tertiary/Disabled/Inverse、bgPrimary/Secondary/Tertiary/Hover/Active/Disabled、borderPrimary/Secondary/Hover/Focus、divider、overlay、scrollbar、tooltip*、shadowColor。度量：borderRadiusSmall/Medium/Large、borderWidth、fontSize*、height*、padding*、gap*、iconSize*、animFastMs/animMediumMs/animSlowMs。风格标志：glass、gradient、neu、brutal。其余名字就是普通变量，供 var() 引用。"
   },
   "code": ":root {\n  --name: \"Emerald\";\n  --base: dark;\n  --primary: #2fa36b;\n  --primary-hover: #37bd7c;\n  --bg-primary: #101713;\n  --text-primary: #e8f3ec;\n  --border-radius-medium: 10;\n  --padding-medium: 12;\n  --glass: 1;\n\n  --my-chip: #1d2c24;   /* plain variable */\n}\n\ntag { background: var(--my-chip); }"
  },
  {
   "cat": {
    "en": "GUI - Skins & CSS",
    "zh": "界面 - 皮肤与 CSS"
   },
   "title": {
    "en": "Styling your own components",
    "zh": "给自己的组件写样式"
   },
   "body": {
    "en": "A custom Control subclass is a plain container: no card frame and no padding of its own, so its children decide how it looks. It is still addressable by its Kind() as the CSS type, by its class list and by its name as #id -- which is how a component gets a skinnable look without drawing anything itself. The class vocabulary the built-in widgets share: primary / info / success / warning / error for role, outline / text / dashed for fill, and tiny / small / medium / large for size.",
    "zh": "自定义的 Control 子类是纯容器：没有卡片外框，也不吃自己的内边距，外观由它的子控件决定。它依然可以被 CSS 选中 —— 类型名就是 Kind()，还有类列表，以及作为 #id 的 name。组件因此不必自绘也能有可换肤的外观。内置控件共用的类词汇：角色用 primary / info / success / warning / error，填充用 outline / text / dashed，尺寸用 tiny / small / medium / large。"
   },
   "code": "/* class MyCard : Control { override string Kind() { return \"mycard\"; } } */\nmycard        { background: #1b1f27; radius: 12; padding: 16; gap: 8; }\nmycard:hover  { border: 1 var(--accent); }\n#SavePanel    { padding: 0; }\n\nbutton.primary.large { font-size: 16; }\nbutton.text          { background: none; border: 0; }"
  },
  {
   "cat": {
    "en": "Standard Library",
    "zh": "标准库"
   },
   "title": {
    "en": "Date & Time",
    "zh": "日期与时间"
   },
   "body": {
    "en": "DateTime.Now() and UtcNow() capture the current time. Read parts with Year(), Month(), Day(), Hour(); format with ToString().",
    "zh": "DateTime.Now() 与 UtcNow() 获取当前时间。用 Year()、Month()、Day()、Hour() 读取各部分；用 ToString() 格式化。"
   },
   "code": "using System;\n\nDateTime now = DateTime.Now();\nConsole.WriteLine(now.ToString());\nConsole.WriteLine(Convert.ToString(now.Year()));"
  },
  {
   "cat": {
    "en": "Standard Library",
    "zh": "标准库"
   },
   "title": {
    "en": "Random Numbers",
    "zh": "随机数"
   },
   "body": {
    "en": "Create a Random, then draw values: Next(), NextBelow(n), Between(lo, hi) and NextDouble(). Use Random.Seeded(seed) for a reproducible sequence.",
    "zh": "创建 Random 后取值：Next()、NextBelow(n)、Between(lo, hi)、NextDouble()。用 Random.Seeded(seed) 得到可复现序列。"
   },
   "code": "using System;\n\nRandom rng = new Random();\nint dice = rng.Between(1, 7);\ndouble r = rng.NextDouble();\nConsole.WriteLine(Convert.ToString(dice));"
  },
  {
   "cat": {
    "en": "Standard Library",
    "zh": "标准库"
   },
   "title": {
    "en": "Math",
    "zh": "数学函数"
   },
   "body": {
    "en": "Math.Sqrt / Pow / Abs / Max / Min / Round / Floor / Ceiling are the built-in numeric functions. They take and return double, so an int argument converts implicitly but the result needs a cast (or Convert.ToInt) before it can go back into an int.",
    "zh": "Math.Sqrt / Pow / Abs / Max / Min / Round / Floor / Ceiling 是内置数学函数。它们接收并返回 double：int 参数会隐式转换，但结果放回 int 前需要显式转换（或 Convert.ToInt）。"
   },
   "code": "double root = Math.Sqrt(16.0);            // 4.0\ndouble p = Math.Pow(2.0, 10.0);             // 1024.0\nint bigger = (int)Math.Max(1, 2);           // 2\nint rounded = (int)Math.Round(3.6);         // 4"
  },
  {
   "cat": {
    "en": "Standard Library",
    "zh": "标准库"
   },
   "title": {
    "en": "DateTime Arithmetic",
    "zh": "日期运算"
   },
   "body": {
    "en": "DateTime is UTC with second precision. Now() / UtcNow() capture the current time, FromDate / FromParts build one, and AddSeconds / AddMinutes / AddHours / AddDays move it. Subtract gives a TimeSpan, whose Total* methods convert units.",
    "zh": "DateTime 是秒级精度的 UTC 时间。Now() / UtcNow() 取当前时间，FromDate / FromParts 构造，AddSeconds / AddMinutes / AddHours / AddDays 平移。Subtract 得到 TimeSpan，用 Total* 方法换算单位。"
   },
   "code": "DateTime now = DateTime.Now();\nDateTime due = now.AddDays(7);\nDateTime start = DateTime.FromDate(2026, 1, 1);\n\nTimeSpan left = due.Subtract(now);\ndouble hours = left.TotalHours();\nConsole.WriteLine(\"hours left: \" + Convert.ToString(hours));\nConsole.WriteLine(due.ToDateString());   // \"YYYY-MM-DD\""
  },
  {
   "cat": {
    "en": "Standard Library",
    "zh": "标准库"
   },
   "title": {
    "en": "Guid",
    "zh": "GUID"
   },
   "body": {
    "en": "Guid.New() returns a new v4 UUID as a 36-character string - the standard id for rows, requests and entities. Guid.Parse validates canonical text (null when invalid), Guid.ToString renders one.",
    "zh": "Guid.New() 返回新的 v4 UUID 字符串（36 字符）——行、请求、实体的标准 id。Guid.Parse 校验规范文本（非法时返回 null），Guid.ToString 渲染。"
   },
   "code": "string id = Guid.New();\nConsole.WriteLine(id);   // e.g. 3f2a...-....-....\n\nGuid parsed = Guid.Parse(id);\nif (parsed != null) { Console.WriteLine(Guid.ToString(parsed)); }"
  },
  {
   "cat": {
    "en": "Standard Library",
    "zh": "标准库"
   },
   "title": {
    "en": "NativeMemory",
    "zh": "原生内存"
   },
   "body": {
    "en": "Zan has no raw pointers; NativeMemory works on nint handles. Alloc reserves bytes, Fill initializes them, Copy moves blocks, GetString / PutString marshal text at an offset, and Free releases - pair every Alloc with a Free.",
    "zh": "Zan 没有裸指针；原生内存通过 nint 句柄操作。Alloc 分配字节，Fill 初始化，Copy 搬运块，GetString / PutString 在偏移处读写文本，Free 释放——每次 Alloc 都要配对 Free。"
   },
   "code": "nint buf = NativeMemory.Alloc(1024);\nNativeMemory.Fill(buf, 0, 1024);\nNativeMemory.PutString(buf, 0, \"zan\", 3);\nstring text = NativeMemory.GetString(buf, 0, 3);   // \"zan\"\nNativeMemory.Free(buf);"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "GET Request",
    "zh": "GET 请求"
   },
   "body": {
    "en": "new HttpClient(host, port) opens a reusable client; GetAsync(path) returns the response body. For a one-off fetch, the static HttpClient.GetAsync(host, port, path) skips the instance. Every call is a coroutine: await suspends this task, not the thread.",
    "zh": "new HttpClient(host, port) 创建可复用的客户端，GetAsync(path) 返回响应体。只请求一次时用静态方法 HttpClient.GetAsync(host, port, path) 即可。所有调用都是协程：await 挂起的是当前任务，不是线程。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Http;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n\n        HttpClient cli = new HttpClient(\"api.local\", 8080);\n        string body = await cli.GetAsync(\"/users/42\");\n        Console.WriteLine(body);\n\n        // one-shot: no client instance needed\n        string page = await HttpClient.GetAsync(\"example.com\", 80, \"/\");\n        Console.WriteLine(Convert.ToString(page.Length) + \" bytes\");\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "Query Parameters",
    "zh": "查询参数"
   },
   "body": {
    "en": "A query string is part of the path: append ?name=value&other=value. Encode values that may contain spaces or & yourself with Uri.Escape so the server parses them back intact.",
    "zh": "查询串是路径的一部分：在路径后面拼 ?name=value&other=value。值里可能出现空格或 & 时，先用 Uri.Escape 编码，服务端才能正确还原。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpClient cli = new HttpClient(\"api.local\", 8080);\n\nstring q = \"/search?kw=\" + Uri.Escape(\"zan lang\") + \"&page=2&size=20\";\nstring body = await cli.GetAsync(q);\nConsole.WriteLine(body);"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "POST JSON",
    "zh": "POST 提交 JSON"
   },
   "body": {
    "en": "PostAsync(path, body) sends the body as-is. Build the payload with JsonValue instead of string concatenation so quoting and escaping are correct, and parse the reply with JsonValue.ParseLenient.",
    "zh": "PostAsync(path, body) 原样发送请求体。用 JsonValue 构造报文而不是拼字符串，引号和转义才不会出错；响应用 JsonValue.ParseLenient 解析。"
   },
   "code": "using System;\nusing System.Net.Http;\nusing System.Json;\n\nHttpClient cli = new HttpClient(\"api.local\", 8080);\ncli.SetHeader(\"Content-Type\", \"application/json\");\n\nJsonValue req = JsonValue.NewObject();\nreq.Set(\"name\", JsonValue.NewStr(\"alice\"));\nreq.Set(\"age\", JsonValue.NewNum(\"30\"));\n\nstring reply = await cli.PostAsync(\"/users\", req.ToJson());\n\nJsonValue res = JsonValue.ParseLenient(reply);\nConsole.WriteLine(\"id=\" + res.Str(\"id\", \"?\"));"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "POST a Form",
    "zh": "POST 表单"
   },
   "body": {
    "en": "A classic HTML form post is Content-Type application/x-www-form-urlencoded with a body of key=value pairs joined by &. Escape each value; that is all a form post is.",
    "zh": "传统 HTML 表单提交就是 Content-Type 为 application/x-www-form-urlencoded、请求体为 & 连接的 key=value。逐个值做转义即可，没有别的魔法。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpClient cli = new HttpClient(\"api.local\", 8080);\ncli.SetHeader(\"Content-Type\", \"application/x-www-form-urlencoded\");\n\nstring form = \"user=\" + Uri.Escape(\"zan\")\n    + \"&pass=\" + Uri.Escape(\"p@ss word\")\n    + \"&remember=1\";\n\nstring reply = await cli.PostAsync(\"/login\", form);\nConsole.WriteLine(reply);"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "PUT & DELETE",
    "zh": "PUT 与 DELETE"
   },
   "body": {
    "en": "PutAsync and DeleteAsync mirror PostAsync and GetAsync; the method is the only difference. RequestAsync(method, path, body) sends any other verb (PATCH, HEAD, OPTIONS).",
    "zh": "PutAsync 与 DeleteAsync 和 PostAsync / GetAsync 用法一致，只是方法不同。其他动词（PATCH、HEAD、OPTIONS）用 RequestAsync(method, path, body) 发送。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpClient cli = new HttpClient(\"api.local\", 8080);\n\nstring updated = await cli.PutAsync(\"/users/42\", \"{\\\"age\\\":31}\");\nstring gone = await cli.DeleteAsync(\"/users/42\");\n\n// any other verb\nstring patched = await cli.RequestAsync(\"PATCH\", \"/users/42\", \"{\\\"age\\\":32}\");"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "Headers & Authentication",
    "zh": "请求头与鉴权"
   },
   "body": {
    "en": "SetHeader(name, value) adds a default header to every request this client sends, which is how tokens, API keys and content negotiation are carried. Calls are chainable and a repeated name replaces the previous value.",
    "zh": "SetHeader(name, value) 给该客户端之后的每个请求都带上这个头，token、API Key、内容协商都靠它。可以链式调用，同名头会覆盖旧值。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpClient cli = new HttpClient(\"api.local\", 8080);\ncli.SetHeader(\"Authorization\", \"Bearer \" + token);\ncli.SetHeader(\"Accept\", \"application/json\");\ncli.SetHeader(\"User-Agent\", \"zan-app/1.0\");\ncli.SetHeader(\"X-Request-Id\", requestId);\n\nstring body = await cli.GetAsync(\"/me\");"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "Status Code & Response Headers",
    "zh": "状态码与响应头"
   },
   "body": {
    "en": "GetAsync/PostAsync return only the body. SendAsync(method, path, body) returns the whole HttpResponse: statusCode, statusText, headers and body, which is what you branch on when an API signals failure with a status rather than a payload.",
    "zh": "GetAsync/PostAsync 只返回响应体。SendAsync(method, path, body) 返回完整的 HttpResponse：statusCode、statusText、响应头和 body——接口用状态码表达失败时就要用它来判断。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpClient cli = new HttpClient(\"api.local\", 8080);\n\nHttpResponse res = await cli.SendAsync(\"GET\", \"/users/42\", \"\");\nConsole.WriteLine(Convert.ToString(res.statusCode) + \" \" + res.statusText);\nConsole.WriteLine(\"type: \" + res.GetHeader(\"Content-Type\"));\n\nif (res.statusCode == 404) {\n    Console.WriteLine(\"not found\");\n} else if (res.statusCode >= 500) {\n    Console.WriteLine(\"server error: \" + res.body);\n} else {\n    Console.WriteLine(res.body);\n}"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "Timeout & Error Handling",
    "zh": "超时与错误处理"
   },
   "body": {
    "en": "SetTimeout(ms) bounds connect and read; a timed-out or refused request yields an empty body, so check before parsing. Wrap the call in try/catch to turn transport failures into your own error path.",
    "zh": "SetTimeout(ms) 同时约束连接和读取；超时或连接被拒时返回空串，所以解析前要先判断。用 try/catch 把传输层失败转换成自己的错误处理路径。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpClient cli = new HttpClient(\"api.local\", 8080);\ncli.SetTimeout(3000);          // milliseconds, connect + read\n\ntry {\n    string body = await cli.GetAsync(\"/slow\");\n    if (body.Length == 0) {\n        Console.WriteLine(\"empty reply: timed out or refused\");\n        return;\n    }\n    Console.WriteLine(body);\n} catch (Exception e) {\n    Console.WriteLine(\"request failed: \" + e.Message());\n}"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "Retry with Backoff",
    "zh": "重试与退避"
   },
   "body": {
    "en": "There is no hidden retry: a request is attempted once. Retry in your own loop with Task.Delay between attempts, doubling the wait so a struggling server is not hammered.",
    "zh": "客户端不会隐式重试，一次调用只发一次请求。自己写循环重试，配合 Task.Delay 退避，等待时间逐次翻倍，避免把已经吃力的服务打垮。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nstatic async string GetWithRetry(HttpClient cli, string path, int tries) {\n    int wait = 200;\n    for (int i = 0; i < tries; i++) {\n        HttpResponse res = await cli.SendAsync(\"GET\", path, \"\");\n        if (res.statusCode >= 200 && res.statusCode < 300) { return res.body; }\n        if (res.statusCode >= 400 && res.statusCode < 500) { return \"\"; }\n        await Task.Delay(wait);\n        wait = wait * 2;\n    }\n    return \"\";\n}"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "HTTPS & Certificates",
    "zh": "HTTPS 与证书"
   },
   "body": {
    "en": "CreateHttps(host, port) speaks TLS. DisableTlsVerify() is for local self-signed servers only; PinPublicKey pins the server SPKI hash, and SetClientCertificate presents a client certificate for mutual TLS.",
    "zh": "CreateHttps(host, port) 走 TLS。DisableTlsVerify() 只用于本地自签名环境；PinPublicKey 固定服务端 SPKI 指纹；SetClientCertificate 提供客户端证书做双向认证。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpClient api = HttpClient.CreateHttps(\"api.example.com\", 443);\nstring body = await api.GetAsync(\"/v1/status\");\n\n// local self-signed server (development only)\nHttpClient dev = HttpClient.CreateHttps(\"127.0.0.1\", 8443);\ndev.DisableTlsVerify();\n\n// certificate pinning + mutual TLS\nHttpClient hard = HttpClient.CreateHttps(\"api.example.com\", 443);\nhard.PinPublicKey(spkiSha256Base64);\nhard.SetClientCertificate(\"client.crt\", \"client.key\");"
  },
  {
   "cat": {
    "en": "HTTP Client",
    "zh": "HTTP 客户端"
   },
   "title": {
    "en": "Download a File",
    "zh": "下载文件"
   },
   "body": {
    "en": "DownloadFileAsync streams the response straight to disk instead of holding it in a string, so a large file costs no memory.",
    "zh": "DownloadFileAsync 把响应直接流式写入磁盘，不在内存里拼字符串，因此下载大文件不吃内存。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Http;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n        await HttpClient.DownloadFileAsync(\n            \"dl.example.com\", 80, \"/zan/zan-1.0.zip\", \"zan-1.0.zip\");\n        Console.WriteLine(\"saved\");\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "Minimal Server",
    "zh": "最小服务端"
   },
   "body": {
    "en": "new Worker(\"http\", host, port) plus an onRequest callback is a complete server: no socket code, no accept loop. count is the number of worker processes; the kernel spreads accepts across them.",
    "zh": "new Worker(\"http\", host, port) 加一个 onRequest 回调就是一个完整服务：不用写 socket，也不用写 accept 循环。count 是 worker 进程数，内核会把新连接分流到各进程。"
   },
   "code": "using System;\nusing System.Net;\nusing System.Net.Http;\n\nclass Program {\n    static async HttpResponse Request(HttpRequest req) {\n        return HttpResponse.Text(\"Hello World \" + req.path);\n    }\n\n    static async void Main() {\n        Worker w = new Worker(\"http\", \"0.0.0.0\", 9501);\n        w.count = 4;                 // 4 worker processes\n        w.onRequest = Program.Request;\n        await Worker.RunAll();\n    }\n}"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "Routing",
    "zh": "路由分发"
   },
   "body": {
    "en": "new HttpServer(host, port).OnRequest(handler) gives full control: the handler receives the parsed request and returns the response, so routing is an ordinary branch on method and path with a 404 fallback.",
    "zh": "new HttpServer(host, port).OnRequest(handler) 提供完整控制：处理函数拿到解析好的请求、返回响应，所以路由就是按 method 和 path 分支，最后兜底 404。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Http;\n\nclass Program {\n    static async HttpResponse Handle(HttpRequest req) {\n        if (req.method == \"GET\" && req.path == \"/health\") {\n            return HttpResponse.Text(\"ok\");\n        }\n        if (req.method == \"GET\" && req.path == \"/users\") {\n            return HttpResponse.Json(\"[{\\\"id\\\":1,\\\"name\\\":\\\"alice\\\"}]\");\n        }\n        if (req.method == \"POST\" && req.path == \"/users\") {\n            return HttpResponse.Json(\"{\\\"created\\\":true}\");\n        }\n        return HttpResponse.NotFound();\n    }\n\n    static async int Run() {\n        Socket.Initialize();\n        HttpServer server = new HttpServer(\"0.0.0.0\", 8080);\n        server.OnRequest(Handle);\n        await server.Start();        // serves until Stop()\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "Reading Request Data",
    "zh": "读取请求数据"
   },
   "body": {
    "en": "HttpRequest carries method, path, queryString, headers and body. GetQueryParam reads one query value, GetFormField reads one urlencoded body field and GetHeader is case-insensitive.",
    "zh": "HttpRequest 里有 method、path、queryString、请求头和 body。GetQueryParam 取单个查询参数，GetFormField 取 urlencoded 表单字段，GetHeader 不区分大小写。"
   },
   "code": "using System;\nusing System.Net.Http;\nusing System.Json;\n\nstatic async HttpResponse Handle(HttpRequest req) {\n    // /search?kw=zan&page=2\n    string kw = req.GetQueryParam(\"kw\");\n    string page = req.GetQueryParam(\"page\");\n\n    // application/x-www-form-urlencoded body\n    string user = req.GetFormField(\"user\");\n\n    // headers (case-insensitive)\n    string auth = req.GetHeader(\"Authorization\");\n\n    // JSON body\n    JsonValue doc = JsonValue.ParseLenient(req.body);\n    string name = doc.Str(\"name\", \"\");\n\n    return HttpResponse.Text(\"kw=\" + kw + \" page=\" + page\n        + \" user=\" + user + \" name=\" + name);\n}"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "JSON API & Status Codes",
    "zh": "JSON 接口与状态码"
   },
   "body": {
    "en": "HttpResponse.Json sets the content type; statusCode / statusText carry the outcome. Return a machine-readable error object with a 4xx status rather than plain text, so clients can branch on it.",
    "zh": "HttpResponse.Json 会设置内容类型；statusCode / statusText 表达结果。校验失败时返回 4xx 加结构化错误对象，而不是一段纯文本，客户端才好判断。"
   },
   "code": "using System;\nusing System.Net.Http;\nusing System.Json;\n\nstatic async HttpResponse CreateTodo(HttpRequest req) {\n    JsonValue doc = JsonValue.ParseLenient(req.body);\n    string title = doc.Str(\"title\", \"\");\n    if (title.Length == 0) {\n        HttpResponse bad = HttpResponse.Json(\"{\\\"error\\\":\\\"title required\\\"}\");\n        bad.statusCode = 400;\n        bad.statusText = \"Bad Request\";\n        return bad;\n    }\n\n    JsonValue item = JsonValue.NewObject();\n    item.Set(\"id\", JsonValue.NewNum(\"1\"));\n    item.Set(\"title\", JsonValue.NewStr(title));\n    item.Set(\"done\", JsonValue.NewBool(false));\n\n    HttpResponse res = HttpResponse.Json(item.ToJson());\n    res.statusCode = 201;\n    res.statusText = \"Created\";\n    return res;\n}"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "Response Types",
    "zh": "响应类型"
   },
   "body": {
    "en": "Ready-made responses cover the common shapes: Text, Json, Ok (HTML), Redirect, NotFound, ServerError and WithStatus for anything else. SetHeader / SetContentType adjust one.",
    "zh": "常见响应都有现成构造：Text、Json、Ok（HTML）、Redirect、NotFound、ServerError，其他状态用 WithStatus。需要微调时用 SetHeader / SetContentType。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpResponse text = HttpResponse.Text(\"plain body\");\nHttpResponse json = HttpResponse.Json(\"{\\\"ok\\\":true}\");\nHttpResponse html = HttpResponse.Ok(\"<h1>Zan</h1>\");\nHttpResponse move = HttpResponse.Redirect(\"/login\");\nHttpResponse miss = HttpResponse.NotFound();\nHttpResponse boom = HttpResponse.ServerError(\"upstream down\");\nHttpResponse teapot = HttpResponse.WithStatus(418, \"I'm a teapot\", \"no coffee\");\n\nHttpResponse file = HttpResponse.Ok(csv);\nfile.SetContentType(\"text/csv\");\nfile.SetHeader(\"Content-Disposition\", \"attachment; filename=report.csv\");"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "Limits & Protection",
    "zh": "限流与自我保护"
   },
   "body": {
    "en": "A public server needs bounds: SetMaxConnections caps concurrent sockets, SetTimeout drops idle connections and SetMaxRequestBytes rejects oversized bodies before they are buffered.",
    "zh": "对外服务必须设上限：SetMaxConnections 限制并发连接，SetTimeout 回收空闲连接，SetMaxRequestBytes 在缓冲之前就拒掉超大请求体。"
   },
   "code": "using System;\nusing System.Net.Http;\n\nHttpServer server = new HttpServer(\"0.0.0.0\", 8080);\nserver.SetMaxConnections(1024);      // refuse new sockets beyond this\nserver.SetTimeout(10000);            // idle connection timeout (ms)\nserver.SetMaxRequestBytes(1048576);  // 1 MB request body cap\nserver.OnRequest(Handle);\nawait server.Start();"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "Processes & Service Commands",
    "zh": "多进程与服务命令"
   },
   "body": {
    "en": "Worker.RunCommand() turns the program into a service binary: start, start -d, stop, restart, reload (drains workers one by one) and status, without touching business code.",
    "zh": "Worker.RunCommand() 让程序变成一个服务可执行文件，支持 start、start -d、stop、restart、reload（逐个平滑替换 worker）、status，业务代码完全不用改。"
   },
   "code": "using System;\nusing System.Net;\nusing System.Net.Http;\n\nclass Program {\n    static async HttpResponse Request(HttpRequest req) {\n        return HttpResponse.Text(\"pid \" + Convert.ToString(ProcessHost.Pid()));\n    }\n\n    static async void Main() {\n        Worker w = new Worker(\"http\", \"0.0.0.0\", 9501);\n        w.count = 4;\n        w.onRequest = Program.Request;\n        await Worker.RunCommand();\n    }\n}\n\n// ./service start -d     run as a daemon\n// ./service reload       graceful restart, no dropped requests\n// ./service status       per-worker requests / QPS / CPU / memory\n// ./service stop"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "HTTPS Server",
    "zh": "HTTPS 服务端"
   },
   "body": {
    "en": "HttpsServer.Create(host, port, certFile, keyFile) is the same server behind TLS: the handler contract is unchanged, only the transport is encrypted.",
    "zh": "HttpsServer.Create(host, port, certFile, keyFile) 就是 TLS 版的同一个服务端：处理函数写法不变，只是传输层加密。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Http;\nusing System.Net.Https;\n\nclass Program {\n    static async HttpResponse Handle(HttpRequest req) {\n        return HttpResponse.Json(\"{\\\"secure\\\":true,\\\"path\\\":\\\"\" + req.path + \"\\\"}\");\n    }\n\n    static async int Run() {\n        Socket.Initialize();\n        HttpsServer server = HttpsServer.Create(\n            \"0.0.0.0\", 8443, \"server.crt\", \"server.key\");\n        server.SetMaxRequestBytes(1048576);\n        server.OnRequest(Handle);\n        await server.Start();\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "Server-Sent Events",
    "zh": "SSE 服务端推送"
   },
   "body": {
    "en": "SSE is one-way push over plain HTTP (text/event-stream), the EventSource API in a browser. The only host is Worker: new Worker(\"sse\", host, port) with an onSseSubscriber callback -- the worker completes the event-stream handshake and calls the callback once per subscriber with an SseConnection. Bind + accept + dispatch all live in Worker; do not start separate accept loops.",
    "zh": "SSE 是 HTTP 上的单向服务器推送（text/event-stream），浏览器里对应 EventSource。唯一宿主是 Worker：new Worker(\"sse\", host, port) 注册 onSseSubscriber 回调——Worker 完成 event-stream 握手并为每个订阅者回调一次 SseConnection。绑定、accept、分发全在 Worker；不要再写独立的 accept 循环。"
   },
   "code": "using System;\nusing System.Net;\nusing System.Net.Sockets;\nusing System.Net.Sse;\nusing System.Threading;\n\nclass Program {\n    static Worker srv;\n    static List<SseConnection> subs = new List<SseConnection>();\n\n    static async void OnSubscriber(SseConnection c) {\n        Console.WriteLine(\"subscriber on \" + c.Path());\n        subs.Add(c);\n        for (int tick = 0; tick < 3; tick++) {\n            int _n = await c.SendAsync(\"price\",\n                \"{\\\"symbol\\\":\\\"ZAN\\\",\\\"px\\\":\" + Convert.ToString(100 + tick) + \"}\");\n        }\n        await c.PingAsync();   // keep-alive comment line\n        c.Close();             // client's NextAsync returns null\n    }\n\n    static async int Run() {\n        Socket.Initialize();\n        srv = new Worker(\"sse\", \"0.0.0.0\", 8090);\n        srv.onSseSubscriber = Program.OnSubscriber;\n        nint ls = srv.BindSocket(false);\n        List<nint> socks = new List<nint>();\n        socks.Add(ls);\n        await Worker.RunAllLoopsOnSockets(socks);  // serves forever\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "HTTP Server",
    "zh": "HTTP 服务端"
   },
   "title": {
    "en": "Subscribing to SSE",
    "zh": "订阅 SSE 事件流"
   },
   "body": {
    "en": "SseClient.ConnectAsync subscribes; NextAsync suspends until the next event and returns null when the server closes the stream. Name() is the event type, Data() the payload (multi-line payloads are reassembled).",
    "zh": "SseClient.ConnectAsync 订阅，NextAsync 挂起等待下一条事件，服务端关闭流时返回 null。Name() 是事件类型，Data() 是负载（多行 data 会自动拼回原样）。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Sse;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n        SseClient c = await SseClient.ConnectAsync(\"127.0.0.1\", 8090, \"/stream\");\n        if (!c.IsConnected()) { Console.WriteLine(\"subscribe failed\"); return 1; }\n\n        while (true) {\n            SseEvent e = await c.NextAsync();\n            if (e == null) { break; }        // stream closed\n            Console.WriteLine(e.Name() + \": \" + e.Data());\n        }\n        c.Close();\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "WebSocket",
    "zh": "WebSocket"
   },
   "title": {
    "en": "WebSocket Client",
    "zh": "WebSocket 客户端"
   },
   "body": {
    "en": "ConnectAsync performs the HTTP upgrade handshake; SendText and RecvText exchange text frames, and RecvText suspends until a frame arrives. IsConnected() reports a failed handshake.",
    "zh": "ConnectAsync 完成 HTTP 升级握手；SendText / RecvText 收发文本帧，RecvText 会挂起直到收到帧。握手失败可用 IsConnected() 判断。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.WebSocket;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n        WebSocketClient c = await WebSocketClient.ConnectAsync(\n            \"127.0.0.1\", 8080, \"/chat\");\n        if (!c.IsConnected()) { Console.WriteLine(\"handshake failed\"); return 1; }\n\n        await c.SendText(\"hello\");\n        string reply = await c.RecvText();\n        Console.WriteLine(\"got: \" + reply);\n\n        await c.Ping();       // heartbeat, server answers with pong\n        await c.Close();      // close frame, then TCP shutdown\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "WebSocket",
    "zh": "WebSocket"
   },
   "title": {
    "en": "WebSocket Server",
    "zh": "WebSocket 服务端"
   },
   "body": {
    "en": "new Worker(\"websocket\", host, port) with an onMessage callback handles the handshake, frame codec and ping/pong; the callback's return value is sent back (default: echo). The same port also serves plain HTTP for non-upgrade requests, swoole's WebSocket\\Server style.",
    "zh": "new Worker(\"websocket\", host, port) 注册 onMessage 回调后处理握手、帧编解码和 ping/pong，回调返回值即应答（默认回显）。同一端口对非升级请求还回落普通 HTTP（swoole 的 WebSocket\\Server 同款）。"
   },
   "code": "using System;\nusing System.Net;\nusing System.Net.Sockets;\nusing System.Net.WebSocket;\nusing System.Threading;\n\nclass Program {\n    static async string Echo(Connection conn, string message) {\n        if (message == \"ping\") { return \"pong\"; }\n        return \"you said: \" + message;\n    }\n\n    static async int Run() {\n        Socket.Initialize();\n        Worker server = new Worker(\"websocket\", \"0.0.0.0\", 8080);\n        server.onMessage = Program.Echo;\n        nint ls = server.BindSocket(false);\n        List<nint> socks = new List<nint>();\n        socks.Add(ls);\n        await Worker.RunAllLoopsOnSockets(socks);  // serves forever\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "WebSocket",
    "zh": "WebSocket"
   },
   "title": {
    "en": "Callback-Style Server",
    "zh": "回调式 WebSocket 服务"
   },
   "body": {
    "en": "The Worker form gives per-connection callbacks: onOpen, onMessage (its return value is sent back as one frame), onClose, plus onRequest so plain HTTP on the same port still works. conn.Send pushes to a client at any time.",
    "zh": "Worker 形式提供按连接的回调：onOpen、onMessage（返回值自动作为一帧回发）、onClose，还有 onRequest 让同端口的普通 HTTP 依然可用。需要主动推送时用 conn.Send。"
   },
   "code": "using System;\nusing System.Net;\nusing System.Net.Http;\n\nclass Program {\n    static async void Open(Connection conn) {\n        Console.WriteLine(\"open #\" + Convert.ToString(conn.GetId()));\n    }\n\n    static async string Message(Connection conn, string data) {\n        return \"{\\\"echo\\\":\\\"\" + data + \"\\\"}\";\n    }\n\n    static async void Closed(Connection conn) {\n        Console.WriteLine(\"close #\" + Convert.ToString(conn.GetId()));\n    }\n\n    static async HttpResponse Request(HttpRequest req) {\n        return HttpResponse.Text(\"plain http on the ws port: \" + req.path);\n    }\n\n    static async void Main() {\n        Worker w = new Worker(\"websocket\", \"0.0.0.0\", 9502);\n        w.count = 4;\n        w.onOpen = Program.Open;\n        w.onMessage = Program.Message;\n        w.onClose = Program.Closed;\n        w.onRequest = Program.Request;\n        await Worker.RunAll();\n    }\n}"
  },
  {
   "cat": {
    "en": "WebSocket",
    "zh": "WebSocket"
   },
   "title": {
    "en": "Secure WebSocket (WSS)",
    "zh": "安全 WebSocket（WSS）"
   },
   "body": {
    "en": "WssClient / WssServer are the TLS variants: the server needs a certificate and key, the client API is identical to plain WebSocket.",
    "zh": "WssClient / WssServer 是 TLS 版本：服务端需要证书和私钥，客户端 API 与普通 WebSocket 完全一致。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.WebSocket.Secure;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n\n        WssServer server = WssServer.Create(\n            \"0.0.0.0\", 8443, \"server.crt\", \"server.key\");\n        Task.Spawn(server.Start());\n\n        WssClient c = await WssClient.ConnectAsync(\"127.0.0.1\", 8443, \"/chat\");\n        await c.SendText(\"secure hello\");\n        Console.WriteLine(await c.RecvText());\n        await c.Close();\n\n        server.Stop();\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "Sockets",
    "zh": "套接字"
   },
   "title": {
    "en": "TCP Client",
    "zh": "TCP 客户端"
   },
   "body": {
    "en": "TcpClient.ConnectAsync opens a connection; SendAsync / RecvAsync move bytes. TCP is a byte stream with no message boundaries, so agree on a framing rule -- here a newline terminator.",
    "zh": "TcpClient.ConnectAsync 建立连接，SendAsync / RecvAsync 收发字节。TCP 是字节流、没有消息边界，必须自己约定分帧规则——这里用换行符结尾。"
   },
   "code": "using System;\nusing System.Net.Sockets;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n        TcpClient c = await TcpClient.ConnectAsync(\"127.0.0.1\", 9000);\n        if (!c.IsConnected()) { Console.WriteLine(\"connect failed\"); return 1; }\n\n        int _n = await c.SendAsync(\"hello\\n\");\n        string reply = await c.RecvAsync(4096);\n        Console.WriteLine(\"reply: \" + reply);\n\n        c.Close();\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "Sockets",
    "zh": "套接字"
   },
   "title": {
    "en": "TCP Server & Framing",
    "zh": "TCP 服务端与分帧"
   },
   "body": {
    "en": "TcpListener accepts on a coroutine; every connection is handed to its own coroutine so one slow peer never blocks the accept loop. A single RecvAsync may return half a message or several, so buffer and split on the delimiter. An empty result means the peer closed.",
    "zh": "TcpListener 在协程里 accept，每条连接交给独立协程处理，慢客户端不会卡住 accept 循环。一次 RecvAsync 可能只收到半条消息、也可能收到多条，所以要缓冲后按分隔符切分。返回空串表示对端已关闭。"
   },
   "code": "using System;\nusing System.Net.Sockets;\n\nclass Program {\n    static async void HandleClient(nint sock) {\n        TcpClient c = TcpClient.FromSocket(sock);\n        string buf = \"\";\n        while (true) {\n            string chunk = await c.RecvAsync(4096);\n            if (chunk.Length == 0) { break; }      // peer closed\n            buf = buf + chunk;\n\n            int nl = buf.IndexOf(\"\\n\");\n            while (nl >= 0) {\n                string line = buf.Substring(0, nl);\n                buf = buf.Substring(nl + 1, buf.Length - nl - 1);\n                int _n = await c.SendAsync(\"echo: \" + line + \"\\n\");\n                nl = buf.IndexOf(\"\\n\");\n            }\n        }\n        c.Close();\n    }\n\n    static async int Run() {\n        Socket.Initialize();\n        TcpListener listener = new TcpListener(\"0.0.0.0\", 9000);\n        listener.Start();\n        while (true) {\n            nint sock = await listener.AcceptAsync();\n            if (sock >= 0) { Task.Spawn(HandleClient(sock)); }\n        }\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "Sockets",
    "zh": "套接字"
   },
   "title": {
    "en": "UDP Datagrams",
    "zh": "UDP 数据报"
   },
   "body": {
    "en": "UdpClient.Bind(host, port) receives on a port; SendToAsync / RecvFromAsync move whole datagrams. UDP keeps message boundaries so no framing is needed, but datagrams can be lost, duplicated or reordered -- add your own timeout and retry.",
    "zh": "UdpClient.Bind(host, port) 在端口上收包，SendToAsync / RecvFromAsync 收发完整数据报。UDP 保留消息边界，不需要分帧；但可能丢包、重复、乱序，需要自己做超时重发。"
   },
   "code": "using System;\nusing System.Net.Sockets;\n\nclass Program {\n    static async void Server() {\n        UdpClient srv = UdpClient.Bind(\"127.0.0.1\", 9100);\n        for (int i = 0; i < 3; i++) {\n            string msg = await srv.RecvFromAsync(4096);\n            Console.WriteLine(\"server got: \" + msg);\n            int _n = await srv.SendToAsync(\"ack: \" + msg, Socket.PeerIp(), Socket.PeerPort());\n        }\n        srv.Close();\n    }\n\n    static async int Run() {\n        Socket.Initialize();\n        Task.Spawn(Server());\n\n        UdpClient cli = UdpClient.Bind(\"127.0.0.1\", 9101);\n        int _s = await cli.SendToAsync(\"ping\", \"127.0.0.1\", 9100);\n        Console.WriteLine(\"client got: \" + await cli.RecvFromAsync(4096));\n        cli.Close();\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "Sockets",
    "zh": "套接字"
   },
   "title": {
    "en": "Callback-Style TCP / UDP",
    "zh": "回调式 TCP / UDP 服务"
   },
   "body": {
    "en": "The Worker form removes the accept loop: register onConnect / onReceive / onClose for TCP or onPacket for UDP; the return value of the handler is sent back, and returning \"\" replies with nothing.",
    "zh": "Worker 形式省掉 accept 循环：TCP 注册 onConnect / onReceive / onClose，UDP 注册 onPacket；处理函数的返回值会自动回给对端，返回 \"\" 表示不回复。"
   },
   "code": "using System;\nusing System.Net;\n\nclass Program {\n    static async void Connect(Connection conn) {\n        Console.WriteLine(\"open #\" + Convert.ToString(conn.GetId()));\n    }\n\n    static async string Receive(Connection conn, string data) {\n        return \"Zan: \" + data;\n    }\n\n    static async string Packet(string data) {\n        return \"Server: \" + data;\n    }\n\n    static async void Main() {\n        Worker tcp = new Worker(\"tcp\", \"0.0.0.0\", 9503);\n        tcp.count = 4;\n        tcp.onConnect = Program.Connect;\n        tcp.onReceive = Program.Receive;\n\n        Worker udp = new Worker(\"udp\", \"0.0.0.0\", 9504);\n        udp.onPacket = Program.Packet;\n\n        await Worker.RunAll();      // both protocols in one process\n    }\n}"
  },
  {
   "cat": {
    "en": "Sockets",
    "zh": "套接字"
   },
   "title": {
    "en": "TLS Connections",
    "zh": "TLS 连接"
   },
   "body": {
    "en": "TlsStream wraps a connected socket in TLS, which is what the HTTPS and WSS clients use underneath. Reach for it when you need an encrypted stream for a protocol of your own.",
    "zh": "TlsStream 把已连接的套接字包装成 TLS 流，HTTPS 和 WSS 客户端内部用的就是它。自定义协议需要加密通道时可以直接用。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Tls;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n        TcpClient tcp = await TcpClient.ConnectAsync(\"example.com\", 443);\n\n        TlsStream tls = TlsStream.Client(tcp.GetSocket(), \"example.com\");\n        int _n = await tls.SendAsync(\"GET / HTTP/1.1\\r\\nHost: example.com\\r\\n\\r\\n\");\n        string head = await tls.RecvAsync(4096);\n        Console.WriteLine(head.Substring(0, 32));\n\n        tls.Close();\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "Messaging",
    "zh": "消息与物联网"
   },
   "title": {
    "en": "MQTT Publish & Subscribe",
    "zh": "MQTT 发布与订阅"
   },
   "body": {
    "en": "MqttClient.ConnectAsync(host, port, clientId) connects; SubscribeAsync registers a topic filter (+ matches one level, # matches the rest) and ReceiveAsync suspends until a message arrives. QoS 1 waits for the broker's PUBACK, QoS 0 does not.",
    "zh": "MqttClient.ConnectAsync(host, port, clientId) 连接；SubscribeAsync 订阅主题过滤器（+ 匹配一层，# 匹配多层），ReceiveAsync 挂起等待消息。QoS 1 会等 broker 的 PUBACK，QoS 0 不等。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Mqtt;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n\n        MqttClient sub = await MqttClient.ConnectAsync(\"127.0.0.1\", 1883, \"sub-1\");\n        int _s = await sub.SubscribeAsync(\"sensors/+/temp\", MqttQos.AtMostOnce);\n\n        MqttClient pub = await MqttClient.ConnectAsync(\"127.0.0.1\", 1883, \"pub-1\");\n        int _p = await pub.PublishAsync(\"sensors/kitchen/temp\", \"21.5\",\n                                        MqttQos.AtLeastOnce);\n\n        Console.WriteLine(\"received: \" + await sub.ReceiveAsync());\n\n        await sub.DisconnectAsync();\n        await pub.DisconnectAsync();\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "Messaging",
    "zh": "消息与物联网"
   },
   "title": {
    "en": "Built-in MQTT Broker",
    "zh": "内置 MQTT Broker"
   },
   "body": {
    "en": "MqttBroker is an MQTT v3.1.1 server written in Zan, so a deployment needs no external broker. RunAsync serves subscriptions, wildcard matching and fan-out to every matching subscriber.",
    "zh": "MqttBroker 是用 Zan 实现的 MQTT v3.1.1 服务端，部署时不需要外部 broker。RunAsync 负责订阅管理、通配符匹配，并把消息扇出给所有匹配的订阅者。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Mqtt;\n\nclass Program {\n    static async int Run() {\n        Socket.Initialize();\n        MqttBroker broker = new MqttBroker(\"0.0.0.0\", 1883);\n        await broker.RunAsync();      // serves until Stop()\n        return 0;\n    }\n\n    static void Main() { int r = await Run(); }\n}"
  },
  {
   "cat": {
    "en": "Messaging",
    "zh": "消息与物联网"
   },
   "title": {
    "en": "CoAP",
    "zh": "CoAP"
   },
   "body": {
    "en": "CoAP is a compact REST over UDP for constrained devices. new CoapClient(host, port) with SetTimeout(ms, retries) handles retransmission; GetAsync / PostAsync / PutAsync / DeleteAsync mirror HTTP and the reply carries a response code and payload.",
    "zh": "CoAP 是面向受限设备的 UDP 版 REST。new CoapClient(host, port) 配合 SetTimeout(ms, 重试次数) 处理重传；GetAsync / PostAsync / PutAsync / DeleteAsync 与 HTTP 对应，响应里带响应码和负载。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Coap;\n\nclass Program {\n    static async int Main() {\n        CoapClient c = new CoapClient(\"127.0.0.1\", 5683).SetTimeout(1000, 2);\n\n        CoapResponse r = await c.GetAsync(\"sensors/temp\");\n        Console.WriteLine(\"GET -> \" + r.CodeText() + \" \" + r.payload);\n\n        CoapResponse w = await c.PutAsync(\"actuators/led\", \"on\");\n        Console.WriteLine(\"PUT -> \" + w.CodeText());\n        return 0;\n    }\n}"
  },
  {
   "cat": {
    "en": "Messaging",
    "zh": "消息与物联网"
   },
   "title": {
    "en": "Modbus TCP",
    "zh": "Modbus TCP"
   },
   "body": {
    "en": "ModbusClient.ConnectAsync(host, port, unitId) speaks Modbus TCP to a PLC or meter: ReadHoldingAsync(address, count) reads registers, WriteRegisterAsync writes one and WriteRegistersAsync writes a block.",
    "zh": "ModbusClient.ConnectAsync(host, port, 从站号) 与 PLC、电表等设备通信：ReadHoldingAsync(地址, 数量) 读保持寄存器，WriteRegisterAsync 写单个，WriteRegistersAsync 写一批。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Modbus;\n\nclass Program {\n    static async int Main() {\n        ModbusClient m = await ModbusClient.ConnectAsync(\"192.168.1.50\", 502, 1);\n\n        List<int> vals = await m.ReadHoldingAsync(0, 4);\n        foreach (int v in vals) { Console.WriteLine(Convert.ToString(v)); }\n\n        int ok = await m.WriteRegisterAsync(2, 1234);\n        List<int> back = await m.ReadHoldingAsync(2, 1);\n        Console.WriteLine(\"wrote=\" + Convert.ToString(ok)\n            + \" read back=\" + Convert.ToString(back[0]));\n\n        m.Close();\n        return 0;\n    }\n}"
  },
  {
   "cat": {
    "en": "Messaging",
    "zh": "消息与物联网"
   },
   "title": {
    "en": "NTP Time Sync",
    "zh": "NTP 时间同步"
   },
   "body": {
    "en": "NtpClient.QueryAsync(host, port, timeoutMs) returns the server's Unix time in seconds (SNTP over UDP), which is how a device corrects its clock without trusting the local one.",
    "zh": "NtpClient.QueryAsync(host, port, 超时毫秒) 返回服务端的 Unix 秒（UDP 上的 SNTP），设备靠它校时，而不是相信本地时钟。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Ntp;\n\nclass Program {\n    static async int Main() {\n        Socket.Initialize();\n        long unix = await NtpClient.QueryAsync(\"pool.ntp.org\", 123, 2000);\n        if (unix <= 0) { Console.WriteLine(\"ntp timeout\"); return 1; }\n        Console.WriteLine(\"unix seconds: \" + Convert.ToString(unix));\n        return 0;\n    }\n}"
  },
  {
   "cat": {
    "en": "Messaging",
    "zh": "消息与物联网"
   },
   "title": {
    "en": "SIP Signalling",
    "zh": "SIP 信令"
   },
   "body": {
    "en": "new SipClient(host, port, user) sends SIP requests over UDP: OptionsAsync probes a server's capabilities and RegisterAsync registers a contact. A null reply means the request timed out; SipMessage.Header reads any header off the answer.",
    "zh": "new SipClient(host, port, 用户) 通过 UDP 发送 SIP 请求：OptionsAsync 探测服务端能力，RegisterAsync 注册地址。返回 null 表示超时；SipMessage.Header 可读取应答里的任意头。"
   },
   "code": "using System;\nusing System.Net.Sockets;\nusing System.Net.Sip;\n\nclass Program {\n    static async int Main() {\n        SipClient c = new SipClient(\"sip.example.com\", 5060, \"zan-demo\");\n\n        SipMessage r = await c.OptionsAsync();\n        if (r == null) { Console.WriteLine(\"OPTIONS timed out\"); return 1; }\n        Console.WriteLine(Convert.ToString(r.status) + \" \" + r.reason\n            + \" allow=\" + r.Header(\"Allow\"));\n\n        SipMessage reg = await c.RegisterAsync(\"alice\", 3600);\n        Console.WriteLine(\"REGISTER -> \" + Convert.ToString(reg.status));\n        return 0;\n    }\n}"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Choosing a Database",
    "zh": "选型速查"
   },
   "body": {
    "en": "Zan ships native drivers plus an ODBC channel. SQLite (embedded, zero setup) for local relational data; ZanDb for JSON documents with no SQL; PgConnection / MySqlConnection for production PostgreSQL / MySQL over the native wire protocol; DbConnection (ODBC) for SQL Server, Oracle, DM, Kingbase, ClickHouse and anything else with a driver; RedisClient for cache and counters. The ORM drives all SQL connections through the IDbExecutor interface, so switching engines does not change business code.",
    "zh": "Zan 内置原生驱动 + ODBC 通道。本地关系型存储用 SQLite（内嵌、零配置）；不想写 SQL 的文档存储用 ZanDb；生产 PostgreSQL / MySQL 用 PgConnection / MySqlConnection（原生线协议）；SQL Server、Oracle、达梦、金仓、ClickHouse 等用 DbConnection（ODBC）；缓存计数用 RedisClient。ORM 通过 IDbExecutor 接口驱动所有 SQL 连接，换数据库不用改业务代码。"
   },
   "code": "using System.Data;            // DbConnection (ODBC), DbParams, DbResult\nusing System.Data.Sqlite;     // SqliteConnection - embedded, zero dependency\nusing System.Data.Postgres;   // PgConnection - native libpq\nusing System.Data.MySql;      // MySqlConnection - native protocol, async\nusing System.Data.Redis;      // RedisClient - cache / counters\nusing System.Data.ZanDb;      // ZanDatabase - embedded document store\nusing System.Data.Orm;        // Model / QueryBuilder / Migration"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "SQLite: Open a Database",
    "zh": "SQLite：打开数据库"
   },
   "body": {
    "en": "SqliteConnection.Open(path) opens (or creates) a database file with no server and no external dependency. IsConnected() reports the result, GetError() explains a failure, Close() releases it.",
    "zh": "SqliteConnection.Open(path) 打开（不存在则创建）一个数据库文件，无需服务端、零外部依赖。IsConnected() 判断是否成功，GetError() 给出失败原因，Close() 释放连接。"
   },
   "code": "using System;\nusing System.Data.Sqlite;\n\nSqliteConnection db = SqliteConnection.Open(\"app.db\");\nif (!db.IsConnected()) {\n    Console.WriteLine(\"open failed: \" + db.GetError());\n    return;\n}\ndb.Execute(\"CREATE TABLE IF NOT EXISTS books (\"\n    + \"id INTEGER PRIMARY KEY AUTOINCREMENT, \"\n    + \"title TEXT NOT NULL, author TEXT, price REAL)\");\ndb.Close();"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Insert with Parameters",
    "zh": "参数化插入"
   },
   "body": {
    "en": "Never paste values into SQL text: write `?` placeholders and pass a DbParams. Add for text, AddInt / AddDouble for numbers, AddNull for NULL. Quotes inside a value need no escaping, and injection is impossible. LastInsertId() returns the auto-increment key just written.",
    "zh": "不要把值拼进 SQL 文本：用 `?` 占位符并传入 DbParams。文本用 Add，数字用 AddInt / AddDouble，空值用 AddNull。值里的引号无需转义，天然防注入。LastInsertId() 取回刚写入的自增主键。"
   },
   "code": "using System;\nusing System.Data;\nusing System.Data.Sqlite;\n\nSqliteConnection db = SqliteConnection.Open(\"app.db\");\ndb.Execute(\"INSERT INTO books (title, author, price) VALUES (?, ?, ?)\",\n    new DbParams().Add(\"Zan in Action\").Add(\"Alice\").AddDouble(59.9));\ndb.Execute(\"INSERT INTO books (title, author, price) VALUES (?, ?, ?)\",\n    new DbParams().Add(\"Compilers\").Add(\"O'Brien\").AddDouble(99.0));\n// An unknown author is NULL, not an empty string.\ndb.Execute(\"INSERT INTO books (title, author, price) VALUES (?, ?, ?)\",\n    new DbParams().Add(\"Anonymous\").AddNull().AddDouble(9.9));\nConsole.WriteLine(\"new id: \" + Convert.ToString(db.LastInsertId()));"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Query and Read Rows",
    "zh": "查询与读取结果"
   },
   "body": {
    "en": "Query returns a DbResult held in memory: RowCount() / ColumnCount() size it, GetString(row, col) and GetByName(row, \"col\") read cells, IsNullByName tells NULL apart from an empty string. ExecuteScalar returns the first cell of the first row - the usual way to read COUNT / MAX.",
    "zh": "Query 返回内存中的 DbResult：RowCount() / ColumnCount() 给出大小，GetString(行, 列) 和 GetByName(行, \"列名\") 取单元格，IsNullByName 区分 NULL 和空串。ExecuteScalar 返回首行首列，常用于 COUNT / MAX。"
   },
   "code": "using System;\nusing System.Data;\nusing System.Data.Sqlite;\n\nDbResult r = db.Query(\n    \"SELECT id, title, author FROM books WHERE price > ? ORDER BY id\",\n    new DbParams().AddDouble(20.0));\nfor (int i = 0; i < r.RowCount(); i = i + 1) {\n    string author = r.GetByName(i, \"author\");\n    if (r.IsNullByName(i, \"author\")) { author = \"(unknown)\"; }\n    Console.WriteLine(\"#\" + r.GetByName(i, \"id\") + \" \"\n        + r.GetByName(i, \"title\") + \" by \" + author);\n}\nConsole.WriteLine(\"count: \" + db.ExecuteScalar(\"SELECT COUNT(*) FROM books\"));"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Update and Delete",
    "zh": "更新与删除"
   },
   "body": {
    "en": "Execute returns the number of affected rows for UPDATE and DELETE, so you can tell a no-op from a real change. Keep the WHERE values in DbParams as well.",
    "zh": "UPDATE 与 DELETE 的 Execute 返回受影响行数，可以据此区分“没匹配到”和“真的改了”。WHERE 里的值同样放进 DbParams。"
   },
   "code": "using System;\nusing System.Data;\n\nint updated = db.Execute(\"UPDATE books SET price = price * 0.5 WHERE title = ?\",\n    new DbParams().Add(\"Compilers\"));\nConsole.WriteLine(\"discounted: \" + Convert.ToString(updated));\n\nint deleted = db.Execute(\"DELETE FROM books WHERE price < ?\",\n    new DbParams().AddDouble(10.0));\nConsole.WriteLine(\"removed: \" + Convert.ToString(deleted));"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Transactions",
    "zh": "事务"
   },
   "body": {
    "en": "BeginTransaction / Commit / Rollback group writes into one atomic unit - and make bulk inserts an order of magnitude faster, because autocommit no longer flushes once per statement. Roll back on any error so a half-written batch never lands.",
    "zh": "BeginTransaction / Commit / Rollback 把多次写入合并为一个原子单元——同时让批量插入快一个数量级，因为不再每条语句都自动提交落盘。出错时回滚，避免写一半的数据留在库里。"
   },
   "code": "using System;\nusing System.Data;\n\ndb.BeginTransaction();\nfor (int i = 0; i < 1000; i = i + 1) {\n    db.Execute(\"INSERT INTO books (title, price) VALUES (?, ?)\",\n        new DbParams().Add(\"batch \" + Convert.ToString(i)).AddDouble(1.0));\n}\ndb.Commit();          // one flush for the whole batch\n\ndb.BeginTransaction();\ndb.Execute(\"DELETE FROM books\");\ndb.Rollback();        // nothing was deleted after all"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Connection Pool",
    "zh": "连接池"
   },
   "body": {
    "en": "A pool reuses connections across coroutines and caps concurrency: AcquireAsync suspends (it does not block a thread) when the pool is full, Release hands the connection back. SqlitePool / PgPool / MySqlPool / FirebirdPool follow the same shape.",
    "zh": "连接池在多个协程间复用连接并给并发封顶：池满时 AcquireAsync 挂起协程（不阻塞线程），Release 归还连接。SqlitePool / PgPool / MySqlPool / FirebirdPool 用法一致。"
   },
   "code": "using System;\nusing System.Data.Sqlite;\n\nstatic async int Total(string path) {\n    SqlitePool pool = new SqlitePool(path, 4);   // at most 4 connections\n    SqliteConnection db = await pool.AcquireAsync();\n    int n = Convert.ToInt(db.ExecuteScalar(\"SELECT COUNT(*) FROM books\"));\n    pool.Release(db);\n    pool.Close();\n    return n;\n}"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "PostgreSQL",
    "zh": "PostgreSQL"
   },
   "body": {
    "en": "PgConnection speaks to PostgreSQL (and openGauss / Kingbase and other PG-protocol engines) through native libpq. OpenParams(host, port, database, user, password) connects synchronously, OpenParamsAsync inside a coroutine. `?` placeholders are rewritten to $1..$n and bound natively.",
    "zh": "PgConnection 通过原生 libpq 连接 PostgreSQL（以及 openGauss、金仓等 PG 协议引擎）。OpenParams(主机, 端口, 库名, 用户, 密码) 同步连接，协程里用 OpenParamsAsync。`?` 占位符会自动改写为 $1..$n 并原生绑定。"
   },
   "code": "using System;\nusing System.Data;\nusing System.Data.Postgres;\n\nPgConnection db = PgConnection.OpenParams(\n    \"127.0.0.1\", 5432, \"postgres\", \"postgres\", \"secret\");\nif (!db.IsConnected()) {\n    Console.WriteLine(\"connect failed: \" + db.GetError());\n    return;\n}\ndb.Execute(\"CREATE TABLE IF NOT EXISTS books (\"\n    + \"id SERIAL PRIMARY KEY, title TEXT NOT NULL, price NUMERIC(8,2))\");\ndb.Execute(\"INSERT INTO books (title, price) VALUES (?, ?)\",\n    new DbParams().Add(\"Zan in Action\").AddDouble(59.9));\nDbResult r = db.Query(\"SELECT id, title FROM books ORDER BY id\");\ndb.Close();"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "MySQL / MariaDB",
    "zh": "MySQL / MariaDB"
   },
   "body": {
    "en": "MySqlConnection implements the MySQL wire protocol in pure Zan (mysql_native_password and caching_sha2_password, no C dependency) and is fully asynchronous. The *ParamsAsync calls use prepared statements over the binary protocol with a statement cache, so a loop prepares once. TiDB / OceanBase and other protocol-compatible engines work the same way.",
    "zh": "MySqlConnection 用纯 Zan 实现 MySQL 线协议（支持 mysql_native_password 与 caching_sha2_password，无 C 依赖），全异步。*ParamsAsync 走二进制协议的预处理语句并带语句缓存，循环插入只 PREPARE 一次。TiDB、OceanBase 等协议兼容引擎同样适用。"
   },
   "code": "using System;\nusing System.Data;\nusing System.Data.MySql;\n\nstatic async int Run() {\n    MySqlConnection db = await MySqlConnection.OpenParamsAsync(\n        \"127.0.0.1\", 3306, \"demo\", \"root\", \"secret\");\n    if (!db.IsConnected()) { return -1; }\n\n    await db.ExecuteParamsAsync(\n        \"INSERT INTO books (title, price) VALUES (?, ?)\",\n        new DbParams().Add(\"Zan in Action\").AddDouble(59.9));\n\n    DbResult r = await db.QueryParamsAsync(\n        \"SELECT id, title FROM books WHERE price > ?\",\n        new DbParams().AddDouble(20.0));\n    db.Close();\n    return r.RowCount();\n}"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "SQL Server, Oracle and ODBC",
    "zh": "SQL Server / Oracle 等 ODBC 引擎"
   },
   "body": {
    "en": "DbConnection reaches every engine with an ODBC driver installed (Windows odbc32, Linux/macOS unixODBC). Convenience factories: OpenSqlServer / OpenOracle / OpenMySQL / OpenPostgreSQL / OpenDM / OpenKingbase / OpenOpenGauss / OpenClickHouse / OpenTDengine / OpenDuckDB / OpenSQLite / OpenFirebird, plus OpenODBC for any DSN or connection string. The API (Execute / Query / ExecuteScalar, with DbParams) is the same for all of them; GetProvider() reports which engine you got.",
    "zh": "DbConnection 可连接任何装有 ODBC 驱动的引擎（Windows 用 odbc32，Linux/macOS 用 unixODBC）。便捷工厂：OpenSqlServer / OpenOracle / OpenMySQL / OpenPostgreSQL / OpenDM（达梦）/ OpenKingbase（金仓）/ OpenOpenGauss / OpenClickHouse / OpenTDengine / OpenDuckDB / OpenSQLite / OpenFirebird，以及万能入口 OpenODBC（任意 DSN 或连接串）。所有引擎共用同一套 API（Execute / Query / ExecuteScalar + DbParams）；GetProvider() 返回实际引擎类型。"
   },
   "code": "using System;\nusing System.Data;\n\nDbConnection db = DbConnection.OpenSqlServer(\n    \"127.0.0.1\", 1433, \"demo\", \"sa\", \"secret\");\n// Any driver by DSN or full connection string:\n// DbConnection db = DbConnection.OpenODBC(\"DSN=mydb;UID=sa;PWD=secret\");\nif (!db.IsConnected()) {\n    Console.WriteLine(\"connect failed: \" + db.GetError());\n    return;\n}\nConsole.WriteLine(\"provider: \" + Convert.ToString(db.GetProvider()));\ndb.Execute(\"INSERT INTO books (id, title, price) VALUES (?, ?, ?)\",\n    new DbParams().AddInt(1).Add(\"Zan in Action\").AddInt(59));\nDbResult r = db.Query(\"SELECT id, title FROM books ORDER BY id\");\ndb.Close();"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Error Handling",
    "zh": "错误处理"
   },
   "body": {
    "en": "Connections report failure instead of crashing: check IsConnected() after opening and read GetError() for the driver's message. Wrap a batch in try/catch plus Rollback so a failed statement cannot leave a half-applied transaction behind.",
    "zh": "连接失败不会崩溃，而是返回状态：打开后检查 IsConnected()，用 GetError() 读取驱动给出的错误信息。批量写入用 try/catch + Rollback 包住，避免失败时留下写了一半的事务。"
   },
   "code": "using System;\nusing System.Data;\n\ntry {\n    db.BeginTransaction();\n    db.Execute(\"UPDATE account SET balance = balance - ? WHERE id = ?\",\n        new DbParams().AddInt(100).AddInt(1));\n    db.Execute(\"UPDATE account SET balance = balance + ? WHERE id = ?\",\n        new DbParams().AddInt(100).AddInt(2));\n    db.Commit();\n} catch (Exception e) {\n    db.Rollback();\n    Console.WriteLine(\"transfer failed: \" + db.GetError());\n}"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "ORM: Entities and CodeFirst",
    "zh": "ORM：实体与建表（CodeFirst）"
   },
   "body": {
    "en": "An entity is a plain class: its fields are the columns, an int field named id is the auto-increment primary key, and the table name is the lower-cased class name. db.SyncStructure<T>() creates the table if it is missing and adds columns that were added to the class, using each provider's own types. The compiler generates the mapping, so there is no reflection and no string column names at the call site.",
    "zh": "实体就是普通类：字段即列，名为 id 的 int 字段是自增主键，表名取类名小写。db.SyncStructure<T>() 在表不存在时建表，类里新增的字段会自动补列，类型按各数据库方言生成。映射由编译器生成，运行期没有反射，调用处也不需要写列名字符串。"
   },
   "code": "using System;\nusing System.Data;\nusing System.Data.Sqlite;\n\nclass Blog {\n    int id;              // auto-increment primary key\n    string title;\n    int clicks;\n    double score;\n    bool online;\n}\n\nSqliteConnection db = SqliteConnection.Open(\"app.db\");\ndb.SyncStructure<Blog>();     // create or extend the table"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "ORM: Lambda Queries",
    "zh": "ORM：lambda 查询"
   },
   "body": {
    "en": "db.Select<T>() starts a typed chain: Where takes a lambda over the entity, so columns are checked by the compiler and autocompleted. == != > >= < <= && || and the string helpers Contains / StartsWith / EndsWith / Like translate to SQL, values are always bound as parameters. OrderBy / OrderByDescending sort, Skip + Take paginate, ToList / First / Count / Any / Sum / Max / Min / Avg finish the chain. WhereIf(cond, lambda) adds a filter only when cond is true.",
    "zh": "db.Select<T>() 开始类型化链式查询：Where 接收实体 lambda，列名由编译器检查、可自动补全。== != > >= < <= && || 以及 Contains / StartsWith / EndsWith / Like 都会翻译成 SQL，值始终以参数绑定。OrderBy / OrderByDescending 排序，Skip + Take 分页，ToList / First / Count / Any / Sum / Max / Min / Avg 收尾。WhereIf(条件, lambda) 只在条件成立时追加过滤。"
   },
   "code": "using System;\nusing System.Data;\n\nList<Blog> hot = db.Select<Blog>()\n    .Where(a => a.clicks >= 8 && a.online)\n    .OrderByDescending(a => a.clicks)\n    .Take(10)\n    .ToList();\n\nBlog one = db.Select<Blog>().Where(a => a.title == \"hello zan\").First();\nint total = db.Select<Blog>().Where(a => a.title.Contains(\"zan\")).Count();\nlong sum = db.Select<Blog>().Sum(a => a.clicks);\n\n// page 2, 20 per page; the keyword filter only applies when one was typed\nstring kw = \"\";\nList<Blog> page = db.Select<Blog>()\n    .WhereIf(kw.Length > 0, a => a.title.Contains(kw))\n    .OrderBy(a => a.id)\n    .Skip(20).Take(20)\n    .ToList();"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "ORM: Insert",
    "zh": "ORM：插入"
   },
   "body": {
    "en": "db.Insert<T>(entity) inserts one row, db.Insert<T>(list) inserts a batch in a single statement, and AppendData adds more rows to the same chain. ExecuteAffrows() returns the affected row count, ExecuteIdentity() returns the new auto-increment id. InsertColumns / IgnoreColumns restrict which columns are written.",
    "zh": "db.Insert<T>(实体) 插入一行，db.Insert<T>(列表) 一条语句批量插入，AppendData 可继续往同一条链里追加。ExecuteAffrows() 返回受影响行数，ExecuteIdentity() 返回新生成的自增 id。InsertColumns / IgnoreColumns 限定写入哪些列。"
   },
   "code": "using System;\nusing System.Data;\n\nBlog b = new Blog();\nb.title = \"hello zan\";\nb.clicks = 3;\nint id = db.Insert<Blog>(b).ExecuteIdentity();\n\nList<Blog> many = new List<Blog> { First(), Second(), Third() };\nint rows = db.Insert<Blog>(many).ExecuteAffrows();\n\n// only these columns are written, the rest keep their database defaults\ndb.Insert<Blog>(b).InsertColumns(a => a.title).ExecuteAffrows();"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "ORM: Update Only the Fields You Mean",
    "zh": "ORM：只更新指定字段"
   },
   "body": {
    "en": "Business code usually changes two or three fields, not a whole row. Set(a => a.field, value) writes exactly those columns and SetIncr(a => a.field, n) emits field = field + n, so no read-modify-write is needed. When you do have a full entity, SetSource(o) plus UpdateColumns(...) / IgnoreColumns(...) restricts the UPDATE to the columns you name. A chain without a Where refuses to run, so a missing condition can never wipe the table.",
    "zh": "业务里更新往往只改两三个字段，而不是整行。Set(a => a.字段, 值) 只写这些列，SetIncr(a => a.字段, n) 生成 字段 = 字段 + n，不必先查再写。手上是完整实体时，用 SetSource(o) 配合 UpdateColumns(...) / IgnoreColumns(...) 限定实际更新的列。没有 Where 的链不会执行，漏写条件不会清空整表。"
   },
   "code": "using System;\nusing System.Data;\n\nint n = db.Update<Blog>()\n    .Set(a => a.title, \"new title\")\n    .SetIncr(a => a.clicks, 1)          // clicks = clicks + 1\n    .Where(a => a.id == id)\n    .ExecuteAffrows();\n\n// a whole entity, but only title is actually written\ndb.Update<Blog>()\n    .SetSource(blog)\n    .UpdateColumns(a => a.title)\n    .ExecuteAffrows();\n\n// everything except the columns you protect\ndb.Update<Blog>()\n    .SetSource(blog)\n    .IgnoreColumns(a => a.clicks)\n    .ExecuteAffrows();\n\nint gone = db.Delete<Blog>().Where(a => a.clicks == 0).ExecuteAffrows();"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "ORM: Dynamic Values (forms, JSON)",
    "zh": "ORM：动态字段（表单、JSON）"
   },
   "body": {
    "en": "Submitted data arrives as a bag of fields, not as a lambda per column. new DbValues().Set(name, value) collects them and <Entity>Cols gives an autocompleted constant per column, so keys stay checked: an unknown key throws instead of reaching SQL. AppendDict inserts one bag or a List<DbValues> batch, SetDict updates exactly the submitted fields, WhereDict builds dynamic conditions. Values are bound as parameters and converted to the column's declared type, so a form that carries everything as text still writes an INTEGER column as a number - a value that cannot convert throws and names the column.",
    "zh": "提交上来的数据是一堆字段，不可能每列写一个 lambda。用 new DbValues().Set(列, 值) 收集，列名用生成的 <Entity>Cols 常量（可自动补全），键会被校验：不属于该实体的键直接抛异常，不会进入 SQL。AppendDict 插入单个字典或 List<DbValues> 批量，SetDict 只更新提交到的字段，WhereDict 拼动态条件。所有值都是参数绑定，并按列声明类型转换——表单里全是字符串也能正确写入 INTEGER 列，转换不了会抛出并指明是哪一列。"
   },
   "code": "using System;\nusing System.Data;\n\nDbValues form = new DbValues()\n    .Set(BlogCols.title, \"hello\")\n    .Set(BlogCols.clicks, \"42\")        // text in, INTEGER column out\n    .Set(BlogCols.score, 1.5);\n\ndb.Insert<Blog>().AppendDict(form).ExecuteAffrows();\n\nList<DbValues> rows = new List<DbValues> { form, other };\ndb.Insert<Blog>().AppendDict(rows).ExecuteAffrows();\n\n// only the submitted fields are updated\ndb.Update<Blog>().SetDict(form).Where(a => a.id == id).ExecuteAffrows();\n\n// dynamic conditions, still parameterised\nDbValues cond = new DbValues().Set(BlogCols.title, \"hello\");\nint n = db.Select<Blog>().WhereDict(cond).Count();\n\nform.Remove(BlogCols.clicks);          // drop a field before executing"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "ORM: Connection Pool and Transactions",
    "zh": "ORM：连接池与事务"
   },
   "body": {
    "en": "The chain runs on any IDbExecutor, so a connection checked out of a pool drives it unchanged. DbPool (or the typed SqlitePool / MySqlPool / PgPool) hands one out with await pool.AcquireAsync(), which suspends the coroutine instead of blocking a thread when the pool is saturated, and Release puts it back - use the typed pool when you also need driver-only members such as transactions. BeginTransaction / Commit / Rollback wrap several chained statements into one unit.",
    "zh": "链式 API 执行在 IDbExecutor 上，因此从连接池取出的连接可以原样使用。DbPool（或类型化的 SqlitePool / MySqlPool / PgPool）用 await pool.AcquireAsync() 取连接，池满时挂起协程而不是阻塞线程，Release 归还；需要驱动特有成员（如事务）时用类型化池。BeginTransaction / Commit / Rollback 把多条链式语句合成一个事务单元。"
   },
   "code": "using System;\nusing System.Data;\nusing System.Data.Sqlite;\n\nstatic async void Work(SqlitePool pool) {\n    SqliteConnection db = await pool.AcquireAsync();\n\n    db.BeginTransaction();\n    db.Insert<Blog>(blog).ExecuteAffrows();\n    db.Update<Blog>().SetIncr(a => a.clicks, 1)\n        .Where(a => a.id == 1).ExecuteAffrows();\n    db.Commit();                       // or db.Rollback();\n\n    List<Blog> open = db.Select<Blog>().Where(a => a.online).ToList();\n    pool.Release(db);                  // always give it back\n}\n\nSqlitePool pool = new SqlitePool(\"app.db\", 8);\nawait Work(pool);\npool.Close();"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Query Builder: Safe SQL",
    "zh": "查询构建器：安全拼 SQL"
   },
   "body": {
    "en": "QueryBuilder writes the SQL text and the bound parameter list separately, so values never enter the statement. From / InsertInto / Update / DeleteFrom start a chain; BuildSelectParams(columns) with SelectParams(), BuildInsertParams() with InsertParams(), BuildUpdateParams() with UpdateParams() and BuildDeleteParams() hand both halves to the connection. Table and column names are validated at build time - an injected identifier throws instead of running.",
    "zh": "QueryBuilder 把 SQL 文本和绑定参数分开生成，值永远不进入语句文本。From / InsertInto / Update / DeleteFrom 开始链式构建；BuildSelectParams(列) 配 SelectParams()、BuildInsertParams() 配 InsertParams()、BuildUpdateParams() 配 UpdateParams()、BuildDeleteParams() 配 SelectParams()，两半一起交给连接执行。表名列名在构建期校验——注入的标识符会直接抛异常，而不是被执行。"
   },
   "code": "using System;\nusing System.Data;\nusing System.Data.Orm;\n\nQueryBuilder q = QueryBuilder.From(\"users\")\n    .WhereEq(\"city\", \"Beijing\")\n    .WhereGeInt(\"age\", 18);\nDbResult r = db.Query(q.BuildSelectParams(\"name, age\"), q.SelectParams());\n\nQueryBuilder ins = QueryBuilder.InsertInto(\"users\")\n    .Set(\"name\", \"O'Brien\").Set(\"city\", \"Beijing\").SetInt(\"age\", 41);\ndb.Execute(ins.BuildInsertParams(), ins.InsertParams());\n\nQueryBuilder up = QueryBuilder.Update(\"users\")\n    .Set(\"city\", \"Hangzhou\").WhereEq(\"name\", \"Bob\");\ndb.Execute(up.BuildUpdateParams(), up.UpdateParams());\n\nQueryBuilder del = QueryBuilder.DeleteFrom(\"users\").WhereLtInt(\"age\", 18);\ndb.Execute(del.BuildDeleteParams(), del.SelectParams());"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Complex Conditions",
    "zh": "复杂条件"
   },
   "body": {
    "en": "The whole condition family stacks as AND unless you say otherwise: WhereEq / WhereNe, WhereGtInt / WhereGeInt / WhereLtInt / WhereLeInt, WhereBetween, WhereIn / WhereNotIn, WhereLike / WhereContains / WhereStartsWith, WhereNull / WhereNotNull, and OrWhere for an OR branch.",
    "zh": "条件族默认以 AND 叠加，需要 OR 时用 OrWhere：WhereEq / WhereNe、WhereGtInt / WhereGeInt / WhereLtInt / WhereLeInt、WhereBetween、WhereIn / WhereNotIn、WhereLike / WhereContains / WhereStartsWith、WhereNull / WhereNotNull。"
   },
   "code": "using System.Data;\nusing System.Data.Orm;\n\nList<string> cities = new List<string> { \"Beijing\", \"Shanghai\" };\n\nQueryBuilder q = QueryBuilder.From(\"users\")\n    .WhereIn(\"city\", cities)\n    .WhereBetween(\"age\", \"20\", \"36\")\n    .WhereStartsWith(\"name\", \"O'\")\n    .WhereNotNull(\"email\")\n    .OrWhere(\"age > 40\")\n    .OrderByDesc(\"age\");\nDbResult r = db.Query(q.BuildSelectParams(\"name, age\"), q.SelectParams());"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Joins and Aggregates",
    "zh": "连接与聚合"
   },
   "body": {
    "en": "Join / LeftJoin add tables on an ON condition (qualify columns as table.column); GroupBy + Having aggregate, Distinct removes duplicates. A LEFT JOIN with WhereNull on the right-hand key is the standard \"rows without a match\" query.",
    "zh": "Join / LeftJoin 按 ON 条件连接多表（列名用 表.列 限定）；GroupBy + Having 做聚合，Distinct 去重。LEFT JOIN 配合对右表主键的 WhereNull，就是标准的“查没有匹配记录的行”。"
   },
   "code": "using System;\nusing System.Data;\nusing System.Data.Orm;\n\n// users with a big order, largest first\nQueryBuilder q = QueryBuilder.From(\"users\")\n    .Join(\"orders\", \"orders.user_id = users.id\")\n    .WhereGtInt(\"orders.amount\", 100)\n    .OrderByDesc(\"orders.amount\");\nDbResult r = db.Query(q.BuildSelectParams(\"users.name, orders.amount\"),\n                      q.SelectParams());\n\n// users with no orders at all\nQueryBuilder none = QueryBuilder.From(\"users\")\n    .LeftJoin(\"orders\", \"orders.user_id = users.id\")\n    .WhereNull(\"orders.id\");\nConsole.WriteLine(db.ExecuteScalar(none.BuildSelectParams(\"COUNT(*)\"),\n                                   none.SelectParams()));\n\n// cities with at least two users\nQueryBuilder g = QueryBuilder.From(\"users\")\n    .GroupBy(\"city\").Having(\"COUNT(*) >= 2\").OrderBy(\"city\");\nDbResult byCity = db.Query(g.BuildSelectParams(\"city, COUNT(*)\"),\n                           g.SelectParams());"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Paging Dialects",
    "zh": "分页方言"
   },
   "body": {
    "en": "Limit + Offset emit LIMIT/OFFSET by default. SQL Server and Oracle want OFFSET .. FETCH instead: call Dialect(SqlDialect.SqlServer) and the builder emits the right form - the rest of the chain is unchanged. GetProvider() tells you which one the connection needs.",
    "zh": "Limit + Offset 默认生成 LIMIT/OFFSET。SQL Server 与 Oracle 需要 OFFSET .. FETCH：调用 Dialect(SqlDialect.SqlServer) 即可生成对应写法，链式的其它部分不变。用 GetProvider() 判断当前连接属于哪一类。"
   },
   "code": "using System.Data;\nusing System.Data.Orm;\n\nQueryBuilder q = QueryBuilder.From(\"users\").OrderBy(\"id\").Limit(20).Offset(40);\nif (db.GetProvider() == DbProvider.SqlServer\n    || db.GetProvider() == DbProvider.Oracle) {\n    q.Dialect(SqlDialect.SqlServer);\n}\nDbResult page = db.Query(q.BuildSelectParams(\"*\"), q.SelectParams());"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Migrations",
    "zh": "数据库迁移"
   },
   "body": {
    "en": "A Migration is a named pair of up/down statements. Migration.RunAll(db, list) applies the ones this database has not seen yet, in order, and records them in a _migrations table - running it again does nothing, so it is safe on every startup.",
    "zh": "Migration 是一对带名字的升级/回滚语句。Migration.RunAll(db, 列表) 按顺序执行该库尚未应用过的迁移，并记录在 _migrations 表中——再次运行什么都不做，因此可以放在每次启动时执行。"
   },
   "code": "using System.Data.Orm;\n\nList<Migration> ms = new List<Migration> {\n    new Migration(\"001_create_posts\",\n        \"CREATE TABLE posts (id INTEGER PRIMARY KEY AUTOINCREMENT, \"\n            + \"user_id INTEGER, title TEXT)\",\n        \"DROP TABLE posts\"),\n    new Migration(\"002_add_posts_body\",\n        \"ALTER TABLE posts ADD COLUMN body TEXT\",\n        \"ALTER TABLE posts DROP COLUMN body\")\n};\n\nMigration.RunAll(db, ms);   // idempotent: safe to call on every start"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Database First",
    "zh": "从已有表反向生成模型"
   },
   "body": {
    "en": "Model.FromTable(db, table) reads an existing table's schema back into a Model, so an existing database needs no hand-written definition. ToDefineCode() prints the equivalent Model.Define(...) source to paste into your project.",
    "zh": "Model.FromTable(db, 表名) 从已有表反推出 Model，接手现成数据库时无需手写定义。ToDefineCode() 输出等价的 Model.Define(...) 源码，可直接粘贴进项目。"
   },
   "code": "using System;\nusing System.Data.Orm;\n\nModel posts = Model.FromTable(db, \"posts\");\nConsole.WriteLine(\"columns: \" + Convert.ToString(posts.ColumnCount())\n    + \" pk=\" + posts.GetPrimaryKey());\nConsole.WriteLine(posts.ToDefineCode());"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "Redis Cache",
    "zh": "Redis 缓存"
   },
   "body": {
    "en": "RedisClient speaks RESP in pure Zan and is asynchronous: ConnectAsync, then SetAsync / GetAsync / DelAsync / ExistsAsync / IncrAsync / ExpireAsync. CommandAsync sends any other command (hashes, lists, ...) and returns a RedisReply. The classic cache-aside pattern is read cache, miss, load, write back with an expiry.",
    "zh": "RedisClient 用纯 Zan 实现 RESP 协议且全异步：ConnectAsync 之后使用 SetAsync / GetAsync / DelAsync / ExistsAsync / IncrAsync / ExpireAsync。CommandAsync 可发送任意其它命令（哈希、列表等），返回 RedisReply。经典的 cache-aside：先查缓存，未命中回源，再回填并设置过期时间。"
   },
   "code": "using System;\nusing System.Data.Redis;\n\nstatic async string UserName(RedisClient r, int id) {\n    string key = \"user:\" + Convert.ToString(id) + \":name\";\n    string cached = await r.GetAsync(key);\n    if (cached != null) { return cached; }\n\n    string fresh = LoadFromDb(id);          // cache miss: go to the database\n    bool ok = await r.SetAsync(key, fresh);\n    int e = await r.ExpireAsync(key, 60);   // expire in 60s\n    return fresh;\n}\n\nstatic async int Run() {\n    RedisClient r = await RedisClient.ConnectAsync(\"127.0.0.1\", 6379);\n    Console.WriteLine(await UserName(r, 42));\n    return 0;\n}"
  },
  {
   "cat": {
    "en": "Database",
    "zh": "数据库"
   },
   "title": {
    "en": "ZanDb Documents",
    "zh": "ZanDb 文档数据库"
   },
   "body": {
    "en": "ZanDb is a built-in single-file document store (WAL crash safety, LRU page cache, no dependency): documents are JsonValue trees, so nested objects and arrays need no schema. A Collection offers Insert / InsertBulk / FindById / Update / Upsert / DeleteById / Count / Exists, EnsureIndex adds a secondary index, and Query() is a lazy LINQ-style chain (WhereEq, Where(lambda), OrderByInt(Desc), Skip, Take, ToList, First, Count).",
    "zh": "ZanDb 是内置的单文件文档数据库（WAL 崩溃安全、LRU 页缓存、零依赖）：文档就是 JsonValue 树，嵌套对象与数组无需事先定义结构。Collection 提供 Insert / InsertBulk / FindById / Update / Upsert / DeleteById / Count / Exists；EnsureIndex 建二级索引；Query() 是延迟求值的 LINQ 风格链（WhereEq、Where(lambda)、OrderByInt(Desc)、Skip、Take、ToList、First、Count）。"
   },
   "code": "using System;\nusing System.Json;\nusing System.Data.ZanDb;\n\nZanDatabase db = ZanDatabase.Open(\"app.zdb\");\nCollection users = db.GetCollection(\"users\");\n\nJsonValue u = JsonValue.NewObject();\nu.Put(\"name\", JsonValue.NewStr(\"Alice\"));\nu.Put(\"city\", JsonValue.NewStr(\"Beijing\"));\nu.Put(\"age\", JsonValue.NewNum(\"30\"));\nint id = users.Insert(u);\n\nusers.EnsureIndex(\"city\");                 // WhereEq(\"city\", ...) hits the index\nList<JsonValue> adults = users.Query()\n    .WhereEq(\"city\", \"Beijing\")\n    .Where((JsonValue x) => x.Int(\"age\", 0) >= 18)\n    .OrderByIntDesc((JsonValue x) => x.Int(\"age\", 0))\n    .Take(10)\n    .ToList();\nConsole.WriteLine(\"found: \" + Convert.ToString(adults.Count));"
  }
 ]
}

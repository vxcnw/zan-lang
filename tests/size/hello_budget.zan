// 体积预算探针：最小 stdlib 程序。--publish 构建的 .text / 文件大小受
// tests/run_size_budget.cmake 硬性预算约束——固定大表回归或交叉链接丢
// --gc-sections 都会在这里爆掉（docs/embedded_runtime_assessment.md）。
class Program {
    static void Main() {
        Console.WriteLine("hi");
    }
}

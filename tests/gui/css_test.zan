using System;
using Gui;

namespace CssTest;

/// Headless verification of the CSS skin pipeline: Css.Parse (comments,
/// selector lists, :root variables, state suffixes) plus StyleSheet.ApplyBox
/// (colors, gradients, borders, radii, spacing, typography, motion). Prints
/// the resolved StyleBox fields so the parsing math can be diffed against a
/// golden file - no window, no driver.
class Program {
    static void FontSources() {
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(":root { --font-size-small: 21; }", names, vals);
        StyleSheet external = Css.ParseWith("label { font-size: var(--font-size-small); }", names, vals);
        StyleSheet local = Css.Parse(":root { --font-size-small: 14; } label { font-size: var(--font-size-small); }");
        StyleSheet shadow = Css.ParseWith(":root { --font-size-small: 16; } label { font-size: var(--font-size-small); }", names, vals);
        StyleSheet alias = Css.ParseWith(":root { --caption: var(--font-size-small); } label { font-size: var(--caption); }", names, vals);
        StyleSheet cycle = Css.ParseWith(":root { --caption: var(--caption); } label { font-size: var(--caption); }", names, vals);
        Program.FontSource(external, "external");
        Program.FontSource(local, "local");
        Program.FontSource(shadow, "shadow");
        Program.FontSource(alias, "alias");
        Program.FontSource(cycle, "cycle");
        names.Add("--font-size-small"); vals.Add("15");
        StyleSheet inherited = Css.ParseWithSources("label { font-size: var(--font-size-small); }", names, vals, 1);
        Program.FontSource(inherited, "inherited");
    }
    static void FontSource(StyleSheet sheet, string name) {
        StyleBox b = new StyleBox(); sheet.ApplyBox(b, "label");
        int physical = b.IsPrescaled(StyleBox.SourceFont()) ? 1 : 0;
        Console.WriteLine($"font-source {name} px {b.fontPx} physical {physical}");
    }

    static StyleSheet Sheet() {
        string css = "/* skin */\n"
            + ":root { --accent: #d92b2b; --radius: 12; }\n"
            + "button, tab.item { background: #202430; color: #e8e8f0;\n"
            + "                   padding: 6 14; radius: var(--radius);\n"
            + "                   border: 2 var(--accent); font-size: 15; }\n"
            + "button.primary { background: linear-gradient(to right, #ffd76a, #e0a020);\n"
            + "                 color: rgb(20, 22, 30); font-weight: bold;\n"
            + "                 box-shadow: 0 4 12 rgba(0, 0, 0, 0.5);\n"
            + "                 transition: 180ms ease-in-out; }\n"
            + "button.primary:hover { background: #ffe08a; opacity: 0.8; }\n"
            + "button.primary:disabled { background: hsl(220, 10%, 30%); }\n"
            + "card { background: transparent; radius: 8 8 20 20;\n"
            + "       border-bottom: 3 navy; margin: 4 8 12 16;\n"
            + "       backdrop-filter: blur(18px); animation: pulse 900ms; }\n"
            + "input:focus { border: 1 #3b82f6; letter-spacing: 2;\n"
            + "              text-align: center; text-overflow: ellipsis; }\n"
            + "option { gap: 7; }\n"
            + "::option { padding-left: 3; }\n"
            + "select::option { padding-right: 4; }\n"
            + ".primary::option { padding-top: 5; }\n"
            + "select.primary::option { padding-bottom: 6; }\n"
            + "#city::option { width: 70; }\n"
            + "select.primary::option:hover { height: 31; }\n"
            + "#city::option:selected { min-width: 80; }\n"
            + "checkbox::box { width: 18; padding-left: 2; }\n"
            + "checkbox::box:hover { height: 19; border: 2 #15803d; }\n"
            + "checkbox::box:checked { background: #16a34a; padding-left: 6; }\n"
            + "slider::track { height: 5; background: #202020; }\n"
            + "slider::fill { height: 3; background: #2563eb; }\n"
            + "slider::thumb { width: 20; height: 20; border: 3 #1d4ed8; }\n"
            + ".success::fill { background: #22c55e; }\n"
            + "progress.success::fill { height: 7; }\n"
            + "progress::label { color: #475569; font-size: 13; }\n";
        return Css.Parse(css);
    }

    static void Box(StyleSheet sheet, string selector) {
        StyleBox b = new StyleBox();
        sheet.ApplyBox(b, selector);
        Console.WriteLine($"--- {selector}");
        Console.WriteLine($"bg {b.bg} to {b.bgTo} dir {b.bgDir} opacity {b.opacity} blur {b.blur}");
        Console.WriteLine($"fg {b.fg} font {b.fontPx} weight {b.fontWeight} align {b.align} spacing {b.letterSpacing} ellipsis {b.ellipsis}");
        Console.WriteLine($"border {b.borderW}/{b.borderColor} bottom {b.borderBottomW}/{b.borderBottomColor}");
        Console.WriteLine($"radius {b.radius} corners {b.radiusTL},{b.radiusTR},{b.radiusBR},{b.radiusBL}");
        Console.WriteLine($"pad {b.padL},{b.padT},{b.padR},{b.padB} margin {b.marL},{b.marT},{b.marR},{b.marB}");
        Console.WriteLine($"shadow {b.shadow} {b.shadowDx},{b.shadowDy} blur {b.shadowBlur} inset {b.shadowInset}");
        Console.WriteLine($"motion {b.transitionMs}ms easing {b.easing} anim {b.anim} {b.animMs}ms");
    }

    static void Part(StyleSheet sheet, int state, string label) {
        StyleBox b = new StyleBox();
        Style.ApplySheet(sheet, b, "option", "primary", "", state);
        Style.ApplyPartSheet(sheet, b, "select", "option", "primary", "city", state);
        Console.WriteLine($"part {label} pad {b.padL},{b.padT},{b.padR},{b.padB} size {b.width},{b.height} min {b.minW} gap {b.gap}");
    }

    static void CheckedPart(StyleSheet sheet, int state, string label) {
        StyleBox b = new StyleBox();
        Style.ApplyPartSheet(sheet, b, "checkbox", "box", "", "", state);
        Console.WriteLine($"checked {label} bg {b.bg} size {b.width},{b.height} pad {b.padL} border {b.borderW}/{b.borderColor}");
    }

    static void RangePart(StyleSheet sheet, string type, string part, string cls) {
        StyleBox b = new StyleBox();
        Style.ApplyPartSheet(sheet, b, type, part, cls, "", Style.SNormal());
        Console.WriteLine($"range {type}.{cls}::{part} bg {b.bg} fg {b.fg} size {b.width},{b.height} font {b.fontPx} border {b.borderW}/{b.borderColor}");
    }

    /// 应用 CSS 通过 ParseWith 认得外部（主题/皮肤）的 :root 变量：
    /// 外部变量参与 var() 展开但不进结果表的 vars；同名时这份 CSS
    /// 自己声明的值胜出。
    static void ExternalVars() {
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(":root { --text-primary: #101828; --accent: #2563eb;\n"
                        + "        --card-blur: 18; }\n", names, vals);
        string css = ":root { --accent: #d92b2b; }\n"
            + "label { color: var(--text-primary); }\n"
            + "button { background: var(--accent); }\n"
            + "card { backdrop-filter: blur(var(--card-blur)px); }\n"
            + "panel { color: var(--nope); background: #111111; }\n";
        StyleSheet sheet = Css.ParseWith(css, names, vals);
        Console.WriteLine($"theme var seen {names.Count} own accent "
                          + sheet.Var("--accent"));
        string leaked = sheet.Var("--text-primary");
        Console.WriteLine($"leaked text-primary [{leaked}]");
        Program.Box(sheet, "label");
        Program.Box(sheet, "button");
        Program.Box(sheet, "card");
        Program.Box(sheet, "panel");
        StyleSheet plain = Css.Parse(css);
        StyleBox pb = new StyleBox();
        plain.ApplyBox(pb, "label");
        Console.WriteLine($"without theme vars fg {pb.fg}");
    }

    /// 渐变方向：关键字与角度都要落到光栅化支持的四向
    /// （0 下、1 右、2 右下、3 上、4 左下），角度吸附到最近的一向。
    static void GradDirs() {
        string css = "a { background: linear-gradient(to bottom, #111111, #222222); }\n"
            + "b { background: linear-gradient(to right, #111111, #222222); }\n"
            + "c { background: linear-gradient(to bottom right, #111111, #222222); }\n"
            + "d { background: linear-gradient(to top, #111111, #222222); }\n"
            + "e { background: linear-gradient(to bottom left, #111111, #222222); }\n"
            + "f { background: linear-gradient(135deg, #111111, #222222); }\n"
            + "g { background: linear-gradient(210deg, #111111, #222222); }\n"
            + "h { background: linear-gradient(#111111, #333333, #222222); }\n"
            + "i { background: linear-gradient(225deg, rgba(255,43,43,0.85),"
            + " rgba(43,107,255,0.35)); }\n";
        StyleSheet sheet = Css.Parse(css);
        List<string> sels = new List<string>();
        sels.Add("a"); sels.Add("b"); sels.Add("c"); sels.Add("d");
        sels.Add("e"); sels.Add("f"); sels.Add("g"); sels.Add("h");
        sels.Add("i");
        for (int i = 0; i < sels.Count; i = i + 1) {
            StyleBox b = new StyleBox();
            sheet.ApplyBox(b, sels[i]);
            Console.WriteLine($"grad {sels[i]} dir {b.bgDir} bg {b.bg} via {b.bgVia} to {b.bgTo}");
        }
    }

    /// 主题导出的按钮/tab token（Style.RootFromTheme）：按钮的角色色、
    /// ghost/secondary 混色与禁用形状都从这里取值，base.css 只消费
    /// token。名字或算法改了这里就会变，因此金标里钉住它们。
    static void ThemeTokens() {
        StyleSheet sheet = Css.Parse(Style.RootFromTheme(Theme.Light()));
        List<string> names = new List<string>();
        names.Add("--primary"); names.Add("--primary-hover");
        names.Add("--primary-pressed"); names.Add("--error-hover");
        names.Add("--neutral-ghost-hover-bg");
        names.Add("--neutral-ghost-active-bg");
        names.Add("--neutral-soft-bg"); names.Add("--neutral-soft-hover-bg");
        names.Add("--neutral-soft-active-bg");
        names.Add("--primary-soft-bg"); names.Add("--primary-ghost-active-bg");
        names.Add("--disabled-bg"); names.Add("--disabled-border");
        names.Add("--disabled-fg");
        // 表单族 token：选区混色在导出时算好，绘制路径不再 LerpColor。
        names.Add("--selection-bg"); names.Add("--scrollbar");
        names.Add("--scrollbar-hover"); names.Add("--field-bg");
        names.Add("--drop-tint");
        // 容器浮层族 token（A2）：遮罩/媒体 scrim 及其上控件色。
        names.Add("--overlay"); names.Add("--scrim");
        names.Add("--scrim-chip"); names.Add("--scrim-chip-hover");
        names.Add("--on-scrim"); names.Add("--tooltip-text-muted");
        for (int i = 0; i < names.Count; i = i + 1) {
            Console.WriteLine($"token {names[i]} {sheet.Var(names[i])}");
        }
    }

    /// 按钮的角色 × 变体 × 状态：颜色规则已从 Style.ButtonDefaults
    /// 迁到 CSS，所以这里按 base.css 的写法验证级联结果——伪状态
    /// 压过类、同权重按出现顺序、`border: 0 <色>` 清掉单边。
    static StyleSheet ButtonSheet() {
        string css = "button { background: var(--bg-primary); color: var(--text-primary);\n"
            + "         border: 1 var(--border-primary); }\n"
            + "button:hover { background: var(--bg-hover); border-color: var(--border-hover); }\n"
            + "button:active { background: var(--bg-active); border-color: var(--border-hover); }\n"
            + "button:focus { border-color: var(--border-focus); }\n"
            + "button.primary { background: var(--primary); color: var(--text-inverse);\n"
            + "                 border: 0 var(--primary); }\n"
            + "button.primary:hover { background: var(--primary-hover);\n"
            + "                       border-color: var(--primary-hover); }\n"
            + "button.primary:active { background: var(--primary-pressed);\n"
            + "                        border-color: var(--primary-pressed); }\n"
            + "button.ghost { background: transparent; color: var(--text-primary);\n"
            + "               border: 1 var(--text-primary); }\n"
            + "button.ghost:hover { background: var(--neutral-ghost-hover-bg);\n"
            + "                     border-color: var(--text-primary); }\n"
            + "button.ghost.primary { color: var(--primary); border-color: var(--primary); }\n"
            + "button.ghost.primary:hover { background: var(--primary-ghost-hover-bg);\n"
            + "                             border-color: var(--primary); }\n"
            + "button.secondary { background: var(--neutral-soft-bg);\n"
            + "                   color: var(--text-primary); border-width: 0; }\n"
            + "button.secondary.error { background: var(--error-soft-bg); color: var(--error); }\n"
            + "button.secondary.error:hover { background: var(--error-soft-hover-bg); }\n"
            + "button.text { background: transparent; color: var(--text-primary);\n"
            + "              border-width: 0; }\n"
            + "button.text:hover { background: var(--bg-hover); }\n"
            + "button.text:selected { color: var(--primary); }\n"
            + "button.text:selected:hover { color: var(--primary-hover); }\n"
            + "button.outline { background: var(--bg-primary); color: var(--text-primary);\n"
            + "                 border: 1 var(--text-primary); }\n"
            + "button.outline.error { color: var(--error); border: 1 var(--error); }\n"
            + "button:disabled { background: var(--disabled-bg);\n"
            + "                  color: var(--disabled-fg);\n"
            + "                  border: 1 var(--disabled-border); }\n"
            + "floatbutton { border-radius: 9999; }\n";
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(Style.RootFromTheme(Theme.Light()), names, vals);
        return Css.ParseWith(css, names, vals);
    }

    static void Btn(StyleSheet sheet, string type, string cls, int state,
                    string label) {
        StyleBox b = new StyleBox();
        Style.ApplySheet(sheet, b, type, cls, "", state);
        Console.WriteLine($"btn {label} bg {b.bg} fg {b.fg} border {b.borderW}/"
                          + $"{b.borderColor} bottom {b.borderBottomW} radius {b.radius}");
    }

    static void ButtonStates() {
        StyleSheet sheet = Program.ButtonSheet();
        int hover = Style.SHover();
        int press = Style.SHover() + Style.SActive();
        Program.Btn(sheet, "button", "", Style.SNormal(), "base");
        Program.Btn(sheet, "button", "", hover, "hover");
        Program.Btn(sheet, "button", "", Style.SFocus(), "focus");
        Program.Btn(sheet, "button", "primary", Style.SNormal(), "primary");
        Program.Btn(sheet, "button", "primary", hover, "primary-hover");
        Program.Btn(sheet, "button", "primary", press, "primary-press");
        Program.Btn(sheet, "button", "ghost", hover, "ghost-hover");
        Program.Btn(sheet, "button", "ghost primary", hover, "ghost-primary-hover");
        Program.Btn(sheet, "button", "secondary", Style.SNormal(), "secondary");
        Program.Btn(sheet, "button", "secondary error", hover, "secondary-error-hover");
        Program.Btn(sheet, "button", "text", Style.SSelected(), "text-selected");
        Program.Btn(sheet, "button", "text", Style.SSelected() + hover,
                    "text-selected-hover");
        Program.Btn(sheet, "button", "outline error", Style.SNormal(), "outline-error");
        Program.Btn(sheet, "button", "primary", Style.SDisabled(), "primary-disabled");
        // 浮动按钮借用整套 button 规则（Style.SheetAlias），同时保留
        // 自己的 `floatbutton` 声明——皮肤不必把按钮规则复制一遍。
        Program.Btn(sheet, "floatbutton", "primary", hover, "float-primary-hover");
    }

    /// 组合伪类（`:selected:hover`）与未知伪类：状态个数计入特异性，
    /// 因此“选中且悬停”的规则压过只写 `.cls:selected` 的规则；
    /// 引擎不认识的伪类永不匹配，而不是退化成无状态规则。
    static void MultiState() {
        string css = "row:selected { color: #111111; }\n"
            + "row.pinned:selected { color: #222222; }\n"
            + "row:selected:hover { color: #333333; }\n"
            + "row:nope { color: #444444; }\n"
            + "row:checked:disabled { color: #555555; }\n";
        StyleSheet sheet = Css.Parse(css);
        int sel = Style.SSelected();
        int selHover = Style.SSelected() + Style.SHover();
        Program.Btn(sheet, "row", "pinned", sel, "sel");
        Program.Btn(sheet, "row", "pinned", selHover, "sel-hover");
        Program.Btn(sheet, "row", "", Style.SChecked(), "checked-only");
        Program.Btn(sheet, "row", "", Style.SChecked() + Style.SDisabled(),
                    "checked-disabled");
    }

    /// 单个 tab 的三种条状变体：颜色全在 CSS 里，选中的卡片式
    /// tab 用 `border` 简写清掉基础规则的下划线。
    static void TabStates() {
        string css = "tab { background: transparent; color: var(--text-secondary); }\n"
            + "tab.card { background: var(--bg-secondary); }\n"
            + "tab:hover { background: var(--bg-hover); color: var(--text-primary); }\n"
            + "tab:selected { background: transparent; color: var(--primary);\n"
            + "               border-bottom: 2 var(--primary); }\n"
            + "tab.card:selected { background: var(--bg-primary); color: var(--primary);\n"
            + "                    border: 1 var(--border-primary); }\n"
            + "tab:disabled { color: var(--text-disabled); }\n";
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(Style.RootFromTheme(Theme.Light()), names, vals);
        StyleSheet sheet = Css.ParseWith(css, names, vals);
        Program.Btn(sheet, "tab", "", Style.SNormal(), "tab");
        Program.Btn(sheet, "tab", "", Style.SHover(), "tab-hover");
        Program.Btn(sheet, "tab", "", Style.SSelected(), "tab-selected");
        Program.Btn(sheet, "tab", "", Style.SSelected() + Style.SHover(),
                    "tab-selected-hover");
        Program.Btn(sheet, "tab", "card", Style.SNormal(), "card");
        Program.Btn(sheet, "tab", "card", Style.SSelected(), "card-selected");
        Program.Btn(sheet, "tab", "", Style.SDisabled(), "tab-disabled");
    }

    /// 表单族（A1 批）：镜像 base.css 的字段与内部-part 规则——字段的
    /// 状态边框与校验类、checkbox/radio/switch/slider 的 part×状态、
    /// 角色类只在 :checked 时重着色。控件代码只在两份已解析样式之间
    /// Blend，所以这里钉住的就是控件最终画出的颜色。
    static StyleSheet FormSheet() {
        string css = "input, textarea, select { background: var(--field-bg);\n"
            + "  color: var(--text-primary); border: 1 var(--border-primary);\n"
            + "  border-radius: 3; }\n"
            + "input:hover, textarea:hover, select:hover { border-color: var(--border-hover); }\n"
            + "input:focus, textarea:focus, select:focus { border-color: var(--border-focus); }\n"
            + "input:disabled, textarea:disabled, select:disabled {\n"
            + "  background: var(--bg-disabled); border-color: var(--border-secondary); }\n"
            + "input::placeholder, textarea::placeholder, select::placeholder {\n"
            + "  color: var(--text-tertiary); }\n"
            + "input::selection, textarea::selection { background: var(--selection-bg); }\n"
            + "input::caret, textarea::caret { background: var(--primary); width: 1; }\n"
            + "input::icon { color: var(--text-tertiary); }\n"
            + "input::icon:hover, input::icon:selected { color: var(--primary); }\n"
            + "input::error { color: var(--error); border: 2 var(--error); font-size: 12; }\n"
            + "textarea::scrollbar, select::scrollbar { width: 6; background: var(--scrollbar); }\n"
            + "textarea::scrollbar-track { background: var(--bg-tertiary); }\n"
            + "select::arrow { color: var(--text-secondary); }\n"
            + "select::clear { color: var(--text-tertiary); }\n"
            + "select::clear:hover { color: var(--error); }\n"
            + "select::checkmark { color: var(--primary); }\n"
            + "select::popup { background: var(--bg-secondary); border-radius: 3; }\n"
            + "input.success, input.success:hover, input.success:focus,\n"
            + "select.success, select.success:hover, select.success:focus {\n"
            + "  border-color: var(--success); }\n"
            + "input.error, input.error:hover, input.error:focus,\n"
            + "select.error, select.error:hover, select.error:focus {\n"
            + "  border-color: var(--error); }\n"
            + "checkbox { gap: 8; }\n"
            + "checkbox::box { width: 18; background: var(--bg-primary);\n"
            + "  border: 1 var(--border-primary); border-radius: 3; }\n"
            + "checkbox:hover::box { border-color: var(--primary-hover); }\n"
            + "checkbox:checked::box { background: var(--primary); border-color: var(--primary); }\n"
            + "checkbox:checked:hover::box { background: var(--primary-hover);\n"
            + "  border-color: var(--primary-hover); }\n"
            + "checkbox::check { color: var(--text-inverse); padding-left: 3; }\n"
            + "radio::indicator { width: 18; background: var(--bg-primary);\n"
            + "  border: 1 var(--border-primary); }\n"
            + "radio:hover::indicator { border-color: var(--primary-hover); }\n"
            + "radio:checked::indicator { border-color: var(--primary); border-width: 2; }\n"
            + "radio::dot { width: 8; background: var(--primary); }\n"
            + "switch::track { width: 40; height: 22;\n"
            + "  background: var(--text-tertiary); padding-left: 2; }\n"
            + "switch:hover::track { background: var(--text-secondary); }\n"
            + "switch:checked::track { background: var(--primary); }\n"
            + "switch:checked:hover::track { background: var(--primary-hover); }\n"
            + "switch::thumb { background: var(--text-inverse);\n"
            + "  box-shadow: 0 1 0 var(--shadow-color); }\n"
            + "slider::track { height: 4; background: var(--bg-tertiary); }\n"
            + "slider::fill { background: var(--primary); }\n"
            + "slider::thumb { width: 18; background: var(--bg-primary);\n"
            + "  border: 2 var(--border-primary); }\n"
            + "slider:hover::thumb, slider:active::thumb { border-color: var(--primary);\n"
            + "  box-shadow: 0 0 4 var(--bg-hover); }\n"
            + "checkbox.success:checked::box { background: var(--success);\n"
            + "  border-color: var(--success); }\n"
            + "checkbox.success:checked:hover::box { background: var(--success-hover);\n"
            + "  border-color: var(--success-hover); }\n"
            + "radio.error:checked::indicator { border-color: var(--error); }\n"
            + "radio.error::dot { background: var(--error); }\n"
            + "switch.warning:checked::track { background: var(--warning); }\n"
            + "slider.success::fill { background: var(--success); }\n"
            + "slider.success:hover::thumb { border-color: var(--success); }\n";
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(Style.RootFromTheme(Theme.Light()), names, vals);
        return Css.ParseWith(css, names, vals);
    }

    static void FormPart(StyleSheet sheet, string type, string part, string cls,
                         int state, string label) {
        StyleBox b = new StyleBox();
        Style.ApplyPartSheet(sheet, b, type, part, cls, "", state);
        Console.WriteLine($"form {label} bg {b.bg} fg {b.fg} border {b.borderW}/"
                          + $"{b.borderColor} size {b.width},{b.height} pad {b.padL}"
                          + $" shadow {b.shadow} blur {b.shadowBlur} dy {b.shadowDy}");
    }

    static void FormStates() {
        StyleSheet sheet = Program.FormSheet();
        int hover = Style.SHover();
        int chk = Style.SChecked();
        int chkHover = chk + hover;
        // 字段根：状态边框与校验类压过 :hover/:focus。
        Program.Btn(sheet, "input", "", Style.SNormal(), "input");
        Program.Btn(sheet, "input", "", hover, "input-hover");
        Program.Btn(sheet, "input", "", Style.SFocus(), "input-focus");
        Program.Btn(sheet, "input", "", Style.SDisabled(), "input-disabled");
        Program.Btn(sheet, "input", "error", Style.SFocus(), "input-error-focus");
        Program.Btn(sheet, "textarea", "", hover, "textarea-hover");
        Program.Btn(sheet, "select", "", Style.SFocus(), "select-focus");
        Program.Btn(sheet, "select", "success", hover, "select-success-hover");
        // 字段内部 part。
        Program.FormPart(sheet, "input", "placeholder", "", Style.SNormal(), "input::placeholder");
        Program.FormPart(sheet, "input", "selection", "", Style.SNormal(), "input::selection");
        Program.FormPart(sheet, "input", "caret", "", Style.SNormal(), "input::caret");
        Program.FormPart(sheet, "input", "icon", "", Style.SNormal(), "input::icon");
        Program.FormPart(sheet, "input", "icon", "", hover, "input::icon-hover");
        Program.FormPart(sheet, "input", "error", "", Style.SNormal(), "input::error");
        Program.FormPart(sheet, "textarea", "scrollbar", "", Style.SNormal(), "textarea::scrollbar");
        Program.FormPart(sheet, "textarea", "scrollbar-track", "", Style.SNormal(), "textarea::scrollbar-track");
        Program.FormPart(sheet, "select", "arrow", "", Style.SNormal(), "select::arrow");
        Program.FormPart(sheet, "select", "clear", "", hover, "select::clear-hover");
        Program.FormPart(sheet, "select", "checkmark", "", Style.SNormal(), "select::checkmark");
        Program.FormPart(sheet, "select", "popup", "", Style.SSelected(), "select::popup");
        // checkbox：off/on/hover 与角色类，控件按进度 Blend off↔on。
        Program.FormPart(sheet, "checkbox", "box", "", Style.SNormal(), "checkbox::box");
        Program.FormPart(sheet, "checkbox", "box", "", hover, "checkbox::box-hover");
        Program.FormPart(sheet, "checkbox", "box", "", chk, "checkbox::box-checked");
        Program.FormPart(sheet, "checkbox", "box", "", chkHover, "checkbox::box-checked-hover");
        Program.FormPart(sheet, "checkbox", "check", "", chk, "checkbox::check");
        Program.FormPart(sheet, "checkbox", "box", "success", chkHover, "checkbox.success::box-checked-hover");
        // radio / switch / slider 的 part×状态。
        Program.FormPart(sheet, "radio", "indicator", "", Style.SNormal(), "radio::indicator");
        Program.FormPart(sheet, "radio", "indicator", "", chk, "radio::indicator-checked");
        Program.FormPart(sheet, "radio", "dot", "", chk, "radio::dot");
        Program.FormPart(sheet, "radio", "indicator", "error", chk, "radio.error::indicator-checked");
        Program.FormPart(sheet, "radio", "dot", "error", chk, "radio.error::dot");
        Program.FormPart(sheet, "switch", "track", "", Style.SNormal(), "switch::track");
        Program.FormPart(sheet, "switch", "track", "", hover, "switch::track-hover");
        Program.FormPart(sheet, "switch", "track", "", chk, "switch::track-checked");
        Program.FormPart(sheet, "switch", "track", "", chkHover, "switch::track-checked-hover");
        Program.FormPart(sheet, "switch", "thumb", "", Style.SNormal(), "switch::thumb");
        Program.FormPart(sheet, "switch", "track", "warning", chk, "switch.warning::track-checked");
        Program.FormPart(sheet, "slider", "track", "", Style.SNormal(), "slider::track");
        Program.FormPart(sheet, "slider", "fill", "", Style.SNormal(), "slider::fill");
        Program.FormPart(sheet, "slider", "thumb", "", Style.SNormal(), "slider::thumb");
        Program.FormPart(sheet, "slider", "thumb", "", hover, "slider::thumb-hover");
        Program.FormPart(sheet, "slider", "thumb", "", Style.SActive(), "slider::thumb-active");
        Program.FormPart(sheet, "slider", "fill", "success", Style.SNormal(), "slider.success::fill");
        Program.FormPart(sheet, "slider", "thumb", "success", hover, "slider.success::thumb-hover");
        // 控件的实际用法：off/on 两份已解析样式按动画进度 Blend。
        StyleBox off = new StyleBox();
        Style.ApplyPartSheet(sheet, off, "switch", "track", "", "", Style.SNormal());
        StyleBox on = new StyleBox();
        Style.ApplyPartSheet(sheet, on, "switch", "track", "", "", chk);
        StyleBox mid = StyleBox.Blend(off, on, 500);
        Console.WriteLine($"form switch-track-mid bg {mid.bg}");
    }

    /// 列表导航族（A3 批）：镜像 base.css 的 part/state 规则，确保
    /// 颜色由样式层解析后再进入各控件的绘制路径。
    static StyleSheet A3Sheet() {
        string css = "tab::close { color: var(--text-tertiary); }\n"
            + "tab::close:hover { background: var(--bg-active); color: var(--error); }\n"
            + "tabs::add { color: var(--text-secondary); }\n"
            + "tabs::add:hover { background: var(--bg-hover); color: var(--primary); }\n"
            + "tabs::chevron { color: var(--text-secondary); }\n"
            + "tabs::chevron:hover { background: var(--bg-hover); color: var(--text-secondary); }\n"
            + "tabs::divider { background: var(--divider); }\n"
            + "tab::indicator { background: var(--primary); }\n"
            + "listview::empty { color: var(--text-secondary); }\n"
            + "listview::header { background: var(--bg-secondary); }\n"
            + "listview::divider { background: var(--divider); }\n"
            + "listview::column { color: var(--text-tertiary); }\n"
            + "listview::cell { color: var(--text-primary); }\n"
            + "listview::cell.secondary { color: var(--text-secondary); }\n"
            + "listrow:hover { background: var(--bg-secondary); }\n"
            + "listrow:selected { background: var(--bg-tertiary); }\n"
            + "treeview::arrow { color: var(--text-tertiary); }\n"
            + "treeview::count { color: var(--text-tertiary); }\n"
            + "treeview::icon { color: var(--text-secondary); }\n"
            + "treeview::icon.dir { color: var(--primary); }\n"
            + "treeview::label { color: var(--text-primary); }\n"
            + "treeview::label:selected { color: var(--primary); }\n"
            + "treeview::indicator { background: var(--primary); }\n"
            + "treeview::empty { color: var(--text-primary); }\n"
            + "treeview::row:hover { background: var(--bg-hover); }\n"
            + "treeview::row:selected { background: var(--bg-active); }\n"
            + "steps::marker { background: var(--border-primary); color: var(--text-primary); }\n"
            + "steps::marker.done, steps::marker.active { background: var(--primary); color: var(--text-inverse); }\n"
            + "steps::marker.error { background: var(--error); color: var(--text-inverse); }\n"
            + "steps::connector { background: var(--border-primary); }\n"
            + "steps::connector.done, steps::connector.active { background: var(--primary); }\n"
            + "steps::title { color: var(--text-secondary); }\n"
            + "steps::title.active { color: var(--text-primary); }\n"
            + "steps::title.error { color: var(--error); }\n"
            + "steps::description { color: var(--text-tertiary); }\n"
            + "pagination::item { background: var(--bg-primary); color: var(--text-primary); border: 1 var(--border-primary); }\n"
            + "pagination::item:selected { background: var(--primary); color: var(--text-inverse); border-color: var(--primary); }\n"
            + "pagination::item:hover { background: var(--bg-hover); border-color: var(--border-hover); }\n"
            + "pagination::arrow { color: var(--text-primary); }\n"
            + "pagination::arrow:disabled { color: var(--text-disabled); }\n"
            + "pagination::ellipsis { color: var(--text-tertiary); }\n"
            + "pagination::label { color: var(--text-secondary); }\n"
            + "breadcrumb::separator { color: var(--text-tertiary); }\n"
            + "breadcrumb::item { color: var(--text-secondary); }\n"
            + "breadcrumb::item.current { color: var(--text-primary); }\n"
            + "timeline::line { background: var(--divider); }\n"
            + "timeline::dot { background: var(--primary); }\n"
            + "timeline::dot.info { background: var(--info); }\n"
            + "timeline::dot.success { background: var(--success); }\n"
            + "timeline::dot.warning { background: var(--warning); }\n"
            + "timeline::dot.error { background: var(--error); }\n"
            + "timeline::time { color: var(--text-tertiary); }\n"
            + "timeline::title { color: var(--text-primary); }\n"
            + "timeline::desc { color: var(--text-secondary); }\n"
            + "pageheader::back { color: var(--text-secondary); }\n"
            + "pageheader::back:hover { color: var(--primary); }\n"
            + "pageheader::title { color: var(--text-primary); }\n"
            + "pageheader::subtitle { color: var(--text-tertiary); }\n"
            + "table::header { background: var(--bg-secondary); color: var(--text-secondary); }\n"
            + "table::divider { background: var(--divider); }\n"
            + "table::row.striped { background: var(--bg-secondary); }\n"
            + "table::cell { color: var(--text-primary); }\n"
            + "virtuallist { background: var(--bg-primary); border: 1 var(--border-secondary); }\n"
            + "virtuallist::divider { background: var(--divider); }\n"
            + "carousel::slide.primary { background: var(--primary); }\n"
            + "carousel::slide.info { background: var(--info); }\n"
            + "carousel::slide.warning { background: var(--warning); }\n"
            + "carousel::slide.error { background: var(--error); }\n"
            + "carousel::slide.success { background: var(--success); }\n"
            + "carousel::caption { color: var(--text-inverse); }\n"
            + "carousel::arrow { background: var(--overlay); color: var(--text-inverse); }\n"
            + "carousel::dot { background: var(--overlay); }\n"
            + "carousel::dot:selected { background: var(--text-inverse); }\n"
            + "sessionlist::filter { color: var(--text-tertiary); }\n"
            + "sessionlist::filter:hover, sessionlist::filter:selected { color: var(--text-secondary); }\n"
            + "sessionlist::divider { background: var(--divider); }\n"
            + "sessionlist::hint { color: var(--text-tertiary); }\n"
            + "sessionlist::title { color: var(--text-secondary); }\n"
            + "sessionlist::title.current { color: var(--text-primary); }\n"
            + "sessionlist::title.archived { color: var(--text-tertiary); }\n"
            + "sessionlist::action { color: var(--text-tertiary); }\n"
            + "sessionlist::action:hover { color: var(--text-primary); }\n"
            + "docktabs::activity { background: var(--divider); }\n"
            + "docktabs::activity.live { background: var(--primary); }\n"
            + "docktab::label { color: var(--text-tertiary); }\n"
            + "docktab::close { color: var(--text-tertiary); }\n"
            + "docktab::close:hover { background: var(--bg-active); color: var(--text-primary); }\n"
            + "dockgroup::drop { background: var(--drop-tint); border: 2 var(--primary); }\n"
            + "dockgroup::ghost { background: var(--bg-primary); border: 1 var(--primary); color: var(--text-primary); }\n";
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(Style.RootFromTheme(Theme.Light()), names, vals);
        return Css.ParseWith(css, names, vals);
    }

    static void A3States() {
        StyleSheet sheet = Program.A3Sheet();
        int hover = Style.SHover();
        int selected = Style.SSelected();
        int active = Style.SSelected() + Style.SHover();
        int disabled = Style.SDisabled();
        Program.FormPart(sheet, "tab", "close", "", Style.SNormal(), "tab::close");
        Program.FormPart(sheet, "tab", "close", "", hover, "tab::close-hover");
        Program.FormPart(sheet, "tabs", "add", "", hover, "tabs::add-hover");
        Program.FormPart(sheet, "tabs", "chevron", "", Style.SNormal(), "tabs::chevron");
        Program.FormPart(sheet, "tabs", "divider", "", Style.SNormal(), "tabs::divider");
        Program.FormPart(sheet, "tab", "indicator", "", Style.SNormal(), "tab::indicator");
        Program.FormPart(sheet, "listview", "empty", "", Style.SNormal(), "listview::empty");
        Program.FormPart(sheet, "listview", "header", "", Style.SNormal(), "listview::header");
        Program.FormPart(sheet, "listview", "divider", "", Style.SNormal(), "listview::divider");
        Program.FormPart(sheet, "listview", "column", "", Style.SNormal(), "listview::column");
        Program.FormPart(sheet, "listview", "cell", "", Style.SNormal(), "listview::cell");
        Program.FormPart(sheet, "listview", "cell", "secondary", Style.SNormal(), "listview::cell-secondary");
        Program.Btn(sheet, "listrow", "", hover, "listrow-hover");
        Program.Btn(sheet, "listrow", "", selected, "listrow-selected");
        Program.FormPart(sheet, "treeview", "arrow", "", Style.SNormal(), "treeview::arrow");
        Program.FormPart(sheet, "treeview", "icon", "", Style.SNormal(), "treeview::icon");
        Program.FormPart(sheet, "treeview", "icon", "dir", Style.SNormal(), "treeview::icon-dir");
        Program.FormPart(sheet, "treeview", "label", "", selected, "treeview::label-selected");
        Program.FormPart(sheet, "treeview", "indicator", "", Style.SNormal(), "treeview::indicator");
        Program.Btn(sheet, "treeview", "", hover, "treeview-row-hover");
        Program.Btn(sheet, "treeview", "", selected, "treeview-row-selected");
        Program.FormPart(sheet, "steps", "marker", "done", Style.SNormal(), "steps::marker-done");
        Program.FormPart(sheet, "steps", "marker", "active", Style.SNormal(), "steps::marker-active");
        Program.FormPart(sheet, "steps", "marker", "error", Style.SNormal(), "steps::marker-error");
        Program.FormPart(sheet, "steps", "connector", "done", Style.SNormal(), "steps::connector-done");
        Program.FormPart(sheet, "steps", "title", "active", Style.SNormal(), "steps::title-active");
        Program.FormPart(sheet, "steps", "description", "", Style.SNormal(), "steps::description");
        Program.FormPart(sheet, "pagination", "item", "", Style.SNormal(), "pagination::item");
        Program.FormPart(sheet, "pagination", "item", "", selected, "pagination::item-selected");
        Program.FormPart(sheet, "pagination", "item", "", hover, "pagination::item-hover");
        Program.FormPart(sheet, "pagination", "arrow", "", Style.SNormal(), "pagination::arrow");
        Program.FormPart(sheet, "pagination", "arrow", "", disabled, "pagination::arrow-disabled");
        Program.FormPart(sheet, "pagination", "ellipsis", "", Style.SNormal(), "pagination::ellipsis");
        Program.FormPart(sheet, "pagination", "label", "", Style.SNormal(), "pagination::label");
        Program.FormPart(sheet, "breadcrumb", "separator", "", Style.SNormal(), "breadcrumb::separator");
        Program.FormPart(sheet, "breadcrumb", "item", "", Style.SNormal(), "breadcrumb::item");
        Program.FormPart(sheet, "breadcrumb", "item", "current", Style.SNormal(), "breadcrumb::item-current");
        Program.FormPart(sheet, "timeline", "line", "", Style.SNormal(), "timeline::line");
        Program.FormPart(sheet, "timeline", "dot", "info", Style.SNormal(), "timeline::dot-info");
        Program.FormPart(sheet, "timeline", "title", "", Style.SNormal(), "timeline::title");
        Program.FormPart(sheet, "timeline", "desc", "", Style.SNormal(), "timeline::desc");
        Program.FormPart(sheet, "pageheader", "back", "", hover, "pageheader::back-hover");
        Program.FormPart(sheet, "pageheader", "title", "", Style.SNormal(), "pageheader::title");
        Program.FormPart(sheet, "pageheader", "subtitle", "", Style.SNormal(), "pageheader::subtitle");
        Program.FormPart(sheet, "table", "header", "", Style.SNormal(), "table::header");
        Program.FormPart(sheet, "table", "divider", "", Style.SNormal(), "table::divider");
        Program.FormPart(sheet, "table", "row", "striped", Style.SNormal(), "table::row-striped");
        Program.FormPart(sheet, "table", "cell", "", Style.SNormal(), "table::cell");
        Program.Btn(sheet, "virtuallist", "", Style.SNormal(), "virtuallist");
        Program.FormPart(sheet, "virtuallist", "divider", "", Style.SNormal(), "virtuallist::divider");
        Program.FormPart(sheet, "carousel", "slide", "primary", Style.SNormal(), "carousel::slide-primary");
        Program.FormPart(sheet, "carousel", "slide", "success", Style.SNormal(), "carousel::slide-success");
        Program.FormPart(sheet, "carousel", "caption", "", Style.SNormal(), "carousel::caption");
        Program.FormPart(sheet, "carousel", "arrow", "", Style.SNormal(), "carousel::arrow");
        Program.FormPart(sheet, "carousel", "dot", "", Style.SNormal(), "carousel::dot");
        Program.FormPart(sheet, "carousel", "dot", "", selected, "carousel::dot-selected");
        Program.FormPart(sheet, "sessionlist", "filter", "", Style.SNormal(), "sessionlist::filter");
        Program.FormPart(sheet, "sessionlist", "filter", "", hover, "sessionlist::filter-hover");
        Program.FormPart(sheet, "sessionlist", "divider", "", Style.SNormal(), "sessionlist::divider");
        Program.FormPart(sheet, "sessionlist", "hint", "", Style.SNormal(), "sessionlist::hint");
        Program.FormPart(sheet, "sessionlist", "title", "current", Style.SNormal(), "sessionlist::title-current");
        Program.FormPart(sheet, "sessionlist", "title", "archived", Style.SNormal(), "sessionlist::title-archived");
        Program.FormPart(sheet, "sessionlist", "action", "", Style.SNormal(), "sessionlist::action");
        Program.FormPart(sheet, "sessionlist", "action", "", hover, "sessionlist::action-hover");
        Program.FormPart(sheet, "docktabs", "activity", "live", Style.SNormal(), "docktabs::activity-live");
        Program.FormPart(sheet, "docktab", "label", "", Style.SNormal(), "docktab::label");
        Program.FormPart(sheet, "docktab", "close", "", hover, "docktab::close-hover");
        Program.FormPart(sheet, "dockgroup", "drop", "", Style.SNormal(), "dockgroup::drop");
        Program.FormPart(sheet, "dockgroup", "ghost", "", Style.SNormal(), "dockgroup::ghost");
        Program.FormPart(sheet, "docktab", "close", "", active, "docktab::close-selected-hover");
    }

    /// 数据密集族（A4 批）：镜像 base.css 的 datatable/chart/designer/hmi
    /// 规则。四族的绘制路径只消费解析后的 StyleBox；条件格式、
    /// 系列色等数据语义色不经过样式层，也不在此钉住。
    static StyleSheet A4Sheet() {
        string css = "datatable { background: var(--bg-primary); color: var(--text-primary); border: 1 var(--border-primary); }\n"
            + "datatable::grid { background: var(--divider); }\n"
            + "datatable::header { background: var(--bg-secondary); color: var(--text-secondary); }\n"
            + "datatable::header:hover { background: var(--bg-hover); }\n"
            + "datatable::icon { color: var(--text-tertiary); }\n"
            + "datatable::icon:selected { color: var(--primary); }\n"
            + "datatable::band { background: var(--bg-tertiary); color: var(--text-secondary); }\n"
            + "datatable::accent { background: var(--primary); color: var(--text-inverse); }\n"
            + "datatable::row { background: var(--bg-primary); color: var(--text-primary); }\n"
            + "datatable::row.striped { background: var(--bg-secondary); }\n"
            + "datatable::row:hover { background: var(--bg-hover); }\n"
            + "datatable::row:selected { background: var(--bg-active); }\n"
            + "datatable::cell { color: var(--text-primary); }\n"
            + "datatable::cell.link { color: var(--primary); }\n"
            + "datatable::mark { color: var(--success); }\n"
            + "datatable::mark.off { color: var(--text-tertiary); }\n"
            + "datatable::progress-track { background: var(--bg-tertiary); }\n"
            + "datatable::progress-fill { background: var(--primary); }\n"
            + "datatable::progress-fill.done { background: var(--success); }\n"
            + "datatable::tag { background: var(--bg-secondary); color: var(--text-secondary); }\n"
            + "datatable::tag.error { color: var(--error); }\n"
            + "datatable::field { background: var(--bg-primary); color: var(--text-primary); border: 1 var(--border-primary); }\n"
            + "datatable::field:focus { border-color: var(--border-focus); }\n"
            + "datatable::editor { background: var(--bg-primary); color: var(--text-primary); border: 2 var(--border-focus); }\n"
            + "datatable::editor.invalid { border-color: var(--error); color: var(--error); }\n"
            + "datatable::button.primary { background: var(--primary); color: var(--text-inverse); }\n"
            + "datatable::button.primary:hover { background: var(--primary-hover); }\n"
            + "chart { background: var(--bg-primary); color: var(--text-primary); border: 1 var(--border-primary); }\n"
            + "chart::legend { color: var(--text-secondary); }\n"
            + "chart::legend.off { color: var(--text-tertiary); background: var(--border-primary); }\n"
            + "chart::grid { background: var(--divider); }\n"
            + "chart::axis { background: var(--border-secondary); color: var(--text-tertiary); }\n"
            + "chart::mark { background: var(--error); color: var(--error); }\n"
            + "chart::zoom-window { background: var(--bg-hover); border: 1 var(--border-hover); }\n"
            + "chart::zoom-handle { background: var(--primary); }\n"
            + "chart::label { color: var(--text-secondary); }\n"
            + "chart::value { color: var(--text-primary); }\n"
            + "chart::tooltip { background: var(--bg-secondary); color: var(--text-secondary); border: 1 var(--border-primary); }\n"
            + "chart::accent { background: var(--primary); }\n"
            + "chart::cell { background: var(--bg-tertiary); border: 1 var(--text-primary); }\n"
            + "designer { background: var(--bg-primary); color: var(--text-primary); border: 1 var(--border-primary); }\n"
            + "designer::accent { background: var(--primary); color: var(--primary); }\n"
            + "designer::accent.warning { background: var(--warning); color: var(--warning); }\n"
            + "designer::badge { color: var(--text-inverse); }\n"
            + "designer::field { background: var(--bg-secondary); color: var(--text-secondary); border: 1 var(--border-secondary); }\n"
            + "designer::panel { background: var(--bg-secondary); color: var(--text-tertiary); border: 1 var(--border-primary); }\n"
            + "designer::panel:active { background: var(--bg-active); }\n"
            + "designer::shadow { background: var(--shadow-color); }\n"
            + "hmi::status { background: var(--info); color: var(--info); }\n"
            + "hmi::status.ok { background: var(--success); color: var(--success); }\n"
            + "hmi::status.warning { background: var(--warning); color: var(--warning); }\n"
            + "hmi::status.error { background: var(--error); color: var(--error); }\n"
            + "hmi::status.off { background: var(--border-secondary); color: var(--text-disabled); }\n"
            + "hmi::accent { background: var(--primary); color: var(--text-inverse); }\n"
            + "hmi::panel { background: var(--bg-secondary); border: 1 var(--border-primary); }\n"
            + "hmi::track { background: var(--bg-tertiary); }\n"
            + "hmi::track:hover { background: var(--bg-hover); }\n";
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(Style.RootFromTheme(Theme.Light()), names, vals);
        return Css.ParseWith(css, names, vals);
    }

    static void A4States() {
        StyleSheet sheet = Program.A4Sheet();
        int hover = Style.SHover();
        int selected = Style.SSelected();
        int focus = Style.SFocus();
        // 数据网格：表面/表头/行状态/单元格/标记/进度/标签/编辑器。
        Program.Btn(sheet, "datatable", "", Style.SNormal(), "datatable");
        Program.FormPart(sheet, "datatable", "grid", "", Style.SNormal(), "datatable::grid");
        Program.FormPart(sheet, "datatable", "header", "", Style.SNormal(), "datatable::header");
        Program.FormPart(sheet, "datatable", "header", "", hover, "datatable::header-hover");
        Program.FormPart(sheet, "datatable", "icon", "", selected, "datatable::icon-selected");
        Program.FormPart(sheet, "datatable", "accent", "", Style.SNormal(), "datatable::accent");
        Program.FormPart(sheet, "datatable", "row", "striped", Style.SNormal(), "datatable::row-striped");
        Program.FormPart(sheet, "datatable", "row", "", hover, "datatable::row-hover");
        Program.FormPart(sheet, "datatable", "row", "", selected, "datatable::row-selected");
        Program.FormPart(sheet, "datatable", "cell", "link", Style.SNormal(), "datatable::cell-link");
        Program.FormPart(sheet, "datatable", "mark", "", Style.SNormal(), "datatable::mark");
        Program.FormPart(sheet, "datatable", "mark", "off", Style.SNormal(), "datatable::mark-off");
        Program.FormPart(sheet, "datatable", "progress-fill", "done", Style.SNormal(), "datatable::progress-fill-done");
        Program.FormPart(sheet, "datatable", "tag", "error", Style.SNormal(), "datatable::tag-error");
        Program.FormPart(sheet, "datatable", "field", "", focus, "datatable::field-focus");
        Program.FormPart(sheet, "datatable", "editor", "invalid", Style.SNormal(), "datatable::editor-invalid");
        Program.FormPart(sheet, "datatable", "button", "primary", hover, "datatable::button-primary-hover");
        // 图表共享框架：面板/图例/轴网/标记/缩放/标签/单元格底色。
        Program.Btn(sheet, "chart", "", Style.SNormal(), "chart");
        Program.FormPart(sheet, "chart", "legend", "", Style.SNormal(), "chart::legend");
        Program.FormPart(sheet, "chart", "legend", "off", Style.SNormal(), "chart::legend-off");
        Program.FormPart(sheet, "chart", "grid", "", Style.SNormal(), "chart::grid");
        Program.FormPart(sheet, "chart", "axis", "", Style.SNormal(), "chart::axis");
        Program.FormPart(sheet, "chart", "mark", "", Style.SNormal(), "chart::mark");
        Program.FormPart(sheet, "chart", "zoom-window", "", Style.SNormal(), "chart::zoom-window");
        Program.FormPart(sheet, "chart", "zoom-handle", "", Style.SNormal(), "chart::zoom-handle");
        Program.FormPart(sheet, "chart", "label", "", Style.SNormal(), "chart::label");
        Program.FormPart(sheet, "chart", "value", "", Style.SNormal(), "chart::value");
        Program.FormPart(sheet, "chart", "tooltip", "", Style.SNormal(), "chart::tooltip");
        Program.FormPart(sheet, "chart", "accent", "", Style.SNormal(), "chart::accent");
        Program.FormPart(sheet, "chart", "cell", "", Style.SNormal(), "chart::cell");
        // 表单设计器：画布面/强调/拖拽浮标/属性面板。
        Program.Btn(sheet, "designer", "", Style.SNormal(), "designer");
        Program.FormPart(sheet, "designer", "accent", "", Style.SNormal(), "designer::accent");
        Program.FormPart(sheet, "designer", "accent", "warning", Style.SNormal(), "designer::accent-warning");
        Program.FormPart(sheet, "designer", "badge", "", Style.SNormal(), "designer::badge");
        Program.FormPart(sheet, "designer", "field", "", Style.SNormal(), "designer::field");
        Program.FormPart(sheet, "designer", "panel", "", Style.SNormal(), "designer::panel");
        Program.FormPart(sheet, "designer", "panel", "", Style.SActive(), "designer::panel-active");
        Program.FormPart(sheet, "designer", "shadow", "", Style.SNormal(), "designer::shadow");
        // HMI：状态色族/主色面/面板/滑轨。
        Program.FormPart(sheet, "hmi", "status", "", Style.SNormal(), "hmi::status");
        Program.FormPart(sheet, "hmi", "status", "ok", Style.SNormal(), "hmi::status-ok");
        Program.FormPart(sheet, "hmi", "status", "warning", Style.SNormal(), "hmi::status-warning");
        Program.FormPart(sheet, "hmi", "status", "error", Style.SNormal(), "hmi::status-error");
        Program.FormPart(sheet, "hmi", "status", "off", Style.SNormal(), "hmi::status-off");
        Program.FormPart(sheet, "hmi", "accent", "", Style.SNormal(), "hmi::accent");
        Program.FormPart(sheet, "hmi", "panel", "", Style.SNormal(), "hmi::panel");
        Program.FormPart(sheet, "hmi", "track", "", Style.SNormal(), "hmi::track");
        Program.FormPart(sheet, "hmi", "track", "", hover, "hmi::track-hover");
    }

    /// A5 第一阶段：弹窗、富菜单与外观抽屉的 part/state 规则。
    static StyleSheet A5Sheet() {
        string css = "popup::scrollbar { background: var(--scrollbar); width: 5; }\n"
            + "contextmenu::header { color: var(--text-tertiary); }\n"
            + "contextmenu::item:disabled { color: var(--text-tertiary); }\n"
            + "themedrawer { background: var(--surface-bg); border-left: 1 var(--border-primary); }\n"
            + "themedrawer::edge { background: var(--drawer-edge-shadow); }\n"
            + "themedrawer::title { color: var(--text-primary); }\n"
            + "themedrawer::close { background: var(--bg-secondary); color: var(--text-secondary); }\n"
            + "themedrawer::close:hover { background: var(--bg-hover); color: var(--text-secondary); }\n"
            + "themedrawer::seg { background: var(--bg-secondary); }\n"
            + "themedrawer::seg-item { color: var(--text-secondary); }\n"
            + "themedrawer::seg-item:hover { background: var(--bg-hover); }\n"
            + "themedrawer::seg-item:selected { background: var(--bg-primary); border: 1 var(--primary); color: var(--primary); }\n"
            + "themedrawer::section { color: var(--text-secondary); }\n"
            + "themedrawer::rule { background: var(--border-primary); }\n"
            + "themedrawer::card { background: transparent; border: 1 var(--border-primary); }\n"
            + "themedrawer::card:hover { border-color: var(--border-hover); }\n"
            + "themedrawer::card:selected { border: 2 var(--primary); }\n"
            + "themedrawer::swatch { border: 1 var(--border-primary); }\n"
            + "themedrawer::swatch:hover { border-color: var(--border-hover); }\n"
            + "themedrawer::swatch:selected { border: 2 var(--primary); }\n"
            + "themedrawer::swatch.default { background: var(--bg-active); }\n"
            + "themedrawer::swatch-check { color: var(--text-inverse); }\n"
            + "themedrawer::chip { background: var(--bg-secondary); border: 1 var(--border-primary); color: var(--text-secondary); }\n"
            + "themedrawer::chip:hover { background: var(--bg-hover); border-color: var(--border-hover); }\n"
            + "themedrawer::chip:selected { background: var(--bg-active); border-color: var(--primary); color: var(--primary); }\n"
            + "themedrawer::chip:disabled { color: var(--text-disabled); }\n"
            + "themedrawer::field { background: var(--bg-secondary); border: 1 var(--border-primary); color: var(--text-secondary); }\n"
            + "themedrawer::field:hover { background: var(--bg-hover); }\n"
            + "themedrawer::field:disabled { color: var(--text-disabled); }\n"
            + "themedrawer::row:hover { background: var(--bg-hover); }\n"
            + "themedrawer::label { color: var(--text-primary); }\n"
            + "themedrawer::radio { border: 1 var(--border-secondary); }\n"
            + "themedrawer::radio:selected { background: var(--primary); border: 2 var(--primary); color: var(--primary); }\n"
            + "themedrawer::switch { background: var(--bg-active); }\n"
            + "themedrawer::switch:selected { background: var(--primary); }\n"
            + "themedrawer::knob { background: var(--on-scrim); }\n"
            + "titlebar { background: var(--bg-secondary); color: var(--text-primary); border-bottom: 1 var(--divider); }\n"
            + "titlebar::brand { color: var(--primary); }\n"
            + "titlebar::title { color: var(--text-primary); }\n"
            + "titlebar::glass { background: var(--titlebar-glass-bg); }\n"
            + "titlebar::sheen { background: var(--titlebar-glass-sheen); }\n"
            + "titlebar::cap { color: var(--text-secondary); }\n"
            + "titlebar::cap:hover { background: var(--bg-hover); color: var(--text-primary); }\n"
            + "titlebar::cap:selected { color: var(--primary); }\n"
            + "titlebar::cap.danger:hover { background: var(--caption-close-bg); color: var(--on-scrim); }\n"
            + "titlebar::edge { background: var(--window-glass-edge); }\n"
            + "titlebar::edge-highlight { background: var(--window-glass-highlight); }\n"
            + "windowbg { background: var(--bg-primary); }\n"
            + "windowbg::sheen { background: var(--window-glass-sheen); }\n"
            + "windowbg::glass { background: var(--window-glass-clear); }\n"
            + "windowbg::fade { background: var(--bg-primary); }\n"
            + "windowbg::blob { background: var(--primary); }\n"
            + "windowbg::blob.info { background: var(--info); }\n"
            + "windowbg::blob.success { background: var(--success); }\n"
            + "windowbg::blob.error { background: var(--error); }\n"
            + "windowbg::shape { background: var(--bg-primary); }\n"
            + "windowbg::shape-shadow { background: var(--shadow-color); }\n";
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(Style.RootFromTheme(Theme.Light()), names, vals);
        return Css.ParseWith(css, names, vals);
    }

    static void A5States() {
        StyleSheet sheet = Program.A5Sheet();
        int hover = Style.SHover();
        int selected = Style.SSelected();
        int disabled = Style.SDisabled();
        Program.FormPart(sheet, "popup", "scrollbar", "", Style.SNormal(),
                          "popup::scrollbar");
        Program.FormPart(sheet, "contextmenu", "header", "", Style.SNormal(),
                          "contextmenu::header");
        Program.FormPart(sheet, "contextmenu", "item", "", disabled,
                          "contextmenu::item-disabled");
        Program.Btn(sheet, "themedrawer", "", Style.SNormal(), "themedrawer");
        Program.FormPart(sheet, "themedrawer", "edge", "", Style.SNormal(),
                         "themedrawer::edge");
        Program.FormPart(sheet, "themedrawer", "title", "", Style.SNormal(),
                         "themedrawer::title");
        Program.FormPart(sheet, "themedrawer", "close", "", Style.SNormal(),
                         "themedrawer::close");
        Program.FormPart(sheet, "themedrawer", "close", "", hover,
                         "themedrawer::close-hover");
        Program.FormPart(sheet, "themedrawer", "seg", "", Style.SNormal(),
                         "themedrawer::seg");
        Program.FormPart(sheet, "themedrawer", "seg-item", "", Style.SNormal(),
                         "themedrawer::seg-item");
        Program.FormPart(sheet, "themedrawer", "seg-item", "", hover,
                         "themedrawer::seg-item-hover");
        Program.FormPart(sheet, "themedrawer", "seg-item", "", selected,
                         "themedrawer::seg-item-selected");
        Program.FormPart(sheet, "themedrawer", "section", "", Style.SNormal(),
                         "themedrawer::section");
        Program.FormPart(sheet, "themedrawer", "rule", "", Style.SNormal(),
                         "themedrawer::rule");
        Program.FormPart(sheet, "themedrawer", "card", "", Style.SNormal(),
                         "themedrawer::card");
        Program.FormPart(sheet, "themedrawer", "card", "", hover,
                         "themedrawer::card-hover");
        Program.FormPart(sheet, "themedrawer", "card", "", selected,
                         "themedrawer::card-selected");
        Program.FormPart(sheet, "themedrawer", "swatch", "", Style.SNormal(),
                         "themedrawer::swatch");
        Program.FormPart(sheet, "themedrawer", "swatch", "", hover,
                         "themedrawer::swatch-hover");
        Program.FormPart(sheet, "themedrawer", "swatch", "", selected,
                         "themedrawer::swatch-selected");
        Program.FormPart(sheet, "themedrawer", "swatch", "default",
                         Style.SNormal(), "themedrawer::swatch-default");
        Program.FormPart(sheet, "themedrawer", "swatch-check", "",
                         Style.SNormal(), "themedrawer::swatch-check");
        Program.FormPart(sheet, "themedrawer", "chip", "", Style.SNormal(),
                         "themedrawer::chip");
        Program.FormPart(sheet, "themedrawer", "chip", "", hover,
                         "themedrawer::chip-hover");
        Program.FormPart(sheet, "themedrawer", "chip", "", selected,
                         "themedrawer::chip-selected");
        Program.FormPart(sheet, "themedrawer", "chip", "", disabled,
                         "themedrawer::chip-disabled");
        Program.FormPart(sheet, "themedrawer", "field", "", Style.SNormal(),
                         "themedrawer::field");
        Program.FormPart(sheet, "themedrawer", "field", "", hover,
                         "themedrawer::field-hover");
        Program.FormPart(sheet, "themedrawer", "field", "", disabled,
                         "themedrawer::field-disabled");
        Program.FormPart(sheet, "themedrawer", "row", "", hover,
                         "themedrawer::row-hover");
        Program.FormPart(sheet, "themedrawer", "label", "", Style.SNormal(),
                         "themedrawer::label");
        Program.FormPart(sheet, "themedrawer", "radio", "", Style.SNormal(),
                         "themedrawer::radio");
        Program.FormPart(sheet, "themedrawer", "radio", "", selected,
                         "themedrawer::radio-selected");
        Program.FormPart(sheet, "themedrawer", "switch", "", Style.SNormal(),
                         "themedrawer::switch");
        Program.FormPart(sheet, "themedrawer", "switch", "", selected,
                         "themedrawer::switch-selected");
        Program.FormPart(sheet, "themedrawer", "knob", "", Style.SNormal(),
                         "themedrawer::knob");
        Program.Btn(sheet, "titlebar", "", Style.SNormal(), "titlebar");
        Program.FormPart(sheet, "titlebar", "brand", "", Style.SNormal(),
                         "titlebar::brand");
        Program.FormPart(sheet, "titlebar", "title", "", Style.SNormal(),
                         "titlebar::title");
        Program.FormPart(sheet, "titlebar", "glass", "", Style.SNormal(),
                         "titlebar::glass");
        Program.FormPart(sheet, "titlebar", "sheen", "", Style.SNormal(),
                         "titlebar::sheen");
        Program.FormPart(sheet, "titlebar", "cap", "", Style.SNormal(),
                         "titlebar::cap");
        Program.FormPart(sheet, "titlebar", "cap", "", hover,
                         "titlebar::cap-hover");
        Program.FormPart(sheet, "titlebar", "cap", "", selected,
                         "titlebar::cap-selected");
        Program.FormPart(sheet, "titlebar", "cap", "danger", hover,
                         "titlebar::cap-danger-hover");
        Program.FormPart(sheet, "titlebar", "edge", "", Style.SNormal(),
                         "titlebar::edge");
        Program.FormPart(sheet, "titlebar", "edge-highlight", "",
                         Style.SNormal(), "titlebar::edge-highlight");
        Program.Btn(sheet, "windowbg", "", Style.SNormal(), "windowbg");
        Program.FormPart(sheet, "windowbg", "sheen", "", Style.SNormal(),
                         "windowbg::sheen");
        Program.FormPart(sheet, "windowbg", "glass", "", Style.SNormal(),
                         "windowbg::glass");
        Program.FormPart(sheet, "windowbg", "fade", "", Style.SNormal(),
                         "windowbg::fade");
        Program.FormPart(sheet, "windowbg", "blob", "", Style.SNormal(),
                         "windowbg::blob");
        Program.FormPart(sheet, "windowbg", "blob", "info", Style.SNormal(),
                         "windowbg::blob-info");
        Program.FormPart(sheet, "windowbg", "blob", "success",
                         Style.SNormal(), "windowbg::blob-success");
        Program.FormPart(sheet, "windowbg", "blob", "error", Style.SNormal(),
                         "windowbg::blob-error");
        Program.FormPart(sheet, "windowbg", "shape", "", Style.SNormal(),
                         "windowbg::shape");
        Program.FormPart(sheet, "windowbg", "shape-shadow", "",
                         Style.SNormal(), "windowbg::shape-shadow");
    }

    /// A6-1：小量控件绘制路径的语义颜色规则。
    static StyleSheet A6Sheet() {
        string css = "buttongroup { color: var(--text-primary); border-color: var(--border-primary); }\n"
            + "ellipsis { color: var(--text-primary); }\n"
            + "empty::icon { color: var(--text-tertiary); }\n"
            + "empty::label { color: var(--text-tertiary); }\n"
            + "floatbutton { background: var(--bg-primary); color: var(--text-primary); border: 1 var(--border-primary); box-shadow: 1 2 0 var(--shadow-color); }\n"
            + "rate::star { color: var(--warning); }\n"
            + "rate::empty { color: var(--border-primary); }\n"
            + "statistic::label { color: var(--text-secondary); }\n"
            + "statistic::value { color: var(--text-primary); }\n"
            + "result::icon { background: var(--bg-secondary); color: var(--info); }\n"
            + "result.success::icon { color: var(--success); }\n"
            + "result.warning::icon { color: var(--warning); }\n"
            + "result.error::icon { color: var(--error); }\n"
            + "result::title { color: var(--text-primary); }\n"
            + "result::desc { color: var(--text-secondary); }\n"
            + "codeblock { border: 1 var(--border-primary); }\n"
            + "codeblock::caption { color: var(--text-primary); }\n"
            + "statusbar { background: var(--bg-secondary); color: var(--text-primary); }\n"
            + "toolstrip { background: var(--bg-secondary); color: var(--text-primary); }\n"
            + "graphview::edge { background: var(--divider); }\n"
            + "graphview::edge.done { background: var(--primary); }\n"
            + "graphview::empty { color: var(--text-tertiary); }\n"
            + "logview::text { color: var(--text-primary); }\n"
            + "logview::empty { color: var(--text-disabled); }\n"
            + "layer::load-label { color: var(--text-secondary); }\n"
            + "cef::placeholder { background: var(--bg-secondary); border: 1 var(--border-primary); }\n"
            + "cef::icon { color: var(--text-tertiary); }\n"
            + "cef::label { color: var(--text-secondary); }\n"
            + "webview::placeholder { background: var(--bg-secondary); border: 1 var(--border-primary); }\n"
            + "webview::icon { color: var(--text-tertiary); }\n"
            + "webview::label { color: var(--text-secondary); }\n"
            + "panel::plain { background: var(--bg-secondary); }\n"
            + "state::surface { background: var(--bg-primary); }\n"
            + "state:disabled::surface { background: var(--bg-disabled); }\n"
            + "state:hover::surface { background: var(--bg-hover); }\n"
            + "state:active::surface, state:selected::surface { background: var(--bg-active); }\n"
            + "state::label { color: var(--text-primary); }\n"
            + "state:disabled::label { color: var(--text-disabled); }\n"
            + "state:selected::label { color: var(--primary); }\n"
            + "state::caption { color: var(--text-secondary); }\n"
            + "state:disabled::caption { color: var(--text-disabled); }\n"
            + "state:hover::caption { color: var(--text-primary); }\n"
            + "state:selected::caption { color: var(--primary); }\n"
            + "state::border { border: 1 var(--border-primary); }\n"
            + "state:disabled::border { border-color: var(--border-secondary); }\n"
            + "state:focus::border { border-color: var(--border-focus); }\n"
            + "state:hover::border, state:active::border { border-color: var(--border-hover); }\n"
            + "state::accent { background: var(--primary); }\n"
            + "progress::track { background: var(--bg-tertiary); }\n"
            + "progress::fill { background: var(--primary); }\n"
            + "progress.info::fill { background: var(--info); }\n"
            + "progress.success::fill { background: var(--success); }\n"
            + "progress.warning::fill { background: var(--warning); }\n"
            + "progress.error::fill { background: var(--error); }\n"
            + "progress::label { color: var(--text-secondary); }\n"
            + "progress::ring-label { color: var(--text-primary); }\n"
            + "spin::accent { color: var(--primary); }\n"
            + "spin.info::accent { color: var(--info); }\n"
            + "spin.success::accent { color: var(--success); }\n"
            + "spin.warning::accent { color: var(--warning); }\n"
            + "spin.error::accent { color: var(--error); }\n"
            + "spin::rest { background: var(--bg-tertiary); }\n"
            + "spin::tail { color: var(--text-tertiary); }\n"
            + "spin::ring { color: var(--border-primary); }\n"
            + "styledtext::selection { background: var(--styledtext-selection); }\n"
            + "styledtext::code { background: var(--styledtext-code-bg); }\n"
            + "styledtext::time { color: var(--text-tertiary); }\n"
            + "styledtext::separator { background: var(--border-secondary); }\n"
            + "styledtext::rail.success { background: var(--styledtext-rail-success); }\n"
            + "styledtext::tint.success { background: var(--styledtext-tint-success); }\n"
            + "styledtext::rail.primary { background: var(--styledtext-rail-primary); }\n"
            + "styledtext::tint.primary { background: var(--styledtext-tint-primary); }\n"
            + "styledtext::rail.tool { background: var(--styledtext-rail-tool); }\n"
            + "styledtext::tint.tool { background: var(--styledtext-tint-tool); }\n"
            + "styledtext::rail.error { background: var(--styledtext-rail-error); }\n"
            + "styledtext::tint.error { background: var(--styledtext-tint-error); }\n"
            + "effects::star { color: var(--text-primary); }\n"
            + "effects::particle.a { color: var(--primary); }\n"
            + "effects::particle.b { color: var(--error); }\n"
            + "effects::particle.c { color: var(--info); }\n"
            + "effects::bokeh.a { color: var(--primary); }\n"
            + "effects::bokeh.b { color: var(--warning); }\n"
            + "effects::bokeh.c { color: var(--error); }\n"
            + "effects::mesh.a { color: var(--primary); }\n"
            + "effects::mesh.b { color: var(--info); }\n"
            + "propertygrid::step { background: var(--bg-tertiary); border: 1 var(--border-primary); color: var(--text-primary); }\n"
            + "propertygrid::step:hover { background: var(--bg-hover); }\n"
            + "propertygrid::empty { color: var(--text-tertiary); }\n"
            + "propertygrid::section { color: var(--text-tertiary); }\n"
            + "propertygrid::label { color: var(--text-secondary); }\n"
            + "propertygrid::value { color: var(--text-primary); }\n"
            + "pivottable::surface { background: var(--bg-primary); border: 1 var(--border-primary); }\n"
            + "pivottable::head { background: var(--bg-secondary); }\n"
            + "pivottable::total { background: var(--bg-tertiary); }\n"
            + "pivottable::corner { color: var(--text-secondary); }\n"
            + "pivottable::head-label, pivottable::total-label { color: var(--text-primary); }\n"
            + "pivottable::head-muted, pivottable::body-empty { color: var(--text-tertiary); }\n"
            + "pivottable::body-label { color: var(--text-secondary); }\n"
            + "pivottable::body-value { color: var(--text-primary); }\n"
            + "pivottable::grid { background: var(--divider); }\n"
            + "pivottable::accent { color: var(--primary); }\n"
            + "pivottable::hover { background: var(--bg-hover); }\n"
            + "pivottable::sel { background: var(--primary-soft-bg); }\n"
            + "chatview::head { color: var(--success); }\n"
            + "chatview::head.primary { color: var(--primary); }\n"
            + "chatview::head.error, chatview::head.danger { color: var(--error); }\n"
            + "chatview::head.tool { color: var(--text-tertiary); }\n"
            + "chatview::body { color: var(--text-primary); }\n"
            + "chatview::body.error, chatview::body.danger { color: var(--error); }\n"
            + "chatview::body.tool { color: var(--text-tertiary); }\n"
            + "chatview::muted { color: var(--text-tertiary); }\n"
            + "chatview::code { color: var(--info); }\n"
            + "codeeditor::surface { background: var(--bg-primary); }\n"
            + "codeeditor::gutter { background: var(--bg-secondary); }\n"
            + "codeeditor::divider { background: var(--divider); }\n"
            + "codeeditor::hover { background: var(--bg-hover); }\n"
            + "codeeditor::breakpoint { background: var(--error); }\n"
            + "codeeditor::line-number { color: var(--text-tertiary); }\n"
            + "codeeditor::caret { background: var(--primary); }\n"
            + "codeeditor::text { color: var(--text-primary); }\n"
            + "ribbon::surface { background: var(--bg-secondary); }\n"
            + "ribbon::divider { background: var(--divider); }\n"
            + "ribbon::item { color: var(--text-secondary); }\n"
            + "ribbon::item:hover { background: var(--bg-hover); }\n"
            + "ribbon::item:active { background: var(--bg-active); }\n"
            + "ribbon::item:disabled { color: var(--text-disabled); }\n"
            + "ribbon::accent { color: var(--primary); }\n"
            + "ribbon::accent:hover { color: var(--primary-hover); }\n"
            + "ribbon::muted { color: var(--text-tertiary); }\n"
            + "ribbon::strip { background: var(--bg-tertiary); }\n"
            + "ribbon::tab { color: var(--text-secondary); }\n"
            + "ribbon::tab:hover { background: var(--bg-hover); color: var(--text-primary); }\n"
            + "ribbon::tab:active { background: var(--bg-secondary); color: var(--primary); }\n"
            + "wizard::preview, wizard::list, wizard::template-panel { background: var(--bg-secondary); border: 1 var(--border-secondary); }\n"
            + "wizard::preview-label, wizard::label { color: var(--text-secondary); }\n"
            + "wizard::preview-caption, wizard::description { color: var(--text-tertiary); }\n"
            + "wizard::preview-device, wizard::sketch-surface { background: var(--bg-primary); border: 1 var(--border-primary); }\n"
            + "wizard::sketch-line { color: var(--border-primary); }\n"
            + "wizard::sketch-fill, wizard::category-panel, wizard::target { background: var(--bg-tertiary); }\n"
            + "wizard::sketch-accent, wizard::accent { background: var(--primary); color: var(--primary); }\n"
            + "wizard::sketch-error { background: var(--error); }\n"
            + "wizard::sketch-panel, wizard::bar { background: var(--bg-secondary); }\n"
            + "wizard::title { color: var(--text-primary); }\n"
            + "wizard::divider { background: var(--divider); }\n"
            + "wizard::close { color: var(--text-primary); }\n"
            + "wizard::close:hover { background: var(--error); }\n"
            + "wizard::list-row-icon, wizard::template-row-icon, wizard::category-row { color: var(--text-secondary); }\n"
            + "wizard::list-row-label, wizard::template-row-label { color: var(--text-primary); }\n"
            + "wizard::list-row-icon:active, wizard::list-row-label:active, wizard::template-row-icon:active, wizard::template-row-label:active, wizard::category-row:active { color: var(--primary); }\n"
            + "wizard::list-row:hover, wizard::category-row:hover, wizard::template-row:hover { background: var(--bg-hover); }\n"
            + "wizard::list-row:active, wizard::category-row:active, wizard::template-row:active { background: var(--bg-active); color: var(--primary); }\n"
            + "wizard::target:hover { background: var(--bg-hover); }\n"
            + "wizard::target:active { background: var(--primary); color: var(--bg-primary); }\n"
            + "wizard::notice { color: var(--error); }\n"
            + "filepicker::label { color: var(--text-secondary); }\n"
            + "filepicker::pane { background: var(--bg-secondary); }\n"
            + "filepicker::pane-border { border: 1 var(--border-secondary); }\n"
            + "filepicker::row { color: var(--text-primary); }\n"
            + "filepicker::row:hover { background: var(--bg-hover); }\n"
            + "filepicker::row:selected { background: var(--primary-hover); }\n"
            + "filepicker::icon { color: var(--text-secondary); }\n"
            + "filepicker::folder-icon { color: var(--primary); }\n"
            + "filepicker::empty { color: var(--text-tertiary); }\n"
            + "filepicker::scrollbar { background: var(--bg-tertiary); color: var(--primary); }\n"
            + "filepicker::status { color: var(--text-tertiary); }\n"
            + "filepicker::status.error { color: var(--error); }\n"
            + "filepicker::filter { background: var(--bg-tertiary); color: var(--text-primary); }\n"
            + "filepicker::filter-border { border: 1 var(--border-primary); }\n"
            + "filepicker::filter:hover { background: var(--bg-hover); }\n"
            + "filepicker::filter:active { background: var(--bg-primary); }\n"
            + "filepicker::filter-shadow { box-shadow: 1 2 0 var(--shadow-color); }\n";
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(Style.RootFromTheme(Theme.Light()), names, vals);
        return Css.ParseWith(css, names, vals);
    }

    static void A6States() {
        StyleSheet sheet = Program.A6Sheet();
        Program.Btn(sheet, "buttongroup", "", Style.SNormal(),
                    "a6::buttongroup");
        Program.Btn(sheet, "ellipsis", "", Style.SNormal(),
                    "a6::ellipsis");
        Program.FormPart(sheet, "empty", "icon", "", Style.SNormal(),
                         "a6::empty-icon");
        Program.FormPart(sheet, "empty", "label", "", Style.SNormal(),
                         "a6::empty-label");
        Program.Btn(sheet, "floatbutton", "", Style.SNormal(),
                    "a6::floatbutton");
        Program.FormPart(sheet, "rate", "star", "", Style.SNormal(),
                         "a6::rate-star");
        Program.FormPart(sheet, "rate", "empty", "", Style.SNormal(),
                         "a6::rate-empty");
        Program.FormPart(sheet, "statistic", "label", "",
                         Style.SNormal(), "a6::statistic-label");
        Program.FormPart(sheet, "statistic", "value", "",
                         Style.SNormal(), "a6::statistic-value");
        Program.FormPart(sheet, "result", "icon", "info", Style.SNormal(),
                         "a6::result-info");
        Program.FormPart(sheet, "result", "icon", "success", Style.SNormal(),
                         "a6::result-success");
        Program.FormPart(sheet, "result", "icon", "warning", Style.SNormal(),
                         "a6::result-warning");
        Program.FormPart(sheet, "result", "icon", "error", Style.SNormal(),
                         "a6::result-error");
        Program.FormPart(sheet, "result", "title", "", Style.SNormal(),
                         "a6::result-title");
        Program.FormPart(sheet, "result", "desc", "", Style.SNormal(),
                         "a6::result-desc");
        Program.Btn(sheet, "codeblock", "", Style.SNormal(),
                    "a6::codeblock");
        Program.FormPart(sheet, "codeblock", "caption", "",
                         Style.SNormal(), "a6::codeblock-caption");
        Program.Btn(sheet, "statusbar", "", Style.SNormal(),
                    "a6::statusbar");
        Program.Btn(sheet, "toolstrip", "", Style.SNormal(),
                    "a6::toolstrip");
        Program.FormPart(sheet, "graphview", "edge", "", Style.SNormal(),
                         "a6::graph-edge");
        Program.FormPart(sheet, "graphview", "edge", "done",
                         Style.SNormal(), "a6::graph-edge-done");
        Program.FormPart(sheet, "graphview", "empty", "", Style.SNormal(),
                         "a6::graph-empty");
        Program.FormPart(sheet, "logview", "text", "", Style.SNormal(),
                         "a6::log-text");
        Program.FormPart(sheet, "logview", "empty", "", Style.SNormal(),
                         "a6::log-empty");
        Program.FormPart(sheet, "layer", "load-label", "", Style.SNormal(),
                         "a6::layer-load-label");
        Program.FormPart(sheet, "cef", "placeholder", "", Style.SNormal(),
                         "a6::cef-placeholder");
        Program.FormPart(sheet, "cef", "icon", "", Style.SNormal(),
                         "a6::cef-icon");
        Program.FormPart(sheet, "cef", "label", "", Style.SNormal(),
                         "a6::cef-label");
        Program.FormPart(sheet, "webview", "placeholder", "", Style.SNormal(),
                         "a6::webview-placeholder");
        Program.FormPart(sheet, "webview", "icon", "", Style.SNormal(),
                         "a6::webview-icon");
        Program.FormPart(sheet, "webview", "label", "", Style.SNormal(),
                         "a6::webview-label");
        Program.FormPart(sheet, "panel", "plain", "", Style.SNormal(),
                         "a6::panel-plain");
        Program.FormPart(sheet, "ribbon", "surface", "", Style.SNormal(),
                         "a6::ribbon-surface");
        Program.FormPart(sheet, "ribbon", "divider", "", Style.SNormal(),
                         "a6::ribbon-divider");
        Program.FormPart(sheet, "ribbon", "item", "", Style.SNormal(),
                         "a6::ribbon-item");
        Program.FormPart(sheet, "ribbon", "item", "", Style.SHover(),
                         "a6::ribbon-item-hover");
        Program.FormPart(sheet, "ribbon", "item", "", Style.SActive(),
                         "a6::ribbon-item-active");
        Program.FormPart(sheet, "ribbon", "item", "", Style.SDisabled(),
                         "a6::ribbon-item-disabled");
        Program.FormPart(sheet, "ribbon", "accent", "", Style.SNormal(),
                         "a6::ribbon-accent");
        Program.FormPart(sheet, "ribbon", "accent", "", Style.SHover(),
                         "a6::ribbon-accent-hover");
        Program.FormPart(sheet, "ribbon", "muted", "", Style.SNormal(),
                         "a6::ribbon-muted");
        Program.FormPart(sheet, "ribbon", "strip", "", Style.SNormal(),
                         "a6::ribbon-strip");
        Program.FormPart(sheet, "ribbon", "tab", "", Style.SNormal(),
                         "a6::ribbon-tab");
        Program.FormPart(sheet, "ribbon", "tab", "", Style.SHover(),
                         "a6::ribbon-tab-hover");
        Program.FormPart(sheet, "ribbon", "tab", "", Style.SActive(),
                         "a6::ribbon-tab-active");
        Program.FormPart(sheet, "wizard", "preview", "", Style.SNormal(),
                         "a6::wizard-preview");
        Program.FormPart(sheet, "wizard", "preview-label", "", Style.SNormal(),
                         "a6::wizard-preview-label");
        Program.FormPart(sheet, "wizard", "preview-caption", "", Style.SNormal(),
                         "a6::wizard-preview-caption");
        Program.FormPart(sheet, "wizard", "preview-device", "", Style.SNormal(),
                         "a6::wizard-preview-device");
        Program.FormPart(sheet, "wizard", "sketch-line", "", Style.SNormal(),
                         "a6::wizard-sketch-line");
        Program.FormPart(sheet, "wizard", "sketch-fill", "", Style.SNormal(),
                         "a6::wizard-sketch-fill");
        Program.FormPart(sheet, "wizard", "sketch-accent", "", Style.SNormal(),
                         "a6::wizard-sketch-accent");
        Program.FormPart(sheet, "wizard", "sketch-error", "", Style.SNormal(),
                         "a6::wizard-sketch-error");
        Program.FormPart(sheet, "wizard", "sketch-surface", "", Style.SNormal(),
                         "a6::wizard-sketch-surface");
        Program.FormPart(sheet, "wizard", "sketch-panel", "", Style.SNormal(),
                         "a6::wizard-sketch-panel");
        Program.FormPart(sheet, "wizard", "bar", "", Style.SNormal(),
                         "a6::wizard-bar");
        Program.FormPart(sheet, "wizard", "title", "", Style.SNormal(),
                         "a6::wizard-title");
        Program.FormPart(sheet, "wizard", "divider", "", Style.SNormal(),
                         "a6::wizard-divider");
        Program.FormPart(sheet, "wizard", "close", "", Style.SNormal(),
                         "a6::wizard-close");
        Program.FormPart(sheet, "wizard", "close", "", Style.SHover(),
                         "a6::wizard-close-hover");
        Program.FormPart(sheet, "wizard", "label", "", Style.SNormal(),
                         "a6::wizard-label");
        Program.FormPart(sheet, "wizard", "description", "", Style.SNormal(),
                         "a6::wizard-description");
        Program.FormPart(sheet, "wizard", "list", "", Style.SNormal(),
                         "a6::wizard-list");
        Program.FormPart(sheet, "wizard", "list-row", "", Style.SNormal(),
                         "a6::wizard-list-row");
        Program.FormPart(sheet, "wizard", "list-row", "", Style.SHover(),
                         "a6::wizard-list-row-hover");
        Program.FormPart(sheet, "wizard", "list-row", "", Style.SActive(),
                         "a6::wizard-list-row-active");
        Program.FormPart(sheet, "wizard", "list-row-icon", "", Style.SNormal(),
                         "a6::wizard-list-row-icon");
        Program.FormPart(sheet, "wizard", "list-row-icon", "", Style.SActive(),
                         "a6::wizard-list-row-icon-active");
        Program.FormPart(sheet, "wizard", "list-row-label", "", Style.SNormal(),
                         "a6::wizard-list-row-label");
        Program.FormPart(sheet, "wizard", "list-row-label", "", Style.SActive(),
                         "a6::wizard-list-row-label-active");
        Program.FormPart(sheet, "wizard", "list-accent", "", Style.SNormal(),
                         "a6::wizard-list-accent");
        Program.FormPart(sheet, "wizard", "category-panel", "", Style.SNormal(),
                         "a6::wizard-category-panel");
        Program.FormPart(sheet, "wizard", "template-panel", "", Style.SNormal(),
                         "a6::wizard-template-panel");
        Program.FormPart(sheet, "wizard", "category-row", "", Style.SNormal(),
                         "a6::wizard-category-row");
        Program.FormPart(sheet, "wizard", "category-row", "", Style.SHover(),
                         "a6::wizard-category-row-hover");
        Program.FormPart(sheet, "wizard", "category-row", "", Style.SActive(),
                         "a6::wizard-category-row-active");
        Program.FormPart(sheet, "wizard", "template-row", "", Style.SNormal(),
                         "a6::wizard-template-row");
        Program.FormPart(sheet, "wizard", "template-row", "", Style.SHover(),
                         "a6::wizard-template-row-hover");
        Program.FormPart(sheet, "wizard", "template-row", "", Style.SActive(),
                         "a6::wizard-template-row-active");
        Program.FormPart(sheet, "wizard", "template-row-icon", "", Style.SNormal(),
                         "a6::wizard-template-row-icon");
        Program.FormPart(sheet, "wizard", "template-row-icon", "", Style.SActive(),
                         "a6::wizard-template-row-icon-active");
        Program.FormPart(sheet, "wizard", "template-row-label", "", Style.SNormal(),
                         "a6::wizard-template-row-label");
        Program.FormPart(sheet, "wizard", "template-row-label", "", Style.SActive(),
                         "a6::wizard-template-row-label-active");
        Program.FormPart(sheet, "wizard", "accent", "", Style.SNormal(),
                         "a6::wizard-accent");
        Program.FormPart(sheet, "wizard", "target", "", Style.SNormal(),
                         "a6::wizard-target");
        Program.FormPart(sheet, "wizard", "target", "", Style.SHover(),
                         "a6::wizard-target-hover");
        Program.FormPart(sheet, "wizard", "target", "", Style.SActive(),
                         "a6::wizard-target-active");
        Program.FormPart(sheet, "wizard", "notice", "", Style.SNormal(),
                         "a6::wizard-notice");
        Program.FormPart(sheet, "filepicker", "label", "", Style.SNormal(),
                         "a6::filepicker-label");
        Program.FormPart(sheet, "filepicker", "pane", "", Style.SNormal(),
                         "a6::filepicker-pane");
        Program.FormPart(sheet, "filepicker", "pane-border", "", Style.SNormal(),
                         "a6::filepicker-pane-border");
        Program.FormPart(sheet, "filepicker", "row", "", Style.SNormal(),
                         "a6::filepicker-row");
        Program.FormPart(sheet, "filepicker", "row", "", Style.SHover(),
                         "a6::filepicker-row-hover");
        Program.FormPart(sheet, "filepicker", "row", "", Style.SSelected(),
                         "a6::filepicker-row-selected");
        Program.FormPart(sheet, "filepicker", "icon", "", Style.SNormal(),
                         "a6::filepicker-icon");
        Program.FormPart(sheet, "filepicker", "folder-icon", "", Style.SNormal(),
                         "a6::filepicker-folder-icon");
        Program.FormPart(sheet, "filepicker", "empty", "", Style.SNormal(),
                         "a6::filepicker-empty");
        Program.FormPart(sheet, "filepicker", "scrollbar", "", Style.SNormal(),
                         "a6::filepicker-scrollbar");
        Program.FormPart(sheet, "filepicker", "status", "", Style.SNormal(),
                         "a6::filepicker-status");
        Program.FormPart(sheet, "filepicker", "status", "error", Style.SNormal(),
                         "a6::filepicker-error");
        Program.FormPart(sheet, "filepicker", "filter", "", Style.SNormal(),
                         "a6::filepicker-filter");
        Program.FormPart(sheet, "filepicker", "filter-border", "", Style.SNormal(),
                         "a6::filepicker-filter-border");
        Program.FormPart(sheet, "filepicker", "filter", "", Style.SHover(),
                         "a6::filepicker-filter-hover");
        Program.FormPart(sheet, "filepicker", "filter", "", Style.SActive(),
                         "a6::filepicker-filter-active");
        Program.FormPart(sheet, "filepicker", "filter-shadow", "", Style.SNormal(),
                         "a6::filepicker-filter-shadow");
        int[] states = new int[]{Style.SNormal(), Style.SDisabled(),
            Style.SHover(), Style.SActive(), Style.SSelected(), Style.SFocus()};
        string[] names = new string[]{"normal", "disabled", "hover", "active",
            "selected", "focus"};
        for (int i = 0; i < states.Length; i = i + 1) {
            Program.FormPart(sheet, "state", "surface", "", states[i],
                             "a6::state-surface-" + names[i]);
            Program.FormPart(sheet, "state", "label", "", states[i],
                             "a6::state-label-" + names[i]);
            Program.FormPart(sheet, "state", "caption", "", states[i],
                             "a6::state-caption-" + names[i]);
            Program.FormPart(sheet, "state", "border", "", states[i],
                             "a6::state-border-" + names[i]);
        }
        Program.FormPart(sheet, "state", "accent", "", Style.SNormal(),
                         "a6::state-accent");
        Program.FormPart(sheet, "progress", "track", "", Style.SNormal(),
                         "a6::progress-track");
        Program.FormPart(sheet, "progress", "fill", "", Style.SNormal(),
                         "a6::progress-fill");
        Program.FormPart(sheet, "progress", "fill", "info", Style.SNormal(),
                         "a6::progress-info");
        Program.FormPart(sheet, "progress", "fill", "success", Style.SNormal(),
                         "a6::progress-success");
        Program.FormPart(sheet, "progress", "fill", "warning", Style.SNormal(),
                         "a6::progress-warning");
        Program.FormPart(sheet, "progress", "fill", "error", Style.SNormal(),
                         "a6::progress-error");
        Program.FormPart(sheet, "progress", "label", "", Style.SNormal(),
                         "a6::progress-label");
        Program.FormPart(sheet, "progress", "ring-label", "", Style.SNormal(),
                         "a6::progress-ring-label");
        Program.FormPart(sheet, "spin", "accent", "info", Style.SNormal(),
                         "a6::spin-info");
        Program.FormPart(sheet, "spin", "rest", "", Style.SNormal(),
                         "a6::spin-rest");
        Program.FormPart(sheet, "spin", "tail", "", Style.SNormal(),
                         "a6::spin-tail");
        Program.FormPart(sheet, "spin", "ring", "", Style.SNormal(),
                         "a6::spin-ring");
        Program.FormPart(sheet, "styledtext", "selection", "", Style.SNormal(),
                         "a6::styledtext-selection");
        Program.FormPart(sheet, "styledtext", "code", "", Style.SNormal(),
                         "a6::styledtext-code");
        Program.FormPart(sheet, "styledtext", "time", "", Style.SNormal(),
                         "a6::styledtext-time");
        Program.FormPart(sheet, "styledtext", "separator", "", Style.SNormal(),
                         "a6::styledtext-separator");
        Program.FormPart(sheet, "styledtext", "rail", "success", Style.SNormal(),
                         "a6::styledtext-rail-success");
        Program.FormPart(sheet, "styledtext", "tint", "success", Style.SNormal(),
                         "a6::styledtext-tint-success");
        Program.FormPart(sheet, "styledtext", "rail", "primary", Style.SNormal(),
                         "a6::styledtext-rail-primary");
        Program.FormPart(sheet, "styledtext", "tint", "primary", Style.SNormal(),
                         "a6::styledtext-tint-primary");
        Program.FormPart(sheet, "styledtext", "rail", "tool", Style.SNormal(),
                         "a6::styledtext-rail-tool");
        Program.FormPart(sheet, "styledtext", "tint", "tool", Style.SNormal(),
                         "a6::styledtext-tint-tool");
        Program.FormPart(sheet, "styledtext", "rail", "error", Style.SNormal(),
                         "a6::styledtext-rail-error");
        Program.FormPart(sheet, "styledtext", "tint", "error", Style.SNormal(),
                         "a6::styledtext-tint-error");
        Program.FormPart(sheet, "effects", "star", "", Style.SNormal(),
                         "a6::effects-star");
        Program.FormPart(sheet, "effects", "particle", "a", Style.SNormal(),
                         "a6::effects-particle-a");
        Program.FormPart(sheet, "effects", "particle", "b", Style.SNormal(),
                         "a6::effects-particle-b");
        Program.FormPart(sheet, "effects", "particle", "c", Style.SNormal(),
                         "a6::effects-particle-c");
        Program.FormPart(sheet, "effects", "bokeh", "b", Style.SNormal(),
                         "a6::effects-bokeh-b");
        Program.FormPart(sheet, "effects", "bokeh", "a", Style.SNormal(),
                         "a6::effects-bokeh-a");
        Program.FormPart(sheet, "effects", "bokeh", "c", Style.SNormal(),
                         "a6::effects-bokeh-c");
        Program.FormPart(sheet, "effects", "mesh", "a", Style.SNormal(),
                         "a6::effects-mesh-a");
        Program.FormPart(sheet, "effects", "mesh", "b", Style.SNormal(),
                         "a6::effects-mesh-b");
        Program.FormPart(sheet, "propertygrid", "step", "", Style.SNormal(),
                         "a6::property-step");
        Program.FormPart(sheet, "propertygrid", "step", "", Style.SHover(),
                         "a6::property-step-hover");
        Program.FormPart(sheet, "propertygrid", "empty", "", Style.SNormal(),
                         "a6::property-empty");
        Program.FormPart(sheet, "propertygrid", "section", "", Style.SNormal(),
                         "a6::property-section");
        Program.FormPart(sheet, "propertygrid", "label", "", Style.SNormal(),
                         "a6::property-label");
        Program.FormPart(sheet, "propertygrid", "value", "", Style.SNormal(),
                         "a6::property-value");
        Program.FormPart(sheet, "chatview", "head", "", Style.SNormal(),
                         "a6::chat-head");
        Program.FormPart(sheet, "chatview", "head", "primary", Style.SNormal(),
                         "a6::chat-head-primary");
        Program.FormPart(sheet, "chatview", "head", "error", Style.SNormal(),
                         "a6::chat-head-error");
        Program.FormPart(sheet, "chatview", "head", "tool", Style.SNormal(),
                         "a6::chat-head-tool");
        Program.FormPart(sheet, "chatview", "body", "", Style.SNormal(),
                         "a6::chat-body");
        Program.FormPart(sheet, "chatview", "body", "error", Style.SNormal(),
                         "a6::chat-body-error");
        Program.FormPart(sheet, "chatview", "body", "tool", Style.SNormal(),
                         "a6::chat-body-tool");
        Program.FormPart(sheet, "chatview", "muted", "", Style.SNormal(),
                         "a6::chat-muted");
        Program.FormPart(sheet, "chatview", "code", "", Style.SNormal(),
                         "a6::chat-code");
        Program.FormPart(sheet, "pivottable", "surface", "", Style.SNormal(),
                         "a6::pivot-surface");
        Program.FormPart(sheet, "pivottable", "head", "", Style.SNormal(),
                         "a6::pivot-head");
        Program.FormPart(sheet, "pivottable", "total", "", Style.SNormal(),
                         "a6::pivot-total");
        Program.FormPart(sheet, "pivottable", "corner", "", Style.SNormal(),
                         "a6::pivot-corner");
        Program.FormPart(sheet, "pivottable", "head-label", "", Style.SNormal(),
                         "a6::pivot-head-label");
        Program.FormPart(sheet, "pivottable", "head-muted", "", Style.SNormal(),
                         "a6::pivot-head-muted");
        Program.FormPart(sheet, "pivottable", "body-label", "", Style.SNormal(),
                         "a6::pivot-body-label");
        Program.FormPart(sheet, "pivottable", "body-value", "", Style.SNormal(),
                         "a6::pivot-body-value");
        Program.FormPart(sheet, "pivottable", "body-empty", "", Style.SNormal(),
                         "a6::pivot-body-empty");
        Program.FormPart(sheet, "pivottable", "grid", "", Style.SNormal(),
                         "a6::pivot-grid");
        Program.FormPart(sheet, "pivottable", "accent", "", Style.SNormal(),
                         "a6::pivot-accent");
        Program.FormPart(sheet, "codeeditor", "surface", "", Style.SNormal(),
                         "a6::editor-surface");
        Program.FormPart(sheet, "codeeditor", "gutter", "", Style.SNormal(),
                         "a6::editor-gutter");
        Program.FormPart(sheet, "codeeditor", "divider", "", Style.SNormal(),
                         "a6::editor-divider");
        Program.FormPart(sheet, "codeeditor", "hover", "", Style.SNormal(),
                         "a6::editor-hover");
        Program.FormPart(sheet, "codeeditor", "breakpoint", "", Style.SNormal(),
                         "a6::editor-breakpoint");
        Program.FormPart(sheet, "codeeditor", "line-number", "", Style.SNormal(),
                         "a6::editor-line-number");
        Program.FormPart(sheet, "codeeditor", "caret", "", Style.SNormal(),
                         "a6::editor-caret");
        Program.FormPart(sheet, "codeeditor", "text", "", Style.SNormal(),
                         "a6::editor-text");
    }

    /// 容器浮层族（A2 批）：镜像 base.css 的 layer/dialog/toast/
    /// notification/popconfirm/photos/popover/tooltip/contextmenu/menu/
    /// scrollbar/splitter/collapse/panel/card 规则。浮层控件只消费
    /// 解析后的 StyleBox，所以这里钉住的就是它们最终画出的颜色。
    static StyleSheet OverlaySheet() {
        string css = "layer { background: var(--bg-primary); color: var(--text-primary);\n"
            + "  border: 1 var(--border-secondary); }\n"
            + "layer::titlebar { background: var(--bg-secondary);\n"
            + "  color: var(--text-primary); accent-color: var(--primary);\n"
            + "  border-bottom: 2 var(--primary); }\n"
            + "layer::cap { color: var(--text-secondary); }\n"
            + "layer::cap:hover { background: var(--bg-hover); color: var(--text-primary); }\n"
            + "layer.danger::cap:hover { background: var(--error); color: var(--on-scrim); }\n"
            + "layer::footer { background: var(--divider); }\n"
            + "layer::mask { background: var(--overlay); }\n"
            + "dialog::overlay { background: var(--overlay); }\n"
            + "dialog::surface { background: var(--bg-primary);\n"
            + "  box-shadow: 2 3 0 var(--shadow-color); }\n"
            + "dialog::icon { color: var(--info); }\n"
            + "dialog.error::icon { color: var(--error); }\n"
            + "dialog::close { color: var(--text-secondary); }\n"
            + "dialog::close:hover { background: var(--bg-hover); }\n"
            + "toast, notification { background: var(--bg-primary);\n"
            + "  color: var(--text-primary); border: 1 var(--border-secondary); }\n"
            + "toast::icon { color: var(--text-secondary); }\n"
            + "toast.success::icon { color: var(--success); }\n"
            + "notification::bar { background: var(--primary); width: 3; }\n"
            + "notification.warning::bar { background: var(--warning); }\n"
            + "popconfirm { background: var(--bg-primary); color: var(--text-primary);\n"
            + "  border: 1 var(--border-secondary); box-shadow: 1 2 0 var(--shadow-color); }\n"
            + "popconfirm::icon { color: var(--warning); }\n"
            + "photos { background: var(--scrim); color: var(--on-scrim); }\n"
            + "photos::chip { background: var(--scrim-chip); color: var(--on-scrim); }\n"
            + "photos::chip:hover { background: var(--scrim-chip-hover); }\n"
            + "popover { background: var(--bg-primary); color: var(--text-primary);\n"
            + "  border: 1 var(--border-secondary); box-shadow: 1 2 0 var(--shadow-color); }\n"
            + "popover::title { color: var(--text-primary); }\n"
            + "popover::body { color: var(--text-secondary); }\n"
            + "tooltip { background: var(--tooltip-bg); color: var(--tooltip-text);\n"
            + "  border: 1 var(--tooltip-border); border-radius: 3;\n"
            + "  box-shadow: 1 3 0 var(--shadow-color); }\n"
            + "tooltip::title { color: var(--on-scrim); }\n"
            + "tooltip::desc { color: var(--tooltip-text-muted); }\n"
            + "tooltip::scrollbar { background: var(--scrim-chip-hover); }\n"
            + "tooltip::scrollbar-track { background: var(--scrim-chip); }\n"
            + "contextmenu { background: var(--bg-secondary); color: var(--text-primary);\n"
            + "  border: 1 var(--border-secondary); box-shadow: 2 2 0 var(--shadow-color); }\n"
            + "contextmenu::item { color: var(--text-primary); }\n"
            + "contextmenu::item:hover { background: var(--bg-hover); }\n"
            + "contextmenu::accel { color: var(--text-tertiary); }\n"
            + "contextmenu::separator { background: var(--divider); }\n"
            + "menu::item { color: var(--text-secondary); }\n"
            + "menu::item:hover { background: var(--bg-hover); color: var(--text-primary); }\n"
            + "menu::item:selected { background: var(--bg-active); color: var(--primary);\n"
            + "  accent-color: var(--primary); }\n"
            + "prompt { background: var(--bg-secondary); color: var(--text-primary);\n"
            + "  border: 1 var(--border-primary); box-shadow: none; }\n"
            + "prompt::caption { color: var(--text-tertiary); }\n"
            + "prompt::field { background: var(--bg-primary); color: var(--text-primary);\n"
            + "  border: 1 var(--border-primary); }\n"
            + "scrollbar { background: var(--bg-tertiary); border-width: 0; }\n"
            + "scrollbar::thumb { background: var(--scrollbar); }\n"
            + "scrollbar::thumb:hover, scrollbar::thumb:active {\n"
            + "  background: var(--scrollbar-hover); }\n"
            + "splitter { background: var(--divider); border-width: 0; }\n"
            + "splitter:hover, splitter:active { background: var(--primary); }\n"
            + "collapse::header { background: var(--bg-primary); color: var(--text-primary); }\n"
            + "collapse::header:hover { background: var(--bg-hover); }\n"
            + "collapse::arrow { color: var(--text-secondary); }\n"
            + "collapse::body { background: var(--bg-secondary); color: var(--text-secondary); }\n"
            + "collapse::divider { background: var(--divider); }\n"
            + "panel::title { color: var(--text-primary); }\n"
            + "panel::divider { background: var(--divider); }\n"
            + "card::title { color: var(--text-primary); }\n"
            + "card::divider { background: var(--divider); }\n";
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        Css.CollectVars(Style.RootFromTheme(Theme.Light()), names, vals);
        return Css.ParseWith(css, names, vals);
    }

    static void OverlayStates() {
        StyleSheet sheet = Program.OverlaySheet();
        int hover = Style.SHover();
        // 窗口面 / 标题栏 / 标题栏小按钮（含 danger 类）/ 遮罩。
        Program.Btn(sheet, "layer", "", Style.SNormal(), "layer");
        Program.FormPart(sheet, "layer", "titlebar", "", Style.SNormal(), "layer::titlebar");
        Program.FormPart(sheet, "layer", "cap", "", Style.SNormal(), "layer::cap");
        Program.FormPart(sheet, "layer", "cap", "", hover, "layer::cap-hover");
        Program.FormPart(sheet, "layer", "cap", "danger", hover, "layer.danger::cap-hover");
        Program.FormPart(sheet, "layer", "footer", "", Style.SNormal(), "layer::footer");
        Program.FormPart(sheet, "layer", "mask", "", Style.SNormal(), "layer::mask");
        // 模态对话框：遮罩/面板/角色图标/关闭钮。
        Program.FormPart(sheet, "dialog", "overlay", "", Style.SNormal(), "dialog::overlay");
        Program.FormPart(sheet, "dialog", "surface", "", Style.SNormal(), "dialog::surface");
        Program.FormPart(sheet, "dialog", "icon", "", Style.SNormal(), "dialog::icon");
        Program.FormPart(sheet, "dialog", "icon", "error", Style.SNormal(), "dialog.error::icon");
        Program.FormPart(sheet, "dialog", "close", "", hover, "dialog::close-hover");
        // toast / 通知：角色类只重着色图标/侧边条。
        Program.Btn(sheet, "toast", "", Style.SNormal(), "toast");
        Program.FormPart(sheet, "toast", "icon", "success", Style.SNormal(), "toast.success::icon");
        Program.FormPart(sheet, "notification", "bar", "", Style.SNormal(), "notification::bar");
        Program.FormPart(sheet, "notification", "bar", "warning", Style.SNormal(), "notification.warning::bar");
        // popconfirm / photos / popover / tooltip。
        Program.Btn(sheet, "popconfirm", "", Style.SNormal(), "popconfirm");
        Program.FormPart(sheet, "popconfirm", "icon", "", Style.SNormal(), "popconfirm::icon");
        Program.Btn(sheet, "photos", "", Style.SNormal(), "photos");
        Program.FormPart(sheet, "photos", "chip", "", Style.SNormal(), "photos::chip");
        Program.FormPart(sheet, "photos", "chip", "", hover, "photos::chip-hover");
        Program.Btn(sheet, "popover", "", Style.SNormal(), "popover");
        Program.FormPart(sheet, "popover", "title", "", Style.SNormal(), "popover::title");
        Program.FormPart(sheet, "popover", "body", "", Style.SNormal(), "popover::body");
        Program.Btn(sheet, "tooltip", "", Style.SNormal(), "tooltip");
        Program.FormPart(sheet, "tooltip", "title", "", Style.SNormal(), "tooltip::title");
        Program.FormPart(sheet, "tooltip", "desc", "", Style.SNormal(), "tooltip::desc");
        Program.FormPart(sheet, "tooltip", "scrollbar", "", Style.SNormal(), "tooltip::scrollbar");
        Program.FormPart(sheet, "tooltip", "scrollbar-track", "", Style.SNormal(), "tooltip::scrollbar-track");
        // 右键菜单 / 导航菜单 / prompt。
        Program.Btn(sheet, "contextmenu", "", Style.SNormal(), "contextmenu");
        Program.FormPart(sheet, "contextmenu", "item", "", hover, "contextmenu::item-hover");
        Program.FormPart(sheet, "contextmenu", "accel", "", Style.SNormal(), "contextmenu::accel");
        Program.FormPart(sheet, "contextmenu", "separator", "", Style.SNormal(), "contextmenu::separator");
        Program.FormPart(sheet, "menu", "item", "", Style.SNormal(), "menu::item");
        Program.FormPart(sheet, "menu", "item", "", hover, "menu::item-hover");
        Program.FormPart(sheet, "menu", "item", "", Style.SSelected(), "menu::item-selected");
        Program.Btn(sheet, "prompt", "", Style.SNormal(), "prompt");
        Program.FormPart(sheet, "prompt", "caption", "", Style.SNormal(), "prompt::caption");
        Program.FormPart(sheet, "prompt", "field", "", Style.SNormal(), "prompt::field");
        // 滚动条 / 分栏拖条：悬停与拖动共用 hover/active 规则。
        Program.Btn(sheet, "scrollbar", "", Style.SNormal(), "scrollbar");
        Program.FormPart(sheet, "scrollbar", "thumb", "", Style.SNormal(), "scrollbar::thumb");
        Program.FormPart(sheet, "scrollbar", "thumb", "", hover, "scrollbar::thumb-hover");
        Program.FormPart(sheet, "scrollbar", "thumb", "", Style.SActive(), "scrollbar::thumb-active");
        Program.Btn(sheet, "splitter", "", Style.SNormal(), "splitter");
        Program.Btn(sheet, "splitter", "", hover, "splitter-hover");
        Program.Btn(sheet, "splitter", "", Style.SActive(), "splitter-active");
        // collapse / panel / card 的标题与分隔部件。
        Program.FormPart(sheet, "collapse", "header", "", Style.SNormal(), "collapse::header");
        Program.FormPart(sheet, "collapse", "header", "", hover, "collapse::header-hover");
        Program.FormPart(sheet, "collapse", "arrow", "", Style.SNormal(), "collapse::arrow");
        Program.FormPart(sheet, "collapse", "body", "", Style.SNormal(), "collapse::body");
        Program.FormPart(sheet, "collapse", "divider", "", Style.SNormal(), "collapse::divider");
        Program.FormPart(sheet, "panel", "title", "", Style.SNormal(), "panel::title");
        Program.FormPart(sheet, "panel", "divider", "", Style.SNormal(), "panel::divider");
        Program.FormPart(sheet, "card", "title", "", Style.SNormal(), "card::title");
        Program.FormPart(sheet, "card", "divider", "", Style.SNormal(), "card::divider");
        // 菜单行的实际用法：normal↔hover 按悬停进度 Blend。
        StyleBox mn = new StyleBox();
        Style.ApplyPartSheet(sheet, mn, "menu", "item", "", "", Style.SNormal());
        StyleBox mh = new StyleBox();
        Style.ApplyPartSheet(sheet, mh, "menu", "item", "", "", hover);
        StyleBox mm = StyleBox.Blend(mn, mh, 500);
        Console.WriteLine($"overlay menu-item-mid fg {mm.fg}");
    }

    /// Layout / shorthand properties that only exist in CSS text (the
    /// builder APIs spell the same fields differently, so both paths are
    /// pinned separately).
    static void Longhand() {
        string css = "a { display: flex; flex-flow: row; flex-wrap: wrap;\n"
            + "    gap: 8 20; place-content: center space-between;\n"
            + "    box-sizing: border-box; }\n"
            + "b { flex: 1 1 0; align-self: center; aspect-ratio: 16 / 9; }\n"
            + "c { flex: none; }\n"
            + "d { flex: 2 0 40%; align-self: flex-end; }\n"
            + "e { border-radius: 10; border-top-left-radius: 0;\n"
            + "    border-bottom-right-radius: 24; }\n"
            + "f { border-top-right-radius: 6; }\n"
            + "g { position: absolute; inset: 4 8; }\n"
            + "h { inset: 1 2 3 4; }\n"
            + "i { rotate: 45; scale: 1.25; translate: 3 -7; }\n"
            + "j { gap: 12; }\n";
        StyleSheet sheet = Css.Parse(css);
        List<string> sels = new List<string>{ "a", "b", "c", "d", "e",
                                             "f", "g", "h", "i", "j" };
        for (int i = 0; i < sels.Count; i = i + 1) {
            StyleBox x = new StyleBox();
            sheet.ApplyBox(x, sels[i]);
            Console.WriteLine($"{sels[i]} display {x.display} dir {x.flexDir} wrap {x.wrap} justify {x.justify} content {x.alignContent}");
            Console.WriteLine($"  gap {x.gap} row {x.rowGap} col {x.colGap} grow {x.grow} shrink {x.shrink} basis {x.basis} pm {x.basisPm} self {x.alignSelf} aspect {x.aspect}");
            Console.WriteLine($"  radius {x.radius} corners {x.radiusTL},{x.radiusTR},{x.radiusBR},{x.radiusBL}");
            Console.WriteLine($"  pos {x.position} inset {x.posT},{x.posR},{x.posB},{x.posL} rotate {x.rotate} scale {x.scale} translate {x.tx},{x.ty}");
        }
    }

    /// 这一批"静默失败"修复的定点：at-rule 的括号配对、`var()` 回退值、
    /// `!important` 的真实权重、`border` 简写的线型、text-shadow、
    /// `linear-gradient(to left)`，以及 Lint 报告。全部是原先
    /// "写了不报错、也没有效果"的那一类，所以每一条都要在金标里留痕。
    static void Coercion() {
        // at-rule 的块必须整体跳过，且不吃掉紧随其后的规则。
        StyleSheet at = Css.Parse(
            "@media (max-width: 600px) { a { color: #ff0000; } }\n"
            + "button { background: #00ff00; }\n"
            + ".x { color: #0000ff; }");
        StyleBox aa = new StyleBox(); at.ApplyBox(aa, "a");
        StyleBox ab = new StyleBox(); at.ApplyBox(ab, "button");
        StyleBox ax = new StyleBox(); at.ApplyBox(ax, ".x");
        Console.WriteLine($"atrule a.fg {aa.fg} button.bg {ab.bg} x.fg {ax.fg}"
                          + $" dropped {at.atRulesDropped}");

        // `@import "x";` 这种语句式 at-rule 不得吞掉后续规则。
        StyleSheet imp = Css.Parse(
            "@import url(\"x.css\"); button { background: #00ff00; }");
        StyleBox ib = new StyleBox(); imp.ApplyBox(ib, "button");
        Console.WriteLine($"atimport button.bg {ib.bg} dropped {imp.atRulesDropped}");

        // var() 回退值：未定义、空值、嵌套函数、#rgba 都要走回退。
        List<string> names = new List<string>();
        List<string> vals = new List<string>();
        StyleSheet vr = Css.ParseWith(
            "a { padding: var(--missing, 7); color: var(--c, #ff0000);"
            + " background: var(--nope, rgba(0,0,0,.5)); }\n"
            + "b { border: var(--nope, 1 solid) #123456;"
            + " border-radius: var(--nope, #); }\n"
            + "c { color: var(--nope, #f008); }\n"
            + "d { color: var(--empty, #abcdef); }",
            names, vals);
        vals.Add("--empty"); vals.Add("");
        names.Add("--empty");
        StyleBox va = new StyleBox(); vr.ApplyBox(va, "a");
        StyleBox vc = new StyleBox(); vr.ApplyBox(vc, "c");
        StyleBox vd = new StyleBox(); vr.ApplyBox(vd, "d");
        Console.WriteLine($"varfb padL {va.padL} fg {va.fg} bg {va.bg}");
        Console.WriteLine($"varfb hex4 fg {vc.fg} empty {vd.fg}");

        // !important：压过后写的普通声明，同一选择器内也成立。
        StyleSheet im = Css.Parse(
            "label { color: #ff0000 !important; }\n"
            + "label { color: #0000ff; }\n"
            + "other { padding: 3 !important; padding: 9; }");
        StyleBox il = new StyleBox(); im.ApplyBox(il, "label");
        StyleBox io = new StyleBox(); im.ApplyBox(io, "other");
        Console.WriteLine($"important label.fg {il.fg} other.padL {io.padL}");
        // 换肤合并：应用表（后合并）的普通声明不得压过皮肤的 important。
        StyleSheet skin = Css.Parse("w { color: #ff0000 !important; }");
        StyleSheet app = Css.Parse("w { color: #0000ff; }");
        skin.MergeSheet(app);
        StyleBox mw = new StyleBox(); skin.ApplyBox(mw, "w");
        Console.WriteLine($"important merge fg {mw.fg}");

        // border 简写的线型词。
        StyleSheet bd = Css.Parse(
            "a { border: 2 solid #ff0000; } b { border: 1 dashed #00ff00; }"
            + " c { border: 3 dotted #0000ff; } d { border: none; }"
            + " e { border-style: dashed; } f { border: 1 double #111111; }");
        List<string> bs = new List<string>{ "a", "b", "c", "d", "e", "f" };
        for (int i = 0; i < bs.Count; i = i + 1) {
            StyleBox x = new StyleBox();
            bd.ApplyBox(x, bs[i]);
            Console.WriteLine($"border {bs[i]} style {x.borderStyle} w {x.borderW} col {x.borderColor}");
        }

        // text-shadow：单层偏移 / ≥3 层零模糊按描边 / none 复位。
        StyleSheet ts = Css.Parse(
            "a { text-shadow: 1px 1px 0 #000000; }\n"
            + "b { text-shadow: 1px 1px 0 #000000, -1px -1px 0 #000000,"
            + " 1px -1px 0 #000000, -1px 1px 0 #000000; }\n"
            + "c { text-shadow: none; }\n"
            + "d { text-shadow: 2px 3px 4px rgba(0,0,0,.5); }\n");
        List<string> tsel = new List<string>{ "a", "b", "c", "d" };
        for (int i = 0; i < tsel.Count; i = i + 1) {
            StyleBox x = new StyleBox();
            ts.ApplyBox(x, tsel[i]);
            Console.WriteLine($"textshadow {tsel[i]} col {x.tsColor} dx {x.tsDx} dy {x.tsDy} outline {x.tsOutline}");
        }

        // `to left` / `270deg` 的横向镜像。
        StyleSheet gl = Css.Parse(
            "a { background: linear-gradient(to left, #ff0000, #0000ff); }\n"
            + "b { background: linear-gradient(270deg, #ff0000, #0000ff); }\n"
            + "c { background: linear-gradient(to right, #ff0000, #0000ff); }\n");
        List<string> gsel = new List<string>{ "a", "b", "c" };
        for (int i = 0; i < gsel.Count; i = i + 1) {
            StyleBox x = new StyleBox();
            gl.ApplyBox(x, gsel[i]);
            Console.WriteLine($"gradleft {gsel[i]} dir {x.bgDir} bg {x.bg} to {x.bgTo}");
        }

        // Lint：不支持的选择器 / 一条都没消费的规则 / 收了但无效果的属性。
        StyleSheet lint = Css.Parse(
            ".a .b { color: #ff0000; }\n"
            + "button > span { color: #00ff00; }\n"
            + "card { wibble: 3; }\n"
            + "z { float: right; color: #ff0000; }\n"
            + ".fine { color: #0000ff; }");
        List<string> audit = lint.Audit();
        Console.WriteLine($"lint count {audit.Count}");
        for (int i = 0; i < audit.Count; i = i + 1) {
            Console.WriteLine($"lint   {audit[i]}");
        }
    }

    /// 语法面本身的定点：通用选择器、属性选择器全部运算符、布尔属性
    /// 到状态位的映射、`:not()`/`:is()`/`:where()` 的匹配与权重、伪元素、
    /// `@supports` 守卫（含值级判定）、`@layer` 摊平，以及 Lint 对
    /// "永不匹配"和"单位没解析"的点名。这些以前都是"写了没反应也
    /// 不报错"的面，所以每一条都要在金标里留痕。
    static void Syntax() {
        // 通用选择器 `*`，以及 `[class]` 的存在性判断（class 属性空串
        // 视为没有该属性）。
        StyleSheet su = Css.Parse("* { padding: 2; }\n[class] { margin: 3; }");
        Program.SynBox("univ cls", su, "button", "primary");
        Program.SynBox("univ bare", su, "button", "");
        Program.SynBox("univ multi", su, "div", "a b");

        // 属性选择器各运算符：`^=` 前缀、`$=` 后缀、`~=` 整词、
        // `|=` 整词或 `词-`、`*=` 子串，以及 `i` 大小写不敏感标志。
        // 每个运算符都给命中/不命中两条，命中给 fg 上色。
        Program.SynFg("caret hit",
                      Css.Parse("[class^=\"btn\"] { color: #ff0000; }"),
                      "button", "btn-primary");
        Program.SynFg("caret miss",
                      Css.Parse("[class^=\"btn\"] { color: #ff0000; }"),
                      "button", "xbtn");
        Program.SynFg("dollar hit",
                      Css.Parse("[class$=\"primary\"] { color: #ff0000; }"),
                      "button", "x primary");
        Program.SynFg("dollar miss",
                      Css.Parse("[class$=\"primary\"] { color: #ff0000; }"),
                      "button", "primary-x");
        Program.SynFg("tilde hit",
                      Css.Parse("[class~=\"tab\"] { color: #ff0000; }"),
                      "button", "a tab");
        Program.SynFg("tilde miss",
                      Css.Parse("[class~=\"tab\"] { color: #ff0000; }"),
                      "button", "tabs");
        Program.SynFg("pipe hit",
                      Css.Parse("[class|=\"nav\"] { color: #ff0000; }"),
                      "button", "nav-item");
        Program.SynFg("pipe miss",
                      Css.Parse("[class|=\"nav\"] { color: #ff0000; }"),
                      "button", "navbar");
        Program.SynFg("star case",
                      Css.Parse("[class*=\"ICON\"] { color: #ff0000; }"),
                      "button", "Icon-x");
        Program.SynFg("star i hit",
                      Css.Parse("[class*=\"icon\" i] { color: #ff0000; }"),
                      "button", "Icon-x");
        Program.SynFg("star i miss",
                      Css.Parse("[class*=\"icon\" i] { color: #ff0000; }"),
                      "button", "xyz");

        // 布尔属性映射到已有状态位：`[disabled]` 就是 `:disabled`，
        // `[disabled="false"]` 反过来（未禁用时命中）；`:enabled` 与
        // `:disabled` 互补。属性取真时只上对应的状态位。
        StyleSheet ss = Css.Parse(
            "button[disabled] { opacity: 10; }\n"
            + "button[disabled=\"false\"] { opacity: 20; }\n"
            + "button[checked] { padding-left: 30; }\n"
            + "button[selected] { padding-right: 40; }\n"
            + "button:enabled { padding-top: 50; }");
        Program.SynAttr("disabled", ss, Style.SDisabled());
        Program.SynAttr("normal", ss, Style.SNormal());
        Program.SynAttr("checked", ss, Style.SChecked());
        Program.SynAttr("selected", ss, Style.SSelected());

        // `:not()` / `:is()` / `:where()`。
        StyleSheet sn = Css.Parse(
            "p:not(.x) { color: #ff0000; }\n"
            + "q:is(.a, .b) { color: #00ff00; }\n"
            + "r:where(.c) { background: #0000ff; }\n"
            + "s:not(.x):not(.y) { accent-color: #111111; }");
        Program.SynPick("not hit", sn, "p", "z");
        Program.SynPick("not miss", sn, "p", "x");
        Program.SynPick("is a", sn, "q", "a");
        Program.SynPick("is b", sn, "q", "b");
        Program.SynPick("is miss", sn, "q", "c");
        Program.SynPick("where hit", sn, "r", "c");
        Program.SynPick("not not hit", sn, "s", "z");
        Program.SynPick("not not miss", sn, "s", "x");

        // `:where()` 计 0 权重：垫不住 `.w` 的类权重；与 type 同权重时
        // 靠文档顺序取胜。`:is()` 取内层权重，能压过同权重的 `.w`。
        Program.SynFg("where loses", Css.Parse(
            "c { color: #ff0000; }\n"
            + "c.w { color: #00ff00; }\n"
            + "c:where(.w) { color: #0000ff; }"), "c", "w");
        Program.SynFg("where ties", Css.Parse(
            "c { color: #ff0000; }\n"
            + "c:where(.w) { color: #0000ff; }"), "c", "w");
        Program.SynFg("is ties", Css.Parse(
            "c { color: #ff0000; }\n"
            + "c.w { color: #00ff00; }\n"
            + "c:is(.w) { color: #0000ff; }"), "c", "w");

        // 伪元素当部件：`::placeholder` 与旧拼写 `:placeholder` 等价，
        // 部件名对上就命中；`::before` 要生成内容，报永不匹配。
        StyleSheet se = Css.Parse(
            "input::placeholder { color: #ff0000; }\n"
            + "input:placeholder { background: #00ff00; }\n"
            + "p::before { color: #0000ff; }");
        Program.SynPart("placeholder", se, "input", "placeholder");
        Program.SynPart("input base", se, "input", "");
        Program.SynPart("before", se, "p", "");

        // `@supports`：值级判定（`display: grid` 引擎没有，守卫为假）、
        // `not` / `and` / `or`，以及守卫假时按条件原文报出。
        StyleSheet sap = Css.Parse(
            "@supports (display: flex) { a { color: #ff0000; } }\n"
            + "@supports (display: grid) { b { color: #00ff00; } }\n"
            + "@supports not (display: grid) { c { color: #0000ff; } }\n"
            + "@supports (display: flex) and (position: sticky) { d { color: #111111; } }\n"
            + "@supports (display: grid) or (position: absolute) { e { color: #222222; } }\n"
            + "f { color: #333333; }");
        List<string> sapSel = new List<string>{ "a", "b", "c", "d", "e", "f" };
        for (int i = 0; i < sapSel.Count; i = i + 1) {
            StyleBox x = new StyleBox();
            sap.ApplyBox(x, sapSel[i]);
            Console.WriteLine($"syn supports {sapSel[i]} fg {x.fg}");
        }
        List<string> sapLint = sap.Audit();
        Console.WriteLine($"syn supports skipped {sap.atGuardsSkipped}"
                          + $" dropped {sap.atRulesDropped}");
        for (int i = 0; i < sapLint.Count; i = i + 1) {
            Console.WriteLine($"syn supports lint {sapLint[i]}");
        }

        // `@layer` 摊平：没有层叠优先级模型，按文档顺序应用（近似），
        // 但不整块丢弃（含匿名层）。
        StyleSheet sl = Css.Parse(
            "@layer base { a { color: #ff0000; } }\n"
            + "@layer theme { a { color: #00ff00; } }\n"
            + "a { background: #0000ff; }\n"
            + "@layer { b { padding-left: 7; } }");
        StyleBox layA = new StyleBox();
        sl.ApplyBox(layA, "a");
        StyleBox layB = new StyleBox();
        sl.ApplyBox(layB, "b");
        Console.WriteLine($"syn layer a.fg {layA.fg} a.bg {layA.bg}"
                          + $" b.padL {layB.padL} dropped {sl.atRulesDropped}");

        // Lint：永不匹配的选择器逐条给原因、未解析单位点名，而 `0`
        // 这种没有单位的零值不能误报。
        StyleSheet ln = Css.Parse(
            "li:first-child { color: #ff0000; }\n"
            + "p::before { color: #00ff00; }\n"
            + "tr:nth-child(2n) { color: #0000ff; }\n"
            + "i:not(:first-child) { color: #111111; }\n"
            + "[hidden] { color: #222222; }\n"
            + "input[type=\"text\"] { color: #333333; }\n"
            + ".pct { padding: 50%; font-size: 1.5rem; }\n"
            + ".zero { margin: 0; }\n"
            + "@supports (display: grid) { .g { color: #444444; } }\n"
            + ".fine { color: #555555; }");
        List<string> lnLint = ln.Audit();
        Console.WriteLine($"synlint count {lnLint.Count}");
        for (int i = 0; i < lnLint.Count; i = i + 1) {
            Console.WriteLine($"synlint {lnLint[i]}");
        }
    }

    /// 一条 `ApplyMatch`（universal / `[class]` 这类无类型选择器走真实
    /// 匹配路径；`ApplyBox` 只是按选择器文本查表，测不出选择器模型）。
    static void SynBox(string tag, StyleSheet s, string type, string cls) {
        StyleBox b = new StyleBox();
        s.ApplyMatch(b, type, cls, "", "", Style.SNormal());
        Console.WriteLine($"syn {tag} padL {b.padL} marL {b.marL}");
    }

    /// 一条"命中就给 fg 上色"的选择器检查。
    static void SynFg(string tag, StyleSheet s, string type, string cls) {
        StyleBox b = new StyleBox();
        s.ApplyMatch(b, type, cls, "", "", Style.SNormal());
        Console.WriteLine($"syn {tag} [{cls}] fg {b.fg}");
    }

    /// 布尔属性 / 状态位映射检查。
    static void SynAttr(string tag, StyleSheet s, int bits) {
        StyleBox b = new StyleBox();
        s.ApplyMatch(b, "button", "", "", "", bits);
        Console.WriteLine($"syn attr {tag} opacity {b.opacity} padL {b.padL}"
                          + $" padR {b.padR} padT {b.padT}");
    }

    /// `:not()`/`:is()`/`:where()` 的三种目标属性一起看。
    static void SynPick(string tag, StyleSheet s, string type, string cls) {
        StyleBox b = new StyleBox();
        s.ApplyMatch(b, type, cls, "", "", Style.SNormal());
        Console.WriteLine($"syn {tag} fg {b.fg} bg {b.bg} accent {b.accent}");
    }

    /// 部件选择器检查（`part` 走 ApplyMatch 的 part 参数）。
    static void SynPart(string tag, StyleSheet s, string type, string part) {
        StyleBox b = new StyleBox();
        s.ApplyMatch(b, type, "", "", part, Style.SNormal());
        Console.WriteLine($"syn part {tag} fg {b.fg} bg {b.bg}");
    }

    /// 在给定节点上跑一次真实匹配（带树上下文），打印 fg/accent。
    static void TreeFg(string tag, StyleSheet s, Control node) {
        StyleBox b = new StyleBox();
        s.ApplyMatchCtx(b, node.StyleType(), node.Class, node.name, "",
                        Style.SNormal(), node);
        Console.WriteLine($"tree {tag} fg {b.fg} accent {b.accent}");
    }

    /// 组合器链与结构性伪类的树上下文匹配。手工搭一棵 retained 树，
    /// 用 ApplyMatchCtx（真实匹配路径，非按文本查表）逐节点看声明：
    /// 子/后代/相邻兄弟/通用兄弟组合器、:first/:last/:only/:nth(-of-type)、
    /// :not(:first-child)、:empty；:has 仍不可判定（Lint 报 never）。
    static void TreeCtx() {
        StyleSheet s = Css.Parse(
            "panel > title { color: #111111; }\n"
            + "panel badge { color: #222222; }\n"
            + "row > label + label { color: #333333; }\n"
            + "row > label ~ label { accent-color: #444444; }\n"
            + "list > item:first-child { color: #555555; }\n"
            + "list > item:last-child { color: #666666; }\n"
            + "list > item:nth-child(2) { color: #777777; }\n"
            + "list > item:nth-child(odd) { background: #888888; }\n"
            + "grid > button:nth-of-type(2n) { color: #999999; }\n"
            + ".cards > card:only-child { color: #aaaaaa; }\n"
            + "panel:has(title) { color: #bbbbbb; }\n"
            + "grid > label:not(:first-child) { accent-color: #cccccc; }\n"
            + "box:empty { color: #dddddd; }\n");
        TNode panel = new TNode();
        panel.InitTNode("panel", "p1");
        TNode title = new TNode();
        title.InitTNode("title", "t");
        panel.Add(title);
        TNode row = new TNode();
        row.InitTNode("row", "r");
        panel.Add(row);
        TNode labelA = new TNode();
        labelA.InitTNode("label", "la");
        TNode labelB = new TNode();
        labelB.InitTNode("label", "lb");
        TNode badge = new TNode();
        badge.InitTNode("badge", "bg");
        row.Add(labelA);
        row.Add(labelB);
        row.Add(badge);
        TNode list = new TNode();
        list.InitTNode("list", "l");
        panel.Add(list);
        TNode item1 = new TNode();
        item1.InitTNode("item", "i1");
        TNode item2 = new TNode();
        item2.InitTNode("item", "i2");
        TNode item3 = new TNode();
        item3.InitTNode("item", "i3");
        list.Add(item1);
        list.Add(item2);
        list.Add(item3);
        TNode grid = new TNode();
        grid.InitTNode("grid", "g");
        panel.Add(grid);
        TNode btn1 = new TNode();
        btn1.InitTNode("button", "b1");
        TNode glabel1 = new TNode();
        glabel1.InitTNode("label", "gl1");
        TNode btn2 = new TNode();
        btn2.InitTNode("button", "b2");
        TNode glabel2 = new TNode();
        glabel2.InitTNode("label", "gl2");
        grid.Add(btn1);
        grid.Add(glabel1);
        grid.Add(btn2);
        grid.Add(glabel2);
        TNode cards = new TNode();
        cards.InitTNode("cards", "cs");
        cards.Class = "cards";
        panel.Add(cards);
        TNode card = new TNode();
        card.InitTNode("card", "c");
        cards.Add(card);
        TNode box = new TNode();
        box.InitTNode("box", "bx");

        // panel > title：直接子代命中；后代规则 `panel badge` 对
        // 隔代的 badge 也命中（panel > badge 才不该命中）。
        Program.TreeFg("title", s, title);
        Program.TreeFg("badge-descendant", s, badge);
        // 相邻兄弟只打 labelB（前面紧邻一个 label）；通用兄弟打
        // labelA 之后的 label，accent 落在 labelB。
        Program.TreeFg("label-a", s, labelA);
        Program.TreeFg("label-b", s, labelB);
        // 结构性：first/last/nth(2)/nth(odd)。item1 同时吃到
        // first-child(fg) 与 odd(bg)；item3 只有 odd 的 bg。
        Program.TreeFg("item1", s, item1);
        Program.TreeFg("item2", s, item2);
        Program.TreeFg("item3", s, item3);
        // nth-of-type 按类型计数：第 2 个 button 命中，第 1 个不命中；
        // not(:first-child) 的 accent 打到两个 grid label。
        Program.TreeFg("btn1", s, btn1);
        Program.TreeFg("btn2", s, btn2);
        Program.TreeFg("grid-label1", s, glabel1);
        Program.TreeFg("grid-label2", s, glabel2);
        // only-child / empty（box 没挂到树上也算：无父即无子）。
        Program.TreeFg("card-only", s, card);
        Program.TreeFg("box-empty", s, box);
        // :has() 不可判定：panel 不得被上色，Lint 报 never。
        Program.TreeFg("panel-no-has", s, panel);
        List<string> relLint = s.Audit();
        Console.WriteLine($"tree lint count {relLint.Count}");
        for (int i = 0; i < relLint.Count; i = i + 1) {
            Console.WriteLine($"tree lint   {relLint[i]}");
        }

        // 无树上下文（immediate 路径）：组合器/结构性选择器不命中，
        // 同一张表里普通规则照常。
        StyleBox flat = new StyleBox();
        s.ApplyMatch(flat, "title", "", "", "", Style.SNormal());
        Console.WriteLine($"tree flat-title fg {flat.fg}");
    }

    /// 长度单位与数学函数：em/rem/pt/vw/vh、ex/ch、
    /// calc/min/max/clamp（可嵌套）、厂商前缀剥离。环境基准用
    /// 裸盒缺省（根字号 16、视口 1280x720），预期值写在注释里。
    static void Units() {
        StyleSheet u = Css.Parse(
            "a { padding-left: 1.5em; padding-top: 2rem; margin-left: 12pt;\n"
            + "    width: 50vw; height: 25vh; }\n"
            + "b { font-size: 20px; padding-right: calc(10px + 2em);\n"
            + "    margin-top: clamp(10px, 5vw, 30px);\n"
            + "    margin-bottom: min(40px, 3vw);\n"
            + "    max-width: max(100px, 20vw); }\n"
            + "c { font-size: 1.25em; letter-spacing: 0.5ch; }\n"
            + "d { -webkit-box-shadow: 1 2 3 #ff0000;\n"
            + "    -moz-transform: rotate(45deg);\n"
            + "    -webkit-transition: 120ms; }\n"
            + "e { padding: calc(2 * (6px + 4px)) 1pc; }\n");
        // a：1.5em 在 font-size 未声明时用根字号 16 -> 24；2rem -> 32；
        //    12pt 按 96dpi -> 16；50vw -> 640；25vh -> 180。
        StyleBox ua = new StyleBox();
        u.ApplyBox(ua, "a");
        Console.WriteLine($"units a padL {ua.padL} padT {ua.padT} marL {ua.marL}"
                          + $" w {ua.width} h {ua.height}");
        // b：font 20；calc(10 + 2em=40) -> 50；clamp(10, 5vw=64, 30) -> 30；
        //    min(40, 3vw=38) -> 38；max(100, 20vw=256) -> 256。
        StyleBox ub = new StyleBox();
        u.ApplyBox(ub, "b");
        Console.WriteLine($"units b font {ub.fontPx} padR {ub.padR} marT {ub.marT}"
                          + $" marB {ub.marB} maxW {ub.maxW}");
        // c：1.25em -> 20；0.5ch 近似半字高 -> 4。
        StyleBox uc = new StyleBox();
        u.ApplyBox(uc, "c");
        Console.WriteLine($"units c font {uc.fontPx} spacing {uc.letterSpacing}");
        // d：前缀剥离后与无前缀声明同义。
        StyleBox ud = new StyleBox();
        u.ApplyBox(ud, "d");
        Console.WriteLine($"units d shadow {ud.shadow} dx {ud.shadowDx}"
                          + $" dy {ud.shadowDy} blur {ud.shadowBlur}"
                          + $" rot {ud.rotate} trans {ud.transitionMs}");
        // e：calc 嵌套括号与乘法 -> 20；1pc = 16px。
        StyleBox ue = new StyleBox();
        u.ApplyBox(ue, "e");
        Console.WriteLine($"units e padT {ue.padT} padR {ue.padR}");
        List<string> uLint = u.Audit();
        Console.WriteLine($"units lint count {uLint.Count}");
        for (int i = 0; i < uLint.Count; i = i + 1) {
            Console.WriteLine($"units lint   {uLint[i]}");
        }
    }

    /// @media 条件求值：min/max-width、暗色与减弱动效偏好、
    /// orientation、逗号（或）、not all and、Level 4 范围语法
    /// （`width >= n`、`n <= width <= m`）。媒体环境就是正在填充
    /// 的 StyleBox（envVw/envVh/mediaDark/mediaReduced），测试直接
    /// 改字段驱动不同设备画像；裸盒缺省 1280x720、亮色。
    static void Media() {
        StyleSheet s = Css.Parse(
            ".m { color: #010101; }\n"
            + "@media (min-width: 800px) { .m { color: #020202; } }\n"
            + "@media (max-width: 640px) { .m { letter-spacing: 4px; } }\n"
            + "@media (prefers-color-scheme: dark) { .m { accent-color: #040404; } }\n"
            + "@media (prefers-reduced-motion: reduce) { .m { background: #050505; } }\n"
            + "@media (orientation: landscape) { .m { border-top: 3px solid #060606; } }\n"
            + "@media (max-width: 100px), (min-width: 2000px) { .m { color: #070707; } }\n"
            + "@media not all and (min-width: 500px) { .m { margin-left: 8px; } }\n"
            + "@media not all and (min-width: 5000px) { .m { margin-right: 9px; } }\n"
            + "@media (width >= 800px) { .m { margin-top: 11px; } }\n"
            + "@media (400px <= width <= 2000px) { .m { margin-bottom: 12px; } }\n");
        // 宽视口亮色：min-width 命中（逗号那条两支都假，not 500px
        // 也假），Level 4 两条都命中。
        StyleBox wide = new StyleBox();
        s.ApplyMatchCtx(wide, "box", "m", "", "", Style.SNormal(), null);
        Console.WriteLine($"media wide fg {wide.fg} marL {wide.marL}"
                          + $" marR {wide.marR} marT {wide.marT}"
                          + $" marB {wide.marB} borderTop {wide.borderTopW}"
                          + $" accent {wide.accent} spacing {wide.letterSpacing}");
        // 窄视口：min-width 掉、max-width 640 命中、not 500px 命中。
        StyleBox narrow = new StyleBox();
        narrow.envVw = 500;
        s.ApplyMatchCtx(narrow, "box", "m", "", "", Style.SNormal(), null);
        Console.WriteLine($"media narrow fg {narrow.fg} spacing {narrow.letterSpacing}"
                          + $" marL {narrow.marL} marT {narrow.marT}");
        // 暗色 + 减弱动效：偏好特征命中。
        StyleBox dark = new StyleBox();
        dark.mediaDark = true;
        dark.mediaReduced = true;
        s.ApplyMatchCtx(dark, "box", "m", "", "", Style.SNormal(), null);
        Console.WriteLine($"media dark accent {dark.accent} bg {dark.bg}");
        List<string> mLint = s.Audit();
        Console.WriteLine($"media lint count {mLint.Count}");
        for (int i = 0; i < mLint.Count; i = i + 1) {
            Console.WriteLine($"media lint   {mLint[i]}");
        }
    }

    static int FgOf(StyleSheet s, string cls) {
        StyleBox b = new StyleBox();
        s.ApplyMatch(b, "box", cls, "", "", Style.SNormal());
        return b.fg;
    }

    static int CountOf(string s, string sub) {
        int n = 0;
        int i = Css.IndexFrom(s, sub, 0);
        while (i >= 0) {
            n = n + 1;
            i = Css.IndexFrom(s, sub, i + sub.Length);
        }
        return n;
    }

    /// @import 展开：相对路径按引入者目录解析、url() 形式、
    /// 媒体限定导入（screen 保留 / print 丢弃）、环检测断链、
    /// 缺文件静默跳过、跨目录多层链。临时文件建在系统临时
    /// 目录，内容固定，可重复运行；断言走解析后的盒值，
    /// 输出不含路径，golden 稳定。
    static void Imports() {
        string dir = Path.GetTempPath() + "zan_css_imp_test";
        if (Directory.Exists(dir)) { Directory.Delete(dir); }
        Directory.CreateDirectory(dir);
        Directory.CreateDirectory(dir + "/nested");
        File.WriteAllText(dir + "/main.css",
            "@import \"tokens.css\";\n"
            + "@import url(\"nested/deep.css\");\n"
            + "@import \"loop1.css\";\n"
            + "@import \"gone.css\";\n"
            + "@import \"media.css\" screen;\n"
            + "@import \"media.css\" print;\n"
            + ".root { color: #101010; }\n");
        File.WriteAllText(dir + "/tokens.css",
            ":root { --imp-accent: #202020; }\n.tok { color: #303030; }\n");
        File.WriteAllText(dir + "/extra.css", ".extra { color: #505050; }\n");
        File.WriteAllText(dir + "/nested/deep.css",
            "@import \"../extra.css\";\n.deep { color: #404040; }\n");
        File.WriteAllText(dir + "/loop1.css",
            "@import \"loop2.css\";\n.l1 { color: #606060; }\n");
        File.WriteAllText(dir + "/loop2.css",
            "@import \"loop1.css\";\n.l2 { color: #707070; }\n");
        File.WriteAllText(dir + "/media.css", ".mq { color: #808080; }\n");
        string text = Css.ResolveImports(dir + "/main.css");
        StyleSheet s = Css.Parse(text);
        Console.WriteLine($"imports tok fg {Program.FgOf(s, "tok")}"
                          + $" extra {Program.FgOf(s, "extra")}"
                          + $" deep {Program.FgOf(s, "deep")}"
                          + $" l1 {Program.FgOf(s, "l1")}"
                          + $" l2 {Program.FgOf(s, "l2")}"
                          + $" mq {Program.FgOf(s, "mq")}"
                          + $" root {Program.FgOf(s, "root")}");
        // 不存在的文件静默跳过；print 限定的导入被丢弃。
        Console.WriteLine($"imports var {s.Var("--imp-accent")}"
                          + $" gone {Program.FgOf(s, "gone")}");
        Console.WriteLine($"imports leftover {Css.IndexOfImport(text) >= 0}"
                          + $" mq-once {Program.CountOf(text, ".mq") == 1}"
                          + $" l1-once {Program.CountOf(text, ".l1") == 1}");
    }

    /// 现代颜色与 currentColor：空格语法 rgb()/hsl() + 斜杠 alpha、
    /// hue 单位、hwb()、oklab()/oklch()、lab()/lch()、color-mix()、
    /// currentColor 记账（级联后替换成最终前景色）、cursor 近似映射。
    /// 白/黑/灰锚点的预期值写在注释里。
    static void Modern() {
        StyleSheet s = Css.Parse(
            "a { color: rgb(0 128 255); }\n"
            + "b { color: rgb(0 128 255 / 50%); }\n"
            + "c { color: hsl(120 50% 50% / 0.5); }\n"
            + "d { color: hsl(120, 50%, 50%); }\n"
            + "e { color: hsl(0.5turn 50% 50%); }\n"
            + "f { color: hwb(0 0% 0%); }\n"
            + "g { color: hwb(120 20% 20%); }\n"
            + "h { color: oklab(1 0 0); }\n"
            + "i { color: oklch(0.5 0 0); }\n"
            + "j { color: lab(100 0 0); }\n"
            + "k { color: lch(50 60 40); }\n"
            + "l { color: color-mix(in srgb, red, blue); }\n"
            + "m { color: color-mix(in srgb, red 25%, blue); }\n"
            + "n { color: #123456; background: currentcolor;\n"
            + "    border-top-color: currentColor; accent-color: currentcolor; }\n"
            + "o { color: currentColor; }\n"
            + "p { cursor: grabbing; }\n"
            + "q { cursor: e-resize; }\n"
            + "r { cursor: n-resize; }\n"
            + "s { cursor: crosshair; }\n");
        string[] names = new string[19];
        names[0] = "a"; names[1] = "b"; names[2] = "c"; names[3] = "d";
        names[4] = "e"; names[5] = "f"; names[6] = "g"; names[7] = "h";
        names[8] = "i"; names[9] = "j"; names[10] = "k"; names[11] = "l";
        names[12] = "m"; names[13] = "n"; names[14] = "o"; names[15] = "p";
        names[16] = "q"; names[17] = "r"; names[18] = "s";
        for (int i = 0; i < 19; i = i + 1) {
            StyleBox b = new StyleBox();
            s.ApplyBox(b, names[i]);
            Console.WriteLine($"modern {names[i]} fg {b.fg} bg {b.bg}"
                              + $" borderTop {b.borderTopColor}"
                              + $" accent {b.accent} cursor {b.cursor}");
        }
        List<string> sLint = s.Audit();
        Console.WriteLine($"modern lint count {sLint.Count}");
        for (int i = 0; i < sLint.Count; i = i + 1) {
            Console.WriteLine($"modern lint   {sLint[i]}");
        }
    }

    static void Main() {
        StyleSheet sheet = Program.Sheet();
        string accent = sheet.Var("--accent");
        string radius = sheet.Var("--radius");
        Console.WriteLine($"empty {sheet.IsEmpty()}");
        Console.WriteLine($"var accent {accent} radius {radius}");
        Program.Box(sheet, "button");
        Program.Box(sheet, "tab.item");
        Program.Box(sheet, "button.primary");
        Program.Box(sheet, "button.primary:hover");
        Program.Box(sheet, "button.primary:disabled");
        Program.Box(sheet, "card");
        Program.Box(sheet, "input:focus");

        Console.WriteLine("--- blend");
        StyleBox from = new StyleBox();
        sheet.ApplyBox(from, "button.primary");
        StyleBox to = new StyleBox();
        sheet.ApplyBox(to, "button.primary:hover");
        StyleBox mid = StyleBox.Blend(from, to, 500);
        Console.WriteLine($"mid bg {mid.bg} opacity {mid.opacity}");

        Console.WriteLine("--- parts");
        Program.Part(sheet, Style.SNormal(), "base");
        Program.Part(sheet, Style.SHover(), "hover");
        Program.Part(sheet, Style.SSelected(), "selected");

        Console.WriteLine("--- checked parts");
        Program.CheckedPart(sheet, Style.SNormal(), "base");
        Program.CheckedPart(sheet, Style.SHover(), "hover");
        Program.CheckedPart(sheet, Style.SChecked(), "checked");
        int checkedHover = Style.SChecked() + Style.SHover();
        Program.CheckedPart(sheet, checkedHover, "checked-hover");
        StyleBox checkedOff = new StyleBox();
        Style.ApplyPartSheet(sheet, checkedOff, "checkbox", "box", "", "",
                             Style.SNormal());
        StyleBox checkedOn = new StyleBox();
        Style.ApplyPartSheet(sheet, checkedOn, "checkbox", "box", "", "",
                             Style.SChecked());
        StyleBox checkedMid = StyleBox.Blend(checkedOff, checkedOn, 500);
        Console.WriteLine($"checked mid pad {checkedMid.padL}");

        Console.WriteLine("--- external vars");
        Program.ExternalVars();

        Console.WriteLine("--- range parts");
        Program.RangePart(sheet, "slider", "track", "");
        Program.RangePart(sheet, "slider", "fill", "");
        Program.RangePart(sheet, "slider", "thumb", "");
        Program.RangePart(sheet, "progress", "fill", "success");
        Program.RangePart(sheet, "progress", "label", "");

        Console.WriteLine("--- gradient dirs");
        Program.GradDirs();

        Console.WriteLine("--- theme tokens");
        Program.ThemeTokens();

        Console.WriteLine("--- button states");
        Program.ButtonStates();

        Console.WriteLine("--- multi state");
        Program.MultiState();

        Console.WriteLine("--- tab states");
        Program.TabStates();

        Console.WriteLine("--- form states");
        Program.FormStates();

        Console.WriteLine("--- A3 states");
        Program.A3States();

        Console.WriteLine("--- A4 states");
        Program.A4States();

        Console.WriteLine("--- A5 states");
        Program.A5States();
        Program.A6States();

        Console.WriteLine("--- overlay states");
        Program.OverlayStates();

        Console.WriteLine("--- layout longhands");
        Program.Longhand();
        Program.FontSources();

        Console.WriteLine("--- coercion (at-rules / var fallback / important / border style / text-shadow / to-left / lint)");
        Program.Coercion();

        Console.WriteLine("--- css syntax (selectors / attribute operators / not-is-where / parts / supports / layer / lint)");
        Program.Syntax();

        Console.WriteLine("--- tree context (combinators / structural pseudo-classes)");
        Program.TreeCtx();

        Console.WriteLine("--- units / math / vendor prefixes");
        Program.Units();

        Console.WriteLine("--- media queries");
        Program.Media();

        Console.WriteLine("--- imports");
        Program.Imports();

        Console.WriteLine("--- modern colors / currentColor / cursor");
        Program.Modern();
    }
}

/// 树上下文测试用的参数化节点：Kind 可指定，从而在同一棵测试树里
/// 混出 panel/title/row/item 等不同"元素类型"。
class TNode : Control {
    string kind;
    void InitTNode(string k, string nodeName) {
        this.InitControl(nodeName, 0);
        kind = k;
    }
    override string Kind() { return kind; }
}

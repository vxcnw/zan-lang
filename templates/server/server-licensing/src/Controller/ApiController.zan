using System;
using System.Web;

namespace ZanWeb;

/* /api 下 JSON 端点基类：不向 AppController 添加任何能力，仅表明意图——答 {code,msg,data} 并以状态码拒绝（绝不用重定向到登录表单），两者均由调度器按 [Custom(Authorization=ApiAuth)] 决定。基类树一层深：AppController → AdminController / ApiController。 */
class ApiController : AppController {
}

namespace ZanWeb.Model.Sys;

/* API 可返回的账号字段；序列化 User 会泄露密码，因此通过此类型投影，sys_user 新增列不会意外扩大响应。 */
class UserBrief {
    int id;
    string username;
    string nickname;
    int departmentId;
    int status;
    int isSuper;
}
# 原版「迷你传奇 / 放置传奇」界面台账

> **权威依据**：`mir2/mir/*.png` —— 34 张原版截图，已按玩法命名。
> · 30 张 **1871×971 整窗截图** = **1.5 倍缩放**的整窗图（逻辑尺寸 = 像素 ÷ 1.5）
> · 4 张 **1128×809 局部特写**（`背包-天书/内功/宝石/技能`，看背包下半部细节）
> · `mir2/mir/说明.txt` —— 玩法说明文档（16KB，25 个系统的规则）
> · `mir2/传奇放置挂机游戏-产品设计文档.md` —— 产品设计文档（30KB）
> 其余推演一律以本文件为准。

---

## 一、全局框架（5 分区，逐像素实测）

客户区逻辑尺寸 **1246×646**（窗口按此创建）。

**原图量法（唯一正确口径）**：整窗截图 1871×971 是 **1.5 倍缩放**的整窗图，
**没有黑边** —— 逻辑值 = 截图像素 **÷ 1.5**（1871÷1.5≈1247、971÷1.5≈647，
差 1px 属取整）。三票独立校验：① 尺寸比 1871/1246=1.5016、971/646=1.5028；
② 左栏/主区分界实测 x=374–375 → **250**（= 下表左栏宽）；③ 背包 6×6 格列距
实测 60px → **40**（= §5.8 的 42 格含边）。

> ⚠️ 旧版「左右各 313 黑边、上下各 162/163 黑边」的建模**已作废**：原图四边
> 都是内容（顶边均亮度 64、左边 41），按减偏移量会整体偏 300+ px。

| 分区 | 边界（逻辑） | 说明 |
|------|------|------|
| 顶部导航 | y `35.3..101.3`（**2 行**） | 第 1 行瓦片 y 35.3..66，第 2 行 y 70..101.3 |
| 左栏（角色信息） | x `0..249`，宽 250 | 全高（不含底部导航） |
| 主区（内容） | x `251..1003`，宽 753 | 随页面变化 |
| 聊天区 | x `1004..1245`，宽 242 | 全高（不含底部导航） |
| 底部导航 | y `600.7..631.3`，高 30.7 | **横跨全窗宽**（含聊天区下方） |

### 顶部导航（2 行 × 10 + 右上角设置齿轮）

> **实测修正（本次重测，替代旧描述）**：瓦片**宽度随文字自适应**，
> 不是等宽 100、也不是等间距 108。左起 x=11.3，瓦片间隙 **6**，
> 瓦片高 **30.7**，行间隙 **2.7**（第 1 行底 66 → 第 2 行顶 70）。

```
第1行: 地图传送 PK竞技场 幻境 BOSS之家 押镖 行会 沙巴克 城池争霸 传说塔 排行榜
第2行: 五行修炼 师徒 秘境 世界BOSS 祭坛 寻宝 将星图谱 回收系统 发红包 至尊极品殿堂
右上角: 设置（蓝色齿轮，= navigation.csv id 30）
```

- **右上角是齿轮不是放大镜** —— 旧版记的「观战（放大镜）」不成立：
  该位实测为蓝色齿轮，与既有 `assets/ui/nav/nav-gear.png` 同形；
  因此页 30「设置」的入口就是这个齿轮，并不存在独立的「观战」按钮。
- **当前页瓦片没有高亮** —— 实测当前页（幻境）与非当前页（地图传送/排行榜）
  的底色 `#1f2936`、边框 `#3f4a57`、文字青 `#00aeff` **三项完全一致**；
  旧版「当前页高亮为橙黄色底」不成立，不要画高亮。
- 每格瓦片的实测 x / w 见 `data/navigation.csv` 的 `x`、`w` 列（逻辑值）。

### 底部导航（1 行 × 10，居中，瓦片**紧贴无间隙**）

```
背包 铁匠铺 交易市场 图鉴 爵位 称谓 成就 商店 帮助 邮箱
```
- 实测：瓦片 y `600.7..631.3`，x 从 **253.3** 到 **990**，**相邻紧贴（间隙 0）**；
  宽度随文字变化：2 字 ≈ 69~70、3 字 ≈ 89、4 字 ≈ 89。
  居中基准是**整窗宽**（不是主区宽）——tiles 中心 621.7 ≈ 窗宽 1247 的一半。
- 左端独立：「战斗统计」（带框）+「状态: 空闲中」（绿字），占 x `0..253`
- 右端独立（聊天区内）：提示行 + 输入框 + 「发送」橙钮
- 「发红包」「至尊极品殿堂」「传说塔」「称谓」带 **NEW** 角标

---

## 二、左栏（角色信息，250 宽，所有页面一致）

| 行 | 内容 | 关键规格 |
|----|------|----------|
| 1 | 头像（**方框 88×88**，橙边框）+ 角色名「看见啥的就是」 | 名字黄色粗体 15px |
| 2 | `黄金` / `VIP` 两个徽章（头像右侧竖排） | 黄金=金色底，VIP=紫色底 |
| 3 | `称谓: 无` `行会: 无` | 灰白小字 |
| 4 | `经验: 1852620/363304` + `升级` 按钮 | 数字青色，升级钮橙底 |
| 5 | **经验进度条**（无边框细条） | 高度约 6 |
| 6 | 两列 4 行数值：`生命/威力`、`法力/战功`、`等级/灵符` | 等级显示 `等级: 23级` |
| 7 | `金币: 760355` + `今日福利` / `充值` / `刷新` 三个按钮 | 金币数青色 |
| 8 | `元宝: 0` | 元宝数青色 |
| 9 | **属性汇总**（3~5 行滚动，左侧彩色图标 14px） | 见下 |
| 10 | **信息滚动区**（约 9 行，橙/绿/白混合） | 见下 |

### 属性汇总（格式固定）
```
⛏ x94 强化生命x90 强化防御x82 强
化魔防x89 木剑x326 金币x41420
⛏ 淬炼x17200 经验x172500
```
- 数字全部为**金色**

### 信息滚动区（格式固定）
```
【信息】玩家 「万万」在幻境第 1 层
击杀BOSS获得
【信息】玩家 「新教师」在幻境第 565 层
击杀BOSS获得
【信息】玩家 「李大爷」在幻境第 2001 层
击杀BOSS获得100元宝 x1
```
- `【信息】玩家` 为**橙色**，玩家名带 `「」` 为**黄色**，其余白/绿
- 末尾有**向下箭头**（可滚动指示）

---

## 三、聊天区（242 宽，所有页面一致）

```
[聊天]  [1;31m「海阔天空cdd」...   → 顶部 4 个 tab：聊天 / 【系统】...
[每天]  「海阔天空cdd」: 好呀呀
[每天]  「海阔天空cdd」: 今天好热
...
星期四
[聊天]  「梦幻进行曲」: 来了来了
[聊天]  「梦幻进行曲」: 还没得啥
...
```
- 频道前缀 `[每天]` `[聊天]` `[备]` 为**黄色方括号**
- 玩家名 `「xxx」` 为**黄色**，冒号后正文**白色**
- 正文内含 `\e[1;31m` 之类 **ANSI 转义未解析**（原版缺陷；我们按正确方式解析）
- 底部：**输入框**（横跨，高约 28）+ **发送**按钮（橙底，宽约 60）

---

## 四、35 页逐页清单

### 4.0 地图内页（从地图列表点图卡进入）⭐ 核心页
**文件**：`地图-进入.png`（「进入」指从地图传送列表点图卡**进入这张图**后的内页，
不是开机首页——开机首页是页 0「地图传送」）。

入口语义（原版）：地图传送页点一张地图卡 → `move` 到该图 → 打开这张图的内页
（BOSS 场景 + 挑战面板 + 战斗日志 + 底部控件）。

| 区域 | 内容 |
|------|------|
| 左上角标签 | `首次击杀本关BOSS通关`（**橙黄底黑字**）；行刑倒计时 `420/420` |
| 主体 | **打怪场景**：地图网格 + 怪物/NPC 名字气泡（`多钩猫` `钉耙猫` `稻草人` `毒蛇`）+ 玩家位置气泡（黄色，给自己名字） |
| 右下角 | 玩家人物立绘 + 血条 `420/420` |
| 右栏（场景右） | `首次击杀本关BOSS通关` 标题 + `挑战BOSS(剩余0次)` 橙钮 + `扫荡` + 战斗属性：**气血/等级/攻击/防御/魔法/魔防/道术/躲避/幸运** |
| 右下 | ☑ `结束后自动挑战BOSS(勾选生效)` |
| 左下 | **战斗日志**（约 6 行）：`【提示】钉耙猫 死亡，清理战场，继续探索…`（白）/`【信息】稻草人 出现在 (X:357, Y:190)，接近目标…`（黄）/`【战斗】正在战斗中…`（红） |
| 底部控件 | `返回` `开始挂机后，可以关闭客户端，离线挂机→` `开始挂机` `说明` |

> 重建为页 31（`Navigate(31)`），由 PageMap 的地图卡点击触发。

### 4.1 顶部导航第 1 行

| # | 按钮 | 主区布局 | 原图 |
|---|------|----------|------|
| 0 | **地图传送** | **4×3 大卡阵**（160×70 卡片，图名压卡片上方居中）+ 左下 `[1][2]` 页码方块 | `地图传送`(旧 c063c06a) |
| 1 | **PK竞技场** | **4×4 头像阵**（约 100×120，头像+排名+挑战钮）+ 左下系统日志 + 右下我的排名/挑战次数/累计战功 | `竞技场.png` |
| 2 | **幻境** | 左：怪物场景大图 + 头像血量条；右：排行列表（名+层数）；底：掉落格 + 自动/挑战幻境/扫荡/说明 | `幻境.png` |
| 3 | **BOSS之家** | 左：BOSS 大图 + 属性栏；右：编号/BOSS名称/等级 表格（12 行）；底：掉落格 + 自动/挑战BOSS | `BOSS之家.png` |
| 4 | **押镖** | 左：镖车大图（人拉车）+ 剩余次数；右：编号/镖车信息 表格 + 押镖钮 | `押镖.png` |
| 5 | **行会** | 表格：排名/行会名称/等级/成员数/会长/行会物资/限制等级/申请 + 底部创建行会 | `行会.png` |
| 6 | **沙巴克** | 左：沙巴克场景 + 占领信息 + 奖励图标；右：编号/行会名称/申请日期 表格 | `沙巴克.png` |
| 7 | **城池争霸** | **4×3 城战卡阵**（城名+城宝金钱+产出+攻占城池+城主+日期）+ 底部说明 | `城池争夺.png` |
| 8 | **传说塔** | 左：传说塔大图（五层）+ 当前/我的层数；右：4×4 头像阵（挑战） | `传说塔.png` |
| 9 | **排行榜** | 顶部 6 个 tab（等级/攻击/魔法/道术/称谓/金币）+ 左：人物大图 + 右：排名表格 | `排行榜.png` |
| — | **观战**（右上角） | **缺图** ⚠️ | — |

### 4.2 顶部导航第 2 行

| # | 按钮 | 主区布局 | 原图 |
|---|------|----------|------|
| 10 | **五行修炼** | 中央金色五角大阵（金木水火土）+ 右侧 5 行升级/注入 + 底部修炼信息 | `五星修炼.png` |
| 11 | **师徒** | 左上：输入对方名称/绑定师徒关系；左：师徒名称/等级/成员/时间表 + 今日活跃度；右：结伴名单 | `师徒.png` |
| 12 | **秘境** | 左：怪物场景大图 + 头像血量条；右：排行列表；底：掉落格 + 自动/挑战秘境/扫荡/说明 | `秘境.png` |
| 13 | **世界BOSS** | 左：大 BOSS 图（等级69）+ 说明；右：编号列表（10行，名+伤害数）；底：掉落格 + 自动/挑战BOSS | `世界BOSS.png` |
| 14 | **祭坛** | 左：圣坛之火大图 + 捐献表；中：金条/金砖/天书/一级宝石 进度；右：物品列表 + 批量捐献 | `祭坛.png` |
| 15 | **寻宝** | 左：寻宝大图（龙壁）+ `开始寻宝` + 自动寻宝；右：3 条规则 + 寻宝图来源 | `寻宝.png` |
| 16 | **将星图谱** | 左：tab 列表（古代名女/上古神剑/十大神器/三国演义/英雄联盟/水浒传/王者荣耀/待开放）；右：**5×2 卡阵**（角色图+气血天赋+x%+价格+激活） | `将星图谱.png` |
| 17 | **回收系统** | 左：分类列表（金币/经验/战功/声望/红包/元宝/积分）；右：道具/回收价格 表格 + 数量 + 最大/回收 | `回收系统.png` |
| 18 | **发红包** | 顶部：元宝/金币/推广币/灵符 余额 + 激活码；主体：**奖励卡阵**（改名卡/大喇叭…价格+购买） | `商店.png` |
| 19 | **至尊极品殿堂** | 主区：**5×3 装备卡阵**（武器之攻/之法/剑之盾/头盔之智/手镯之敏/戒指之灵…+ 登记录入 + 主人/属性 + 复制费） | `至尊极品宝鉴.png` |

### 4.3 底部导航

| # | 按钮 | 主区布局 | 原图 |
|---|------|----------|------|
| 20 | **背包** | 见 4.4 详解 | `背包.png` + 4 张特写 |
| 21 | **铁匠铺** | 左：铁匠铺大图 + 开始锻造 + 功能列表（诅咒/隐藏/升级/法宝/百炼/血之诅咒/开光/升级/转移/传说/技能）；右：背包格 + 洗练；下：金币洗练/其他洗练 说明 | `铁匠铺.png` |
| 22 | **交易市场** | 左：物品分类 tab（全部/武器/防具/首饰/消耗类/杂物类/材料类/符文）+ 过滤条件 + 搜索；右：表格（名称/价格/寄售人/加密/时长/操作） | `交易市场.png` |
| 23 | **图鉴** | 左：图鉴分类列表（图鉴：祈祷/沃玛/祖玛/圣战/法神/天尊）；右：**3 列卡阵**（装备格+属性）；右栏：神兵图鉴 7 条说明 | `图鉴.png` |
| 24 | **爵位** | 表格：爵位/玩家/累计贡献/气血/法力/攻击/魔法/道术/防御/魔防/速度/经验加成 + 底部元宝/捐献 | `爵位.png` |
| 25 | **称谓** | 表格：称谓/需要声望/需要金钱/等级 → 1级→5级→10级→20级→30级→40级→50级 + 领取钮 | `称谓.png` |
| 26 | **成就** | 左：成就分类列表；右：成就说明 + 进度 + 领取 + 奖励图标（多列） | `成就.png` |
| 27 | **商店** | 左：商店分类；右：**3 列商品卡阵**（图标+数量+价格+购买） | 与 `商店.png` 交叉（见注） |
| 28 | **帮助** | 左：**垂直 tab**（新手帮助/常见问题/进阶玩法/公式算法/地图BOSS/交易权限/传说塔/红包/符文之语 NEW/会员介绍）；右：内容区 | `帮助.png` |
| 29 | **邮箱** | 左：邮件标题/发件人/发送日期 + 删除 + 正文区 + 附件格；右：编号/邮件标题 列表 | `邮箱.png` |
| 30 | **设置** | 未在导航中，但截图存在（偏好设置/挂机设置/改名换姓/其他设置/界面风格/仓库命名/清空邮件） | `设置.png` |

> **注**：`商店.png` 与 `发红包` 页布局高度相似（都是余额条 + 卡阵 + 购买钮）。需再确认「发红包」页是否为独立页；原版第 2 行第 9 格标注为「发红包」，但图中标题区显示的是充值项。**存疑，重建时按余额条+卡阵通用组件实现**。

### 4.4 背包页详解（页 20）⭐

整页分 **上 / 下** 两段，中间一条分隔线。

**上段**：
| 区域 | 内容 |
|------|------|
| 左 | 人物立绘（约 235×300，站在**装备栏中间**）：左列 3 格（项链/手镯/戒指）+ 右列 3 格（勋章/宝石/腰带）+ 底部 6 格（靴子等） |
| 中 | **6×6 = 36 格物品栏**（格 42×42，深底金边，数量角标左上） |
| 中左竖排按钮 | `切换仓库` `入库(F)` `加锁` `使用(A)` `整理` `放入图鉴` `仓库3` `一键熔炼` `卖出(X)`（宽约 76，高 30） |

**下段**：顶部 6 个平铺 tab
```
取出 | 说明 | 天书 | 内功 | 宝石 | 技能
```
- **取出**（默认）：2 行 × 5 格 + 每格下方「取出」钮
- **天书**：2 行 × 5 格 + 每格「取出」钮；右侧属性面板 `攻击/+0%` `魔法/+0%` `道术/+0%` `防御/+0%` `魔防/+0%` `负重/0/0`
- **内功**：左：**内功人物立绘**（盘坐，紫光）；右：2 列 × 5 行 → `内功: 金钟罩` / `等级: 0` / `效果: +0%防御` / `战功: 2.0000` / `金币: 1000.0000` + `升级` 钮（第二列同构：`铁布衫`）
- **宝石**：2 列 × 4 行，每行 = **装备类型图标**（剑/甲/帽/鞋 与 项链/手镯/戒指/另） + **4 格锁位**（未镶嵌显示 🔒）
- **技能**：2 列 × 4 行 → 每项 = 技能图标 + `强化攻击(0)` + 进度条 `106/250` + `Lv.2` `TP:0` + `升级` 钮

**下段左侧**（固定，不随 tab 变）：
- 顶部 `法师(3)` 标签 + `创建` `创建` 两个钮（**这是多角色槽位**）
- **职业选择**：`战士`（剑）`法师`（法杖）`道士`（符）三个图标
- **性别选择**：`男` `女` 两个图标
- `创建英雄` `取消` 两个按钮

> ⚠️ 意外发现：背包页左侧含**创建角色**功能。与登录流程里的建角页可能是同一个组件。

---

## 五、通用组件规格（复用于所有页面）

### 5.1 主区内容框
```css
position: relative;
background: #0b0e14 ~ #10141c;   /* 近黑，带轻微蓝 */
border: 1px solid #2a3348;
```

### 5.2 Tab
- 竖排：宽 **86**，高 30，间隔 2
- 横排（背包下段）：宽约 **76**，高 26
- 选中态：底色 `#1a3a5c`，**浅蓝描边**
- 文字：`#a8b8d0`，选中 `#ffffff`

### 5.3 数据表格（称谓精测；爵位 / 排行榜 / 图鉴 / 交易市场 / 回收系统共用）
几何单一真相源 = `Spec.Tbl*`，共享组件 = `src/TableUi.zan`。

| 量 | 实测（设备像素） | 逻辑 → `Spec` |
|----|------------------|---------------|
| 容器 | x **374..1490**（页内容满宽），上缘 y **164** = 页内容顶 | 上/左留白 0 |
| 容器底 / 外框 | `#1d2633` / 1 设备 `#3d4756` | `.tbl-box` |
| 表头带 | y **167..207**（高 41），底色 `#103555`，文字 `#00a4e9` 居中 | `TblHeadH = Px(27)` |
| 表头带宽度 | **满宽**（374..1489）；带内文字按数据列栅格、左缩 12 | 内缩走 `TblPadL` |
| 数据行 | 行高 **45**、行缝 **4.5**（节距 **49.5**），格子底 `#121f2d` | `TblRowH = Px(30)`、`TblRowGap = Px(3)` |
| 行顶 / 末行 | 214 / 759（12 行）；容器底框 809..811 | 12 行 |
| 列宽（称谓） | **131 / 123 / 142 / 102 / 93×5 / 100**，列缝 4.5 | `TblNameCols()` |
| 首列左缘 | **386**（容器左缘 374 + 12） | `TblPadL = Px(8)` |
| 页脚 | 钮 y **829..859**（高 30，距表底 18）；左「当前称谓：无」x 392..518 | `TblFootGap = Px(12)` |
| 高亮行（自己） | 整行底 `#570d41`；**连格缝也是紫红**（格子必须透明） | `.tbl-row-hl` / `.tbl-cell-hl` |

逐列文字色（原版实测；属性列值为 `0 - 0` 时**一律转灰** `#6a6a6a`）：

| 列 | 色 | 列 | 色 |
|----|----|----|----|
| 称谓 | `#eb7600` 橙 | 攻击 | `#d4436a` 品红 |
| 需要声望 | `#dcdcdc` 白 | 魔法 | `#0071d4` 蓝 |
| 需要金钱 | `#ebeb00` 黄 | 道术 | `#00e000` 绿 |
| 等级 | `#00d4d4` 青 | 防御 | `#d4d400` 黄 |
| 领取钮 | 框 `#3f4956` / 底 `#1f2936` / 字 `#089cf3` | 魔防 | `#d400d4` 紫 |

字号：表头 **13** 逻辑（字高 19 设备）、数据 **12**（字高 18）。

行内点号：第一列是 `筑基·一阶` 的**间隔号 U+00B7**（不是下点、不是空格），
字符集里有，直接写。

#### 5.3b 爵位（页 24）的差异点

同一套 `TableUi`，只换列宽表与页脚左组：

| 量 | 实测（设备） | 逻辑 → `Spec` |
|----|--------------|---------------|
| 列宽（12 列） | **92 / 170 / 96 / 70 / 69 / 70 / 69 / 70 / 69 / 70 / 111 / 103** | `TblLordCols()` |
| 高亮行 | 第 **4** 行（国公·焱森粥熟夷）= 自己，整行 `#570d41` | `LordSelfRow()` |
| 页脚左组左缘 | 元宝组 **392** / 累计捐献组 **671** / 我的爵位组 **916** | `LordFootCol1W/2W` |
| 元宝数量框 | **453..534**（82 设备），框 `#1d5486` 蓝、底 `#171e29`、字黄 | `LordFootBoxW` |
| 捐献钮 | **537..618**（82 设备），框 `#3f4956`、底 `#1f2936`、字 `#089cf3` | `LordFootBoxW` |

逐列色：爵位 `#eb7600` 橙 / 玩家 `#00d4d4` 青 / 累计贡献 `#ebeb00` 黄 /
速度与经验加成 `#ebeb00` 黄；**七列属性值按爵位分档**（与原版一致，不是
逐列固定色）：亲王 `#d40000` 红 / 国公 `#d46a00` 橙 / 侯爵 `#00d400` 绿 /
伯爵 `#b8b8b8` 浅灰。页脚两条状态值：「累计捐献」橙、「我的爵位」品红。
分页共 **5** 页（称谓是 21 页）。

**陷阱（都是实测踩出来的，已固化进 `TableUi.zan` + `.tbl-*` CSS）**：
1. 容器 `Padding` 的**底部内边距不计入容器测量高**——最后一行会贴到外框上
   （表底落 802，应 811）。上下留白必须用**定高撑条**显式占位。
2. 行节距实测 **49.5 设备**，整数设备像素下只能是 49/50 交替。**原版自己就是
   交替的**（行顶 214/263/313/362… 差 49/50 混排）；全用 49 会让第 12 行低
   6 设备、表底落 804。用 `TblRowHAt(r) = RowH + (r % 2)` 复现。
3. `Panel.Row()/Column()` 会置 `layout`，`With()` 据此把子项 dock 改成 3/1。
   要在格子里**手动摆位**（如居中的「领取」钮）必须用 `Add()` + `DockManual()`
   + `Place()`——用 `With()` 会贴左并拉满行高（实测偏 9/9 设备）。
4. **`text-align` 对 `Label` 无效，居中必须靠格子的 flex `justify-content`**。
   `Label.OnPaint` 直接在盒子左缘 `DrawText`（`StyleBox.DrawLabel` 才认
   `text-align`，Label 没走那条路），所以 `.tbl-c0 { text-align: center }` 是
   死规则——第一版整表文字贴左、与原版差 **29 设备**。正解：格子挂
   `.tbl-head-c / .tbl-cell { display:flex; justify-content:center;
   align-items:center; }`，标签按自身测量宽参与 flex 排布。高亮行的格子要
   同时挂 `.tbl-cell` 与 `.tbl-cell-hl`（后者只覆写底色为透明）。
5. 页脚右端靠 `.bag-fill`（`flex-grow:1`）顶开，**容器必须是
   `display:flex`**（`.tbl-foot`）；不是 flex 时 flex-grow 失效，右侧一组会
   紧贴状态文字（实测整组落 531..930，应 1152..1481）。
6. 标准库 `Pagination` 默认按主题 `heightMedium`（34 逻辑）出钮，比原版页脚
   高一大截。皮肤加 `pagination { height: 20; font-size: 12; }` 收紧。
7. 页脚左组**组距不等**（279/245 设备），用 `Gap` 凑不出来——每组一个定宽盒，
   且宽度要**扣掉页脚容器自己的 4 设备缝**（`LordFootCol1W = Px(184)` 对应
   392→671）。

### 5.3c 行会（页 5）的差异点
几何单一真相源 = `Spec.TblGuildCols()` + `Spec.Guild*`，页面 = `src/PageGuild.zan`，
行骨架仍复用 `TableUi.TblRowBoxes`（容器/表头/行高/列缝与 §5.3 完全一致）：

| 量 | 实测（设备像素） | 逻辑 → `Spec` |
|----|------------------|---------------|
| 列宽 | **89 / 183 / 97 / 129 / 169 / 165 / 121 / 120**，列缝 4.5、首列左缘 386 | `TblGuildCols()` |
| 表头 | 排名 / 行会名称 / 等级 / 成员数 / 会长 / 行会物资 / 限制等级 / 申请 | `GuildHeads()` |
| 等级列 | 一枚**红椭圆徽章** 47×20（x 692..738、y 225..244），**12 行逐像素相同** | `GuildLvW/H = Px(32)/Px(15)`（含 1 设备描边） |
| 申请列 | 钮 **97×27**（x 1379..1475），比称谓「领取」的 82 宽 | `GuildApplyW/H = Px(65)/Px(18)` |
| 页脚左组 | 「创建行会」钮 150×30 在 x **399**（容器左 374 + 25）+ 16 设备缝 + 提示文字 | `GuildFootPadL = Px(17)` / `GuildCreateW = Px(100)` / `GuildCreateGap = Px(11)` |
| 页脚右组 | 只有 上一页 / 1/13 / 下一页，**没有「说明」钮** | `TblFootOpt(..., note: false)` |

逐列文字色：排名 `#dcdcdc`、行会名称/会长/行会物资 `#00d4d4` 青、成员数
`#d4d400` 黄、限制等级 `#d4436a` 品红（与称谓「攻击」同色）；提示文字
`#b8b8b8`（比称谓/爵位页脚的 `#dcdcdc` 暗一档）。

**等级徽章不是数字而是「二十」两个字**：原版 12 行徽章**逐像素相同**（是
「行会等级」的图标位，Mir2 行会等级上限就是 20）。字迹在原图里只有 15×38
设备、糊成一团，识别方法是——把白色像素二值化打成 ASCII 点阵，看清笔画
是「左二（上短下长）+ 右十（竖贯中横）」，再拿候选字渲染对齐（`二十`
IoU 0.353，且结构逐笔吻合），而不是靠肉眼猜。

**陷阱（本页新踩的）**：
8. **行里有一列不是文字时，别另写一套行骨架**：`TableUi` 里把行拆成
   `TblRowBoxes(cells, r, rowCls)`（行类/行高/列缝/左内缩）+ 造格子的
   `TblCellCls / TblCellBox / TblActionCell`，页面只负责拼 `List<Panel>`。
   `TblRowCls / TblActionRowW` 都是它的薄封装——列缝与行高只写一次，
   否则「等级列是徽章」这种页面会自己长出一份会漂移的几何。
9. **`pagination` 的 simple 档与原版不是同一个控件形态**：simple 档是
   「上一页 + 可编辑当前页输入框（宽固定 46 逻辑）+ 散排的 `/ N` 文本 +
   下一页」——原版是**一个框里写 `1/13`**。改用标准库组件是既定方向（用户
   明确要求分页用分页组件），所以这个差异**登记为已知偏差**，不手搓。
   另外 `上一页/下一页` 的字号取自 `pagination::arrow`（不是 `pagination`
   自身的 `font-size`），不写会落主题默认档、实测宽 19 设备。
10. 同一张参考图里**页脚右组的位置各页不同**（称谓/爵位 1218..1480、
    行会/排行榜 1212..1456），不是我的布局漂移——原版自己就不一致，别按
    某一页去「修」另一页。

### 5.3d 排行榜（页 9）的差异点
几何单一真相源 = `Spec.Rank*`，页面 = `src/PageRank.zan`，行骨架仍复用
`TableUi.TblHeadAt / TblCellClsAt / TblRowBoxesAt`（默认档见 §5.3）：

| 量 | 实测（设备像素） | 逻辑 → `Spec` |
|----|------------------|---------------|
| 左面板 | 377..655 × **167..876**（1 设备蓝框 `#1e5588`、底 `#243040`、内边距 10、两盒缝 7） | `RankPanelPad/Gap`、`RankLeftW/H()` |
| 立绘卡 | 388..644 × 178..552；底图 `assets/ui/00010.jpg` 铺满，**10 个空格框已烘在图里** | `RankArtW/H`、`RankSlot*` |
| 属性盒 | 388..644 × 560..865（底 `#171e29`）；标题带 572..591，9 行文字节距 29 | `RankStatsH/PadT/PadL`、`RankStatH` |
| 页签行 | 6 枚 **126 宽**、缝 12、y 177..219；底 `#1f2936`、框 `#3f4956`，**没有选中态** | `RankTabH/Gap/Top` |
| 表格 | 659..1483 × **232..808**（1 设备蓝框），表头带 236..274（`#103555`）+ 2 设备蓝线 275..276 | `RankHeadH/HeadLine/HeadGap` |
| 行 | 首行顶 **285/286**、行高 39、**节距 43**、列缝 4、左内缩 8、底留白 10 | `RankRowH`、`RankHeadGap = Px(6)` |
| 列宽 | **77 / 172 / 137 / 93 / 104 / 204**（排名/玩家/等级/ID/职业/属性） | `RankCols()` |
| 页脚 | 分页 1/10、左内缩 4、**没有「说明」钮**、灰字提示 x 666 起 | `RankFootPadL`、`TblFootOpt(..., false)` |

逐列色：排名/职业白 `#f0f0f0`、玩家/等级青 `#00f0f0`、ID 品红 `#ff4060`、
属性整行黄（标签 `#c0c000`、数值 `#e8e800`）、表头 ~`#04c0ff`。属性盒 9 行
逐行不同色：等级 `#f47bc0`、生命 `#f87c03`、法力 `#7c7efd`、攻击 `#05d1e0`、
魔法 `#0b77f3`、道术 `#0ad508`、防御 `#f7eb06`、魔防 `#a605f7`、
幸运 `#df7306`，标题 `#f0e020`。

**左面板是「自己」**：立绘卡标题「第一个行会 会长」印在底图上，属性盒标题
「玩家名(称号)」——这一屏「自己」= 榜一（标题里的名字与第 1 行同名）。

**陷阱（本页新踩的）**：
11. **表格数据要一行一实体，不要平行列**（用户点出）：原先 5 个平行 `List`
    （名/等级/属性/ID/职业）靠下标对齐，`RankId(r)/RankJob(r)` 甚至**每渲染
    一行新建一个 12 元素 List 再取下标**；等级/经验存字符串后，6 枚页签
    （等级/攻击/魔法/道术/称谓/金币）按值排序根本无从谈起。改法：
    `class RankRow { name; job; id; peak; exp }`，`LevelText()/ExpText()` 收进
    实体，名次 = 下标 + 1。同类改造已同步称谓/爵位/行会（`TitleRow`/`LordRow`/
    `GuildRow`），并把「万位分隔」「百分号」收成 `Rules.Wan/Percent`——
    **手打字符串的数字列（`50,1300`、`1,0000`、`92490177127`）是同一类债**：
    格式规则无处校验，数值列一律存数、显示时才拼字。
    验收方式：种子页实体化改造后与改造前的**同一页像素 dump 逐像素零差异**。
12. **`Panel.Row()` 是停靠布局，不是 flex**：`flex-grow`/`align-items` 只在
    CSS 显式声明 `display:flex; flex-direction:row` 时才被采纳。页签行 6 枚
    曾因此叠成一枚（`Grow()` 无效、`Prefer(0,h)` 宽 0）；左面板被拉满整页高
    则是 `align-items` 默认 stretch 的结果（`.rank-page` 给 flex +
    `align-items:flex-start` 后才对上 167..876）。
13. **`Prefer(w, 0)` 是「撑满可用高度」，不是「高度自适应」**：布局把声明
    高度 ≤ 0 当 fill，左面板必须写显式高度 `RankLeftH()`（710 设备）。
14. **`Panel.StylePadT()` 曾忽略 `padSet`**：类上声明过 `padding` 时会把
    显式 `Padding(top, …)` 顶掉（左面板顶从 167 掉到 164）。已在标准库修
    （`stdlib/Gui/Widget/Panel.zan`：`padSet` 优先，`StylePadL/R/B` 本来就是这个
    语义），属标准库改动，走 smoke/standard 验收。

**登记为已知偏差（不在 Zan 侧绕）**：
- 属性列字号取 11（原版等效 12）：本引擎同字号字宽比原版宽约 7%，12 号时
  「经验：604765589748973」会溢出成省略号；11 号墨迹 185 设备 vs 原版 187。
- 数据只有「等级榜」这一屏：6 枚页签点击只切演示态（`RankPickTab` 记日志），
  **不编造**攻击/金币等榜的数据——行实体已是将来接真实数据后排序的前提。
- 立绘卡 10 枚道具里有两枚在原素材库无一一对应：玫瑰花束取 `01340.png`
  （`items.csv` 里没有它的行）、银底绿纹戒指取 `02814.png`（颜色器型最接近），
  均未确认。
- 本机客户端宽 1888（原版 1866）：撑满宽度的表框右缘比原版宽约 22 设备，
  纯 DPI 差异，非代码缺陷（固定几何全部对齐到 ±2 设备以内）。
- 末行底缘 805 vs 原版 807：原版行距非匀速（43/42/45…，均值 43.3），我按
  43 匀速，12 行累计差 3~5 设备。

### 5.4 卡片阵
| 用途 | 卡尺寸 | 列×行 | 列距 | 行距 |
|------|--------|-------|------|------|
| 地图传送 | **160×70** | 4×3（每页 20，可视 12） | 20 | 15 |
| 城池争霸 | 160×70 | 4×3 | 20 | 15 |
| 将星图谱 | **约 145×175** | 5×2 | 10 | 10 |
| 至尊殿堂 | 约 145×110 | 5×3 | 10 | 12 |
| 商店（精测见下） | **230×53** | **3×6**（每页 18） | **14** | **12** |
| 发红包 | 未实现，待测 | — | — | — |

> **陷阱**：`Grid.Of(n)` 会强制等分覆盖子项宽度，定宽卡片必须用 **Panel.Row + Gap** 手排。

**商店卡阵精测（`商店.png`，设备像素，1 逻辑 = 1.5 设备）**——这行最初被量成
「2 列 / 350×81 卡」，根因见下方陷阱：

| 量 | 实测 | 逻辑值 → `Spec` |
|----|------|-----------------|
| 内容区左右界 | 374 / **1490**（聊天面板左沿） | 页面横贯到聊天面板 |
| 卡 1 / 2 / 3 | 396..741 / 762..1107 / 1128..1473 | 卡宽 345 → `ShopCardW = Px(230)` |
| 列距 / 行距 | 21 / 18.6 | `ShopCardGapX = Px(14)` / `ShopCardGapY = Px(12)` |
| 行顶 | 244 / 342 / 439 / 537 / 634 / 732 | 卡高 79 → `ShopCardH = Px(53)`，一页 6 行 |
| 左内缩 / 右内缩 | 22 / 17 | `ShopPadL = Px(15)` |
| 货币行 tab | 398..517 / 526..644 / 653..772 / 781..899 | 每个 120×42，距内容左 24、顶 18 |
| 激活码输入框 / 使用钮 | 1077..1378 / 1388..1466 | `Px(200)` / `Px(52)` |
| 卡内图标石框 | 411..471 × 247..320 | 60×73 → `Px(40) × Px(49)` |
| 卡内价格胶囊 | 626..721 × 254..278（金边圆角） | 95×24 → `Px(63) × Px(16)` |
| 卡内下行 | y 290..313；数量(35) + 值(64) + +/− + 购买(73) | 行高 24 → `Px(16)` |
| 页脚 | y 830..860；左「元宝：N」、右 兑换元宝(135) + 分页器 | `ShopFootH = Px(20)` |

> **陷阱（列数会数错）**：同一张参考图里不同页的**内容区右界并不一致**——
> 地图页到 1128 就停了，商店页却一直排到聊天面板左沿 1490。按错误的右界
> （1128）去数列，3 列会被数成 2 列，卡宽/列距也全部偏大（实拍偏差：
> 卡宽 +5、列距 −4、行距 −2）。**正确做法：先在全宽范围扫卡片行的竖直
> 边框，把每列的左右缘都列出来，再定列数与卡宽。**

### 5.5 头像阵（PK竞技场 / 传说塔）

**实测（`竞技场.png`，设备像素；1 逻辑 = 1.5 设备 @150%）**：

| 量 | 实测 | 逻辑值 → `Spec` |
|----|------|-----------------|
| 列数 × 行数 | 8 × 2（16 格） | `ArenaCols/ArenaRows/ArenaCards` |
| 卡（头像）宽 × 高 | 106 × 106 | `ArenaCardW = ArenaCardH = Px(71)` |
| 列距（同排相邻卡左缘差） | 138 | `ArenaColGap = Px(21)` |
| 行距（两排卡顶差） | 约 229（含名字/排名/挑战钮三行） | `ArenaRowGap = Px(26)` |
| 名字 / 排名 行高 | 21 / 21 | `ArenaNameH = ArenaRankH = Px(14)` |
| 挑战钮 | 98 × 26 | `Px(65) × Px(17)` |
| 卡阵在主区 | 399..1470（左留白 26 / 右 20） | 居中后夹 `MapMargin` 下限 |

- 内容：头像（`assets/ui/00043..00048.jpg` 循环取图，`cover`）+ 名字（黄）
  + 「排名: N」（绿字标签 + 青字数字）+ 「挑战」钮（深蓝底浅蓝字）。
- 前 3 名的头像框换色：金 / 品红 / 青（`ArenaFacePath` + `.arena-face-1/2/3`）。
- 卡阵下分两块：**左 = 战斗日志**（5 行：`[系统]` 红、`[提示]` 黄、正文灰，
  行高 23 逻辑），**右 = 战功面板**（我的排名 / 挑战次数 / 挑战时间 / 累计战功，
  行高 24 + 底部两钮 87×28）；橙字提示贴主区底。
- 实测偏差：卡阵左右留白原版 26/20、本实现 31/39（窗口比原版宽 22 设备，
  多出来的宽度平摊到两侧），卡宽/列距/行距逐项一致。

### 5.6 分页器
```
说明   上一页   1/5   下一页
```
- 尺寸 **26×22**

> **实现偏差（用户拍板）**：页码一律用标准库 `Gui.Widget.Pagination`
> （`‹ 1 2 ›` 按钮行，高 = 主题 `heightMedium` = 34 逻辑），不再自绘原版的
> `[1][2]` 方块数字。用户原话「分页有分页组件」。挂接方式：`SignalInt`
> 持当前页，`Change` 事件里 `BuildPage()` 整页重建。
### 5.7 场景页（幻境 / 秘境 / BOSS之家 / 世界BOSS / 押镖 / 挂机主界面）
- 左侧：场景/怪物大图约 **280×280**，居中偏上
- 左上角：**等级/名称标签**（黄底黑字，如 `等级69` `幻境1` `秘境1` `镖车` `首次击杀本关BOSS通关`）
- 底部：**掉落格横排**（约 6~14 格，格 34×34，深底金边）
- 怪物血条：**红色**横条 + `n/n` 数字
- 右侧：排行列表，**行高 24**，格式 `名字 (巅峰1500级) 层数`
- 控件：`自动` `挑战BOSS/幻境/秘境` `扫荡` `说明`

### 5.8 背包格（通用）
```css
width: 42px; height: 42px;
background: #0a0c12;
border: 1px solid #3a4055;   /* 金边 #6a5a30 用于装备格 */
/* 数量角标：左上角，白字 11px */
```

### 5.9 复选框 / 开关
- 复选框：**蓝底白勾** 13×13
- 开关钮：橙底圆角

### 5.10 通用按钮
| 类型 | 规格 |
|------|------|
| 主按钮（升级/购买/挑战/开始） | 橙黄渐变底 `#e8a020→#c87810`，深色字，圆角 3 |
| 次按钮（说明/上页/下页） | 深蓝底 `#1a3a5c`，浅蓝字 |
| 取消/灰按钮 | `#3a4458` 底，`#8a94a8` 字 |

---

## 六、原版**没有**的东西（禁止添加）

针对地图传送页，原版**明确不存在**：
- ❌ 价格标签 ❌ 转生档位 ❌ 气血天赋 ❌ 「进入」激活按钮
- ❌ 图谱积分 ❌ 翻页箭头图标（只有 `[1][2]` 方块数字）
- ❌ 左列图名（图名压在**图内上方居中**）

---

## 七、玩法规则要点（摘自 `说明.txt`）

> 影响界面结构的规则，写代码时必须遵守。

| 系统 | 关键规则（影响 UI） |
|------|--------------------|
| **技能** | 10 个突破丹 = 1 级；每转生送 1 次全技能免费突破 → 技能页需显示「免费突破次数」 |
| **天书** | 转生 +1 负重，上限 10；特效天书重量 4 → 天书页需显示「负重: n/10」 |
| **内功** | 消耗战功 + 金币，递增；巅峰前不建议 → 内功页显示当前消耗 |
| **宝石** | 幻境出一级、秘境出二级；合成有几率带天赋 → 宝石页 4 槽/件 |
| **幻境** | 每天签到重置层数；有挑战失败次数限制；1500 层后出周年庆号角 |
| **秘境** | 可挑战层数上限 = **人物等级 + 100**；巅峰可挑战 |
| **世界BOSS** | **挑战次数与 BOSS之家共享**（重要！两个页面共用一个计数器） |
| **BOSS之家** | 编号 1-5 前期装备 / 6-14 转生证明 / 15-23 龙珠 / 24-28 渡劫证明；**挑战次数与世界BOSS共享** |
| **押镖** | 每天签到重置 5 次；每次 100 秒；可被劫镖 |
| **沙巴克** | 车轮战 KOF；每周一次；需会长提交祖玛头像或 100W 物资申请 |
| **城池** | 每人最多占 1 个城；占领后 1 小时内不能打其他城；7 天内领取一次，超 7 天可被踢 |
| **传说塔** | 一层本服、其余跨服；每层 +5% 经验加成；塔顶失败方 10% 掉装备 |
| **祭坛** | 每消耗 1000 进度兑换 1 次幻境重置；排名每日签到奖励 500/400/300/200 进度 |
| **图谱** | 积分 = 转生次数（0转1分…巅峰11分）；不能提取 |
| **极品殿堂** | 每件 +5% 经验加成，上限 50%（10 件）；复制收 10% 手续费 |
| **五行** | 一转后可用五行碎片激活；终身会员提高倍率 |
| **师徒** | 拜师条件 ≤60 级，出师 80 级，师父需 2 转；活跃度 100% 时徒弟领取翻倍 |
| **爵位** | >3000元宝=乡绅，>15000金=豪杰，>30000金=县令，>50000金=大臣；前 19 名有专属爵位 |
| **称谓** | 属性**递加不叠加**（筑基1阶+1攻1魔，2阶共+1攻1魔1道） |
| **洗练** | 普通洗练消耗金币有降系数风险；特殊洗练消耗元宝不降系数 |
| **龙珠** | 吞噬灵石升级，上限 = 转生等级×10；大90%/中50%/小10% 成功率，每次 100 元宝 |
| **天赋转移** | 消耗 1000 + 装备等级×10 的巅峰矿石；项链转移幸运、靴子转移速度 |
| **传说培养** | 每次 100 颗传说矿石；单条上限 = 人物等级/5 |
| **神兵图鉴** | 属性 = 系数×基数/10；满系数 = 装备等级/5 |

---

## 八、实测方法（可复现）

**从原图量尺寸**：像素 **÷ 1.5** = 逻辑值（口径见 §一「原图量法」）。
`_scratch/shoot_pages.py`、`zpx2png.py` 已随 `_scratch` 清理，不再存在；
重出截图直接用 `tools/build.ps1` + `testing-gui-screenshot` skill 的
`win-shot.ps1`（按 PID 抓自己启动的窗口）。

```bash
# 原图量取示例：量到像素再折逻辑
python -c "print(1871/1.5, 971/1.5)"   # -> 1247.3 647.3
```

导航瓦片坐标（离线壳 1642×1016，`dump tree` 实测）：
```
nav-0=(71,84)   nav-1=(195,84)  nav-2=(319,84)  nav-3=(443,84)  nav-4=(567,84)
nav-5=(691,84)  nav-6=(815,84)  nav-7=(939,84)  nav-8=(1063,84) nav-9=(1187,84)
nav-10=(71,160) ... nav-20=(71,980) ... nav-30=(1567,84)
```

---

## 九、重建优先级

> 进度以代码为准：`BuildPage()` 的真分支就是已完成页（0 / 1 / 2 / 3 / 4 /
> 12 / 13 / 20 / 24 / 25 / 27 / 31），其余走 `BuildPlaceholder()`。做完一页就把
> 状态改成 ✅。

| 阶段 | 页面 | 状态 |
|------|------|------|
| **P0** | 全局框架 + 左栏 + 聊天区 + 底部导航 | ✅ 已成型 |
| **P1** | **地图内页 / 挂机战斗界面**（页 31）| ✅ 已实现（`PageMapInner.zan`） |
| **P2** | 地图传送（页 0） | ✅ 已实现（`PageMap.zan`） |
| **P3** | 背包（页 20，含 6 个子 tab） | ✅ 已实现（`PageBag.zan`） |
| **P4** | 场景类页（幻境 2 / 秘境 12 / BOSS之家 3 / 世界BOSS 13 / 押镖 4） | ⏳ 复用页 31 的场景版式 |
| **P5** | 余下表格类页（交易市场 22 / 邮箱 29 / 成就 26 / 图鉴 / 回收系统） | ⏳ 共享组件 `TableUi.zan` 已就位（§5.3）；数据层定式 = **一行一实体**（§5.3d 陷阱 11） |
| **P5a** | **称谓（页 25）** | ✅ 已实现（`PageTitle.zan`，10 列 + 领取钮 + 分页 21 页） |
| **P5b** | **爵位（页 24）** | ✅ 已实现（`PageLord.zan`，12 列 + 高亮行 + 元宝/捐献页脚 + 分页 5 页） |
| **P5c** | **行会（页 5）** | ✅ 已实现（`PageGuild.zan`，8 列 + 等级徽章 + 申请钮 + 创建行会页脚 + 分页 13 页） |
| **P5d** | **排行榜（页 9）** | ✅ 已实现（`PageRank.zan`，6 枚页签 + 左立绘/属性面板 + 6 列表格 + 分页 1/10），差异点与偏差见 §5.3d |
| **P6** | 卡阵类页（城池争夺 7 / 将星图谱 16 / 至尊殿堂 19 / PK竞技场 1 / 传说塔 8） | ⏳ |
| **P6a** | **商店（页 27）** | ✅ 已实现（`PageShop.zan`，3×6 卡阵） |
| **P7** | 其余（沙巴克 6 / 祭坛 14 / 寻宝 15 / 五行修炼 10 / 师徒 11 / 图鉴 23 / 回收系统 17 / 铁匠铺 21 / 帮助 28 / 设置 30） | ⏳ |
| **弹窗** | 发红包（页 18）= 红包弹窗，不占整页 | ⏳ |

**组件复用盘点（原引擎控件清单 → 本项目组件）**：原版 DM3 的 UI 控件清单在
`docs/arpg/03-UI控件与皮肤系统.md` §4，其中**格子类控件**是 `bagBox`（包裹格子）
/ `itemBox`（道具格子）/ `skillBox`（技能格子）/ `quickBox`（快捷格子），事件走
`拿起`/`放下` + `引擎.鼠标道具`。**Zan 侧没有移植这一族**——`stdlib/Game/Arpg`
只有格子**数据模型** `ArpgInventory`/`ArpgEquipSlot`（`Runtime.zan`，随
`ArpgActor` 用）与声明式 UI 运行时 `ArpgControlKind`（label / button / imageBox /
textBox / progressBar / richTextBox / prefab / custom，**没有格子类**）。所以本项目的
格子显示统一走 `src/Slots.zan`（背包 / 掉落 / 装备环 / 宝石 / 技能格共用一个
组件），表格走 `src/TableUi.zan`，卡阵走各页的 `Panel.Row + Gap`。

---

## 十、缺口清单（需用户补图 / 已决）

### 已决（2026-09-10 用户拍板）

| 项 | 结论 |
|----|------|
| **发红包（页 18）** | **不是整页，是一个红包弹窗**。发红包导航格触发弹窗（余额 + 发送金额 + 抢红包），不占主区整页。§4.2 的「存疑」结案。 |
| **商店 vs 发红包** | 不是同一页：商店（页 27）= 3 列商品卡阵整页；发红包 = 弹窗。`商店.png` 只作商店页依据。 |

### 仍缺图（不能像素还原，需补图或授权自拟）

| 缺失项 | 说明 |
|--------|------|
| **地图传送（页 0）** | 产品设计文档索引进的 `c063c06a` 不在 `mir/` 内，开机首页无原图可对拍。该页已按早前实测实现（Spec 有 160×70/列距 20/行距 15），**可继续但不具备叠加复核条件**。 |
| **观战**（旧记「右上角放大镜」） | **不存在**：实测右上角是设置齿轮（= 页 30）。此项从缺口清单移除。 |
| **战斗统计**（左下角入口） | 有入口（底栏左端带框按钮）但无独立页图；Shell 目前导航到页 26「成就」，语义存疑，待定夺 |
| **背包-取出 详细态** | 有 `说明` tab 的整体图，但「说明」子页内容未见 |
| **创建角色流程** | 背包页左侧有建角组件，但独立的登录/建角流程无图 |
| **各页 hover/选中态** | 无图 |

### 图集缺口（有原图，但需从 `assets/ui` 认领）

| 项 | 说明 |
|----|------|
| **导航按钮独有图标** | ✅ **已补齐**：31 格逐格从 30 张整窗图的导航条切出 `assets/ui/nav/page-00.png … page-29.png`（+ 设置齿轮 `nav-gear.png`），`data/navigation.csv` 的 `icon` 列逐格指过去，不再复用通用图标。 |

### 11.0 `assets/ui` 素材台账（**动手写界面前必读**）

`tools/minimir/ui_index_map.csv` 是从原版 `game.exe` **静态分析**出的
权威台账（275 张 ui 包图 → 引用它的功能函数 → 推断模块）。`tools/minimir/README.md`
§"UI 布局与素材→玩法模块映射" 是它的文字说明。

**铁律：写/改任何页面前，先查这张表，能整图用整图，不要自己拼控件、
不要自己配边框色。** 已被这张表纠正过的三次错误（都发生在包裹页）：

| 我当时的做法 | 台账实际说的 | 正确做法 |
|---|---|---|
| 用 CSS `border: 1px #6a5a30` 画 6×6 物品格 | `00002/00004.png` (42×37) = **通用底图(包裹格背景)** | 直接用 `00002.png` 撑满格子 |
| 把 `00061-00064.png` 当"装备环 4 帧动画" | `00063.png` = **排行榜/列表行背景**（`00061/62/64` 是排行榜行） | 排行榜行用它们，不是装备格 |
| 用 CSS 渐变色做 8 帧"金边呼吸动画" | 工程里**没有帧动画概念**——`00001-00005.png` 同形不同色 = **品质色**（红/黄/洋红/蓝/绿 5 档） | 空格子用 `00002`，带道具按品质挑 `00001-00005` |

已确认可直接用的整图（省掉大量自绘）：

| index | 尺寸 | 台账归属 |
|-------|------|----------|
| `00001-00005.png` | 42×37 | 包裹格**品质色**边框（红/黄/洋红/蓝/绿） |
| `00002/00004.png` | 42×37 | **包裹格背景**（通用底图） |
| `00006.png` | 539×26 | **通用窗口框/关闭钮**（15+ 窗口共用） |
| `00009/00010.jpg` | 168×250 | **男 / 女立绘**（同一套格位：右列格顶 43/81/131/170、左列 132/171，10 个空格框已烘在图里；包裹页用 00009，排行榜/立绘卡用 00010） |
| `00011/12/14/15.png` | 232×54 | 某面板头（`0x474934` 在用） |
| `00021-00026.png` | 27/37/49/61/73/85 ×33 | 按钮条 6 档宽（蓝） |
| `00031-00036.png` | 同上 | 按钮条 6 档宽（黄）= 蓝档号 +10 |
| `00043-00048.jpg` | 62×60 | 头像 6 张（左栏循环取图用） |
| `00061-00064.png` | 72×70 | **排行榜行**（`00063` 是通用列表行背景） |
| `00081.jpg` | 550×385 | 表格整图（带表头：排名/玩家/等级/ID/职业/属性） |
| `00082/00083.jpg` | 488×249 | 网格整图（5×8） |
| `00132/00134.png` | 735×30 | **焦点/选中标志（通用）**——所有列表选中态走它 |
| `00171/00173.png` | 521×26 | 列表行 |
| `00183.jpg` | 483×107 | **列表行背景（通用）** |
| `00191.png` | 250×80 | 转生/角色信息窗 |
| `00193.jpg` | 14×14 | 已读标识（邮件/消息） |
| `00211.png` | 45×43 | **物品格子（通用）**——填满型石质底 |
| `00255.png` | 26×32 | 格子锁标志 |
| `00257-00259.png` | 120×170 | 图鉴卡（`tupu` 包 460 张同期） |

**还没认领的**（`ui_index_map.csv` 里 `module_guess` 为空且没 mainscreen 命中的）：
`00221-00234.png` (37×37)、`00117-00120.png` (38×37)、`00141-00160.png` (34×15)、
`00100-00109.png` (19×24)、`00051-00054.png` (12×14)、`00041/00042.png` (32×4)。
要新页面前先在这些里找，找不到再考虑复用或自绘（并在此表登记）。

---

## 十一、本项目布局定式与框架偏差（非原版实测，改动前先读）

原版是那个原版，但**本项目的实现有几条用户拍板/框架逼出来的定式**。
它们不是"原版实测值"，所以单独列在这里；`Spec.zan` 是这些值的唯一出处。

### 11.1 竖向节奏只有一个间距

`ContentTopPad = BotTileBottom = NavRowGap = Px(4)`（150% 屏 = 6 设备像素）。
原版顶栏瓦片直接贴标题栏（顶 0）、底下却空 12.7 逻辑像素，用户点名
**「按钮贴顶了下面有间距，这也是不行的，上下间距要是一致的，最好和按钮
之间的间距也统一」**。改后实测：标题栏下 6 / 两行瓦片间 6 / 瓦片行下 6 /
底排瓦片下 6，四段全等。

### 11.2 顶栏整行左移 8 逻辑像素 + 右端与左端留白配平

`NavRowShift() = Px(11) - LeftPad()`。原版第 1 格实测 x=11，比正下方左栏
面板左缘（3）缩进 8，看着不齐；用户要求**「按钮左移，对齐下面」**。瓦片
宽与格间隙仍是实测值，只有这一行的起点偏离原图；改后顶栏瓦片、左栏面板、
底排瓦片左缘实测都在 x=4（设备像素）。

左移后右端离聊天面板还有 43 设备像素，而左端只有 4，用户：
**「这个右侧应该跟左侧间距保持一致」**。第一次实现是"格间隙均分铺满"，
被否：**「之前按钮的间距是可以的，是要整体左移 不是该按钮」**——间距是
实测值，不许动。最终定式（用户：**「两排按钮各自增加几个像素宽也能达到
对齐效果」**）：

- 每排**行末加一条 `LeftPad` 宽的撑条**，行首边距与行末边距相等；
- `.nav-row { display: flex }` + `.navtile { flex-grow: 1 }`：行里剩下的
  宽度**平分给各瓦片**（按钮各宽几个设备像素），撑条（flex-grow 0）不动，
  所以**格间隙逐条与实测一致**；`.navtile-gear { flex-grow: 0 }` 让设置
  齿轮保持正方形（33×33 逻辑），余数由引擎给最后一个可增长项
  （`ArrangeFlexLine` 的余数规则），每排右端因此**落在同一列**：
  实测三排右缘都在 1507..1509，聊天面板左缘 1513，左端 4。
- 三排（顶栏两排 + 底排）右端同列，与原版一致（原版三排右缘同为 988 逻辑）。
- **坑**：`Panel.Row()` 默认走**停靠布局**（`With()` = 左停靠），不做剩余
  空间分配，只按各控件的 `StyleWidth` 摆位——只写 `flex-grow` 而不给容器
  `display: flex`，`flex-grow` **一点作用都没有**（本轮实测一次踩空）。

### 11.3 窗口高 625（原版 646）——"元素整体上收"

`Spec.WindowH()`。卡阵在主区里居中后，上下留白比左右各多 17 逻辑像素，
收掉这 17 两边才配平；用户原话**「整个窗口高度应该可以缩小一点啊，
元素整体上收」**。实测窗口从 1888×1019 收到 1888×993（设备像素）。

### 11.4 地图传送页（页 0）的竖向定式

用户原话**「地图不是垂直居中的啊」**。定式：
- **卡阵在主区里垂直居中**：`topInset = (pageH - wallH) / 2`（下限 `MapMargin`），
  实测上下留白 80 / 82 设备像素（含标题栏后的 6）。
- **页码贴主区底**（原版页码本来就在左下）、与卡阵左缘对齐：`pagerTop =
  pageH - MapMargin - PagerH`，撑不下一个卡阵行距（`MapRowGap`）时退到卡阵
  下沿。5 行卡阵居中后底下只剩约一个行距的余量，所以实际是"紧贴卡阵下沿"。
- **`.map-page` 不许再留 CSS padding**：它曾在 CSS 里留 8 逻辑像素，等于从
  主区上下各偷 12 设备像素，而 `pageH` 是按整块主区算的 → 块被挤扁 24 像素，
  居中看着对、实际靠"挤"对齐（实测 pager 被裁掉 2 像素）。间距一律在布局
  代码里算，不在 CSS 里留。

### 11.5 框架偏差（记录在案，**不在 Zan 侧绕**）

1. **无边框窗口的客户区被 OS 撑大**：150% 屏上 `Win32Shell` 建窗/`ResizeClient`
   仍按 `AdjustWindowRect(WS_OVERLAPPEDWINDOW)` 给无边框窗口加了一圈外框，
   实测画布比设计尺寸大 **(22, 56) 设备像素**（设计 1866×963 → 实到 1888×1019）。
   → 所以**一切居中必须按运行期画布算**（`app.ClientWidth/Height()`），拿设计值
   居中会偏右偏下。
2. **页面要在窗口尺寸变化后重建**：`SetClientSize` 之后画布要等窗口处理完
   resize 才变，紧接着建页会拿着 resize 之前的旧画布算（实测算出 494×427，
   只有真实主区 1138×788 的一半，两个 inset 都撞下限，卡阵被顶到左上角）。
   现在 `main.zan` 用 `form.Every(120, ...)` 轮询客户区尺寸，变了就 `BuildPage()`。
   **尺寸必须"重建成功后"再记下**——记在重建之前，一次异常就会把这次 resize
   永久吞掉，窗口停在旧布局上。
